WIKI.concept({
slug:'context-window',

tldr:'모델이 한 번의 추론에서 동시에 참조할 수 있는 [토큰](#/c/token)의 최대 개수 — 입력과 출력을 합친 상한.',

why:'컨텍스트 윈도우를 넘기면 오래된 대화가 잘려나가거나 요청 자체가 거부된다. 그리고 윈도우가 길다고 그 안의 모든 정보를 모델이 똑같이 잘 쓰는 것도 아니라서, "문맥에 넣었으니 된다"는 가정이 실무에서 가장 자주 배신당하는 지점이다.',

sections:[
 {h:'무엇이 그 안에 들어가는가', d:'시스템 프롬프트, 이전 대화 turn 전체(멀티턴 챗봇이라면 히스토리 전부), 방금 사용자가 보낸 메시지, 그리고 모델이 이제부터 생성할 출력까지 — 이 전부가 같은 토큰 예산을 나눠 쓴다. 예를 들어 128K 컨텍스트 모델에 120K 토큰짜리 문서를 넣으면 출력으로 쓸 수 있는 토큰이 8K 미만으로 줄어든다. RAG 로 검색된 문서를 프롬프트에 이어붙이는 것도 이 예산을 그대로 소비한다.'},
 {h:'왜 유한한가', d:'[셀프 어텐션](#/c/self-attention)은 시퀀스 길이 $n$ 에 대해 $O(n^2)$ 로 계산량·메모리가 늘어난다 — 토큰마다 다른 모든 토큰과의 관계를 계산하기 때문이다. 길이를 두 배로 늘리면 비용은 네 배가 된다. 또한 모델은 [위치 인코딩](#/c/positional-encoding)이 학습 때 본 길이 범위 안에서만 위치 정보를 안정적으로 일반화하므로, 학습 시 본 적 없는 길이로 그냥 늘리면 성능이 무너진다 — 이를 다루려는 방법이 [YaRN](#/p/yarn), [LongRoPE](#/p/longrope) 같은 컨텍스트 확장 기법이다.'},
 {h:'길다고 다 쓰는 게 아니다', d:'컨텍스트가 늘어난다고 그 안의 모든 정보에 모델이 균등하게 접근하는 게 아니다. [Lost in the Middle](#/p/lost-in-the-middle)은 긴 문맥의 양 끝(맨 앞·맨 뒤)에 있는 정보는 잘 찾아내지만 중간에 묻힌 정보는 정답률이 눈에 띄게 떨어진다는 것을 실측으로 보였다 — U자형 성능 곡선이다. 그래서 "컨텍스트 윈도우가 1M 토큰이니 문서를 다 던져 넣으면 알아서 찾아 쓰겠지"라는 기대는 종종 틀린다. 핵심 정보는 프롬프트의 앞이나 끝에 배치하거나, 애초에 [RAG](#/c/rag-c)로 관련 부분만 추려 넣는 편이 안전하다.'},
 {h:'실무에서', d:'긴 대화를 이어갈 때는 오래된 turn 을 요약해서 압축하거나 아예 버리는 전략(sliding window, summarization)이 필요하다. [KV 캐시](#/c/kv-cache) 관점에서도 컨텍스트가 길수록 서빙 메모리가 선형으로 커지므로, 컨텍스트 윈도우 크기는 곧 서버 비용과 직결된다. [StreamingLLM](#/p/streaming-llm) 같은 방법은 아주 긴 스트림을 무한정 붙잡지 않고 앞부분의 몇 개 "싱크" 토큰만 유지한 채 나머지를 슬라이딩하는 식으로 이 문제를 완화한다.'}
],

math:[
 {tex:'\\text{Attention 비용}\\propto n^2\\cdot d', expr:'셀프 어텐션의 계산량', d:'$n$ 은 시퀀스(컨텍스트) 길이, $d$ 는 모델 차원. 길이를 키우면 비용이 제곱으로 늘어나는 것이 컨텍스트 윈도우가 무한히 커지지 못하는 근본 이유다.'}
],

diagram:{type:'stack', cap:'128K 토큰 예산을 한 번의 요청이 어떻게 나눠 쓰는가.',
 items:['시스템 프롬프트 (예: 1K)','대화 히스토리 (예: 40K)','이번 사용자 입력 (예: 5K)','← 남는 예산이 출력 가능 토큰']},

confuse:[
 {a:'컨텍스트 윈도우', b:'모델 파라미터 수', d:'전자는 한 번에 읽을 수 있는 토큰 개수의 상한이고, 후자는 모델 자체의 크기(가중치 개수)다. 파라미터가 많다고 컨텍스트가 긴 것도, 컨텍스트가 길다고 모델이 더 똑똑한 것도 아니다 — 서로 독립적인 스펙이다.'},
 {a:'컨텍스트 윈도우', b:'[KV 캐시](#/c/kv-cache)', d:'컨텍스트 윈도우는 모델이 지원하는 이론적 최대 토큰 수(설계·학습에 의해 정해짐)고, KV 캐시는 그 토큰들을 실제로 처리하며 서버 메모리에 쌓이는 실물 데이터다. 윈도우가 128K 여도 메모리가 부족하면 실제로는 그만큼 못 채울 수 있다.'}
],

pitfalls:[
 '"컨텍스트 윈도우가 넉넉하니 다 넣자"는 [Lost in the Middle](#/p/lost-in-the-middle) 문제 때문에 오히려 정답률을 떨어뜨릴 수 있다.',
 '입력 토큰과 출력 토큰이 같은 예산을 공유한다는 걸 잊고 긴 입력을 넣으면, 출력이 중간에 잘리는 문제를 겪는다.',
 '컨텍스트 윈도우 스펙만 보고 "이 모델이 긴 문서를 잘 이해한다"고 단정하면 안 된다 — 실제 긴 문맥 활용 능력(long-context 벤치마크 성능)은 스펙상 최대 길이와 별개다.'
],

papers:['longrope','yarn','streaming-llm','lost-in-the-middle'],
terms:['token','kv-cache','self-attention','positional-encoding']
});
