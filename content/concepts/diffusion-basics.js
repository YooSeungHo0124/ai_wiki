WIKI.concept({
slug:'diffusion-basics',

tldr:'데이터에 잡음을 단계적으로 더하는 정방향 과정을 정의하고, 그 반대 방향으로 잡음을 조금씩 걷어내는 것을 학습해 순수 잡음에서 데이터를 생성하는 모델이다.',

why:'이미지·오디오 생성의 사실상 표준이 된 방식인데, "왜 한 번에 안 만들고 굳이 수십~수백 단계로 나눠서 만드는가"를 이해해야 [CFG](#/p/cfg)·스케줄러·스텝 수 같은 실무 손잡이들이 왜 그렇게 설계됐는지 감이 잡힌다.',

sections:[
 {h:'정방향 과정', d:'원본 데이터 $x_0$ 에 아주 작은 가우시안 잡음을 매 스텝 조금씩 섞어 $x_1, x_2, \\dots, x_T$ 로 점점 잡음에 가깝게 만든다. $T$ 가 충분히 크면 $x_T$ 는 원본 정보가 거의 남지 않은 순수 가우시안 잡음에 도달한다. 이 과정은 학습이 필요 없는 고정된 확률 규칙이다 — 잡음을 얼마나 섞을지 스케줄만 미리 정해두면 된다.'},
 {h:'역방향 과정과 학습', d:'모델이 학습하는 것은 이 정방향의 반대, 즉 $x_t$ 에서 한 스텝 전의 $x_{t-1}$ 을 복원하는 것이다. 실제로는 $x_{t-1}$ 을 직접 예측하지 않고, $x_t$ 에 섞여 있는 잡음 성분 $\\epsilon$ 을 예측하도록 신경망을 학습시킨다([DDPM](#/p/ddpm)) — 잡음을 알면 원본 방향을 역산할 수 있기 때문이다. 학습이 끝나면 순수 잡음 $x_T$ 에서 시작해 이 잡음 예측을 반복적으로 빼나가면서 $x_0$ 까지 되돌아가는 것이 생성 과정이다.'},
 {h:'왜 한 번에 안 만드는가', d:'잡음 $x_T$ 에서 원본 $x_0$ 로 가는 변환은 매우 복잡한 비선형 사상이라, 신경망 한 번의 순전파로 직접 근사하면 오차가 크다(GAN 이 이 한 번에 생성 방식을 쓰고, 학습이 불안정한 이유 중 하나다). 반면 $x_t$ 에서 $x_{t-1}$ 로 가는 한 스텝은 잡음이 아주 조금만 바뀌는 국소적인 변화라 훨씬 단순한 함수로 근사할 수 있다. 즉 어려운 문제 하나를 푸는 대신, 쉬운 문제 여러 개(각 스텝의 잡음 제거)로 쪼개 반복 적용함으로써 전체적으로 복잡한 변환을 안정적으로 달성하는 것이다. 스텝 수를 줄이면(빠른 샘플링) 각 스텝이 다시 더 큰 도약을 요구하게 되어 품질이 떨어지는 트레이드오프가 생긴다.'},
 {h:'빠른 샘플링과 잠재공간', d:'[DDIM](#/p/ddim)은 매 스텝을 확률적으로 뽑지 않고 결정적(deterministic) 경로로 재구성해, 원래 필요했던 스텝 수보다 훨씬 적은 스텝으로도 비슷한 품질에 도달하게 한다 — 같은 잡음 제거 목표를 다른 샘플링 경로로 도달하는 방식이다. [LDM](#/p/ldm)(Latent Diffusion Model)은 확산 과정을 픽셀 공간이 아니라 오토인코더로 압축한 저차원 잠재공간에서 수행해, 각 스텝의 연산량 자체를 줄인다 — 스텝 수를 줄이는 DDIM 과는 다른 축의 최적화다.'},
 {h:'실무에서', d:'스텝 수·스케줄러 선택은 품질과 속도의 직접적인 트레이드오프다. 스텝을 너무 줄이면 세부 디테일이 뭉개지거나 구조가 무너지고, 너무 늘리면 한계 이득 대비 시간만 늘어난다. 같은 스텝 수라도 스케줄러(DDPM/DDIM/그 외 solver)에 따라 도달하는 품질이 달라지므로, 속도가 중요한 서비스에서는 스케줄러 자체를 바꾸는 것이 스텝 수를 무작정 줄이는 것보다 효율적인 경우가 많다.'}
],

math:[
 {tex:'q(x_t\\mid x_{t-1})=\\mathcal{N}(x_t;\\sqrt{1-\\beta_t}\\,x_{t-1},\\beta_t I)', expr:'정방향 한 스텝', d:'$\\beta_t$ 는 스텝 $t$ 에서 섞는 잡음의 양(스케줄로 미리 정함). 이전 상태를 아주 조금 줄이고 그만큼 가우시안 잡음을 더하는 것을 $T$ 번 반복하면 $x_T$ 가 표준정규분포에 가까워진다.'},
 {tex:'L=\\mathbb{E}_{x_0,\\epsilon,t}\\big[\\lVert \\epsilon-\\epsilon_\\theta(x_t,t)\\rVert^2\\big]', expr:'학습 목표(단순화된 DDPM loss)', d:'실제 섞인 잡음 $\\epsilon$ 과 신경망 $\\epsilon_\\theta$ 이 $x_t$ 와 시점 $t$ 를 보고 예측한 잡음의 평균제곱오차를 최소화한다. 네트워크는 "이 상태에 섞인 잡음이 무엇인지" 하나만 맞히면 되는 비교적 단순한 회귀 문제를 학습한다.'}
],

diagram:{type:'loop', cap:'정방향으로 잡음을 더하고, 학습된 모델로 역방향으로 걷어낸다.',
 steps:['x0(원본)','정방향: 잡음 조금씩 추가 → xT(순수 잡음)','역방향: 학습된 모델이 잡음 조금씩 제거','x0 복원(생성)']},

confuse:[
 {a:'확산 모델', b:'GAN', d:'GAN 은 생성자가 잡음에서 이미지를 한 번의 순전파로 직접 만들고 판별자와 경쟁하며 학습한다. 확산 모델은 여러 스텝에 걸쳐 잡음을 점진적으로 제거하며, 적대적 학습 없이 잡음 예측이라는 단순한 회귀 목표로 학습한다는 점이 근본적으로 다르다. 학습 안정성은 확산 모델이 대체로 더 낫다고 보고되지만, 그 대가로 샘플링에 여러 스텝이 필요하다.'},
 {a:'[DDPM](#/p/ddpm)', b:'[DDIM](#/p/ddim)', d:'DDPM 은 매 스텝을 확률적으로 샘플링하는 원조 방식이고, DDIM 은 같은 학습된 모델을 결정적 경로로 재해석해 적은 스텝으로도 샘플링할 수 있게 한 방법이다. 학습은 동일하고 샘플링 절차만 다르다.'},
 {a:'확산 과정을 도는 공간', b:'스텝 수', d:'[LDM](#/p/ldm)은 "어느 공간(픽셀 vs 잠재)에서 확산을 돌리는가"의 문제이고, DDIM 은 "몇 스텝을 도는가"의 문제다. 둘은 서로 독립적인 최적화 축이라 함께 쓸 수 있다 — 실제로 Stable Diffusion 계열은 둘 다 적용한다.'}
],

pitfalls:[
 '"확산 모델은 무조건 느리다"는 것은 절반만 맞다 — DDIM 류의 빠른 샘플러와 잠재공간 축소([LDM](#/p/ldm))를 쓰면 스텝 수와 스텝당 비용을 모두 줄일 수 있다.',
 '스텝 수를 늘리면 품질이 항상 좋아진다고 생각하기 쉽지만, 스케줄러가 이미 수렴한 지점을 넘어서면 추가 스텝은 체감 이득이 거의 없다.',
 '잡음 예측 목표(loss)가 단순 MSE 라고 해서 확산 모델의 학습 자체가 쉽다는 뜻은 아니다 — 스케줄·구조 설계에 따라 품질 편차가 크다.',
 '"잡음을 예측한다"와 "원본을 직접 예측한다"를 같은 것으로 생각하기 쉽지만, 파라미터화 방식(잡음 예측 vs $x_0$ 예측 vs velocity 예측)에 따라 학습 안정성과 샘플링 품질이 달라진다는 보고가 있다.',
 '잠재공간에서 확산을 돌리는 [LDM](#/p/ldm)은 오토인코더의 압축 품질에 최종 생성 품질이 상한선으로 걸린다 — 오토인코더가 복원하지 못하는 디테일은 확산 과정이 아무리 잘 돼도 되살아나지 않는다.'
],

papers:['ddpm','ldm','cfg'],
terms:['guidance']
});
