WIKI.concept({
slug:'param-count',

tldr:'모델의 파라미터 수·연산량(FLOPs)·메모리 사용량을 학습 전에 미리 어림잡는 계산으로, GPU를 몇 장 빌려야 하는지를 결정하는 실무의 출발점이다.',

why:'"7B 모델을 학습하려면 GPU가 몇 장 필요한가"는 실무에서 가장 자주 나오는 질문이고, 답은 파라미터 수 하나가 아니라 가중치+경사+옵티마이저 상태+활성값을 모두 더한 값이다. 이 계산을 못 하면 GPU를 빌려놓고 메모리 부족(OOM)으로 학습이 시작도 못 하거나, 반대로 필요 이상으로 큰 클러스터를 빌려 예산을 낭비한다.',

sections:[
 {h:'파라미터 수 근사', d:'디코더 전용 트랜스포머의 비임베딩 파라미터 수는 층 수 $n_{layer}$, 은닉 차원 $d_{model}$ 에 대해 $N\\approx 12\\,n_{layer}\\,d_{model}^2$ 로 근사한다(GPT-3 논문 계열). 이는 셀프 어텐션의 Q/K/V/O 투영( $4d_{model}^2$ )과 FFN의 확장·축소 투영(보통 $4d_{model}$ 폭이므로 $8d_{model}^2$ )을 합친 값이다. 여기에 어휘 크기 $V$ 만큼의 임베딩·출력층 $2Vd_{model}$ 을 더하면 전체 파라미터 수가 나온다 — 큰 모델일수록 임베딩 비중은 상대적으로 작아진다.'},
 {h:'학습 연산량(FLOPs)', d:'토큰 하나를 순전파하는 데 필요한 연산은 대략 $2N$ FLOPs(파라미터마다 곱셈+덧셈 1회씩)이고, 역전파는 순전파의 약 2배 연산이 더 필요해 학습 스텝 전체로는 토큰당 약 $6N$ FLOPs가 든다는 것이 [스케일링 법칙](#/p/scaling-laws)류 논문들이 쓰는 어림값이다. 학습 토큰 수를 $D$ 라 하면 전체 학습 연산량은 $C\\approx 6ND$ 로 근사되며, 이 식 하나로 "이 모델을 이만큼 데이터로 학습하면 GPU-시간이 얼마나 드는가"를 역산할 수 있다.'},
 {h:'학습 메모리(16B)', d:'혼합 정밀도 + Adam 옵티마이저를 쓰는 표준 학습에서 파라미터 하나당 저장해야 하는 것은: FP16 가중치(2바이트) + FP16 경사(2바이트) + FP32 마스터 가중치(4바이트) + Adam의 1차 모멘트 $m$(4바이트) + 2차 모멘트 $v$(4바이트), 합쳐서 **파라미터당 16바이트**다([ZeRO](#/p/zero) 논문이 이 분해를 명시적으로 정리했다). 여기에 배치 크기·시퀀스 길이·층 수에 비례해 커지는 활성값 메모리가 따로 더해지며, [경사 체크포인팅](#/c/checkpointing)으로 이 활성값 부분을 크게 줄일 수 있다.'},
 {h:'7B 모델 구체적 숫자', d:'파라미터 70억(7B) 모델의 모델 상태(가중치+경사+옵티마이저)만 계산하면 $7\\times10^9 \\times 16\\text{bytes}\\approx 112\\text{GB}$ 가 나온다 — GPU 한 장(예: A100 80GB)에도 다 못 올라가는 크기라, [ZeRO](#/p/zero)/[FSDP](#/p/fsdp) 같은 상태 샤딩이나 여러 GPU 분산이 사실상 필수다. 여기에 활성값 메모리가 배치·시퀀스 길이에 따라 수십GB가 추가로 붙는다. 반면 **추론**은 옵티마이저 상태·경사가 필요 없고 가중치만 있으면 되므로, FP16 가중치 기준 $7\\times10^9\\times2\\text{bytes}=14\\text{GB}$ 로 학습의 1/8 이하다 — 여기에 [KV 캐시](#/c/kv-cache)가 시퀀스 길이·배치에 비례해 별도로 더해진다.'},
 {h:'실무에서', d:'모델 카드·논문에 파라미터 수만 적혀 있어도 위 공식으로 학습·추론 메모리를 즉석에서 어림잡을 수 있어야 GPU 사양을 고를 수 있다. 실무에서 자주 쓰는 어림 규칙은 "학습에는 파라미터당 약 16바이트(+활성값), 추론에는 파라미터당 2바이트(FP16 기준, +KV캐시)"다. 양자화([Quantization](#/c/quantization))로 추론 가중치를 INT8·INT4로 더 줄이면 이 2바이트가 1바이트·0.5바이트로 더 줄어든다.'}
],

math:[
 {tex:'N\\approx 12\\,n_{layer}\\,d_{model}^2', expr:'transformer non-embedding params', d:'디코더 전용 트랜스포머의 어텐션+FFN 파라미터 근사치. 실제로는 어휘 임베딩 $2Vd_{model}$ 을 더해야 전체 파라미터 수가 된다.'},
 {tex:'C\\approx 6ND', expr:'training compute', d:'$N$ 은 파라미터 수, $D$ 는 학습 토큰 수. 순전파 $2N$ + 역전파 약 $4N$ FLOPs/토큰의 합으로, 학습에 필요한 총 GPU 연산량을 어림잡는 데 쓴다.'},
 {tex:'\\text{train mem}\\approx 16N\\ \\text{bytes} + \\text{activations}', expr:'training memory (mixed precision + Adam)', d:'가중치(2B fp16+4B fp32)+경사(2B)+Adam 모멘트 2개(4B×2)=16바이트/파라미터. 활성값은 배치·시퀀스 길이·체크포인팅 여부에 따라 별도로 더해진다.'},
 {tex:'\\text{inference mem}\\approx 2N\\ \\text{bytes} + \\text{KV cache}', expr:'inference memory (fp16)', d:'가중치만 FP16으로 올리면 파라미터당 2바이트. 여기에 시퀀스 길이·배치에 비례하는 KV 캐시가 추가된다.'}
],

code:{lang:'python', d:'7B 모델 기준 학습·추론 메모리를 어림잡는 계산. 활성값·KV캐시는 별도로 더해야 한다는 것을 주석으로 명시했다.', src:'N = 7e9  # 파라미터 수\n\n# 학습 (혼합정밀도 + Adam): 파라미터당 16바이트\ntrain_model_state_gb = N * 16 / 1e9   # ~112 GB (+ activations)\n\n# 추론 (FP16 가중치만): 파라미터당 2바이트\ninfer_weight_gb = N * 2 / 1e9         # ~14 GB (+ KV cache)\n\n# 학습 연산량 (토큰 D개 학습 시)\nD = 300e9  # 예: 3000억 토큰\ntrain_flops = 6 * N * D               # ~1.26e22 FLOPs'},

diagram:{type:'stack', cap:'학습은 추론보다 훨씬 많은 상태를 GPU에 들고 있어야 한다.',
 layers:[
  {t:'FP16 가중치', note:'2바이트/파라미터'},
  {t:'FP16 경사', note:'2바이트/파라미터'},
  {t:'FP32 마스터 가중치', note:'4바이트/파라미터'},
  {t:'Adam m,v', note:'4+4바이트/파라미터'}
 ]},

confuse:[
 {a:'파라미터 수', b:'FLOPs', d:'파라미터 수는 모델에 저장된 숫자의 개수(메모리와 직결)이고, FLOPs는 그 모델로 데이터를 한 번 처리하는 데 필요한 연산 횟수(속도·전력과 직결)다. 같은 파라미터 수라도 MoE처럼 일부만 활성화하는 구조는 FLOPs가 훨씬 적을 수 있다.'},
 {a:'학습 메모리', b:'추론 메모리', d:'학습은 경사·옵티마이저 상태까지 들고 있어야 해서 파라미터당 약 16바이트가 필요하지만, 추론은 가중치만 있으면 되어 파라미터당 2바이트(FP16)면 충분하다 — 약 8배 차이가 난다.'},
 {a:'모델 상태 메모리', b:'활성값(activation) 메모리', d:'모델 상태(가중치·경사·옵티마이저)는 파라미터 수에만 비례하지만, 활성값 메모리는 배치 크기·시퀀스 길이·층 수에 비례해 따로 커진다. 배치를 키우다 OOM이 나는 것은 대개 후자 때문이다.'}
],

pitfalls:[
 '파라미터 수만 보고 "GPU 메모리(GB)와 같은 숫자(B)면 들어가겠지"라고 어림잡으면 학습 시 최소 8배(16바이트/2바이트) 이상 틀린다.',
 'FLOPs 계산에서 역전파 비용을 빼먹고 순전파 $2N$ 만 계산하면 실제 학습 연산량을 3배가량 과소평가하게 된다.',
 'KV 캐시·활성값처럼 배치·시퀀스 길이에 비례해 커지는 메모리를 무시하고 모델 상태만 계산하면, 긴 컨텍스트·큰 배치에서 예상보다 훨씬 빨리 메모리가 바닥난다.'
],

papers:['scaling-laws','chinchilla'],
terms:['mixed-precision-c','checkpointing','kv-cache','quantization']
})
