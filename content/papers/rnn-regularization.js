WIKI.paper({
slug:'rnn-regularization',
venue:'ICLR 2015',
authors:'Zaremba (NYU), Sutskever, Vinyals (Google Brain)',
arxiv:'1409.2329',

tldr:'[LSTM](#/p/lstm)에 [드롭아웃](#/p/dropout)을 적용하는 올바른 방법을 제시한 논문. 드롭아웃을 **순환 연결에는 걸지 않고 층 사이 비순환 연결에만** 건다는 단 하나의 규칙으로, "RNN에는 드롭아웃이 잘 안 먹힌다"는 당시 통념을 뒤집었다.',

context:'드롭아웃은 피드포워드 신경망의 과적합을 크게 줄이는 것으로 알려져 있었지만, RNN·LSTM에는 잘 적용되지 않는다는 것이 2014년 당시의 정설이었다. [Bayer et al. 2013](https://arxiv.org/abs/1312.4569)은 순환 구조가 드롭아웃의 노이즈를 시간축으로 증폭시켜 학습을 오히려 방해한다고 주장했다. 그 결과 실무에서는 드롭아웃 없이 과적합을 피하려고 RNN을 작게 유지할 수밖에 없었고, 이는 곧 성능 상한을 스스로 낮추는 선택이었다. 이 논문은 "드롭아웃 자체가 문제가 아니라 어디에 거는지가 문제"라는 가설로 이 통념을 검증한다.',

ideas:[
 {h:'드롭아웃을 비순환 연결에만 건다',
  lead:'같은 시점 층 사이($h_t^{l-1}\\to h_t^{l}$)에는 드롭아웃을 걸고, 시간을 가로지르는 순환 연결($h_{t-1}^{l}\\to h_t^{l}$)에는 걸지 않는다.',
  d:'표준 LSTM 갱신식 $(i,f,o,g)=T_{2n,4n}(h_t^{l-1},\\,h_{t-1}^{l})$에서, 이 논문은 입력 $h_t^{l-1}$에만 드롭아웃 연산자 $D$를 적용해 $D(h_t^{l-1})$로 바꾸고, 이전 시점의 은닉 상태 $h_{t-1}^{l}$은 그대로 둔다. 이 한 줄이 논문의 전부이자 핵심 기여다.'},
 {h:'왜 순환 연결엔 걸면 안 되는가 — 정보가 매 스텝 훼손된다',
  lead:'순환 연결에 드롭아웃을 걸면 오래 기억해야 할 정보가 타임스텝마다 반복적으로 손상된다.',
  d:'LSTM의 핵심은 셀 상태 $c_t$가 시간이 지나도 정보를 온전히 나를 수 있다는 것이다. 만약 $h_{t-1}\\to h_t$ 순환 경로에 드롭아웃을 걸면, $t-2$ 시점의 정보가 $t+2$ 시점에 도달하기까지 매 스텝 노이즈가 곱해져 장기 의존성을 학습하는 LSTM의 존재 이유 자체를 훼손한다. 반대로 비순환(층 간) 연결에만 걸면, 같은 정보가 네트워크를 통과하며 정확히 $L+1$번(층 수만큼)만 손상되고 **몇 스텝을 거치든 이 손상 횟수는 늘어나지 않는다** — 이것이 저자들이 Figure 3으로 보여주는 핵심 논증이다.'},
 {h:'큰 모델 + 강한 드롭아웃이 작은 모델보다 낫다',
  lead:'과적합 걱정 없이 은닉 유닛을 늘리고 50~65%의 높은 드롭아웃 비율로 정칙화한다.',
  d:'드롭아웃 없이 과적합을 피하려면 모델을 작게 유지해야 했다(이 논문의 비정칙 베이스라인은 층당 200유닛). 이 논문은 대신 층당 650유닛(medium)·1500유닛(large)의 훨씬 큰 LSTM을 쓰면서 비순환 연결에 50%·65%의 드롭아웃을 걸어, 모델을 키우는 쪽이 압도적으로 유리하다는 것을 보였다.'}
],

diagram:{type:'compare', cap:'드롭아웃을 어디에 거는가의 차이 — 이 논문 이전엔 이 구분이 없었다.',
 left:{t:'순환 연결에도 드롭아웃', items:['시간축 정보가 매 스텝 훼손','장기 의존성 학습 방해','RNN에 드롭아웃 효과 없다는 통념의 원인']},
 right:{t:'비순환 연결에만 드롭아웃', items:['순환 경로는 그대로 보존','정보 손상 횟수 = 층 수(고정)','과적합 크게 감소, 모델을 키울 수 있음']}},

math:[
 {expr:'(i,f,o,g) = (sigm,sigm,sigm,tanh)( T_2n,4n [ D(h_t^{l-1}); h_{t-1}^{l} ] )',
  tex:'\\begin{pmatrix}i\\\\f\\\\o\\\\g\\end{pmatrix}=\\begin{pmatrix}\\text{sigm}\\\\\\text{sigm}\\\\\\text{sigm}\\\\\\tanh\\end{pmatrix}T_{2n,4n}\\begin{pmatrix}\\mathbf{D}(h_t^{l-1})\\\\h_{t-1}^{l}\\end{pmatrix}',
  d:'드롭아웃 연산자 $\\mathbf{D}$(인자의 임의 부분집합을 0으로 만듦)가 **층 사이 입력 $h_t^{l-1}$에만** 적용되고, 이전 시점의 은닉 상태 $h_{t-1}^{l}$에는 적용되지 않는다. 이후 셀·은닉 상태 갱신 $c_t^l=f\\odot c_{t-1}^l+i\\odot g$, $h_t^l=o\\odot\\tanh(c_t^l)$은 표준 LSTM과 동일하다.'}
],

numbers:[
 {k:'PTB 테스트 perplexity · 비정칙', v:'114.5', d:'과적합을 피하려 층당 200유닛으로 축소한 베이스라인'},
 {k:'PTB 테스트 perplexity · medium 정칙', v:'82.7', d:'층당 650유닛, 비순환 연결 드롭아웃 50%'},
 {k:'PTB 테스트 perplexity · large 정칙', v:'78.4', d:'층당 1500유닛, 비순환 연결 드롭아웃 65%'},
 {k:'앙상블 최고 성능', v:'68.7', d:'large 정칙 LSTM 38개 평균, 단일 모델(114.5) 대비 대폭 개선'},
 {k:'기울기 클리핑', v:'norm 5(medium) / 10(large)', d:'미니배치 크기로 정규화한 gradient norm 상한'}
],

impact:'이 논문 이후 "LSTM에 드롭아웃을 걸 때는 비순환 연결에만 건다"는 규칙이 사실상 표준 관행이 됐다. 복잡한 새 아키텍처나 정칙화 기법을 발명한 것이 아니라, 기존 기법을 **어디에 적용하는지**를 정확히 짚어낸 것이 기여의 전부라는 점이 특이하다. 언어 모델링뿐 아니라 음성 인식·이미지 캡셔닝·기계번역 등 이 논문이 실험한 네 과제 모두에서 일관되게 과적합이 줄었다.',

legacy:[
 '**LSTM 학습의 사실상 표준** — 이후 대부분의 LSTM/GRU 기반 시퀀스 모델 구현체([TensorFlow](#/p/tensorflow), PyTorch 등)가 이 방식의 드롭아웃을 기본 옵션으로 제공',
 '**"기법이 아니라 배치가 중요하다"는 선례** — 이후 정칙화·정규화 기법 연구에서 어디에 적용하는지를 먼저 따지는 관행에 영향',
 '**[seq2seq](#/p/seq2seq)·번역 모델의 과적합 대응 표준 도구** — [ConvS2S](#/p/convs2s) 이전 세대의 LSTM 기반 번역기들이 이 정칙화 방식을 채택'
],

pitfalls:[
 '**"드롭아웃을 LSTM에 거는 방법"이 아니라 "어디에 걸지 않는지"가 핵심이다.** 비율을 튜닝하는 문제가 아니라 순환 연결이라는 특정 경로를 완전히 예외로 둬야 한다는 구조적 규칙이다 — 일반 드롭아웃 구현을 그대로 RNN에 적용하면 이 논문이 고친 문제를 그대로 재현한다.',
 '**같은 아이디어를 독립적으로 발견한 팀이 있다.** [Pham et al. 2013](https://arxiv.org/abs/1312.4569)이 손글씨 인식에 같은 방법을 이미 적용했고, 저자들도 이를 논문에서 인정한다 — "최초 발견"이 아니라 폭넓은 실증과 대중화가 이 논문의 기여다.',
 '**모든 연결에 드롭아웃을 안 거는 것이 아니다.** 입력 임베딩과 층 사이 연결에는 여전히 드롭아웃이 걸린다 — "RNN에는 드롭아웃을 아예 쓰지 않는다"로 오해하기 쉽다.'
],

figures:[
 {f:'fig2-dropout-placement.png',
  cap:'가로 화살표(같은 층에서 시간을 가로지르는 순환 연결)는 실선 — 드롭아웃이 걸리지 않는다. 세로 점선 화살표(층과 층 사이, 입력에서 출력으로)에만 드롭아웃이 걸린다. 두 줄은 각각 LSTM 두 층을 나타낸다.',
  src:'원문 Figure 2, p.3'}
],

quotes:[
 {t:'The main idea is to apply the dropout operator only to the non-recurrent connections.',
  src:'Section 3.2, p.2'}
],

links:[
 {t:'arXiv 1409.2329 — Recurrent Neural Network Regularization', u:'https://arxiv.org/abs/1409.2329'},
 {t:'GitHub: wojzaremba/lstm (원저자 구현)', u:'https://github.com/wojzaremba/lstm'}
]
});
