WIKI.concept({
  slug:'moe',

  tldr:'층 하나를 여러 전문가로 쪼개고 토큰마다 일부만 켜서, 파라미터는 늘리되 토큰당 연산량은 그대로 두는 구조.',

  why:'요즘 큰 모델 상당수가 MoE 다. "총 파라미터 671B, 활성 37B" 같은 표기를 읽으려면 이 구조를 알아야 하고, 서빙할 때 메모리는 총 파라미터를 따라가는데 속도는 활성 파라미터를 따라간다는 비대칭이 비용 계산을 완전히 바꾼다.',

  sections:[
    {h:'무엇인가', d:'Transformer 블록의 FFN 자리에 전문가(expert) 여러 개를 두고, 라우터가 각 토큰마다 그중 k개만 고른다. 나머지 전문가는 그 토큰에 대해 아예 계산하지 않는다. 전체 파라미터는 전문가 수만큼 늘지만 토큰 하나가 실제로 통과하는 파라미터는 k개분뿐이다. 이것을 조건부 계산(conditional computation)이라 부른다.'},
    {h:'왜 통하나', d:'스케일링 법칙은 파라미터가 많을수록 손실이 낮아진다고 말하지만, 밀집(dense) 모델은 파라미터를 늘리면 연산량도 같이 는다. MoE 는 그 연결을 끊는다. 같은 연산 예산으로 더 많은 파라미터를 가질 수 있고, 전문가들이 서로 다른 종류의 입력을 맡도록 분화하리라는 기대가 깔려 있다.'},
    {h:'라우팅이 어려운 이유', d:'라우터는 이산적 선택을 하는데 그것을 미분 가능하게 학습시켜야 한다. 그리고 특정 전문가로만 토큰이 몰리면 나머지가 놀고 학습도 안 된다 — 부하 불균형(load imbalance)이다. 보조 손실로 균형을 강제하는 것이 표준이고, [Expert Choice](#/p/expert-choice) 는 아예 전문가가 토큰을 고르게 뒤집어 이 문제를 구조적으로 없앴다(대신 자기회귀 생성에서 인과성이 깨지는 대가가 있다).'},
    {h:'실무에서', d:'총 파라미터와 활성 파라미터를 반드시 구분해서 읽어야 한다. GPU 메모리에는 모든 전문가가 올라가야 하므로 메모리 요구는 총 파라미터를 따른다. 반면 지연시간은 활성 파라미터에 가깝다. 즉 MoE 는 "메모리는 비싸고 연산은 싼" 환경에서 유리하다. 미세조정이 밀집 모델보다 불안정하다는 보고도 많다.'}
  ],

  math:[
    {tex:'y=\\sum_{i\\in\\mathcal{T}} g_i(x)\\,E_i(x)', expr:'MoE 층의 출력',
     d:'$E_i$ 는 i번째 전문가, $g_i(x)$ 는 라우터가 준 가중치다. $\\mathcal{T}$ 는 라우터가 고른 상위 k개 전문가의 집합이고, 나머지 전문가는 $g_i=0$ 이라 계산 자체를 건너뛴다.'}
  ],

  confuse:[
    {a:'총 파라미터', b:'활성 파라미터', d:'전자는 메모리를, 후자는 연산량과 지연시간을 결정한다. "16.4B MoE" 라고만 쓰면 둘 중 무엇인지 알 수 없다.'},
    {a:'MoE', b:'앙상블', d:'앙상블은 여러 모델을 모두 돌려 평균 내지만, MoE 는 일부만 골라 돌린다. 목적도 반대다 — 앙상블은 정확도를 위해 비용을 더 쓰고, MoE 는 비용을 아끼려고 쪼갠다.'},
    {a:'MoE', b:'멀티헤드 어텐션', d:'둘 다 "여러 개로 나눈다" 지만 멀티헤드는 모든 헤드를 항상 계산한다. MoE 의 핵심은 안 쓰는 쪽을 건너뛰는 것이다.'}
  ],

  pitfalls:[
    '"파라미터 수" 만 보고 밀집 모델과 성능을 비교하면 안 된다. 연산량 기준으로 비교해야 공정하다.',
    '전문가가 실제로 의미 있는 분화를 한다는 보장은 없다. 해석 가능한 분담이 관찰되는 경우도 있지만 기대만큼은 아니다.',
    '서빙이 까다롭다 — 전문가가 여러 장치에 흩어지면 토큰마다 통신이 생기고, 배치 안에서 토큰들이 서로 다른 전문가로 가면 부하가 들쭉날쭉해진다.'
  ],

  papers:['moe-shazeer','switch','glam','expert-choice','deepseek-moe','st-moe'],
  terms:['transformer-block','param-count','model-selection','cost-optimization']
})
