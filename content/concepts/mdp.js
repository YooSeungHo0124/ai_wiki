WIKI.concept({
slug:'mdp',

tldr:'에이전트-환경 상호작용을 상태·행동·전이확률·보상·할인율 다섯 요소로 정형화한 수학적 틀로, 강화학습 알고리즘 대부분이 이 위에서 정의된다.',

why:'[RLHF](#/c/rlhf)의 "정책이 토큰을 하나씩 생성하며 보상을 받는다"는 설명도, 로봇이 관절 토크를 출력하는 문제도 전부 MDP 로 정식화된다. MDP 를 모르면 왜 "현재 상태만 보고 결정해도 충분하다"는 가정이 강화학습 전체의 기반인지, 그리고 그 가정이 깨질 때(부분 관측) 무엇이 바뀌는지를 이해할 수 없다.',

sections:[
 {h:'다섯 요소', d:'MDP 는 튜플 $(S,A,P,R,\\gamma)$ 로 정의된다. $S$ 는 가능한 상태 집합, $A$ 는 행동 집합, $P(s"|s,a)$ 는 상태 $s$ 에서 행동 $a$ 를 했을 때 다음 상태 $s"$ 로 전이할 확률, $R(s,a)$ 는 그 전이에서 받는 보상, $\\gamma\\in[0,1]$ 은 할인율이다. 에이전트는 이 다섯 요소가 정의하는 세계 안에서 누적 할인 보상을 최대화하는 정책 $\\pi(a|s)$ 를 찾는다.'},
 {h:'마르코프 성질', d:'"마르코프(Markov)"라는 이름은 다음 상태의 확률분포가 과거 전체 이력이 아니라 **현재 상태와 현재 행동에만** 의존한다는 가정에서 온다: $P(s_{t+1}|s_t,a_t,s_{t-1},a_{t-1},\\dots)=P(s_{t+1}|s_t,a_t)$. 이 가정 덕분에 정책도 가치 함수도 "현재 상태"만 입력으로 받으면 충분해지고, 과거를 전부 들고 다닐 필요가 없어진다. 이것이 강화학습 알고리즘 대부분이 간단한 함수(신경망 하나)로 정책을 표현할 수 있는 이유다.'},
 {h:'상태 설계가 성능을 좌우한다', d:'현실 문제는 있는 그대로는 마르코프 성질을 만족하지 않는 경우가 많다 — 예를 들어 화면 한 프레임만으로는 공이 어느 방향으로 움직이는지 알 수 없다. 이럴 때는 최근 몇 프레임을 스택으로 쌓거나 속도·가속도 같은 파생 특징을 상태에 포함시켜 "충분 통계량"에 가깝게 만든다. 상태를 어떻게 정의하느냐가 문제를 실제로 풀 수 있는지 없는지를 가르는 경우가 많다.'},
 {h:'부분 관측 MDP(POMDP)', d:'에이전트가 진짜 상태 $s$ 를 직접 보지 못하고 그로부터 파생된 관측 $o$ 만 받는 경우를 POMDP 라 한다 — 카메라 영상만으로 로봇 팔의 정확한 관절 각도 전체를 알 수 없는 경우가 그렇다. POMDP 에서는 관측 하나만으로 마르코프 성질이 성립하지 않으므로, RNN·트랜스포머처럼 과거 관측을 누적해 내부 상태(belief state)를 유지하는 구조가 필요해진다. [RLHF](#/c/rlhf)의 언어 생성도 엄밀히는 "지금까지 생성한 전체 토큰 시퀀스"가 상태라서, 트랜스포머의 어텐션이 이 역할을 한다.'}
],

math:[
 {tex:'\\text{MDP}=(S,A,P,R,\\gamma)', expr:'MDP definition', d:'$S$ 상태 집합, $A$ 행동 집합, $P(s"|s,a)$ 전이확률, $R(s,a)$ 보상함수, $\\gamma$ 할인율. 이 다섯 요소만 정의되면 "최적 정책이 무엇인가"라는 질문이 수학적으로 잘 정의된 문제가 된다.'}
],

diagram:{type:'loop', cap:'MDP 의 상태-행동-전이 순환.',
 nodes:[
  {t:'상태 $s_t\\in S$'},
  {t:'행동 선택', s:'$a_t\\in A$'},
  {t:'상태 전이', s:'$P(s_{t+1}|s_t,a_t)$'},
  {t:'보상 수령', s:'$R(s_t,a_t)$'}
 ]},

confuse:[
 {a:'MDP', b:'POMDP', d:'MDP 는 에이전트가 현재 상태 $s$ 를 정확히 관측한다고 가정하고, POMDP 는 상태에서 파생된 불완전한 관측 $o$ 만 받는다고 가정한다. 실무에서 "지금 이 문제가 마르코프한가"를 먼저 따져보지 않으면, 마르코프를 가정한 알고리즘(단일 프레임 입력의 DQN 등)이 왜 잘 안 되는지 원인을 못 찾는다.'},
 {a:'상태(state)', b:'관측(observation)', d:'상태는 환경의 "진짜" 전체 정보이고 관측은 에이전트가 실제로 받는 신호다. 완전 관측 환경에서는 둘이 같다고 가정하지만, 이미지 한 장·센서 값 하나처럼 부분적인 정보만 받는 경우가 실무에서는 오히려 더 흔하다.'},
 {a:'전이확률 $P$', b:'정책 $\\pi$', d:'전이확률은 환경이 정하는 것으로 에이전트가 바꿀 수 없다. 정책은 에이전트가 학습으로 바꾸는 것이다. 이 둘을 헷갈리면 "환경이 확률적이라 정책이 확률적이어야 한다"는 오해로 이어질 수 있는데, 결정론적 전이에서도 확률적 정책(탐험을 위해)은 유용하다.'}
],

pitfalls:[
 '문제를 MDP 로 정식화할 때 상태에 필요한 정보를 빠뜨리면(예: 속도 없이 위치만) 마르코프 성질이 깨져 어떤 알고리즘을 써도 최적 정책에 수렴하지 못한다.',
 '할인율 $\\gamma$ 를 임의로 정하지 말고 "몇 스텝 뒤 보상까지 실질적으로 고려하고 싶은가"를 먼저 정해야 한다 — $\\gamma$ 가 클수록 유효 계획 지평(effective horizon)이 길어진다.',
 'MDP 는 보상 함수 $R$ 이 "정확히 원하는 목표"를 표현한다고 가정한다 — 실제로는 이 가정이 자주 깨지고, 그 결과가 [보상 설계와 보상 해킹](#/c/reward-design)이다.'
],

papers:['dqn'],
terms:['rl-basics','policy-value','bellman','exploration']
});
