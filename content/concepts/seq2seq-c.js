WIKI.concept({
slug:'seq2seq-c',

tldr:'시퀀스 투 시퀀스는 길이가 다를 수 있는 입력 시퀀스를 받아 길이가 다를 수 있는 출력 시퀀스를 생성하는 문제 정식화로, 번역·요약·음성 인식의 공통 틀이다.',

why:'분류는 "입력 하나에 라벨 하나"지만 번역은 "문장 하나에 문장 하나"이고 길이조차 다르다. 이 근본적인 구조 차이를 모르면 왜 번역 모델이 분류 모델과 완전히 다른 손실 함수·디코딩 절차(빔 서치 등)를 필요로 하는지 이해할 수 없다.',

sections:[
 {h:'문제 정식화', d:'seq2seq는 입력 시퀀스 $x_1,\\dots,x_m$ 을 받아 출력 시퀀스 $y_1,\\dots,y_n$ 을 생성하는데, $m$ 과 $n$ 이 같을 필요가 없다는 것이 핵심이다. "I love you"(3단어)를 "나는 당신을 사랑합니다"(4어절)로 번역하듯, 입력과 출력의 길이·구조가 원래 맞지 않는 과제를 다루기 위한 정식화다. [Sequence to Sequence Learning](#/p/seq2seq) 논문이 이 틀을 신경망으로 처음 체계화했다.'},
 {h:'인코더-디코더로 구현', d:'초기 seq2seq는 인코더 RNN이 입력 전체를 하나의 고정 길이 벡터로 압축하고, 디코더 RNN이 그 벡터로부터 출력을 한 토큰씩 생성하는 구조였다. 그러나 입력이 길어지면 고정 길이 벡터 하나에 모든 정보를 욱여넣는 것이 병목이 됐고, [Bahdanau attention](#/p/bahdanau)이 디코더가 매 스텝마다 입력의 관련 부분을 다시 찾아볼 수 있게 해 이 병목을 풀었다. 이후 [Transformer](#/p/transformer)가 RNN을 완전히 어텐션으로 대체하며 지금의 [인코더-디코더](#/c/encoder-decoder) 구조로 정착했다.'},
 {h:'티처 포싱 학습', d:'학습 시에는 디코더가 이전 스텝에서 자신이 만든 (틀릴 수도 있는) 출력을 다시 입력으로 쓰는 대신, 정답 시퀀스를 한 칸씩 밀어 입력으로 준다 — 이를 teacher forcing이라 한다. 이렇게 하면 학습이 안정적이고 병렬화하기 쉽지만, 추론 시에는 정답이 없어 모델 자신의 이전 출력을 다시 입력으로 써야 하므로 학습과 추론 사이에 괴리(exposure bias)가 생긴다 — 학습 때는 한 번도 틀린 입력을 본 적 없는 모델이 추론 때는 스스로의 실수를 계속 입력받게 되는 구조적 문제다.'},
 {h:'디코딩 전략', d:'출력을 한 토큰씩 만들 때 매번 가장 확률 높은 토큰만 그리디(greedy)하게 고르면 전체적으로 최적이 아닌 문장이 나올 수 있다. [빔 서치](#/c/beam-search)는 여러 후보 시퀀스를 동시에 추적해 더 나은 전체 확률을 갖는 시퀀스를 찾는다. 출력 시퀀스는 언제 끝날지 미리 알 수 없어 종료 토큰(`<eos>`)을 별도로 두고 이 토큰이 나오면 생성을 멈추는 방식을 쓴다 — 입력·출력 길이가 가변적이라는 seq2seq의 정의 자체가 이 종료 판단 메커니즘을 요구한다.'}
],

diagram:{type:'flow', cap:'가변 길이 입력이 가변 길이 출력으로 바뀌는 과정.',
 nodes:[
  {t:'입력 시퀀스', s:'길이 m'},
  {t:'인코더', s:'문맥 표현으로 압축'},
  {t:'디코더', s:'한 토큰씩 자기회귀 생성'},
  {t:'출력 시퀀스', s:'길이 n, eos까지'}
 ]},

confuse:[
 {a:'seq2seq', b:'단순 분류', d:'분류는 입력 하나에 고정된 라벨 집합 중 하나를 맞히는 문제라 출력 형태가 고정돼 있지만, seq2seq는 출력 자체가 가변 길이 시퀀스라 "언제 멈출지"까지 모델이 결정해야 한다.'},
 {a:'teacher forcing', b:'추론 시 자기회귀 생성', d:'teacher forcing은 학습 중 정답을 디코더 입력으로 강제하는 기법이고, 추론에서는 정답이 없으므로 모델이 만든 이전 토큰을 다음 입력으로 쓴다. 이 학습-추론 방식 차이가 exposure bias의 원인이다.'},
 {a:'[인코더-디코더](#/c/encoder-decoder)', b:'seq2seq', d:'seq2seq는 "가변 길이 입력을 가변 길이 출력으로 바꾼다"는 문제 정식화이고, 인코더-디코더는 그 문제를 푸는 대표적인 아키텍처 패턴이다. seq2seq 문제를 반드시 인코더-디코더로만 풀어야 하는 것은 아니다 — 디코더-온리 LLM도 입력+출력을 한 시퀀스로 이어 붙여 사실상 seq2seq를 수행한다.'}
],

pitfalls:[
 'teacher forcing으로 학습된 모델이 검증 손실은 낮은데 실제 추론에서 품질이 떨어지면, exposure bias로 인한 오류 누적을 의심해야 한다.',
 '출력 길이를 입력 길이와 같다고 가정하고 파이프라인을 짜면(예: 고정 길이 버퍼) 번역·요약처럼 길이가 크게 달라지는 과제에서 출력이 잘리는 버그가 난다.',
 '그리디 디코딩만 쓰고 [빔 서치](#/c/beam-search)를 안 쓰면 모델 성능 자체는 같아도 최종 출력 품질이 체감상 떨어질 수 있다 — 디코딩 전략도 성능의 일부다.'
],

papers:['seq2seq','bahdanau','t5'],
terms:['encoder-decoder','beam-search','nlp-tasks']
});
