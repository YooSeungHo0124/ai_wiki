WIKI.concept({
slug:'serving-arch',

tldr:'학습이 끝난 모델을 실제 요청에 응답하도록 배치하는 방식으로, 요청을 어디서 받고(온라인/배치), 모델을 어떻게 메모리에 올리고, 여러 요청을 어떻게 묶어 처리할지를 결정한다.',

why:'같은 모델이라도 서빙 구조를 어떻게 짜느냐에 따라 지연시간이 10배, 처리량이 100배 차이 날 수 있다. 모델 개발자가 "정확도 높은 모델을 만드는 것"까지만 신경 쓰고 서빙 구조를 모르면, 실제로는 못 쓸 정도로 느리거나 비싼 시스템을 만들게 된다.',

sections:[
 {h:'요청-응답 경로', d:'클라이언트 요청은 보통 로드밸런서 → API 서버 → 모델 서버(추론 실행) 순서를 거친다. 모델 서버는 모델을 미리 메모리(또는 GPU)에 올려두고 대기하다가, 요청이 오면 전처리 → 순전파(forward pass) → 후처리를 거쳐 응답한다. 이 "모델을 미리 로드해둔다"는 점이 핵심이다 — 요청마다 모델을 새로 불러오면 지연시간이 감당할 수 없이 커진다.'},
 {h:'배칭(batching)', d:'GPU 는 요청 하나씩 순차 처리하는 것보다 여러 요청을 한 번에 묶어(배치) 처리할 때 처리량이 훨씬 높다 — 행렬 연산의 병렬성을 제대로 활용하기 때문이다. 하지만 배치를 채우려고 요청을 무작정 기다리면 지연시간이 늘어난다. 이 트레이드오프를 다루는 것이 [배치 vs 실시간 추론](#/c/batch-vs-realtime)의 핵심이고, [vLLM](#/p/vllm) 같은 서빙 엔진은 이미 진행 중인 배치에 새 요청을 계속 끼워 넣는 연속 배칭(continuous batching)으로 지연시간과 처리량을 동시에 개선한다.'},
 {h:'수평 확장과 라우팅', d:'트래픽이 늘면 모델 서버를 여러 대로 복제(replica)해 로드밸런서가 요청을 나눠준다. GPU 메모리에 큰 모델(LLM 등)이 안 들어가면 하나의 모델을 여러 GPU 에 쪼개 올리는 병렬화(텐서 병렬·파이프라인 병렬)가 필요해진다. 어느 쪽이든 "요청이 늘어도 지연시간이 크게 늘지 않게" 여분의 용량을 미리 확보해두는 오토스케일링 정책이 함께 따라간다.'},
 {h:'상태 없는(stateless) 설계', d:'서빙 서버는 대부분 상태를 갖지 않도록(stateless) 설계한다 — 요청 하나의 처리가 다른 요청에 의존하지 않아야 서버를 자유롭게 늘리고 줄일 수 있다. 다만 LLM 의 [KV 캐시](#/c/kv-cache)처럼 세션 내에서 상태를 재사용해야 성능이 크게 좋아지는 경우, 상태 없는 설계와 캐시 재사용 사이에서 절충(세션을 특정 서버에 고정하는 라우팅 등)이 필요해진다.'}
],

diagram:{type:'flow', cap:'추론 요청이 서빙 시스템을 통과하는 경로.',
 steps:['클라이언트 요청','로드밸런서 라우팅','모델 서버 전처리','배치 구성·순전파','후처리 후 응답']},

confuse:[
 {a:'서빙(serving)', b:'배포(deployment)', d:'배포는 새 모델 버전을 서빙 환경에 올리는 "행위"(파이프라인의 한 단계)이고, 서빙은 그렇게 올라온 모델이 계속 요청에 응답하는 "상시 운영 상태"다. 배포는 순간이고 서빙은 지속이다.'},
 {a:'수직 확장', b:'수평 확장', d:'수직 확장은 서버 한 대의 GPU·메모리를 더 강력한 것으로 바꾸는 것이고, 수평 확장은 같은 서버를 여러 대로 복제하는 것이다. 큰 모델 하나가 GPU 한 장에 안 들어갈 때는 수직 확장(더 큰 GPU) 또는 모델 병렬화가 필요하고, 단순히 트래픽이 많을 때는 수평 확장이 비용 효율적이다.'},
 {a:'지연시간(latency)', b:'처리량(throughput)', d:'지연시간은 요청 하나가 응답을 받기까지 걸리는 시간이고, 처리량은 단위 시간당 처리 가능한 요청 수다. 배칭은 처리량을 높이지만 개별 요청의 지연시간을 늘릴 수 있어, 서빙 구조 설계는 이 둘 중 무엇을 우선할지 정하는 것에서 시작한다.'}
],

pitfalls:[
 '요청마다 모델을 디스크에서 새로 로드하는 구조는 개발 단계에서는 문제없어 보이다가 실제 트래픽에서 지연시간이 급증하는 흔한 초보 실수다 — 모델은 반드시 메모리에 상주시켜야 한다.',
 '배칭을 늘리면 처리량은 오르지만 배치를 채우려 기다리는 시간 때문에 개별 요청의 지연시간(특히 꼬리 지연시간, tail latency)이 늘어날 수 있다 — 처리량 지표만 보고 배치 크기를 올리면 사용자 체감 속도가 나빠질 수 있다.',
 '트래픽이 튀는(spiky) 서비스에서 오토스케일링 반응 속도가 느리면, 스케일업이 끝나기 전에 이미 요청이 쌓여 타임아웃이 발생한다 — 평균 트래픽이 아니라 피크 트래픽 기준으로 용량을 설계해야 한다.'
],

papers:['vllm','tensorflow'],
terms:['batch-vs-realtime','cost-optimization','kv-cache','model-registry']
});
