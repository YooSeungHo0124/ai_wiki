WIKI.concept({
slug:'vae-c',

tldr:'인코더가 입력을 하나의 점이 아니라 확률분포로 매핑하고 그 분포를 정규분포에 가깝게 규제해, 잠재 공간 어디서 샘플링해도 그럴듯한 데이터를 디코딩할 수 있게 만드는 생성 모델이다.',

why:'일반 [오토인코더](#/c/autoencoder)는 왜 생성 모델로 못 쓰는지, [잠재 공간](#/c/latent-space)을 "매끄럽게" 만든다는 말이 수식으로 정확히 무엇을 뜻하는지 모르면 VAE 가 왜 재매개화 트릭이라는 우회로를 쓰는지, KL 항이 왜 꼭 필요한지 이해할 수 없다.',

sections:[
 {h:'왜 그냥 오토인코더로는 생성이 안 되는가', d:'[오토인코더](#/c/autoencoder)는 각 입력 $x$ 를 잠재 공간의 한 점 $z$ 에 결정론적으로 대응시킬 뿐, $z$ 공간 전체가 어떤 분포를 이루는지는 신경 쓰지 않는다. 그 결과 학습 데이터들이 잠재 공간에 듬성듬성·불규칙하게 흩어지고, 그 사이의 빈틈이나 임의의 점 $z$ 를 디코딩하면 무엇이 나올지 보장이 없다 — 즉 "새 $z$ 를 뽑아서 새 데이터를 만든다"는 생성 절차 자체가 정의되지 않는다. VAE 는 이 문제를 "잠재 공간이 연속적이고 알려진 분포(보통 표준정규분포)를 따르도록" 강제해서 해결한다.'},
 {h:'인코더를 분포로', d:'VAE 의 인코더는 $z$ 하나를 출력하는 대신 근사 사후분포 $q_\\phi(z\\mid x)=\\mathcal{N}(\\mu_\\phi(x),\\sigma_\\phi^2(x))$ 의 평균과 분산을 출력한다. 디코더는 이 분포에서 뽑은 $z$ 로부터 $x$ 를 복원한다. 문제는 "샘플링"이라는 연산이 미분 불가능해서 $\\mu,\\sigma$ 로 역전파가 안 된다는 점이다 — 이를 해결하는 것이 재매개화 트릭(reparameterization trick)이다: $z=\\mu_\\phi(x)+\\sigma_\\phi(x)\\odot\\epsilon,\\ \\epsilon\\sim\\mathcal{N}(0,I)$ 로 무작위성을 $\\epsilon$ 이라는 외부 상수로 분리하면, $\\mu,\\sigma$ 는 그대로 미분 가능한 경로에 남아 표준적인 역전파로 학습할 수 있다.'},
 {h:'ELBO: 복원과 규제의 줄다리기', d:'VAE 는 데이터의 실제 로그가능도 $\\log p(x)$ 를 직접 계산할 수 없어서, 그 하한(ELBO, evidence lower bound)을 대신 최대화한다. ELBO 는 복원 항과 KL 항 두 개로 나뉜다 — 복원 항은 디코딩된 $\\hat{x}$ 가 원본 $x$ 를 잘 재현하도록 밀고, KL 항은 인코더가 만든 분포 $q_\\phi(z\\mid x)$ 를 사전분포 $p(z)=\\mathcal{N}(0,I)$ 에 가깝게 당긴다. 이 KL 규제가 없으면 인코더는 각 입력마다 분산을 0에 가깝게 줄여 사실상 결정론적 점으로 되돌아가버리고([오토인코더](#/c/autoencoder)와 다를 바 없어진다), 규제가 너무 강하면 모든 입력이 원점 근처의 같은 분포로 뭉개져 복원 품질이 무너진다([posterior collapse](#/c/vae-c)라 불리는 현상). [엔트로피·KL](#/c/entropy-kl)에서 다룬 KL 발산이 여기서는 "인코더가 만든 분포가 사전분포에서 얼마나 벗어났는가"를 재는 벌점으로 쓰인다.'},
 {h:'실무에서', d:'순수 VAE 로 생성한 이미지는 GAN·확산 모델보다 흐릿한 경향이 있다고 보고된다 — 복원 항이 픽셀 단위 오차를 평균적으로 최소화하는 방향으로 학습되기 때문에 세부 디테일보다 안전한 평균값 쪽으로 수렴하기 쉽다. 그래서 오늘날 VAE 단독으로 최종 이미지를 만드는 경우는 드물고, 대신 [LDM](#/p/ldm)처럼 "확산 모델이 작업할 압축된 [잠재 공간](#/c/latent-space)을 만드는 인코더·디코더" 역할, 또는 잠재를 이산 코드북으로 양자화하는 [VQ-VAE](#/p/vqvae) 형태로 더 많이 쓰인다.'}
],

math:[
 {tex:'z=\\mu_\\phi(x)+\\sigma_\\phi(x)\\odot\\epsilon,\\quad \\epsilon\\sim\\mathcal{N}(0,I)', expr:'재매개화 트릭', d:'인코더가 낸 평균 $\\mu_\\phi(x)$ 와 표준편차 $\\sigma_\\phi(x)$ 에, 학습과 무관한 표준정규 잡음 $\\epsilon$ 을 곱하고 더해 $z$ 를 만든다. 무작위성이 $\\epsilon$ 에만 있으므로 $\\mu_\\phi,\\sigma_\\phi$ 는 일반적인 미분 가능한 연산으로 취급되어 역전파가 가능하다.'},
 {tex:'\\mathcal{L}_{\\text{ELBO}}=\\underbrace{\\mathbb{E}_{q_\\phi(z\\mid x)}[\\log p_\\theta(x\\mid z)]}_{\\text{복원}}-\\underbrace{D_{KL}\\big(q_\\phi(z\\mid x)\\,\\|\\,p(z)\\big)}_{\\text{규제}}', expr:'evidence lower bound', d:'첫 항은 인코딩된 $z$ 로부터 $x$ 를 얼마나 잘 복원하는지(로그가능도), 둘째 항은 인코더가 만든 사후분포가 표준정규 사전분포 $p(z)$ 에서 얼마나 벗어났는지를 재는 KL 발산이다. 이 하한을 최대화(=음의 값을 최소화)하는 것이 학습 목표이며, 둘째 항이 바로 잠재 공간을 연속적이고 샘플링 가능하게 만드는 장치다.'}
],

diagram:{type:'flow', cap:'분포로 인코딩하고, 재매개화로 미분 가능하게 샘플링한 뒤 복원.',
 nodes:[
  {t:'x', s:'입력'},
  {t:'μ, σ', s:'인코더 출력'},
  {t:'z=μ+σ⊙ε', s:'재매개화'},
  {t:'디코더'},
  {t:'복원+KL', s:'ELBO'}
 ]},

confuse:[
 {a:'[autoencoder](#/c/autoencoder)', b:'VAE', d:'오토인코더는 $z$ 를 결정론적 점으로 뽑고 복원 오차만 최소화해 샘플링 가능한 생성 모델이 아니다. VAE 는 $z$ 를 분포로 뽑고 KL 항으로 그 분포를 사전분포에 규제해, 학습 후 사전분포에서 직접 $z$ 를 샘플링해 새 데이터를 생성할 수 있다.'},
 {a:'VAE', b:'[GAN](#/c/gan-c)', d:'VAE 는 명시적인 로그가능도 하한(ELBO)을 최대화하는 우도 기반 학습이라 안정적이지만 결과가 흐릿한 경향이 있다. GAN 은 생성자·판별자의 적대적 경쟁으로 학습해 더 선명한 결과를 내지만 학습이 불안정하고 모드 붕괴가 생길 수 있다.'},
 {a:'복원 손실', b:'KL 항', d:'복원 손실은 디코딩된 $\\hat{x}$ 가 원본과 얼마나 비슷한지를 재는 항이고, KL 항은 인코더가 만든 분포가 사전분포와 얼마나 다른지를 재는 항이다. 둘은 서로 반대 방향으로 당기며(복원은 정보를 더 담으려 하고 KL 은 정보를 사전분포 쪽으로 지우려 한다), 이 균형이 무너지면 posterior collapse 나 흐릿한 복원이 생긴다.'}
],

pitfalls:[
 '재매개화 트릭을 "학습을 안정시키는 기법" 정도로 생각하기 쉽지만, 실은 샘플링이라는 미분 불가능한 연산을 역전파 가능한 형태로 우회하는 구조적 장치다 — 이게 없으면 인코더 파라미터에 그래디언트가 아예 흐르지 않는다.',
 'ELBO 를 최대화하는 것이 진짜 로그가능도 $\\log p(x)$ 를 최대화하는 것과 완전히 같다고 착각하기 쉽지만, ELBO 는 어디까지나 하한이다 — 인코더 분포 $q_\\phi(z\\mid x)$ 가 진짜 사후분포에서 멀수록 이 하한과 실제 로그가능도 사이의 간극(그 자체가 KL 발산이다)이 커진다.',
 'KL 항의 가중치를 무심코 키우면 posterior collapse(모든 입력이 같은 분포로 뭉개짐)가 생겨 복원 품질이 무너질 수 있다 — 복원과 규제는 항상 트레이드오프 관계다.',
 'VAE 의 흐릿함을 항상 "모델이 부족해서"라고 오인하기 쉽지만, 픽셀 단위 복원 손실(가우시안 우도 가정)이 본질적으로 평균으로 수렴하는 경향을 만든다는 구조적 원인이 크다.'
],

papers:['vae','vqvae'],
terms:['autoencoder','entropy-kl','latent-space','gan-c']
});
