WIKI.concept({
slug:'eval-harness',

tldr:'벤치마크 점수를 실제로 산출하는 프롬프트 형식·정답 추출·채점 코드 전체를 가리키며, 같은 모델이라도 하네스가 다르면 점수가 달라진다.',

why:'"이 모델이 MMLU 에서 85점을 받았다"는 문장은 하네스를 명시하지 않으면 사실상 비교 불가능한 정보다. 리더보드에 올라온 두 모델의 점수 차이가 실제 능력 차이인지, 각자 다른 하네스로 잰 결과의 우연한 차이인지 구분하지 못하면 잘못된 모델을 고르는 실무적 실수로 이어진다. 모델을 선택하는 의사결정이 벤치마크 점수 하나에 달려 있는 경우가 많다는 점에서 이 문제는 사소하지 않다.',

sections:[
 {h:'하네스가 하는 일', d:'벤치마크는 문제와 정답 집합일 뿐이고, 그 문제를 모델에게 어떤 형식으로 보여주고(프롬프트 템플릿), 모델의 자유 형식 출력에서 정답을 어떻게 골라내고(정답 추출), 그것을 정답과 어떻게 비교해 맞았다 틀렸다를 가를지(채점 로직)는 전부 eval harness 가 정한다. 같은 벤치마크 문제라도 하네스 구현이 다르면 이 세 단계 각각에서 결과가 갈릴 수 있다. 즉 "벤치마크 점수"라고 부르는 하나의 숫자는 사실 데이터셋과 하네스 구현이 결합된 결과물이다.'},
 {h:'재현 문제: 형식', d:'같은 질문이라도 객관식 선택지를 "A)"로 표시하는지 "1."로 표시하는지, 시스템 프롬프트를 넣는지, 질문 앞에 몇 개의 예시(few-shot)를 붙이는지에 따라 모델의 답이 달라질 수 있다. 프롬프트 템플릿 하나만 바뀌어도 같은 모델이 같은 벤치마크에서 다른 점수를 낼 수 있다.'},
 {h:'재현 문제: 추출', d:'모델이 "정답은 B입니다" 처럼 서술형으로 답했을 때 이를 정규식으로 "B"를 뽑아내는 방식, 마지막 줄만 보는 방식, 특정 형식(예: 마크다운 굵게 표시)을 요구하는 방식 등 정답 추출 규칙이 하네스마다 다르다. 모델이 정답을 알고 있어도 하네스가 기대하는 형식과 다르게 답하면 추출에 실패해 오답으로 채점될 수 있다.'},
 {h:'재현 문제: 예시', d:'few-shot 벤치마크는 프롬프트에 넣는 예시 개수·순서·선택 방식이 점수에 영향을 준다 — 예시를 몇 개 넣을지, 무작위로 고를지 고정할지가 하네스마다 다르다. 채점 시 대소문자·공백·구두점을 어떻게 정규화하는지도 하네스마다 미묘하게 달라, 의미상 맞는 답을 형식 차이로 틀렸다고 채점하는 경우가 생긴다.'},
 {h:'리더보드의 위험', d:'서로 다른 랩이 각자의 하네스로 자체 측정한 점수를 하나의 리더보드에 나란히 올리면, 그 차이가 모델 능력 차이인지 하네스 차이인지 분간할 수 없다. [HELM](#/p/helm) 은 여러 모델을 **동일한 하네스·동일한 프롬프트**로 일괄 평가해 이 문제를 줄이려는 시도다 — "같은 잣대로 쟀다"는 점 자체가 HELM 의 핵심 기여다. [LiveCodeBench](#/p/livecodebench) 는 코드 벤치마크에서 흔한 또 다른 재현성 문제 — 문제가 학습 데이터에 이미 포함됐을 가능성(오염) — 를 해결하기 위해 모델 학습 이후 공개된 문제만 골라 평가하는 시점 기반 접근을 취한다.'},
 {h:'실무에서', d:'벤치마크 점수를 인용할 때는 하네스 이름과 버전, few-shot 개수, 프롬프트 템플릿 출처를 함께 밝혀야 재현·비교가 가능하다. 자체적으로 모델을 평가할 때는 비교 대상 모델들을 반드시 같은 하네스·같은 프롬프트로 돌려야 하며, 서로 다른 출처의 공개 점수를 그대로 표로 옮겨 비교하는 것은 피해야 한다.'},
 {h:'자체 하네스를 만들 때', d:'사내 평가 파이프라인을 새로 구축할 때는 프롬프트 템플릿·정답 추출 규칙·정규화 방식을 코드로 고정하고 버전 관리해야, 몇 달 뒤 "그때 그 점수가 어떻게 나온 건지" 재현할 수 있다. 모델 버전이 바뀔 때마다 같은 하네스로 전체 재평가를 자동화해두면, 점수 변화가 모델 때문인지 하네스 때문인지 헷갈릴 일이 줄어든다.'}
],

diagram:{type:'flow', cap:'같은 벤치마크, 다른 하네스가 점수를 바꾸는 세 지점.',
 steps:['문제·정답 집합(고정)','프롬프트 템플릿 적용','모델 출력 생성','정답 추출 규칙 적용','채점(정규화 포함)']},

confuse:[
 {a:'벤치마크', b:'eval harness', d:'벤치마크는 문제와 정답의 고정된 집합(데이터셋)이고, eval harness 는 그 문제를 모델에게 어떻게 묻고 답을 어떻게 채점할지를 정하는 코드다. 같은 벤치마크라도 하네스가 다르면 점수가 달라질 수 있다.'},
 {a:'하네스 차이', b:'모델 버전 차이', d:'같은 이름의 모델이라도 API 제공자가 내부적으로 버전을 조용히 업데이트하면 점수가 달라질 수 있는데, 이는 하네스가 아니라 모델 자체가 바뀐 것이다. 점수 변화의 원인이 하네스인지 모델 버전인지 구분하려면 모델 버전을 고정하고 하네스만 통제된 비교가 필요하다.'},
 {a:'[HELM](#/p/helm)', b:'개별 하네스(lm-eval-harness 등)', d:'HELM 은 여러 모델을 동일 조건으로 비교하는 것을 목표로 한 평가 프레임워크·리더보드에 가깝고, 개별 도구는 특정 벤치마크를 어떤 형식으로 채점할지 구현한 실행 코드다. HELM 도 내부적으로는 특정 하네스 구현을 쓴다.'},
 {a:'오염(contamination)', b:'하네스 차이', d:'오염은 벤치마크 문제가 모델의 학습 데이터에 이미 포함돼 점수가 부풀려지는 문제이고, 하네스 차이는 같은 문제·같은 모델이라도 채점 방식이 달라 점수가 달라지는 문제다. 둘 다 리더보드 비교를 신뢰하기 어렵게 만들지만 원인과 해결책이 다르다 — [LiveCodeBench](#/p/livecodebench) 는 오염 문제를, HELM 은 하네스 차이 문제를 겨냥한다.'},
 {a:'zero-shot 평가', b:'few-shot 평가', d:'zero-shot 은 예시 없이 질문만 던져 모델의 기본 능력을 재는 것이고, few-shot 은 프롬프트에 몇 개의 풀이 예시를 먼저 보여준 뒤 질문을 던지는 것이다. 같은 벤치마크라도 이 설정이 다르면 점수가 크게 달라질 수 있어, 비교할 때 반드시 몇-shot 인지 명시해야 한다.'}
],

pitfalls:[
 '"모델 A가 벤치마크 X에서 모델 B보다 높다"는 리더보드 문구만 보고 실제 능력 차이라고 단정하기 쉽지만, 서로 다른 하네스로 측정했다면 그 차이는 무의미할 수 있다.',
 '자체 평가를 할 때 프롬프트 템플릿이나 few-shot 예시 선택을 문서화하지 않으면, 몇 달 뒤 같은 평가를 재현하려 해도 같은 점수가 나오지 않는다.',
 '모델이 정답을 알고도 형식이 하네스의 정답 추출 규칙과 안 맞아 오답 처리되는 경우를 "모델이 틀렸다"고 오해하기 쉽다 — 하네스의 추출 로직을 먼저 확인해야 한다.',
 '공개된 리더보드 점수를 그대로 인용하면서 그 점수가 어떤 하네스·프롬프트·few-shot 설정으로 나왔는지 확인하지 않는 경우가 흔하다 — 출처 문서의 각주까지 확인해야 실제 비교 가능성을 판단할 수 있다.',
 '벤치마크 점수 하나가 모델의 전반적인 실력을 대표한다고 여기기 쉽지만, 실제 배포 환경의 입력 분포는 벤치마크 문제 분포와 다른 경우가 많아 벤치마크 1위 모델이 실제 서비스에서는 기대만큼 성능이 안 나올 수 있다.'
],

code:{lang:'text', d:'같은 벤치마크를 서로 다른 하네스 설정으로 돌렸을 때 점수 차이가 나는 지점의 예.', src:
'harness_A: {template:"A) B) C) D)", few_shot:5, extractor:"regex last-line"}\n'+
'harness_B: {template:"1. 2. 3. 4.", few_shot:0, extractor:"first-token"}\n'+
'# 같은 모델, 같은 문제셋인데도 harness_A와 B는 다른 점수를 낼 수 있다\n'+
'# -> 비교 시 반드시 하네스 이름·버전·few_shot 수를 함께 기록'},

papers:['helm','mmlu','livecodebench'],
terms:['benchmark-pitfall','data-leakage','ab-test','inference-params']
});
