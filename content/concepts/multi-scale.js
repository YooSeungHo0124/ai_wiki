WIKI.concept({
slug:'multi-scale',

tldr:'다중 스케일 처리는 서로 다른 해상도의 특징 맵을 함께 써서 크고 작은 물체를 동시에 잘 검출하는 기법으로, 특징 피라미드가 이를 구현하는 표준 방식이다.',

why:'같은 사진 안에 멀리 있는 사람(작게 찍힘)과 가까이 있는 사람(크게 찍힘)이 함께 있으면, 특징 맵 한 층만으로는 둘 다 잘 잡기 어렵다. 검출 모델의 성능 표에서 "소형 물체(APs)가 유독 낮다"는 결과를 보면 가장 먼저 의심해야 할 것이 다중 스케일 설계다.',

sections:[
 {h:'왜 한 층으로는 부족한가', d:'CNN 백본은 층이 깊어질수록 [수용 영역](#/c/receptive-field)이 넓어지고 해상도는 낮아진다. 얕은 층은 해상도가 높아 작은 물체의 위치는 잘 담지만 문맥 정보(의미)가 약하고, 깊은 층은 의미는 강하지만 해상도가 낮아 작은 물체가 몇 픽셀로 뭉개지거나 아예 사라진다. 백본 마지막 층 하나만 헤드에 넣는 구조는 이 때문에 소형 물체 검출에서 체계적으로 불리하다.'},
 {h:'특징 피라미드', d:'[FPN](#/p/fpn)은 백본의 여러 깊이에서 나온 특징 맵을 층 하나로 합치지 않고, 각 스케일별로 예측을 하되 서로 정보를 주고받게 한다. 깊은 층(의미 강함, 해상도 낮음)의 정보를 위쪽 경로(top-down)로 전달해 얕은 층(해상도 높음)과 합치면, 해상도가 높으면서도 의미가 강한 특징 맵을 여러 단계에서 얻는다. 검출 헤드는 이 여러 스케일 각각에서 독립적으로 물체를 찾고, 결과를 합쳐 NMS로 정리한다 — 작은 물체는 고해상도 층이, 큰 물체는 저해상도 층이 주로 담당하게 되는 식이다.'},
 {h:'더 효율적인 변형들', d:'FPN 이후 연구들은 이 다중 스케일 융합을 더 정교하거나 가볍게 만드는 데 집중했다. [EfficientDet](#/p/efficientdet)의 BiFPN은 위에서 아래로만 정보를 흘리는 FPN과 달리 양방향(top-down + bottom-up)으로 반복 융합하면서도 연산량을 억제하는 가중치 결합을 쓴다. 목표는 같다 — 여러 스케일 정보를 더 적은 연산으로 더 잘 섞는 것.'},
 {h:'실무에서', d:'검출 결과를 분석할 때 COCO 평가 지표는 [mAP](#/c/iou-map)를 물체 크기별로 나눠 APs(소형)·APm(중형)·APl(대형)로도 보고한다. APs만 유독 낮다면 다중 스케일 설계(피라미드 층 수, 얕은 층의 채널 수, 입력 해상도)를 먼저 점검하는 것이 순서다. 반대로 입력 해상도를 무작정 키우면 소형 물체 성능은 오르지만 연산량이 제곱으로 늘어, 실무에서는 해상도·피라미드 깊이·속도 사이의 트레이드오프를 조정하게 된다.'}
],

diagram:{type:'flow', cap:'FPN: 깊은 층의 의미를 위로 전달해 얕은 층과 합친다.',
 nodes:[
  {t:'백본 다단 특징', s:'얕음(고해상도)~깊음'},
  {t:'top-down 전달', s:'깊은 층 → 얕은 층'},
  {t:'스케일별 융합', s:'해상도+의미 결합'},
  {t:'스케일별 예측', s:'헤드가 각 층서 검출'}
 ]},

confuse:[
 {a:'다중 스케일 학습(입력 크기 변화)', b:'특징 피라미드(다중 스케일 특징)', d:'다중 스케일 학습은 학습 중 입력 이미지 크기를 이리저리 바꿔가며 다양한 스케일에 모델을 노출시키는 [데이터 증강](#/c/augmentation) 기법이고, 특징 피라미드는 하나의 입력에서 여러 해상도 특징 맵을 동시에 활용하는 아키텍처 설계다. 이름은 비슷하지만 다른 층위의 이야기다.'},
 {a:'[백본](#/c/backbone-neck-head)', b:'다중 스케일 넥(FPN)', d:'백본은 다단 해상도 특징을 만들어내는 쪽이고, FPN 같은 다중 스케일 넥은 그 여러 해상도를 받아 서로 섞는 쪽이다. FPN 자체는 백본 없이 존재할 수 없다.'}
],

pitfalls:[
 '피라미드 층 수를 늘리면 무조건 좋아질 거라 기대하기 쉬운데, 층이 늘수록 연산·메모리 비용도 늘어 실제로는 최적 층 수가 데이터의 물체 크기 분포에 따라 갈린다.',
 '입력 해상도만 올리고 피라미드 구조는 그대로 두면 소형 물체 성능 개선이 기대보다 작을 수 있다 — 해상도와 피라미드 설계는 함께 봐야 한다.',
 'APs(소형 물체 mAP)가 낮은 원인을 다중 스케일 설계가 아니라 무작정 앵커나 손실 함수 쪽에서만 찾으면 헛수고가 될 수 있다.'
],

papers:['fpn','efficientdet'],
terms:['backbone-neck-head','bbox-nms','anchor-free']
});
