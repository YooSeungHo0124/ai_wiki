WIKI.concept({
slug:'local-llm',

tldr:'클라우드 API 대신 개인 GPU·CPU 에서 모델을 직접 돌리는 방식으로, 대개 양자화로 크기를 줄인 GGUF 같은 포맷과 llama.cpp 류 런타임을 쓴다.',

why:'API 를 쓰면 데이터가 외부로 나가고 요청마다 비용이 붙는다. 로컬 실행은 이 두 문제를 해결하지만 "내 GPU 에 이 모델이 올라가는가"를 계산할 줄 모르면 다운로드하고 나서야 VRAM 부족으로 실패하는 일을 반복하게 된다. 양자화 수준을 얼마나 낮출 수 있는지가 품질과 배포 가능 여부를 동시에 결정한다.',

sections:[
 {h:'GGUF 가 뭔가', d:'GGUF 는 llama.cpp 생태계가 쓰는 모델 파일 포맷으로, 가중치와 함께 토크나이저·메타데이터를 한 파일에 담아 CPU·GPU 어디서든 같은 파일로 실행할 수 있게 만든다. 원본 모델(대개 FP16/BF16)을 GGUF 로 변환하면서 동시에 다양한 수준으로 양자화한 버전을 함께 배포하는 것이 일반적이다.'},
 {h:'양자화 수준이 뜻하는 것', d:'원래 16비트로 저장되던 가중치를 더 적은 비트로 근사해 저장하는 것이 양자화다. Q4_K_M 같은 표기에서 숫자(4)는 대략적인 비트 수, K 는 양자화 방식의 세대, M 은 그 안에서의 세부 변형(작은/중간/큰 크기와 품질의 절충)을 가리킨다 — 숫자가 작을수록 파일은 작아지지만 원래 가중치와의 오차가 커진다. [LLM.int8()](#/p/llm-int8)은 8비트 양자화에서도 이상치(outlier) 채널을 따로 고정밀도로 처리해 성능 저하를 최소화하는 방법을 보였고, [AWQ](#/p/awq)는 활성화 값 분포를 보고 중요한 가중치 채널을 선별적으로 더 정밀하게 보존하는 방식으로 낮은 비트에서도 품질을 지켰다 — "몇 비트인가"만큼 "어떻게 비트를 배분하는가"가 품질을 좌우한다는 것을 보여준 연구들이다.'},
 {h:'필요한 VRAM 계산', d:'가장 거친 어림은 파라미터 수 × 비트 수 ÷ 8 이다. 70억(7B) 파라미터 모델을 16비트(FP16)로 올리면 약 14GB, 8비트로 양자화하면 약 7GB, 4비트로 양자화하면 약 3.5GB 가 가중치 자체에 필요하다. 여기에 [KV 캐시](#/c/kv-cache)(컨텍스트 길이·배치에 비례)와 실행 오버헤드를 몇 GB 더 얹어야 실제로 돌아간다 — 딱 가중치 크기만큼만 VRAM 이 있으면 된다고 생각하면 실행 중 OOM 을 겪는다.'},
 {h:'품질 손실', d:'양자화는 공짜가 아니다. 비트를 낮출수록 특히 추론·수학처럼 정밀한 확률 분포 차이에 민감한 작업에서 품질 저하가 먼저 드러나는 경향이 있고, 단순 대화·요약 같은 작업은 상대적으로 덜 민감하다. Q4 대까지는 원본과 체감 차이가 크지 않다는 것이 널리 퍼진 경험칙이지만, 이는 모델·작업마다 다르므로 실제 목표 작업으로 직접 비교해 보는 것이 안전하다.'},
 {h:'실무에서', d:'GPU 메모리가 제한적이라면 "가장 큰 모델을 가장 낮은 비트로" 돌리는 것과 "더 작은 모델을 높은 비트로" 돌리는 것 사이에서 실제 작업으로 비교해 골라야 한다 — 무조건 파라미터 수가 큰 쪽이 이기지 않는다. 오프로딩(일부 레이어를 CPU 메모리에 두는 것)은 VRAM 부족을 메우지만 그만큼 속도가 크게 떨어지므로, 지연이 중요한 용도라면 모델을 줄이는 편이 낫다.'}
],

math:[
 {tex:'\\text{VRAM}\\approx \\dfrac{P\\cdot b}{8}\\;(\\text{GB, }P\\text{는 파라미터 수(10억 단위)})', expr:'가중치 메모리 어림', d:'$P$ 는 파라미터 수(단위 10억), $b$ 는 비트 수. 예: 7B 모델을 4비트로 양자화하면 $7\\times4/8=3.5$GB. 여기에 KV 캐시와 런타임 오버헤드를 몇 GB 더해야 실제 필요량이 된다.'}
],

diagram:{type:'compare', cap:'같은 모델도 비트 수에 따라 크기·품질이 갈린다.',
 left:{t:'FP16 원본', items:['품질 기준선','7B 기준 약 14GB','고사양 GPU 필요']},
 right:{t:'Q4 양자화(GGUF)', items:['7B 기준 약 3.5GB','일반 GPU·CPU 가능','일부 품질 손실']}},

confuse:[
 {a:'양자화(quantization)', b:'[증류](#/c/distillation-c)', d:'양자화는 같은 파라미터를 더 적은 비트로 근사해 저장하는 것이고, 증류는 아예 더 작은 파라미터 수의 새 모델을 학습시켜 원본을 모방하게 하는 것이다. 둘은 함께 쓸 수 있다 — 증류한 작은 모델을 다시 양자화하는 식이다.'},
 {a:'GGUF', b:'[GPTQ](#/p/gptq)·[AWQ](#/p/awq) 같은 양자화 기법', d:'GGUF 는 파일 포맷이고 GPTQ·AWQ 는 가중치를 어떻게 양자화할지 결정하는 알고리즘이다. GGUF 파일 안에 다양한 양자화 기법으로 만든 가중치가 담길 수 있다 — 포맷과 양자화 방법은 다른 층위다.'},
 {a:'Q4_K_M 같은 표기', b:'정확한 4비트 균일 양자화', d:'표기의 숫자는 평균적인 비트 수에 가깝고, 실제로는 레이어·채널마다 다른 정밀도를 섞어 쓰는 경우가 많다 — "4비트"라는 말이 모든 가중치가 똑같이 4비트라는 뜻은 아니다.'}
],

pitfalls:[
 '파라미터 수만 보고 VRAM 을 가늠하면 KV 캐시·오버헤드를 빠뜨려 실제 실행에서 OOM 이 난다.',
 '비트를 낮출수록 무조건 속도가 빨라진다고 생각하기 쉽지만, 연산 자체가 낮은 비트를 지원하지 않는 하드웨어에서는 역양자화 오버헤드로 오히려 느려질 수 있다.',
 '작은 모델을 낮은 비트로 돌리는 것과 큰 모델을 더 낮은 비트로 무리하게 욱여넣는 것 중 후자가 항상 나은 것은 아니다 — 실제 목표 작업으로 비교해야 한다.'
],

papers:['llm-int8','awq','bitnet'],
terms:['kv-cache','distillation-c','quantization']
});
