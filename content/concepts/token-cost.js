WIKI.concept({
slug:'token-cost',

tldr:'LLM API 비용은 입력·출력 토큰 수에 각각 다른 단가를 곱해 계산되며, 대화가 길어질수록 매 턴 전체 문맥을 다시 보내 비용이 누적된다.',

why:'"토큰당 얼마"라는 말만 보고 대충 계산하면 실제 청구서와 크게 어긋난다. 입력과 출력의 단가가 다르고, 멀티턴 대화는 매 턴 이전 대화 전체를 다시 입력으로 보내기 때문에 대화가 길어질수록 비용이 선형이 아니라 훨씬 빠르게 불어난다. 이 구조를 모르면 "왜 챗봇 기능 하나가 예상보다 몇 배 비싸게 나왔는가"를 설명할 수 없다.',

sections:[
 {h:'입력·출력 단가', d:'대부분의 LLM API 는 입력 토큰(프롬프트)과 출력 토큰(생성된 응답)에 서로 다른 단가를 매긴다. 출력 토큰이 입력 토큰보다 단가가 몇 배 높은 것이 일반적인데, 이는 [스트리밍](#/c/streaming-output) 문서에서 다룬 것처럼 입력을 한 번에 처리하는 prefill 은 연산을 병렬로 밀어붙일 수 있는 반면, 출력을 만드는 decode 는 토큰을 하나씩 순차 생성해야 해서 같은 토큰 수라도 GPU 시간을 더 많이 쓰기 때문이다. 요금 구조를 볼 때 "토큰당 단가"를 입력·출력 구분 없이 하나로 뭉뚱그려 계산하면 틀린다.'},
 {h:'캐시된 입력', d:'같은 시스템 프롬프트나 긴 문서를 여러 요청에 걸쳐 반복해서 보내는 경우, 일부 API 는 이전에 처리한 입력 앞부분을 캐시해 뒀다가 재사용하면 그 구간의 단가를 크게 낮춰준다(프롬프트 캐싱). 이는 앞서 계산해 둔 [KV 캐시](#/c/kv-cache)를 재사용해 prefill 연산을 다시 하지 않아도 되기 때문에 가능한 할인이다 — 시스템 프롬프트나 few-shot 예시처럼 매 요청 앞부분이 동일한 경우 실무 비용을 크게 줄이는 지렛대다.'},
 {h:'대화가 누적되는 이유', d:'멀티턴 대화 API 는 보통 상태를 서버가 기억하지 않는다 — 매 턴마다 지금까지의 대화 전체(시스템 프롬프트 + 이전 모든 턴)를 다시 입력 토큰으로 보내야 모델이 문맥을 알 수 있다. 즉 $k$ 번째 턴의 입력 토큰 수는 그 턴까지 쌓인 대화 길이에 비례해서 커지고, 대화 전체의 총 입력 토큰 비용은 턴 수에 대해 대략 제곱에 가깝게 늘어난다. 캐싱이 없다면 10턴짜리 대화는 각 턴이 독립일 때보다 훨씬 비싸다.'},
 {h:'계산 예시', d:'입력 단가를 출력 단가의 1/4 이라 가정하고, 한 턴에 입력 500 토큰·출력 300 토큰이 오간다고 하자. 캐싱이 전혀 없는 5턴 대화라면 각 턴의 입력은 이전 턴들의 입력+출력이 누적되어 1턴 500, 2턴 1300(=500+300+500), 3턴 2100, 4턴 2900, 5턴 3700 토큰이 되고, 5턴 총 입력 토큰은 이 다섯 값의 합인 10,500 토큰이다. 반면 매 턴을 독립 요청으로 계산하면 500×5=2,500 토큰에 불과하다 — 같은 "질문 5개"인데 대화 맥락을 유지한다는 이유만으로 입력 비용이 4배 이상 뛴다. 프롬프트 캐싱을 켜면 반복되는 앞부분(시스템 프롬프트+이전 대화)에 할인 단가가 적용되어 이 누적 효과를 상당히 줄일 수 있다.'}
],

math:[
 {tex:'C=\\sum_{k=1}^{T}\\big(p_{in}\\cdot n^{in}_k+p_{out}\\cdot n^{out}_k\\big)', expr:'대화 전체 비용', d:'$T$ 는 턴 수, $n^{in}_k$ 는 $k$번째 턴에 실제로 보내는 입력 토큰 수(누적된 이전 대화 포함), $n^{out}_k$ 는 그 턴의 출력 토큰 수, $p_{in},p_{out}$ 은 각각의 단가. 누적 없이 매 턴이 독립이라고 잘못 가정하면 $n^{in}_k$ 를 과소평가해 총 비용을 실제보다 훨씬 낮게 예측하게 된다.'}
],

diagram:{type:'flow', cap:'턴이 늘수록 매번 보내는 입력이 누적된다.',
 steps:['턴1: 입력 500','턴2: 입력 500+응답1+질문2','턴3: 앞의 전체 누적','...','턴5: 누적 입력 최대']},

confuse:[
 {a:'입력 토큰 단가', b:'출력 토큰 단가', d:'출력이 더 비싼 것이 일반적이다 — decode 가 토큰마다 순차적으로 GPU 시간을 쓰는 반면 입력은 한 번에 병렬 처리되기 때문이다. 둘을 같은 단가로 어림잡아 계산하면 예산이 크게 틀어진다.'},
 {a:'캐시된 입력 할인', b:'[KV 캐시](#/c/kv-cache)', d:'KV 캐시는 서버가 생성 도중 Key·Value 를 재사용하는 추론 시점 메커니즘이고, 프롬프트 캐싱 할인은 그 메커니즘 덕분에 반복되는 입력 구간의 요금을 낮춰주는 과금 정책이다. 후자는 전자가 있어야 가능한 상품이다.'},
 {a:'토큰 수', b:'글자 수·단어 수', d:'토큰 단가는 [토크나이저](#/c/tokenizer)가 나눈 조각 수 기준이며 한국어처럼 조각이 잘게 쪼개지는 언어는 같은 글자 수라도 영어보다 토큰 수가 더 나오는 경우가 흔하다 — 글자 수만 보고 비용을 어림하면 틀리기 쉽다.'}
],

pitfalls:[
 '멀티턴 대화 비용을 "턴당 비용 × 턴 수"로 선형 계산하면 실제보다 크게 과소평가한다 — 누적 입력을 반영해야 한다.',
 '입력·출력 단가를 하나로 뭉쳐 평균으로 어림하면, 출력이 긴 작업(요약·긴 코드 생성)의 비용을 과소평가하기 쉽다.',
 '프롬프트 캐싱은 반복되는 앞부분에만 적용되므로, 매 턴 앞부분 내용을 조금씩 바꾸면(타임스탬프 삽입 등) 캐시가 깨져 할인을 못 받을 수 있다.'
],

papers:['gpt3','chinchilla'],
terms:['kv-cache','streaming-output','tokenizer','context-management']
});
