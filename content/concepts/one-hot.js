WIKI.concept({
slug:'one-hot',

tldr:'범주형 값을 범주 개수만큼의 차원을 가진 벡터로 표현해, 해당 범주 위치만 1이고 나머지는 모두 0으로 두는 인코딩 방식.',

why:'범주형 변수를 신경망·표 모델에 넣으려면 숫자로 바꿔야 하는데, 순서가 없는 범주에 그냥 정수(0,1,2...)를 매기면 모델이 존재하지 않는 크기·순서 관계(2가 0보다 "크다")를 학습해 버린다. one-hot은 이 문제를 없애는 가장 단순한 방법이지만, 범주 수가 커지면 차원이 폭발하고 희소해진다는 대가가 있어 언제 [임베딩](#/c/embedding)으로 넘어가야 하는지 판단이 필요하다. 딥러닝 입문서에서 가볍게 지나치기 쉽지만, 범주형 데이터를 다루는 실무에서는 계속 마주치는 기본기다.',

sections:[
 {h:'무엇인가', d:'범주가 $K$ 개면 각 범주를 $K$ 차원 벡터로 표현하고, 그 범주에 해당하는 인덱스만 1, 나머지는 0으로 둔다. 예를 들어 {빨강, 초록, 파랑} 중 "초록"은 $[0,1,0]$ 이 된다. 이렇게 하면 어떤 두 범주 벡터든 서로 직교(orthogonal)하고 거리가 같아(유클리드 거리는 항상 $\\sqrt2$), 모델 입장에서 범주 사이에 암묵적인 순서나 거리 관계가 전혀 없다고 취급된다.'},
 {h:'정수 인코딩과의 차이', d:'같은 범주를 그냥 0,1,2,...로 매기는 레이블 인코딩(label encoding)은 값 하나만 저장하면 되어 차원 낭비가 없지만, 선형 모델·거리 기반 모델은 이 숫자를 크기 정보로 오해한다. 트리 기반 모델([결정 트리](#/c/decision-tree), [부스팅](#/c/boosting))은 각 분기점에서 "이 값보다 큰가 작은가"가 아니라 "이 집합에 속하는가"로 분기할 수 있어 정수 인코딩도 비교적 잘 다루지만, 선형 모델·신경망에는 one-hot이 기본이다.'},
 {h:'차원 폭발과 희소성', d:'범주 수가 수만~수백만(단어 사전, 사용자 ID 등)이면 one-hot 벡터는 대부분이 0인 매우 희소한(sparse) 고차원 벡터가 된다. 이 벡터에 곱해지는 첫 번째 가중치 행렬은 사실상 "그 인덱스에 해당하는 행 하나를 그대로 골라오는" 룩업(lookup) 연산과 수학적으로 동일하다 — 이것이 바로 [임베딩](#/c/embedding) 층의 정체다. 그래서 어휘가 큰 문제에서는 one-hot을 명시적으로 만들지 않고 임베딩 룩업으로 바로 대체한다.'},
 {h:'출력층에서의 one-hot', d:'분류 문제의 정답 레이블도 흔히 one-hot으로 표현한다. [소프트맥스](#/c/softmax) 출력(확률분포)과 one-hot 정답 사이의 [교차 엔트로피](#/c/entropy-kl)를 계산하면, 정답 클래스가 아닌 항들은 $\\log(\\cdot)\\times 0$ 으로 사라져 정답 클래스의 예측 확률에만 손실이 걸린다. 레이블 스무딩(label smoothing)은 이 정답 1을 0.9 정도로 낮추고 나머지에 작은 값을 나눠 주어, 모델이 정답에 과도하게 확신하지 않도록 만드는 변형이다.'},
 {h:'멀티라벨과 카운트 벡터', d:'한 샘플이 여러 범주에 동시에 속할 수 있으면(멀티라벨) 1이 여러 개인 멀티-핫(multi-hot) 벡터를 쓴다. 각 범주가 몇 번 등장했는지까지 담고 싶으면(예: 문서 안 단어 빈도) 0/1 대신 등장 횟수를 넣는 카운트 벡터(bag-of-words)로 확장할 수 있다 — one-hot은 이런 표현들의 가장 단순한 특수 사례(등장 여부만, 정확히 하나)로 볼 수 있다.'}
],

code:{lang:'python', d:'정수 레이블을 one-hot으로 바꾸는 예. 교차 엔트로피 손실은 대개 one-hot을 명시적으로 만들지 않고 정수 인덱스를 바로 받는다는 점도 함께 보여준다.', src:'import numpy as np\n\ndef one_hot(labels, num_classes):\n    out = np.zeros((len(labels), num_classes))\n    out[np.arange(len(labels)), labels] = 1\n    return out\n\n# labels = [2, 0, 1], num_classes = 3\n# -> [[0,0,1], [1,0,0], [0,1,0]]\n\n# PyTorch CrossEntropyLoss는 one-hot이 아니라\n# 정수 레이블(class index)을 직접 받는다.\nimport torch.nn as nn\nloss_fn = nn.CrossEntropyLoss()  # target: LongTensor([2, 0, 1])'},

diagram:{type:'compare', cap:'같은 범주형 값을 표현하는 두 방식의 차이.',
 left:{t:'정수(레이블) 인코딩', items:[
  '차원 1개로 저장',
  '암묵적 순서·거리 생김',
  '트리 모델엔 무난']},
 right:{t:'One-Hot 인코딩', items:[
  '차원 K개(범주 수)',
  '범주 간 거리 모두 동일',
  '범주 많으면 희소·고차원']}},

confuse:[
 {a:'One-Hot 인코딩', b:'[임베딩](#/c/embedding)', d:'One-hot은 범주마다 직교하는 고정 벡터를 주는 것이고, 임베딩은 그 one-hot을 학습 가능한 저차원 실수 벡터로 투영(projection)한 것이다. 임베딩은 학습을 통해 의미가 비슷한 범주끼리 벡터가 가까워지도록 배울 수 있지만 one-hot은 그런 학습이 애초에 불가능한 고정된 표현이다.'},
 {a:'One-Hot', b:'멀티-핫(Multi-hot)', d:'One-hot은 한 샘플이 정확히 하나의 범주에만 속할 때(1이 하나) 쓰고, 멀티-핫은 여러 범주에 동시에 속할 수 있을 때(1이 여럿) 쓴다. 멀티라벨 분류의 정답 표현이 대표적인 멀티-핫 사례다.'},
 {a:'레이블 인코딩', b:'순서형(ordinal) 인코딩', d:'둘 다 정수를 매긴다는 점은 같지만, 순서형 인코딩은 "낮음<중간<높음"처럼 범주 사이에 실제 순서가 있을 때 그 순서를 의도적으로 반영해 매기는 것이고, 레이블 인코딩은 순서 없는 범주에 편의상 정수를 붙인 것이라 모델이 순서로 오해하면 문제가 된다.'},
 {a:'One-Hot 인코딩', b:'[소프트맥스](#/c/softmax) 출력', d:'One-hot은 정답(레이블)을 고정된 이산 벡터로 나타낸 것이고, softmax 출력은 모델이 낸 연속적인 확률분포다. 학습은 이 둘 사이의 차이([교차 엔트로피](#/c/entropy-kl))를 줄이는 방향으로 진행되며, one-hot은 그 비교의 기준점(정답) 역할을 한다.'},
 {a:'One-Hot', b:'해싱 트릭(hashing trick)', d:'One-hot은 범주마다 고유한 차원을 하나씩 배정해 차원 수가 범주 수와 같아진다. 해싱 트릭은 범주를 해시 함수로 고정된 작은 차원 수의 버킷에 강제로 매핑해, 범주가 아무리 많아도 차원을 일정하게 유지하는 대신 서로 다른 범주가 같은 버킷에 충돌할 위험을 감수한다.'}
],

pitfalls:[
 '순서가 있는 범주(학력, 등급 등)를 일반 정수 인코딩으로 다루면 문제없지만, 순서가 없는 범주에 정수를 매기고 선형 모델에 그대로 넣으면 존재하지 않는 크기 관계를 학습해 버린다.',
 '범주 수가 매우 큰 변수를 그대로 one-hot으로 만들면 메모리와 계산량이 급격히 커진다 — 이때는 해싱 트릭(hashing trick)이나 임베딩으로 대체해야 한다.',
 '학습 데이터에 없던 새 범주가 추론 시 등장하면(unseen category) one-hot 벡터를 어떻게 만들지 미리 정의해 둬야 한다 — 보통 "기타(unknown)" 항목을 예비로 하나 둔다.',
 '스케일링과 함께 쓸 때 one-hot으로 만든 0/1 컬럼까지 표준화·정규화 대상에 포함시키면 의미가 이상해진다 — 범주형 one-hot 컬럼은 보통 [특징 스케일링](#/c/scaling-features) 대상에서 제외한다.'
],

papers:['word2vec'],
terms:['embedding','entropy-kl','decision-tree','softmax','scaling-features','mlp','feature-engineering']
})
