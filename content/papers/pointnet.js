WIKI.paper({
slug:'pointnet',
venue:'CVPR 2017',
authors:'Qi, Su, Mo, Guibas (Stanford University)',
arxiv:'1612.00593',

tldr:'라이다·스캐너가 뽑아내는 **점군(point cloud)을 복셀이나 다중 뷰 이미지로 바꾸지 않고 그대로** 신경망에 넣어 분류·분할을 푼 최초의 논문. 점마다 MLP를 태우고 max pooling으로 모으는 것만으로 순서 없는 집합을 처리할 수 있음을 이론과 실험으로 보였다.',

context:'2016년까지 3D 딥러닝은 점군을 있는 그대로 쓰지 않았다. 복셀 그리드로 바꿔 3D CNN을 돌리거나(VoxNet 계열), 여러 각도에서 렌더링한 2D 이미지로 바꿔 2D CNN을 돌리는(MVCNN 계열) 우회가 표준이었다. 두 방식 모두 변환 과정에서 **데이터가 불필요하게 부풀거나 기하 정보가 손실**된다. 점군을 직접 쓰지 못하는 이유는 단순하다 — 점군은 `N`개의 점이 **정해진 순서 없이** 나열된 집합이라, 입력 순서를 바꿔도 같은 물체여야 하는데 CNN·RNN은 입력 순서에 민감하다. 이 논문은 "점군을 이미지·복셀 흉내를 내지 않고 집합 그 자체로 처리하는 신경망을 설계할 수 있는가"라는 질문에서 출발한다.',

ideas:[
 {h:'대칭 함수로 순열 불변성을 만든다',
  lead:'점마다 독립적으로 함수를 적용한 뒤 max 같은 대칭 함수로 모으면 입력 순서가 사라진다.',
  d:'`N!`가지 입력 순서 모두에 같은 출력을 내려면, 순서에 무관한 **대칭 함수**로 정보를 모아야 한다. 이 논문은 각 점에 공유 가중치 MLP `h`를 독립적으로 적용해 고차원 특징으로 올린 뒤, 차원별로 **max pooling**을 취해 하나의 전역 벡터로 합친다. `+`나 `×`처럼 max도 입력 순서를 바꿔도 결과가 같은 대칭 연산이라는 점이 핵심이다.'},
 {h:'정렬도 RNN도 아니고 왜 max pooling인가',
  lead:'정렬은 고차원에서 안정된 순서가 존재하지 않고, RNN은 순서에 민감해 순열 불변성을 보장 못한다.',
  d:'순서를 없애는 방법으로 논문은 세 가지를 검토한다 — 1) 정렬해서 표준 순서를 만들기, 2) 시퀀스로 보고 RNN을 모든 순열로 augmentation, 3) 대칭 함수로 집계. 저자들은 고차원 공간에는 점 섭동에 안정적인 정렬이 존재하지 않음을 반증으로 보이고(그런 정렬이 있다면 고차원-1차원 사이의 거리 보존 사상이 존재해야 하는데 불가능하다), RNN augmentation은 실험적으로 순수 대칭 함수보다 불안정함을 보여 max pooling을 택한다.'},
 {h:'universal approximation: 임의의 연속 집합 함수를 근사한다',
  lead:'Hausdorff 거리에 연속인 집합 함수는 `γ∘MAX(h(x_i))` 형태로 임의 정밀도까지 근사 가능하다.',
  d:'Theorem 1은 Hausdorff 거리 기준으로 연속인 임의의 집합 함수 `f`를, `MAX` pooling 뒤 차원 수 `K`(병목 차원, 논문 구현에서 1024)가 충분히 크면 `f(S) ≈ γ(MAX_i h(x_i))` 형태로 얼마든지 가깝게 근사할 수 있다고 증명한다. 네트워크 구조가 임의의 함수 근사가 아니라 **집합에 대한** 임의의 함수 근사임을 이론으로 못박은 것이 이 논문의 이론적 알맹이다.'},
 {h:'임계 점 집합(critical points)과 강건성',
  lead:'출력은 전체 `n`개 점이 아니라 max를 이긴 소수의 임계 점에만 좌우된다.',
  d:'max pooling을 통과한 각 채널은 그 채널 값이 가장 큰 **딱 한 점**에 의해 결정된다. 따라서 전역 특징 벡터는 최대 `K`(=1024)개의 "임계 점"만으로 완전히 결정되고, 나머지 점은 지우거나(개수만 유지되면) 노이즈를 더해도 출력이 바뀌지 않는다. 이것이 실험에서 점의 최대 50%를 지워도 정확도가 크게 떨어지지 않는 이유이자, 물체 형태를 스케치처럼 요약하는 "임계 점 골격"이 시각화로 드러나는 이유다.'},
 {h:'T-Net: 정렬을 아키텍처가 아니라 학습으로',
  lead:'입력과 특징 공간을 정렬하는 작은 변환 행렬을 미니 네트워크(T-Net)가 예측해 곱한다.',
  d:'점군의 회전·이동에 불변해야 한다는 요구를 손으로 정규화하는 대신, T-Net이라는 작은 서브네트워크가 `3×3` 정렬 행렬을 예측해 입력 좌표에 곱하고, 같은 아이디어를 `64×64` 특징 공간에도 적용한다. 특징 공간 변환은 차원이 커서 최적화가 불안정하므로, 변환 행렬을 직교 행렬에 가깝게 만드는 정규화 항을 softmax 손실에 더한다.'}
],

diagram:{type:'flow', cap:'분류 네트워크(위쪽 파란 박스)의 흐름. 입력·특징 정렬(T-Net)을 거쳐 점별 MLP로 올리고 max pooling으로 순서를 없앤 전역 특징을 얻는다. 분할 네트워크(아래)는 이 전역 특징을 점별 특징과 이어붙여 점마다 점수를 낸다.',
 nodes:[
  {t:'입력 점군', s:'n×3, 무순서'},
  {t:'입력 정렬', s:'T-Net 3×3'},
  {t:'점별 MLP', s:'64,64 공유가중치'},
  {t:'특징 정렬', s:'T-Net 64×64'},
  {t:'점별 MLP', s:'64,128,1024'},
  {t:'max pooling', s:'n×1024→1024', acc:true, a:'대칭함수'},
  {t:'분류 MLP', s:'512,256,k'}
 ]},

math:[
 {expr:'f({x1,...,xn}) ≈ γ( MAX_i { h(x_i) } )',
  tex:'f(\\{x_1,\\dots,x_n\\}) \\approx \\gamma\\!\\left(\\underset{i=1,\\dots,n}{\\text{MAX}}\\, h(x_i)\\right)',
  d:'Theorem 1의 핵심 형태. `h`는 점별 MLP, `MAX`는 채널별 최댓값, `γ`는 이어지는 MLP. 이 구조만으로 Hausdorff 거리에 연속인 임의의 집합 함수를 근사할 수 있다.'},
 {expr:'L_reg = ||I − A·Aᵀ||²_F',
  tex:'L_{reg} = \\lVert I - AA^{\\top} \\rVert_F^2',
  d:'T-Net이 예측한 특징 정렬 행렬 `A`를 직교 행렬에 가깝게 묶는 정규화 손실. 직교 변환은 입력 정보를 잃지 않으면서 최적화를 안정시킨다.'}
],

numbers:[
 {k:'ModelNet40 분류 정확도', v:'89.2%', d:'포인트 입력(1점군, 1024점)만으로 이미지 80뷰를 쓰는 MVCNN(90.1%)에 근접, 복셀 기반 VoxNet(85.9%)은 앞섬'},
 {k:'ShapeNet part segmentation', v:'mIoU 83.7%', d:'16개 카테고리 평균, 점 단위 mIoU 기준 — [nuScenes](#/p/nuscenes) 같은 검출 mAP와는 다른 지표'},
 {k:'처리 속도', v:'초당 약 100만 점', d:'분류 기준, GPU 1장 — 다중뷰 CNN 대비 압도적으로 가볍다고 원문이 주장'},
 {k:'입력 변환 효과', v:'+0.8%p', d:'input transform 하나만 추가했을 때 ModelNet40 정확도 향상폭'},
 {k:'점 삭제 강건성', v:'20% 삭제 시에도 80%대 유지', d:'furthest point sampling으로 최대 절반을 지워도 정확도가 급락하지 않음(Fig 6)'},
 {k:'전역 특징 차원(병목 K)', v:'1024', d:'max pooling 직전 채널 수 = Theorem 1의 근사 정밀도를 결정하는 K'}
],

impact:'점군을 **날것 그대로** 신경망 입력으로 쓸 수 있음을 보여 복셀화·다중뷰 렌더링이라는 전처리 단계를 통째로 걷어냈다. "순서 없는 집합을 어떻게 신경망에 넣는가"라는 질문에 **점별 공유 MLP + 대칭 함수**라는 답을 내놓은 것은 이후 point-set 처리 전반의 표준 레시피가 됐고, 라이다를 쓰는 자율주행 3D 인지 계보 전체가 여기서 갈라진다. 다만 원 논문의 max pooling은 **전역** 특징 하나만 만들기 때문에, 국소 구조(이웃 점들 사이의 기하학적 관계)를 전혀 보지 못한다는 한계가 뒤따르는 [PointNet++](#/p/pointnet2)에서 바로 지적된다.',

legacy:[
 '**지역성 보완** — [PointNet++](#/p/pointnet2)가 계층적 샘플링·그룹핑으로 PointNet이 놓친 국소 구조를 채움',
 '**복셀 파이프라인 안의 부품화** — [VoxelNet](#/p/voxelnet)이 복셀 하나하나의 내부 인코더로 PointNet 아이디어를 재사용(Voxel Feature Encoding)',
 '**속도 계보** — VoxelNet의 3D 합성곱을 걷어낸 [PointPillars](#/p/pointpillars)까지, "점을 어떻게 묶어 벡터화하는가"라는 이 논문의 질문이 라이다 검출기 계보를 관통',
 '**집합 처리 일반화** — 3D 이외에도 순서 없는 입력(그래프 노드 집합, 포인트 기반 생성 등)을 다루는 여러 후속 연구에 "점별 MLP + 대칭 함수" 패턴이 차용됨'
],

pitfalls:[
 '**"점 순서를 무시한다"가 "점 개수도 무시한다"는 뜻이 아니다.** 네트워크는 가변 개수 `n`을 받지만, 배치 처리를 위해 실무 구현은 보통 고정 개수(예: 1024점)로 샘플링해서 넣는다.',
 '**max pooling이 전역 특징 하나만 만든다는 것은 한계다.** 두 점 사이의 상대적 거리·이웃 관계 같은 국소 기하는 이 구조로 포착되지 않는다 — 바로 이 지점이 [PointNet++](#/p/pointnet2)의 출발점이다.',
 '**ModelNet40 89.2%는 단일 시점(1뷰) 포인트 입력 기준이다.** 같은 표의 MVCNN 90.1%는 80개 렌더링 뷰를 쓴 결과라 입력 정보량 자체가 다르다 — 단순 수치 비교로 "이미지 기반이 더 낫다"고 읽으면 조건을 놓친 것이다.'
],

figures:[
 {f:'fig2-architecture.png',
  cap:'위쪽 파란 박스가 분류 네트워크: nx3 입력이 T-Net 정렬 → 공유 MLP → 다시 T-Net 정렬 → 공유 MLP(1024차원)를 거쳐 max pool로 1024차원 전역 특징이 된다. 아래 노란 박스가 분할 네트워크: 이 전역 특징(1024)을 점별 특징(64)과 이어붙여(nx1088) 점마다 m개 점수를 낸다. "shared"라 적힌 회색 상자가 모든 점에 같은 가중치를 쓴다는 표시.',
  src:'원문 Figure 2, p.3'}
],

quotes:[
 {t:'Point cloud is an important type of geometric data structure. Due to its irregular format, most researchers transform such data to regular 3D voxel grids or collections of images.',
  src:'Abstract, p.1'}
],

links:[
 {t:'arXiv 1612.00593 — PointNet', u:'https://arxiv.org/abs/1612.00593'},
 {t:'공식 코드 (charlesq34/pointnet)', u:'https://github.com/charlesq34/pointnet'}
]
});
