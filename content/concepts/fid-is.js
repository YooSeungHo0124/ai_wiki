WIKI.concept({
slug:'fid-is',

tldr:'Inception Score 는 생성 이미지 혼자서 선명함과 다양성을 재고, FID 는 생성 분포와 실제 분포의 거리를 재 상대적으로 더 신뢰받는다.',

why:'생성 모델(GAN·diffusion) 논문의 표에 나오는 숫자 하나만 보고 모델을 비교하면 함정에 빠진다. 두 지표가 무엇을 재는지, 어떤 가정을 깔고 있는지, 표본 수가 다르면 왜 비교가 무의미해지는지를 모르면 "FID 가 낮으니 더 좋은 모델"이라는 흔한 오독을 하게 된다.',

sections:[
 {h:'Inception Score: 생성물 혼자 평가', d:'사전학습된 Inception 분류기에 생성 이미지를 통과시켜 두 가지를 잰다 — 개별 이미지의 클래스 확률이 한 클래스에 뾰족하게 몰려 있는지(선명함, 알아볼 수 있는 대상인지), 그리고 전체 생성 이미지들의 클래스 분포가 다양한 클래스에 고르게 퍼져 있는지(다양성). 실제 데이터셋 없이 생성물만으로 계산할 수 있다는 것이 장점이지만, 동시에 큰 약점이기도 하다.'},
 {h:'FID: 실제 분포와의 거리', d:'Inception 네트워크의 중간층 특징을 실제 이미지 집합과 생성 이미지 집합 각각에서 뽑아 두 특징 분포를 다변량 가우시안으로 근사한 뒤, 두 가우시안 사이의 거리(Fréchet distance)를 계산한다. 실제 데이터 분포를 직접 참조하기 때문에 생성물이 실제 분포에서 얼마나 벗어나 있는지(모드 붕괴, 아티팩트, 다양성 부족 모두 포함)를 더 폭넓게 반영한다. 값은 낮을수록 좋다.'},
 {h:'Inception Score 의 비판', d:'[Inception Score 비판](#/p/inception-score-note) 이 지적하듯, IS 는 실제 데이터를 전혀 참조하지 않으므로 생성물이 실제 이미지와 얼마나 닮았는지는 재지 못한다 — 클래스별로 "그럴듯하고 다양하게" 보이기만 하면 실제와 전혀 다른 이미지도 높은 점수를 받을 수 있다. 또한 ImageNet 으로 학습된 Inception 분류기에 의존하므로 ImageNet 과 성격이 다른 도메인(얼굴, 의료 영상 등)에는 잘 맞지 않는다.'},
 {h:'FID 의 가정과 함정', d:'[FID](#/p/fid) 논문 자체가 밝히듯 FID 는 특징 공간의 분포가 가우시안이라는 가정에 의존하는데, 실제 특징 분포가 이 가정과 얼마나 다른지는 검증하기 어렵다. 더 실무적인 함정은 표본 수 의존성이다 — FID 는 표본이 적을수록 편향되게(실제보다 나쁘게) 추정되므로, 논문마다 사용한 표본 수(1만 장, 5만 장 등)가 다르면 FID 숫자를 직접 비교할 수 없다. 같은 모델도 표본 수를 바꾸면 FID 값 자체가 달라진다.'}
],

math:[
 {tex:'\\text{IS}=\\exp\\Big(\\mathbb{E}_x\\big[D_{KL}(p(y|x)\\,\\|\\,p(y))\\big]\\Big)', expr:'Inception Score', d:'$p(y|x)$ 는 이미지 $x$ 에 대한 Inception 분류기의 클래스 확률(뾰족할수록 선명함), $p(y)=\\mathbb{E}_x[p(y|x)]$ 는 전체 생성물의 평균 클래스 분포(고르게 퍼질수록 다양성). 이 둘의 KL 발산이 클수록(개별은 확신 있고 전체는 다양할수록) IS 가 높다.'},
 {tex:'\\text{FID}=\\|\\mu_r-\\mu_g\\|^2+\\text{Tr}\\big(\\Sigma_r+\\Sigma_g-2(\\Sigma_r\\Sigma_g)^{1/2}\\big)', expr:'Fréchet Inception Distance', d:'$(\\mu_r,\\Sigma_r)$ 는 실제 이미지 특징의 평균·공분산, $(\\mu_g,\\Sigma_g)$ 는 생성 이미지 특징의 평균·공분산 — 둘 다 가우시안으로 가정. 두 가우시안이 완전히 같으면 FID=0.'}
],

confuse:[
 {a:'Inception Score', b:'FID', d:'IS 는 생성물만으로 계산(실제 데이터 불필요, 선명함+다양성), FID 는 실제 데이터 분포와의 거리(닮음의 정도)를 잰다. 오늘날 논문은 FID 를 주 지표로, IS 를 보조로 쓰는 경우가 많다.'},
 {a:'FID', b:'정밀도-재현율(생성 모델 맥락)', d:'생성 모델 평가에서도 정밀도-재현율 개념이 따로 쓰이는데(생성물이 실제 분포 안에 있는지=정밀도, 실제 분포를 얼마나 커버하는지=재현율), 이는 분류 문제의 [정밀도·재현율](#/c/precision-recall)과 이름만 같을 뿐 분포 커버리지를 재는 다른 지표다. FID 는 이 둘을 하나의 스칼라로 뭉뚱그린다.'}
],

pitfalls:[
 '표본 수가 다른 FID 값을 직접 비교하면 안 된다 — 표본이 적을수록 FID 가 편향되게 높게(나쁘게) 나온다. 논문마다 같은 표본 수(보통 5만 장)를 썼는지 확인해야 한다.',
 'IS 가 높다고 실제 이미지와 닮았다는 뜻은 아니다 — 실제 데이터를 아예 참조하지 않는 지표이기 때문이다.',
 'FID 는 Inception 특징 공간의 가우시안 가정에 의존한다 — 이 가정이 깨지는 도메인(ImageNet 과 통계가 많이 다른 이미지)에서는 FID 의 절대값보다 같은 설정 내 상대 비교로만 써야 안전하다.'
],

papers:['fid','improved-gan','inception-score-note'],
terms:['bleu-rouge','calibration','benchmark-pitfall']
});
