WIKI.concept({
slug:'svm-c',

tldr:'두 클래스 사이의 마진(여백)을 최대화하는 결정 경계를 찾는 분류기로, 커널 트릭으로 비선형 경계도 선형 문제처럼 풀 수 있으며 회귀에도 SVR 로 확장된다.',

why:'2000년대~2010년대 초반 이미지·텍스트 분류의 표준이었고, [AlexNet](#/p/alexnet) 이전까지 많은 벤치마크에서 최고 성능을 냈다. 마진 최대화라는 개념 자체가 일반화 이론과 직결되어 있어, 왜 딥러닝에서도 넓은 마진(margin)이나 평평한 손실 지형(flat minima)을 좋은 신호로 보는지 이해하는 배경이 된다. 데이터가 적고 특징이 잘 설계된 문제(생물정보학의 유전자 발현 분류 등)에서는 지금도 SVM 이 실무 기본값으로 남아 있다.',

sections:[
 {h:'마진을 최대화한다는 것', d:'두 클래스를 나누는 직선(초평면)은 무수히 많을 수 있다. SVM 은 그중에서 가장 가까운 데이터 점들(서포트 벡터)까지의 거리가 최대가 되는 경계를 고른다. 마진이 넓을수록 새로운 데이터가 조금 달라져도 잘못 분류될 여지가 줄어든다는 직관이 이 선택의 근거다. 이 직관은 나중에 통계적 학습 이론에서 VC 차원과 일반화 오차 상한을 통해 정식으로 뒷받침된다.'},
 {h:'하드 마진과 소프트 마진', d:'데이터가 완벽히 선형 분리 가능하면 모든 점이 마진 밖에 있도록 강제하는 하드 마진을 쓸 수 있지만, 현실 데이터는 노이즈나 겹치는 클래스가 있어 대개 불가능하다. 소프트 마진은 일부 점이 마진을 침범하거나 잘못 분류되는 것을 허용하되 그 정도에 벌점(슬랙 변수)을 매기고, $C$ 라는 하이퍼파라미터로 "마진을 넓히는 것"과 "오분류를 줄이는 것" 사이의 균형을 조절한다. $C$ 는 정규화 강도의 역수처럼 동작해 크면 [로지스틱 회귀](#/c/logistic-regression)의 정규화를 약하게 준 것과 비슷한 효과를 낸다.'},
 {h:'커널 트릭', d:'원래 공간에서 선형 분리가 안 되는 데이터도 더 높은 차원으로 보내면 선형 분리가 가능해지는 경우가 많다. 커널 트릭은 그 고차원 매핑 $\\phi(x)$ 를 실제로 계산하지 않고도 두 점의 내적 $\\phi(x_i)\\cdot\\phi(x_j)$ 값만 커널 함수 $K(x_i,x_j)$ 로 바로 계산하는 기법이다. RBF(가우시안) 커널은 사실상 무한 차원으로 매핑하는 효과를 내면서도 계산은 원래 차원에서 끝난다. 선형 커널·다항 커널·RBF 커널 순으로 표현력이 커지지만 그만큼 과적합 위험과 하이퍼파라미터 튜닝 부담도 함께 커진다는 트레이드오프가 있다.'},
 {h:'왜 밀렸는가', d:'SVM 은 학습 시간과 메모리가 대략 데이터 개수의 제곱~세제곱에 비례해 늘어나는 커널 계산 때문에 샘플이 수만~수십만을 넘어가면 급격히 느려진다. 반면 신경망은 [SGD](#/c/sgd) 로 미니배치 단위로 학습해 데이터 크기에 거의 선형으로 비용이 늘고, 특징을 사람이 설계하지 않아도 표현을 스스로 학습한다. 이미지넷 규모의 데이터가 등장하면서 이 확장성 차이가 결정적이었다 — SVM 은 커널로 "고정된 비선형성"을 주는 반면 신경망은 계층을 쌓아 데이터에 맞는 비선형성 자체를 학습한다는 근본적인 차이도 한몫했다.'}
],

math:[
 {tex:'\\min_{w,b}\\ \\tfrac12\\|w\\|^2 \\ \\text{s.t.}\\ y_i(w^Tx_i+b)\\ge 1', expr:'하드 마진 SVM', d:'$\\|w\\|$ 를 최소화하는 것이 마진 $2/\\|w\\|$ 를 최대화하는 것과 같다. 제약 조건은 모든 점이 경계로부터 최소한의 여백을 두고 올바른 쪽에 있어야 함을 뜻한다.'},
 {tex:'\\min_{w,b,\\xi}\\ \\tfrac12\\|w\\|^2+C\\sum_i \\xi_i \\ \\text{s.t.}\\ y_i(w^Tx_i+b)\\ge 1-\\xi_i,\\ \\xi_i\\ge0', expr:'소프트 마진 SVM', d:'$\\xi_i$ 는 $i$ 번째 점이 마진을 얼마나 침범했는지 나타내는 슬랙 변수. $C$ 가 크면 침범에 큰 벌점을 줘 마진이 좁아지고 학습 데이터에 더 맞추려 하며(과적합 위험↑), $C$ 가 작으면 마진을 넓히는 쪽을 우선해 더 단순한 경계를 얻는다(과소적합 위험↑).'},
 {tex:'K(x_i,x_j)=\\exp(-\\gamma\\|x_i-x_j\\|^2)', expr:'RBF(가우시안) 커널', d:'두 점이 가까울수록 1에, 멀수록 0에 가까운 값을 준다. $\\gamma$ 가 크면 가까운 점에만 민감해져 경계가 복잡해지고(과적합 위험), 작으면 경계가 부드러워진다.'}
],

diagram:{type:'compare', cap:'커널 트릭은 저차원의 비선형 문제를 고차원의 선형 문제로 바꾼다.',
 left:{t:'원래 공간', items:['선형 분리 불가능','곡선 경계 필요']},
 right:{t:'커널로 매핑한 공간', items:['(암묵적) 고차원 사상','선형 초평면으로 분리']}},

confuse:[
 {a:'SVM', b:'[로지스틱 회귀](#/c/logistic-regression)', d:'둘 다 선형 결정 경계를 찾지만 SVM 은 마진(경계와 가장 가까운 점 사이 거리)을 최대화하는 힌지 손실을 쓰고, 로지스틱 회귀는 모든 점의 확률적 로그 손실을 최소화한다. SVM 은 서포트 벡터가 아닌 점들은 경계에 영향을 주지 않지만, 로지스틱 회귀는 모든 점이 항상 영향을 준다.'},
 {a:'서포트 벡터', b:'전체 학습 데이터', d:'학습이 끝난 SVM 의 경계는 오직 서포트 벡터(마진 위 또는 마진을 침범한 점들)만으로 결정된다. 나머지 점들은 지워도 경계가 바뀌지 않는다 — 이 성질이 SVM 을 메모리 효율적으로 만드는 이유 중 하나이며, 서포트 벡터 개수가 적을수록 모델이 더 단순하다는 신호로도 쓸 수 있다.'},
 {a:'커널 SVM', b:'신경망의 특징 학습', d:'커널 SVM 은 사람이 고른 커널 함수로 고정된 방식의 비선형 매핑을 쓰고 그 매핑 안에서 최적의 선형 경계만 학습한다. 신경망은 매핑 자체(특징 표현)를 데이터로부터 학습한다 — 이 차이가 대규모 비정형 데이터에서 신경망이 앞서는 근본 이유다. 반대로 데이터가 적어 표현을 새로 학습할 여유가 없을 때는 고정된 커널이 오히려 과적합을 막아주는 장점으로 작용하기도 한다.'}
],

pitfalls:[
 'RBF 커널을 쓸 때 특징 스케일을 맞추지 않으면 거리 계산이 스케일이 큰 특징에 지배당해 커널이 무의미해진다 — [특징 스케일링](#/c/scaling-features)이 필수다.',
 '$C$ 와 $\\gamma$ 를 한 번에 하나만 튜닝하고 넘어가는 경우가 있는데, 둘은 서로 얽혀 있어 그리드 탐색으로 함께 조정해야 한다 — 예를 들어 $\\gamma$ 를 키우면서 $C$ 를 그대로 두면 과적합 경향이 두 배로 겹칠 수 있다.',
 '데이터가 수만 건을 넘으면 커널 SVM 학습 시간이 급격히 늘어난다는 걸 모르고 그대로 돌렸다가 학습이 끝나지 않는 경우가 흔하다 — 이 규모에서는 선형 SVM 이나 다른 모델을 먼저 고려해야 한다.',
 'SVM 이 확률을 준다고 착각하기 쉽지만 기본 출력은 클래스와 결정 함수값(경계까지의 거리)일 뿐이다 — 확률이 필요하면 별도의 [보정(calibration)](#/c/calibration) 절차(Platt scaling)를 추가로 거쳐야 한다.'
],

code:{lang:'python', d:'RBF 커널 SVM 을 C, gamma 그리드로 튜닝하는 예',
 src:'from sklearn.svm import SVC\nfrom sklearn.model_selection import GridSearchCV\n\nparams = {"C":[0.1,1,10], "gamma":[0.001,0.01,0.1]}\nsvc = GridSearchCV(SVC(kernel="rbf"), params, cv=5)\nsvc.fit(X_train, y_train)   # 특징 스케일링을 먼저 해야 한다\nprint(svc.best_params_)     # C·gamma 는 함께 튜닝해야 의미가 있다'},

papers:['alexnet'],
terms:['logistic-regression','decision-tree','sgd','scaling-features','calibration']
});
