WIKI.concept({
slug:'seg-types',

tldr:'의미 분할은 픽셀마다 클래스만 매기고, 인스턴스 분할은 같은 클래스라도 개체를 구분하며, 파놉틱 분할은 이 둘을 한 장의 맵으로 합친다.',

why:'"세그멘테이션 모델 써주세요"라는 요청은 세 가지 중 무엇을 원하는지에 따라 완전히 다른 아키텍처와 라벨 형식을 요구한다. 이 구분을 모르면 자동차 100대가 찍힌 사진에서 "자동차"라는 클래스 맵만 뽑아놓고 "몇 대인지 세달라"는 요청에 답을 못 하는 상황이 생긴다.',

sections:[
 {h:'의미 분할: 클래스만', d:'거리 사진에 자동차 세 대가 나란히 서 있다고 하자. 의미 분할(semantic segmentation)은 픽셀마다 "이건 자동차, 저건 도로, 저건 하늘"처럼 클래스 라벨 하나만 매긴다. 세 대의 자동차는 서로 붙어 있으면 그냥 "자동차" 픽셀 덩어리 하나로 뭉쳐 보이고, 몇 대인지는 이 출력만으로 알 수 없다. [FCN](#/p/fcn)이 이 과제를 픽셀 단위 분류로 정식화한 대표작이다.'},
 {h:'인스턴스 분할', d:'같은 장면에서 인스턴스 분할(instance segmentation)은 "자동차 A, 자동차 B, 자동차 C"처럼 같은 클래스라도 각 개체를 별도 마스크로 구분한다. [Mask R-CNN](#/p/mask-rcnn)처럼 먼저 물체 후보를 [바운딩 박스](#/c/bbox-nms)로 찾고 그 안에서 픽셀 마스크를 예측하는 방식이 대표적이다. 대신 인스턴스 분할은 보통 "셀 수 있는 물체(countable thing)"만 다루고, 하늘·도로처럼 셀 수 없는 배경(stuff)은 아예 대상에서 빠진다.'},
 {h:'파놉틱 분할', d:'[파놉틱 분할](#/p/panoptic)은 이 둘을 합쳐, 셀 수 있는 thing(자동차 A/B/C 각각)과 셀 수 없는 stuff(도로 전체, 하늘 전체)를 하나의 출력 맵으로 동시에 표현한다. 즉 도로·하늘은 의미 분할처럼 클래스 덩어리로, 자동차는 인스턴스 분할처럼 개체별로 구분된다. 평가 지표도 따로 만들어졌는데, [파놉틱 분할](#/p/panoptic) 논문이 제안한 PQ(Panoptic Quality)는 검출 정확도(RQ, Recognition Quality)와 분할 정확도(SQ, Segmentation Quality)의 곱으로 정의돼, 물체를 놓치거나 헛짚는 문제와 마스크 경계가 부정확한 문제를 한 숫자에 함께 반영한다.'},
 {h:'셋을 하나의 모델로', d:'예전에는 세 과제마다 별도 아키텍처를 썼지만, [Mask2Former](#/p/mask2former)는 "이미지를 마스크 집합으로 분할한다"는 공통 정식화 위에서 클래스 정의만 바꿔 의미·인스턴스·파놉틱 분할을 하나의 모델·하나의 학습 방식으로 처리한다. 쿼리마다 마스크와 클래스를 예측하게 하고, stuff 클래스는 쿼리 하나가 이미지 전체의 해당 영역을 통째로 맡게 하는 식으로 thing/stuff 구분을 흡수한다. 실무에서 "요즘은 분할 모델을 따로따로 안 만든다"는 말이 나오는 배경이 이것이다.'}
],

diagram:{type:'compare', cap:'같은 장면(자동차 3대+도로+하늘)을 세 방식이 어떻게 다르게 출력하는가.',
 left:{t:'의미 분할', items:['픽셀마다 클래스 하나','자동차 3대 = 한 덩어리','도로·하늘도 클래스 맵']},
 right:{t:'인스턴스 분할', items:['개체별 별도 마스크','자동차 A/B/C 구분','도로·하늘은 대상 아님'], acc:true}
},

confuse:[
 {a:'의미 분할', b:'인스턴스 분할', d:'의미 분할은 "이 픽셀이 무슨 클래스인가"만 답하고 개체 수는 세지 못한다. 인스턴스 분할은 "이 픽셀이 몇 번째 개체에 속하는가"까지 답하지만 배경류(stuff)는 다루지 않는다.'},
 {a:'thing', b:'stuff', d:'thing은 자동차·사람처럼 셀 수 있고 경계가 뚜렷한 개체, stuff는 하늘·도로·잔디처럼 셀 수 없고 덩어리로만 존재하는 배경이다. 인스턴스 분할은 thing만, 의미 분할은 thing과 stuff를 구분 없이, 파놉틱 분할은 thing은 개체별로 stuff는 덩어리로 함께 다룬다.'},
 {a:'PQ (Panoptic Quality)', b:'[mAP](#/c/iou-map)', d:'mAP는 검출/인스턴스 분할에서 신뢰도 순위 전체에 대한 정밀도-재현율 곡선 면적이지만, PQ는 순위를 매기지 않고 IoU 0.5 이상으로 매치된 것만 대상으로 검출 품질과 분할 품질을 곱해 하나의 점수로 낸다. 두 지표는 계산 방식 자체가 다르다.'}
],

pitfalls:[
 '"세그멘테이션"이라는 말만 듣고 의미 분할용 라벨(클래스 맵)로 인스턴스를 세려 하면 애초에 불가능한 요청이 된다 — 개체 수가 필요하면 라벨링 단계부터 인스턴스 단위여야 한다.',
 '파놉틱 분할에서 thing과 stuff의 경계 클래스 정의는 데이터셋마다 다르다 — 같은 "자동차"도 어떤 데이터셋에서 주차장에 정지된 차는 stuff로 취급하는 등 예외가 있을 수 있다.',
 'PQ가 낮다고 곧바로 "마스크 품질이 나쁘다"고 단정하면 안 된다 — RQ(검출 자체를 놓침)가 원인인지 SQ(마스크 경계가 부정확)가 원인인지 나눠서 봐야 한다.'
],

papers:['fcn','mask-rcnn','panoptic'],
terms:['bbox-nms','iou-map','backbone-neck-head']
});
