WIKI.concept({
slug:'transfer-learning',

tldr:'다른 과제·데이터로 이미 학습된 모델의 표현을 새 과제로 옮겨 쓰는 것으로, 처음부터 학습하는 것보다 적은 데이터·시간으로 더 나은 성능을 낸다.',

why:'실무에서 라벨 데이터가 충분한 경우는 드물다. ImageNet 사전학습 백본이나 LLM 사전학습 가중치를 가져다 쓰는 게 기본값이 된 지금, 전체를 미세조정할지 일부만 학습할지 판단을 잘못하면 데이터가 적을 때 과적합으로 망가지거나, 데이터가 충분한데도 성능을 다 못 끌어낸다.',

sections:[
 {h:'왜 되는가', d:'큰 데이터로 학습된 모델의 앞쪽 층들은 특정 과제에 종속되지 않는 범용 특징(이미지라면 에지·질감, 텍스트라면 문법·의미 구조)을 배운다. [ImageNet](#/p/imagenet) 사전학습이나 LLM 사전학습이 학습하는 것은 사실 "이 특정 라벨"이 아니라 "이런 종류의 데이터를 표현하는 좋은 방법"이다. 새 과제가 이 범용 표현과 충분히 관련 있으면, 그 표현 위에 적은 데이터만 얹어도 잘 작동한다.'},
 {h:'특징 추출 vs 미세조정', d:'특징 추출(feature extraction)은 사전학습된 백본을 통째로 얼리고(freeze) 마지막에 새 출력층만 붙여 그 층만 학습한다. 미세조정(fine-tuning)은 백본의 가중치도 (전체 또는 일부) 함께 갱신한다. 전자는 파라미터 수가 적어 과적합 위험이 낮고 학습이 빠르지만, 도메인이 많이 다르면 성능 상한이 낮다. 후자는 표현 자체를 새 도메인에 맞게 재조정할 수 있어 성능 상한이 높지만, 데이터가 적으면 원래 가진 좋은 표현을 오히려 망가뜨리는 과적합("catastrophic forgetting"과도 이어지는 현상)이 일어나기 쉽다.'},
 {h:'언제 어느 쪽인가', d:'판단 기준은 두 가지, **새 데이터의 양**과 **원래 데이터와의 도메인 거리**다. 데이터가 적고 도메인이 가까우면(예: ImageNet 사전학습 → 다른 자연 이미지 분류) 특징 추출이나 마지막 몇 층만 미세조정하는 것으로 충분하다. 데이터가 많고 도메인이 멀면(예: 자연 이미지 → 의료 영상, 영어 텍스트 → 특정 도메인 전문 용어) 전체 미세조정이 필요하고, 그래도 원래 표현이 크게 도움이 된다. 데이터가 적은데 도메인도 멀면 가장 어려운 경우로, [BiT](#/p/bit) 류의 대규모 사전학습이나 데이터 증강, 더 강한 정칙화가 필요하다.'},
 {h:'도메인 적응과의 관계', d:'전이학습은 "과제가 달라도 된다"는 것이 전제인 넓은 개념이고, [도메인 적응](#/p/dann)은 과제(예: 분류 클래스)는 같은데 입력 분포(출처 도메인 vs 대상 도메인, 예: 합성 이미지 vs 실제 사진)만 다른 특수 경우를 다룬다. DANN 류는 도메인을 구분하지 못하도록 특징을 적대적으로 학습시켜, 라벨이 없는 대상 도메인에서도 원본 도메인에서 학습한 분류기가 작동하게 만든다. 즉 도메인 적응은 전이학습의 부분집합이자, "타깃 도메인에 라벨이 아예 없을 때" 쓰는 특화 기법이다.'}
],

diagram:{type:'compare', cap:'데이터 양과 도메인 거리로 특징 추출과 미세조정 중 무엇을 쓸지 가른다.',
 left:{t:'특징 추출 (백본 동결)', items:['데이터 적음 + 도메인 가까움','학습 파라미터 적어 과적합 낮음','성능 상한은 낮음']},
 right:{t:'전체/부분 미세조정', items:['데이터 많음 또는 도메인 멀음','표현 자체를 재조정','데이터 적으면 과적합 위험']}},

confuse:[
 {a:'전이학습', b:'멀티태스크 학습', d:'전이학습은 순차적이다 — 과제 A로 먼저 학습하고 그 표현을 과제 B로 옮긴다. 멀티태스크 학습은 동시에 여러 과제의 손실을 함께 최적화해 하나의 모델이 여러 과제를 동시에 잘하게 만든다. 사전학습-미세조정은 전이학습, 하나의 모델로 분류+탐지를 동시에 학습하는 것은 멀티태스크다.'},
 {a:'전이학습', b:'[PEFT](#/c/peft)', d:'전이학습은 "무엇을 옮길 것인가"에 대한 개념이고, PEFT는 "그 미세조정을 어떻게 값싸게 할 것인가"에 대한 방법론이다. LoRA·어댑터는 전이학습(미세조정)을 수행하는 한 가지 효율적인 방식이지 별개의 패러다임이 아니다.'},
 {a:'도메인 적응', b:'일반적 전이학습', d:'일반 전이학습은 타깃 도메인에도 라벨이 어느 정도 있다고 가정하는 경우가 많다. 도메인 적응은 타깃 도메인에 라벨이 전혀 없는(unsupervised domain adaptation) 상황을 특히 겨냥하며, 소스와 타깃의 분포 차이를 명시적으로 줄이는 손실 항을 추가한다는 점에서 방법론적으로 구분된다.'}
],

pitfalls:[
 '"사전학습 모델을 쓰면 무조건 낫다"는 착각 — 도메인이 아주 멀면(예: 자연 이미지 → 위성 이미지 스펙트럼 채널이 다른 경우) 사전학습 표현이 오히려 방해가 될 수 있다.',
 '전체 미세조정 시 학습률을 사전학습 때와 같은 크기로 쓰면 좋은 초기 표현이 몇 스텝 만에 무너진다 — 백본은 작은 학습률, 새 층은 큰 학습률로 나누는 것이 일반적이다.',
 '특징 추출만 하고 성능이 안 나올 때, 데이터를 늘리기 전에 "얼린 층 일부를 풀어서 미세조정"을 먼저 시도하지 않는 경우가 많다 — 점진적 unfreeze가 데이터 추가 수집보다 싸다.'
],

papers:['bit','imagenet','dann'],
terms:['pretrain-finetune','peft','supervision-types']
});
