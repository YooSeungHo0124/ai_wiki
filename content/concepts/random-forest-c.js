WIKI.concept({
slug:'random-forest-c',

tldr:'서로 다른 데이터·특징 부분집합으로 학습한 여러 [결정 트리](#/c/decision-tree)의 예측을 평균·투표해 분산을 줄이는 배깅(bagging) 기반 앙상블로, 개별 트리는 깊게 키우고 평균으로 분산만 억누른다.',

why:'표 데이터(정형 데이터) 문제에서 지금도 가장 빠르게 시도해볼 만한 강력한 베이스라인이다. 하이퍼파라미터에 크게 둔감하고, 특징 스케일링이나 결측치 처리 같은 전처리 부담이 적으면서도 대부분의 캐글식 정형 데이터 문제에서 웬만한 딥러닝보다 잘 먹힌다 — [나무가 아직 이긴다](#/p/trees-still-win) 가 보인 것도 이 현상이다. 실무에서 "일단 랜덤 포레스트부터 돌려보자"는 말이 흔한 것은 성능뿐 아니라 튜닝에 들이는 시간 대비 얻는 성능이 매우 안정적이기 때문이다.',

sections:[
 {h:'배깅: 부트스트랩 + 집계', d:'배깅(bootstrap aggregating)은 원본 데이터에서 중복을 허용해 무작위로 뽑은 부분집합(부트스트랩 샘플) 마다 트리를 하나씩 독립적으로 학습시킨 뒤, 예측을 평균(회귀) 또는 다수결 투표(분류)로 합친다. 트리마다 보는 데이터가 조금씩 달라 각 트리의 오차가 서로 다른 방향을 향하게 되고, 평균을 내면 그 오차들이 상쇄된다. 부트스트랩 샘플 크기는 보통 원본 데이터와 같게 두되 중복 추출이라 실제로 서로 다른 샘플이 섞여 들어간다.'},
 {h:'특징도 무작위로 자른다', d:'랜덤 포레스트는 배깅에 한 가지를 더한다 — 각 분할 노드에서 전체 특징 중 무작위로 뽑은 일부만 후보로 고려한다. 이렇게 하면 트리들이 항상 가장 강력한 특징 하나로만 분할하며 서로 비슷해지는 것을 막아 트리 사이의 상관을 낮추고, 그 결과 평균의 분산 감소 효과가 더 커진다. 분류에서는 보통 전체 특징 수의 제곱근을, 회귀에서는 전체의 3분의 1을 후보 크기로 쓰는 것이 흔한 기본값이다.'},
 {h:'병렬 학습, 편향은 그대로', d:'각 트리가 서로 독립적으로 학습되기 때문에 완전히 병렬로 학습할 수 있다 — 이것이 순차적으로 학습하는 [부스팅](#/c/boosting)과의 가장 큰 실무적 차이다. 대신 배깅은 개별 트리의 편향(bias)을 줄이지는 못한다 — 트리 하나하나가 구조적으로 못 잡는 패턴은 아무리 평균 내도 못 잡는다. 배깅은 분산을 줄이는 기법이지 편향을 줄이는 기법이 아니다. 그래서 트리 하나를 지나치게 얕게(과소적합하도록) 만들면 아무리 많이 모아도 성능이 낮은 천장에 막힌다 — 개별 트리는 충분히 깊게(낮은 편향) 키우고 배깅으로 분산만 잡는 것이 정석이다.'},
 {h:'언제 무엇을 쓰나', d:'특징 수가 수십~수백 개인 정형 데이터, 데이터 크기가 수만~수백만 행 수준, 결측치·이상치가 섞여 있고 빠른 베이스라인이 필요할 때 랜덤 포레스트가 1순위다. 이미지·텍스트·오디오 같은 비정형 데이터나, 특징 간 복잡한 계층적 표현을 스스로 학습해야 하는 경우, 데이터가 수천만 건 이상으로 매우 크고 GPU 병렬화 이득이 큰 경우는 딥러닝이 앞선다. 표 데이터에서조차 순수 정확도만 보면 [부스팅](#/c/boosting) 계열(XGBoost 등)이 랜덤 포레스트를 근소하게 이기는 경우가 많아, 실무에서는 랜덤 포레스트를 베이스라인으로 먼저 돌리고 최종 모델은 부스팅으로 가는 경우가 흔하다.'},
 {h:'특징 중요도', d:'랜덤 포레스트는 학습 과정에서 자연스럽게 특징 중요도를 계산해준다 — 각 특징이 트리들에서 분할에 쓰일 때 불순도를 평균적으로 얼마나 줄였는지를 합산하는 방식이 대표적이다(MDI, mean decrease in impurity). 이는 신경망에서는 별도의 사후 설명 기법(SHAP 등)이 필요한 것과 달리 모델 자체에 내장된 해석 도구라 실무에서 "어떤 특징이 중요한가"를 빠르게 훑어보는 첫 단계로 즐겨 쓴다.'}
],

math:[
 {tex:'\\hat y(x)=\\dfrac1M\\sum_{m=1}^M T_m(x)', expr:'배깅 앙상블 예측(회귀)', d:'$T_m(x)$ 는 $m$ 번째 트리의 예측, $M$ 은 트리 개수. 분류는 평균 대신 다수결 투표나 확률 평균을 쓴다. 각 $T_m$ 이 서로 독립에 가까울수록(상관이 낮을수록) 평균의 분산 $\\text{Var}(\\hat y)$ 이 트리 하나의 분산보다 크게 줄어든다.'},
 {tex:'\\text{Var}(\\hat y)=\\rho\\sigma^2+\\dfrac{1-\\rho}{M}\\sigma^2', expr:'앙상블 분산의 상관 분해', d:'$\\sigma^2$ 는 트리 하나의 분산, $\\rho$ 는 트리들 사이의 평균 상관. $M$ 을 아무리 늘려도 두 번째 항만 0에 가까워질 뿐 $\\rho\\sigma^2$ 항은 남는다 — 그래서 트리 개수보다 트리 간 상관 $\\rho$ 를 낮추는 것(특징 무작위 선택)이 랜덤 포레스트 성능에 더 결정적이다.'}
],

diagram:{type:'stack', cap:'서로 다른 부분집합으로 학습한 트리들을 평균 내 분산을 줄인다.',
 layers:[{t:'원본 데이터'},{t:'부트스트랩 샘플 N개'},{t:'트리 N개 병렬 학습'},{t:'평균·투표로 집계'}]},

confuse:[
 {a:'배깅(랜덤 포레스트)', b:'[부스팅](#/c/boosting)', d:'배깅은 트리를 병렬·독립적으로 학습해 분산을 줄이고, 부스팅은 트리를 순차적으로 학습하며 이전 트리의 오차를 다음 트리가 보완해 편향을 줄인다. 배깅은 과적합에 강하고 병렬화가 쉬우며, 부스팅은 대개 더 정확하지만 과적합에 더 취약하고 순차적이라 느리다. 둘 다 약한 학습기를 여러 개 쓴다는 점은 같지만 "왜 여러 개를 쓰는가"에 대한 답이 다르다는 것이 핵심 차이다.'},
 {a:'랜덤 포레스트', b:'단순 배깅 트리', d:'배깅만 하면 모든 트리가 매 분할에서 전체 특징을 다 보기 때문에 강한 특징이 있으면 트리들이 서로 비슷해진다. 랜덤 포레스트는 분할마다 특징도 무작위 부분집합으로 제한해 트리 간 상관을 추가로 낮춘다 — 이 특징 무작위성이 랜덤 포레스트를 "랜덤"하게 만드는 핵심이다.'},
 {a:'OOB(out-of-bag) 오차', b:'검증셋 오차', d:'부트스트랩 샘플링은 매 트리마다 평균적으로 데이터의 약 3분의 1을 뽑지 않는다(OOB). 이 안 뽑힌 데이터로 그 트리를 평가하면 별도 검증셋 없이도 일반화 성능을 근사할 수 있다 — 데이터가 적을 때 유용한 랜덤 포레스트만의 특권이며, k-겹 교차검증만큼 비싸지 않으면서도 비슷한 정보를 준다.'}
],

pitfalls:[
 '트리 개수를 늘리면 성능이 계속 좋아질 거라 기대하지만, 어느 지점부터는 성능이 정체되고 학습·추론 비용만 늘어난다 — 무한정 늘리는 게 능사가 아니다.',
 '개별 트리의 편향이 크면(너무 얕거나 특징이 애초에 타깃과 관련이 없으면) 아무리 많은 트리를 평균 내도 그 편향은 사라지지 않는다.',
 '특징 중요도(feature importance)를 볼 때 카디널리티가 높은 범주형 변수나 연속형 변수가 낮은 카디널리티 변수보다 인위적으로 중요하게 나오는 편향이 있다 — 순열 중요도(permutation importance) 같은 대안과 비교해야 한다.',
 '랜덤 포레스트는 학습 데이터 범위 밖의 값을 잘 외삽(extrapolation)하지 못한다 — 트리는 학습 데이터에서 본 값들의 평균으로만 예측하기 때문에, 시계열처럼 값이 계속 증가하는 타깃을 예측할 때 선형회귀보다 못한 경우가 흔하다.'
],

code:{lang:'python', d:'OOB 점수로 별도 검증셋 없이 일반화 성능을 근사하는 예',
 src:'from sklearn.ensemble import RandomForestClassifier\n\nrf = RandomForestClassifier(\n    n_estimators=300, max_features="sqrt",\n    oob_score=True, n_jobs=-1)\nrf.fit(X_train, y_train)\nprint(rf.oob_score_)   # 검증셋 없이도 얻는 일반화 성능 근사치'},

papers:['xgboost','trees-still-win'],
terms:['decision-tree','boosting','ensemble']
});
