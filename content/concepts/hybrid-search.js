WIKI.concept({
slug:'hybrid-search',

tldr:'키워드 검색(BM25)과 밀집 벡터 검색을 함께 써서, 고유명사·코드·숫자는 키워드로 정확히 잡고 표현이 달라도 의미가 통하는 문장은 벡터로 잡는 검색 방식.',

why:'[의미 검색](#/c/semantic-search) 하나만 쓰면 "모델명 GPT-4o"나 에러 코드 "E1042"처럼 정확한 문자열이 핵심인 질의에서 뜻밖에 실패한다. 반대로 [BM25](#/p/bm25) 키워드 검색만 쓰면 동의어·바꿔 쓴 표현을 놓친다. 왜 실무 검색 시스템이 둘 중 하나가 아니라 거의 항상 둘을 같이 쓰는지, 그리고 두 점수를 어떻게 합쳐야 하는지를 모르면 검색 품질 개선의 절반을 못 건드리게 된다. 특히 고객 지원·코드 검색처럼 정확한 식별자와 자연어 질문이 뒤섞여 들어오는 실무 환경에서는 이 선택이 체감 품질을 크게 좌우한다.',

sections:[
 {h:'왜 하나만으론 부족한가', d:'[BM25](#/p/bm25) 같은 희소(sparse) 키워드 검색은 문서에 그 단어가 실제로 등장하는지를 직접 세기 때문에 고유명사·모델 번호·에러 코드·정확한 수치처럼 "정확히 이 토큰이 있어야 하는" 질의에 강하다. 반면 이런 단어는 학습 데이터에 자주 등장하지 않는 경우가 많아 밀집 벡터 임베딩이 그 의미를 정교하게 구분해 담아내기 어렵고, 고차원 임베딩 공간에서는 [차원의 저주](#/c/curse-dimensionality) 때문에 이런 희귀한 세부 차이가 흐려지기 쉽다. 반대로 밀집 벡터 검색은 "반려견 산책"과 "강아지 데리고 나가기"처럼 겹치는 단어가 없어도 의미가 통하는 문장을 찾는 데 강하지만, 정확히 같은 단어가 있어야 할 때는 오히려 의미가 비슷한 다른 문서에 밀려날 수 있다.'},
 {h:'점수를 합치는 법', d:'가장 단순한 방법은 BM25 점수와 코사인 유사도를 가중합하는 것인데, 두 점수는 애초에 스케일과 분포가 전혀 다르다(BM25는 상한이 없고 문서 길이에 영향받는 반면 코사인은 -1~1) — 그래서 단순 가중합은 점수를 정규화하지 않으면 한쪽이 결과를 거의 지배해버리는 문제가 생긴다. RRF(Reciprocal Rank Fusion)는 점수 값 자체를 쓰지 않고 **각 방법에서의 순위**만 이용해 이 문제를 피한다 — 두 방법 각각에서 문서가 몇 등이었는지만 보고 그 등수의 역수를 합산하므로, 점수 스케일이 서로 달라도 정규화 없이 바로 결합할 수 있다.'},
 {h:'가중치 조정의 실무 감각', d:'RRF는 순위만 쓰기 때문에 별도 정규화 없이 쓰기 편하지만, "이번 질의는 키워드가 더 중요하다/의미가 더 중요하다"처럼 상황별로 비중을 세밀하게 조정하고 싶다면 정규화한 점수의 가중합 쪽이 다이얼을 더 직접적으로 쥘 수 있다. 코드 검색·상품 코드 조회처럼 정확한 토큰 매칭이 중요한 도메인은 키워드 쪽 가중치를 높이고, 고객 문의처럼 표현이 다양한 자연어 질의는 벡터 쪽 가중치를 높이는 식으로 실무에서는 도메인별로 이 비율을 조정한다.'},
 {h:'제3의 길: 희소 벡터', d:'[SPLADE](#/p/splade)는 키워드도 벡터도 아닌 절충안이다 — 어휘 사전 크기의 희소 벡터를 학습하되, 각 차원의 가중치를 신경망으로 예측해 원래 문서에 없던 단어라도 의미상 관련 있으면 가중치를 부여한다(용어 확장). 그 결과 BM25처럼 역색인으로 빠르게 검색할 수 있으면서도 순수 키워드 매칭보다 동의어에 더 강하다 — "키워드냐 벡터냐"의 이분법 자체를 흐리는 방향이며, 하이브리드 검색의 두 축을 하나의 인덱스 형태로 통합하려는 시도로 볼 수 있다.'}
],

math:[
 {tex:'\\text{RRF}(d)=\\sum_{r\\in \\{\\text{keyword},\\text{dense}\\}} \\dfrac{1}{k+\\text{rank}_r(d)}',
  expr:'Reciprocal Rank Fusion', d:'$\\text{rank}_r(d)$ 는 검색 방법 $r$(키워드 또는 벡터)에서 문서 $d$ 가 몇 등을 했는지이고, $k$ 는 상위 등수가 지나치게 큰 영향을 미치지 않도록 완충하는 상수(흔히 60)이며, 값을 키우면 등수 차이의 영향이 완만해지고 줄이면 1등에 더 큰 가중치가 쏠린다. 각 방법에서의 등수만 쓰기 때문에 BM25 점수와 코사인 유사도처럼 스케일이 다른 두 지표를 정규화 없이 그대로 합칠 수 있다.'}
],

diagram:{type:'flow', cap:'같은 질의를 두 방식으로 각각 검색한 뒤 순위를 합쳐 최종 결과를 낸다.',
 nodes:[
  {t:'질의'},
  {t:'BM25 검색', s:'정확한 토큰 매칭'},
  {t:'벡터 검색', s:'의미 유사도'},
  {t:'점수/순위 결합', s:'RRF 또는 가중합'},
  {t:'최종 결과'}
 ]},

confuse:[
 {a:'하이브리드 검색', b:'[reranking](#/c/reranking)', d:'하이브리드 검색은 서로 다른 두 1단계 검색 방법(키워드·벡터)의 결과를 결합해 더 나은 후보 집합을 만드는 것이고, 리랭킹은 어떤 방법으로 얻었든 그 후보를 2단계에서 더 무거운 모델로 재정렬하는 것이다. 실무에서는 하이브리드로 후보를 뽑은 뒤 리랭킹으로 다듬는 순서로 함께 쓰는 경우가 많다.'},
 {a:'RRF(순위 기반)', b:'가중합(점수 기반)', d:'RRF는 각 방법의 등수만 이용해 스케일이 다른 점수를 신경 쓰지 않고 바로 합칠 수 있어 구현이 간단하다. 가중합은 점수 자체를 정규화한 뒤 합쳐야 하지만, 그 대신 "키워드 70% : 벡터 30%"처럼 비중을 더 직접적으로 통제할 수 있어, A/B 테스트로 최적 비율을 찾는 팀에서는 가중합을 선호하기도 한다.'},
 {a:'[BM25](#/p/bm25)', b:'[SPLADE](#/p/splade)', d:'BM25는 문서에 실제로 등장한 단어의 빈도만으로 점수를 매기는 전통 통계 기법이라 어휘가 정확히 일치해야 한다. SPLADE는 학습된 신경망으로 희소 벡터의 가중치를 예측해, 문서에 없던 관련 단어에도 점수를 줄 수 있는 "학습된 키워드 검색"에 가깝다.'}
],

pitfalls:[
 '"벡터 검색이 다 잡아줄 것"이라고 믿고 키워드 검색을 빼면, 정확한 모델명·에러 코드·상품 번호 같은 질의에서 뜻밖에 검색이 실패한다 — 이런 실패는 벤치마크 평균 점수에는 잘 드러나지 않고 실사용에서 불쑥 나타난다.',
 '가중합 방식에서 점수를 정규화하지 않고 그대로 더하면, 값 범위가 훨씬 큰 쪽(대개 BM25)이 결과를 사실상 지배해 벡터 검색을 넣은 의미가 없어질 수 있다.',
 'RRF의 상수 $k$ 나 가중합의 비율을 한 번 정해두고 모든 질의에 고정하면, 질의 성격(키워드성 vs 자연어 질문)에 따라 어느 한쪽에 유리하거나 불리한 결과가 계속 반복될 수 있다 — 질의 유형별로 가중치를 다르게 주는 것도 실무 선택지다.',
 'BM25 인덱스와 벡터 인덱스를 서로 다른 시스템에 따로 운영하면, 문서를 갱신·삭제할 때 한쪽만 업데이트하고 다른 쪽을 빠뜨리는 실수로 두 인덱스가 조용히 어긋날 수 있다 — 동기화 여부를 반드시 점검해야 하며, 가능하면 하나의 쓰기 경로에서 두 인덱스를 함께 갱신하도록 파이프라인을 설계하는 편이 안전하다.'
],

code:{lang:'python', d:'RRF로 두 검색 결과(순위 리스트)를 합치는 최소 구현.',
 src:'def rrf(keyword_ranked, dense_ranked, k=60):\n    scores = {}\n    for rank, doc in enumerate(keyword_ranked):\n        scores[doc] = scores.get(doc, 0) + 1 / (k + rank + 1)\n    for rank, doc in enumerate(dense_ranked):\n        scores[doc] = scores.get(doc, 0) + 1 / (k + rank + 1)\n    return sorted(scores, key=scores.get, reverse=True)'},

papers:['bm25','dpr','splade'],
terms:['semantic-search','similarity-metric','reranking','curse-dimensionality']
});
