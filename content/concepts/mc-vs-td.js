WIKI.concept({
slug:'mc-vs-td',

tldr:'몬테카를로는 에피소드가 끝날 때까지 기다려 실제 누적 보상으로 가치를 추정하고(비편향·고분산), TD는 한 스텝 뒤의 추정치로 즉시 갱신한다(편향·저분산) — 이 편향-분산 트레이드오프가 대부분의 강화학습 설계 선택의 뿌리다.',

why:'[PPO](#/p/ppo)의 [GAE](#/p/gae)가 λ 하나로 이 둘 사이를 조절하는 이유, [DQN](#/p/dqn)이 왜 한 스텝(또는 n스텝)만 보고도 학습이 되는 이유, RLHF 에서 긴 시퀀스의 보상을 어떻게 각 토큰에 나눠줄지 고민하는 이유가 전부 여기서 나온다. 이 대비를 모르면 "왜 어떤 알고리즘은 에피소드 전체가 끝나야 학습하고, 어떤 건 한 스텝마다 학습하는가"를 설명할 수 없다.',

sections:[
 {h:'몬테카를로(MC)', d:'에피소드를 끝까지 실행한 뒤, 실제로 관측된 보상들을 그대로 더해 리턴 $G_t=\\sum_{k=0}^{\\infty}\\gamma^k r_{t+k}$ 을 계산하고 이를 $V(s_t)$ 의 타깃으로 쓴다. 이 리턴은 실제로 일어난 일을 그대로 반영하므로 **편향이 없다**(참값의 기댓값과 정확히 같다). 하지만 에피소드 하나하나의 보상 시퀀스는 환경의 무작위성과 정책의 확률적 선택이 여러 스텝 누적된 결과라서 **분산이 크다** — 같은 상태에서 시작해도 에피소드마다 리턴이 크게 다를 수 있다.'},
 {h:'TD(Temporal Difference)', d:'한 스텝만 실제로 진행한 뒤, 나머지 미래는 현재 학습 중인 가치 함수의 **추정치**로 대체한다: 타깃은 $r_t+\\gamma V(s_{t+1})$. [벨만 방정식](#/c/bellman)의 재귀를 한 스텝만 전개해 쓰는 것이다. 이 타깃은 아직 정확하지 않은 $V(s_{t+1})$ 을 참값처럼 취급하므로 **편향**이 생긴다. 하지만 무작위성이 한 스텝치만 개입하므로 **분산은 작다** — 그리고 에피소드가 끝나기 전에도, 심지어 끝이 없는(episodic 하지 않은) 문제에서도 매 스텝 학습할 수 있다.'},
 {h:'n스텝과 그 사이', d:'MC 와 TD(1스텝)는 스펙트럼의 양 끝이다. n스텝 리턴은 $n$ 스텝만큼은 실제 보상을 쓰고 그 이후는 추정치로 대체한다 — $n$ 이 클수록 MC 에, 작을수록 TD 에 가까워진다. $n$ 을 늘리면 분산은 늘고 편향은 줄며, 줄이면 반대다. 이 트레이드오프를 하이퍼파라미터 하나로 매끄럽게 조절하는 것이 [GAE](#/p/gae)의 λ다 — λ=0 이면 순수 TD(1스텝), λ=1 이면 순수 MC 와 동일해진다.'},
 {h:'LLM 정렬에서의 함의', d:'RLHF 에서 보상은 보통 응답 시퀀스가 끝난 뒤 한 번(시퀀스 레벨)만 주어진다. 이를 각 토큰 스텝의 가치로 되돌려 계산할 때 TD 방식(부트스트래핑)을 쓰면 학습 신호가 빠르고 분산이 낮지만, 크리틱(가치 함수)이 부정확하면 편향된 신호가 전 토큰에 퍼진다. [PPO](#/p/ppo) 기반 RLHF 구현이 GAE 를 쓰는 이유가 바로 이 트레이드오프를 조절하기 위해서다.'}
],

math:[
 {tex:'G_t=\\sum_{k=0}^{\\infty}\\gamma^k r_{t+k}\\ \\ (\\text{MC}),\\qquad \\text{target}_t=r_t+\\gamma V(s_{t+1})\\ \\ (\\text{TD})', expr:'MC return vs TD target', d:'MC 의 $G_t$ 는 실제로 관측된 보상을 끝까지 다 더한 값이고, TD 타깃은 딱 한 스텝의 실제 보상 $r_t$ 뒤에 학습 중인 추정치 $V(s_{t+1})$ 을 붙인 값이다. 둘 다 $V(s_t)$ 를 갱신하는 목표값으로 쓰인다.'}
],

diagram:{type:'compare', cap:'MC와 TD의 편향-분산 트레이드오프.',
 left:{t:'몬테카를로', items:['에피소드 끝까지 대기','실제 보상만 사용','비편향, 고분산']},
 right:{t:'TD (1스텝)', items:['한 스텝마다 갱신','추정치로 부트스트랩','편향 존재, 저분산']}},

confuse:[
 {a:'편향(bias)', b:'분산(variance)', d:'편향은 추정치의 기댓값이 참값과 얼마나 다른가(TD 는 부정확한 $V$ 를 참값처럼 써서 편향이 생긴다), 분산은 같은 상황을 반복 측정했을 때 추정치가 얼마나 들쭉날쭉한가(MC 는 긴 무작위 시퀀스를 통째로 반영해 분산이 크다)를 잰다. 둘을 동시에 0으로 만들 수 없다는 것이 이 대비의 핵심 긴장이다.'},
 {a:'TD', b:'부트스트래핑', d:'TD 는 학습 방법의 이름이고, 부트스트래핑은 그 방법이 쓰는 메커니즘(추정치로 추정치를 갱신)의 이름이다. [벨만 방정식](#/c/bellman)이 재귀 구조를 제공하고, TD 는 그 재귀를 한 스텝만 실행해 부트스트래핑하는 구체적 알고리즘이다.'},
 {a:'$n$스텝 리턴', b:'GAE의 λ', d:'$n$ 스텝 리턴은 정수 $n$ 하나를 골라 그 지점까지만 실제 보상을 쓰는 이산적 선택이고, [GAE](#/p/gae)는 여러 $n$ 스텝 리턴을 $\\lambda^{n-1}$ 가중치로 지수적으로 평균 내 하나의 연속적인 다이얼(λ∈[0,1])로 편향-분산을 조절한다.'}
],

pitfalls:[
 '"TD 가 항상 더 낫다"고 단순화하면 안 된다 — 편향이 누적되면(가치 함수 자체가 초반에 부정확) 학습이 잘못된 방향으로 수렴할 수 있고, 이 경우 MC 의 비편향성이 오히려 안전하다.',
 'MC 는 에피소드가 반드시 끝나야(terminal state 도달) 리턴을 계산할 수 있어서, 끝이 없거나 아주 긴 태스크(연속 제어, 장기 대화)에는 그대로 적용하기 어렵다.',
 'GAE 의 λ 를 1에 가깝게 두면 MC 에 가까워져 분산이 커지고, 0에 가깝게 두면 크리틱의 편향에 더 의존하게 된다 — 크리틱이 부정확한 학습 초반에 λ 를 낮게 두면 오히려 해로울 수 있다.'
],

papers:['dqn','gae'],
terms:['bellman','mdp','actor-critic','onpolicy-offpolicy']
});
