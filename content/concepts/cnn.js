WIKI.concept({
slug:'cnn',

tldr:'이미지처럼 격자 구조를 가진 데이터에 지역 연결·가중치 공유를 적용해, 완전연결보다 훨씬 적은 파라미터로 평행이동에 강인한 특징을 계층적으로 뽑는 신경망.',

why:'백본을 고를 때(ResNet이냐 ViT냐), 왜 이미지 입력을 굳이 합성곱으로 처리하는지, 파라미터 수가 왜 이렇게 다른지를 모르면 모델 크기·연산량·데이터 요구량을 가늠할 수 없다. CNN의 세 가지 설계 원칙은 이후 ViT가 "일부러 포기한" 것이 무엇인지 이해하는 기준점이기도 하다.',

sections:[
 {h:'완전연결이 안 되는 이유', d:'224×224×3 이미지를 완전연결(FC) 층 하나로 처리하면 입력 뉴런 15만 개가 은닉 뉴런 하나마다 각각의 가중치를 가져야 한다. 은닉 유닛 1000개만 둬도 파라미터가 1.5억 개를 넘고, 이미지가 조금만 커져도 폭발적으로 늘어난다. 더 근본적인 문제는 위치 불변성이 없다는 것 — 고양이가 왼쪽 위에 있을 때 학습한 가중치가 오른쪽 아래에 있는 고양이에는 전혀 재사용되지 않는다.'},
 {h:'세 가지 설계 원칙', d:'CNN은 이 문제를 세 가지 아이디어로 푼다. **지역 연결(local connectivity)**: 각 출력 뉴런은 입력 전체가 아니라 작은 [수용 영역](#/c/receptive-field)만 본다. **가중치 공유(weight sharing)**: 같은 커널이 이미지 전체를 슬라이딩하며 같은 패턴(예: 세로 엣지)을 위치와 무관하게 찾는다. **계층적 특징(hierarchical features)**: 얕은 층은 엣지·색 같은 저수준 패턴을, 깊은 층은 이들을 조합한 눈·바퀴 같은 고수준 패턴을 학습한다 — 이 계층 구조가 [수용 영역](#/c/receptive-field)이 층마다 커지기 때문에 가능하다.'},
 {h:'전형적인 구조', d:'[합성곱](#/c/convolution) → 활성함수 → [풀링](#/c/pooling)을 여러 번 반복해 공간 해상도를 줄이고 채널 수를 늘리다가, 마지막에 전역 평균 풀링이나 flatten으로 벡터를 만들어 분류기(FC 층)에 넣는다. [LeNet](#/p/lenet)이 이 틀을 처음 제시했고, [AlexNet](#/p/alexnet)이 GPU로 스케일을 키웠으며, [ResNet](#/p/resnet)이 [잔차 연결](#/c/residual)로 100층 이상을 학습 가능하게 만들었다.'},
 {h:'오늘날의 위치', d:'ViT 이후로 대규모 데이터가 있을 때는 Transformer 계열이 CNN의 정확도를 넘어서는 경우가 많아졌다. 하지만 CNN의 지역성·가중치 공유라는 귀납적 편향은 데이터가 적을 때 더 빨리 수렴하게 해 주므로, 소규모 데이터셋·엣지 디바이스·객체 탐지 백본에서는 여전히 CNN(또는 CNN-Transformer 하이브리드)이 기본 선택으로 남아 있다.'}
],

math:[
 {tex:'\\#\\text{params}_{FC} = (H\\cdot W\\cdot C_{in})\\times N_{out},\\quad \\#\\text{params}_{conv} = k^2\\cdot C_{in}\\cdot C_{out}',
  expr:'FC vs conv 파라미터 수', d:'FC는 입력 전체 크기에 비례하는 파라미터를 은닉 유닛마다 새로 갖지만, 합성곱은 커널 크기 k와 채널 수에만 비례하고 이미지 크기와 무관하다. 224×224×3 입력, 출력 채널 64, 3×3 커널이면 conv는 1,728개, 같은 출력을 FC로 내려면 수천만 개가 필요하다.'}
],

diagram:{type:'stack', cap:'전형적인 CNN은 공간 해상도를 줄이며 채널(특징의 수)을 늘리는 방향으로 쌓인다.',
 layers:[
  {t:'입력 이미지', s:'224×224×3'},
  {t:'Conv+ReLU+Pool', s:'×N 반복, 해상도↓ 채널↑'},
  {t:'GAP / Flatten', s:'전역 평균 풀링, 벡터로 변환'},
  {t:'완전연결 + Softmax', s:'클래스 확률'}
 ]},

confuse:[
 {a:'CNN', b:'완전연결(MLP)', d:'MLP는 모든 입력-출력 쌍에 독립적인 가중치를 두어 위치 정보를 구조적으로 활용하지 못한다. CNN은 지역 연결과 가중치 공유로 "같은 패턴은 어디에 있든 같은 필터로 찾는다"는 가정을 아키텍처에 박아 넣은 것이다.'},
 {a:'CNN', b:'ViT', d:'CNN은 지역성·평행이동 불변성을 아키텍처가 강제하는 귀납적 편향(inductive bias)이 강한 모델이라 데이터가 적어도 잘 학습된다. ViT는 이런 가정 없이 [셀프 어텐션](#/c/self-attention)으로 전역 관계를 처음부터 학습하므로, 대신 훨씬 많은 데이터나 사전학습이 필요하다.'}
],

pitfalls:[
 '"합성곱은 평행이동에 불변(invariant)하다"는 정확한 말이 아니다 — 커널이 위치와 무관하게 같은 반응을 낸다는 점에서 등변(equivariant)이고, 뒤따르는 [풀링](#/c/pooling)이 그 등변성을 근사적으로 불변성으로 바꿔 준다.',
 '파라미터가 적다고 연산량(FLOPs)도 적은 것은 아니다. 가중치는 공유되지만 커널은 출력 위치마다 다시 계산되므로, 해상도가 크면 연산량은 여전히 크다.',
 'CNN이 "이미지 전용"이라는 오해가 있는데, 1D 합성곱은 시계열·오디오·텍스트에도 쓰이고 그래프·포인트클라우드용 변형도 있다 — 핵심은 격자形 구조와 지역 상관관계의 존재다.'
],

papers:['lenet','alexnet','resnet'],
terms:['convolution','pooling','receptive-field','residual','self-attention']
});
