WIKI.concept({
slug:'kv-cache',

tldr:'자기회귀 생성 중 이전 토큰들의 Key·Value 벡터를 매 스텝 다시 계산하지 않도록 저장해 두는 메모리.',

why:'KV 캐시가 없으면 토큰을 하나 생성할 때마다 지금까지의 전체 시퀀스에 대해 [어텐션](#/c/attention-c)을 처음부터 다시 계산해야 해서 생성이 시퀀스 길이에 대해 제곱으로 느려진다. 반대로 캐시를 켜면 속도는 빨라지지만 그 대가로 메모리가 [컨텍스트 윈도우](#/c/context-window) 길이·배치 크기에 비례해 커져, 실제 서빙에서 "GPU 메모리가 왜 이렇게 빨리 차는가"의 주범이 된다.',

sections:[
 {h:'왜 캐시가 필요한가', d:'자기회귀 디코딩은 토큰을 한 번에 하나씩 생성하고, 매 스텝 새 토큰까지 포함한 시퀀스 전체를 다시 모델에 태운다. [셀프 어텐션](#/c/self-attention)에서 각 쿼리 토큰은 그 이전 모든 토큰의 Key·Value 와 내적해 어텐션을 계산하는데, 캐시가 없으면 $t$번째 토큰을 만들 때 1번부터 $t-1$번째 토큰의 Key·Value 를 전부 처음부터 다시 계산하게 된다. 이는 이미 이전 스텝에서 계산해 뒀던 값과 완전히 동일하다 — 매 스텝 같은 계산을 반복하는 낭비다. KV 캐시는 한 번 계산한 각 토큰의 Key·Value 를 저장해 뒀다가, 다음 스텝에서는 새로 생긴 토큰 하나의 Key·Value 만 계산해 이어 붙이는 방식으로 이 중복을 없앤다. 캐시가 없으면 전체 생성 비용이 시퀀스 길이 $n$에 대해 $O(n^3)$(스텝마다 $O(n^2)$ 어텐션을 $n$번) 에 가깝고, 캐시가 있으면 $O(n^2)$(스텝마다 새 토큰 하나의 $O(n)$ 어텐션을 $n$번) 수준으로 줄어든다.'},
 {h:'메모리가 커지는 이유', d:'캐시는 레이어마다, 어텐션 헤드마다, 시퀀스의 모든 토큰마다 Key 벡터와 Value 벡터를 따로 저장한다. 배치로 여러 요청을 동시에 처리하면 그만큼 배로 늘어난다. 이 네 가지 요인(레이어 수·헤드 수·시퀀스 길이·배치 크기)이 전부 곱으로 들어가기 때문에, 컨텍스트가 길어지거나 동시 요청이 늘어나면 캐시 메모리가 순식간에 GPU 메모리를 잠식한다.'},
 {h:'이래서 나온 최적화들', d:'캐시 크기 자체를 줄이려는 방향에서 [MQA](#/p/mqa)(모든 쿼리 헤드가 Key·Value 헤드 1개를 공유)와 [GQA](#/p/gqa)(헤드를 몇 개 그룹으로 묶어 공유)가 나왔다 — 헤드 수 $H$에 비례하던 캐시를 그룹 수만큼으로 줄인다. 캐시를 고정 크기 블록으로 나눠 운영체제 가상메모리처럼 관리해 메모리 단편화·낭비를 줄이는 방향에서 [PagedAttention(vLLM)](#/p/vllm)이 나왔다. 캐시가 길어질 때 중요도가 낮은 과거 토큰의 KV 를 통째로 버려서 길이 자체를 줄이는 방향에서 [H2O](#/p/h2o)(heavy-hitter 토큰만 남기고 축출)가 나왔다. 세 방향 모두 "왜 캐시가 이렇게 큰가"라는 같은 문제를 서로 다른 축(헤드 수·메모리 배치·시퀀스 길이)에서 공략한 것이다.'},
 {h:'실무에서', d:'서빙 프레임워크(vLLM, TensorRT-LLM 등)를 고를 때 "KV 캐시를 어떻게 관리하는가"가 동시 처리 가능한 요청 수(throughput)를 직접 좌우한다. 긴 컨텍스트를 유지하는 멀티턴 챗봇을 서빙할수록 캐시 메모리 압박이 커지므로, 대화 히스토리를 무한정 늘리기보다 오래된 턴을 요약·삭제하는 전략이 비용과 직결된다. 모델 카드에서 GQA·MQA 채택 여부를 확인하는 것도 서빙 비용을 가늠하는 실무적 습관이다.'}
],

math:[
 {tex:'\\text{KV 캐시 크기}\\approx 2\\cdot L\\cdot H\\cdot n\\cdot d_h\\cdot b\\cdot(\\text{bytes/elem})', expr:'KV 캐시 메모리', d:'$L$ 레이어 수, $H$ Key/Value 헤드 수, $n$ 시퀀스 길이, $d_h$ 헤드 차원, $b$ 배치 크기. 맨 앞 2 는 Key 와 Value 두 텐서를 각각 저장하기 때문이다. 이 다섯 요인이 전부 곱으로 들어가므로 시퀀스가 길어지거나 배치가 커지면 캐시가 선형으로, 여러 요인이 동시에 커지면 그보다 더 빠르게 늘어난다.'}
],

diagram:{type:'compare', cap:'캐시 없이 매 스텝 재계산 vs 캐시로 증분만 계산.',
 left:{t:'캐시 없음', items:['매 스텝 전체 시퀀스 재계산','스텝마다 O(n²) 어텐션','전체 생성 O(n³)에 가까움']},
 right:{t:'KV 캐시 사용', items:['이전 Key·Value 재사용','새 토큰 1개만 추가 계산','전체 생성 O(n²) 수준']}},

confuse:[
 {a:'KV 캐시', b:'[컨텍스트 윈도우](#/c/context-window)', d:'컨텍스트 윈도우는 모델이 지원하는 토큰 수의 이론적 상한이고, KV 캐시는 그 토큰들을 실제로 처리하며 쌓이는 실물 메모리다. 윈도우 스펙이 커도 서버 메모리가 부족하면 캐시가 그만큼 못 쌓여 실제 처리 가능한 길이가 줄어든다.'},
 {a:'[MQA](#/p/mqa)/[GQA](#/p/gqa)', b:'[PagedAttention](#/p/vllm)', d:'MQA·GQA 는 헤드 구조를 바꿔 캐시 자체의 크기를 줄이는 모델 설계 기법이고, PagedAttention 은 이미 정해진 크기의 캐시를 메모리에 어떻게 효율적으로 배치·관리할지를 다루는 서빙 시스템 기법이다. 층위가 다르다 — 함께 쓸 수 있다.'},
 {a:'캐시 축출(H2O)', b:'[컨텍스트 윈도우](#/c/context-window) 확장', d:'H2O 같은 캐시 축출은 이미 처리한 토큰의 KV 를 버려 메모리를 아끼는 서빙 시점 기법이고, YaRN·LongRoPE 같은 컨텍스트 확장은 모델이 애초에 더 긴 시퀀스를 이해하도록 만드는 학습·추론 시점 기법이다.'}
],

pitfalls:[
 '"모델이 크니까 느리다"고만 생각하기 쉽지만, 긴 컨텍스트에서는 KV 캐시 메모리 대역폭이 연산량보다 먼저 병목이 되는 경우가 많다.',
 'KV 캐시는 배치 크기에도 비례한다 — 동시 요청 수를 늘리면 처리량은 늘지만 캐시 메모리도 같이 늘어나 어느 지점에서 OOM(메모리 부족)으로 막힌다.',
 'MQA·GQA 로 캐시를 줄이면 헤드마다 다른 표현을 유지하던 표준 멀티헤드 어텐션보다 표현력이 줄 수 있다는 트레이드오프가 있다 — 공짜로 얻는 최적화가 아니다.'
],

papers:['vllm','h2o','mqa'],
terms:['context-window','self-attention','multi-head']
});
