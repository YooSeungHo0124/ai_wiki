WIKI.concept({
slug:'linear-regression',

tldr:'입력의 가중합으로 연속값을 예측하는 가장 단순한 모델로, 잔차 제곱합을 최소화하는 계수를 닫힌 형태(closed-form) 또는 [경사하강법](#/c/gradient-descent)으로 구하며 릿지·라쏘로 정규화한다.',

why:'모든 회귀·신경망 학습의 출발점이 여기다. 선형회귀를 정확히 이해하면 신경망의 마지막 선형층이 무엇을 하는지, 왜 정규화가 필요한지, 왜 특징 스케일링이 중요한지가 한 번에 풀린다. 실무에서도 베이스라인으로 가장 먼저 돌려보는 모델이다 — 복잡한 모델이 이걸 못 이기면 뭔가 잘못된 것이다. 또한 통계학에서 100년 넘게 다듬어진 이론(신뢰구간, 유의성 검정, 잔차 진단)이 그대로 딸려 오기 때문에, "이 특징이 정말 유의미한가"를 통계적으로 따져야 하는 분석 업무에서는 지금도 신경망보다 먼저 선택된다.',

sections:[
 {h:'모델의 형태', d:'예측값은 입력 특징들의 가중합에 절편을 더한 것 — $\\hat y = w^Tx+b$. 학습이란 결국 이 $w,b$ 를 데이터에 가장 잘 맞도록 정하는 과정이다. 특징이 하나면 직선, 여럿이면 초평면(hyperplane)을 데이터에 맞추는 것과 같다. "선형"이라는 이름은 계수 $w$ 에 대해 선형이라는 뜻이지 입력 $x$ 자체가 직선이어야 한다는 뜻은 아니다 — $x^2$ 나 $\\log x$ 같은 특징을 미리 만들어 넣으면 곡선 관계도 선형회귀의 틀 안에서 표현할 수 있다(다항 회귀).'},
 {h:'최소제곱법과 닫힌 해', d:'실제값과 예측값의 차이(잔차)를 제곱해 합한 것을 최소화하면 $w=(X^TX)^{-1}X^Ty$ 라는 닫힌 형태의 해가 나온다. 행렬 역산이 가능한 크기(특징 수가 수천 이하)면 반복 없이 한 번에 답을 구할 수 있다 — 이게 [경사하강법](#/c/gradient-descent) 없이도 풀리는 몇 안 되는 모델인 이유다. 특징이 매우 많거나 $X^TX$ 가 역행렬을 갖지 않으면(다중공선성) 경사하강법이나 정규화가 필요해진다.'},
 {h:'가정이 깨지면', d:'선형회귀는 오차가 정규분포를 따르고 등분산성을 가지며 특징 간 독립이라는 가정 위에 서 있다. 실제 관계가 곡선이거나(비선형성), 이상치가 많거나(제곱 손실은 이상치에 매우 민감하다), 특징끼리 강하게 상관되면(다중공선성, 계수가 불안정해짐) 예측력과 계수 해석 둘 다 무너진다. 잔차 대 예측값 산점도를 그려 패턴(깔때기 모양, 곡선 추세)이 보이는지 확인하는 것이 이 가정들이 깨졌는지 점검하는 가장 실용적인 방법이다.'},
 {h:'실무에서', d:'표 데이터에서 특징과 타깃의 관계가 대략 선형이거나, 계수 자체(어떤 특징이 얼마나 영향을 주는가)를 설명해야 하는 상황(보험료 산정, 정책 효과 분석)에서 여전히 1순위로 쓴다. 비선형 관계가 강하면 [결정 트리](#/c/decision-tree) 계열이나 [부스팅](#/c/boosting)이 정확도에서 이긴다 — 다만 그만큼 해석은 어려워진다.'},
 {h:'릿지와 라쏘', d:'특징이 많거나 다중공선성이 있으면 계수에 [정규화(regularization)](#/c/regularization) 항을 더해 안정시킨다. 릿지(L2)는 계수를 0에 가깝게 고르게 줄여 모든 특징을 조금씩 남기고, 라쏘(L1)는 일부 계수를 정확히 0으로 만들어 특징 선택 효과를 낸다. 둘 다 정규화 세기 $\\lambda$ 가 클수록 모델이 단순해지고(편향↑ 분산↓), 작을수록 일반 선형회귀에 가까워진다(편향↓ 분산↑).'}
],

math:[
 {tex:'\\hat y=w^Tx+b', expr:'선형 예측', d:'$x$ 는 입력 특징 벡터, $w$ 는 각 특징의 가중치, $b$ 는 절편. 특징 하나짜리라면 $\\hat y=wx+b$ 로 흔히 아는 직선의 방정식이다.'},
 {tex:'L(w,b)=\\dfrac1n\\sum_i (y_i-\\hat y_i)^2', expr:'평균제곱오차(MSE)', d:'실제값 $y_i$ 와 예측값 $\\hat y_i$ 의 차이를 제곱해 평균낸 것. 제곱을 쓰기 때문에 큰 오차에 불균형하게 민감하다 — 이상치 하나가 전체 계수를 크게 흔들 수 있다.'},
 {tex:'w=(X^TX)^{-1}X^Ty', expr:'정규방정식(normal equation)', d:'MSE 를 $w$ 에 대해 미분해 0으로 놓고 풀면 나오는 닫힌 형태 해. $X^TX$ 가 역행렬을 가져야 하며([고윳값·SVD](#/c/eigen-svd) 관점에서 특이값이 0에 가까우면 수치적으로 불안정해진다), 특징 수가 매우 크면 이 역산 자체가 비싸다.'}
],

diagram:{type:'flow', cap:'잔차를 줄이는 방향으로 계수를 맞춘다.',
 steps:['데이터 (x,y) 산점도','직선 y=wx+b 가정','잔차 (y-ŷ) 계산','잔차 제곱합 최소화','최적 w,b 확정']},

confuse:[
 {a:'선형회귀', b:'[로지스틱 회귀](#/c/logistic-regression)', d:'이름은 비슷하지만 선형회귀는 연속값을 예측하는 회귀 모델이고 로지스틱 회귀는 확률(0~1)을 거쳐 클래스를 예측하는 분류 모델이다. 로지스틱 회귀는 선형회귀의 출력에 시그모이드를 씌운 것으로 볼 수 있지만 손실함수도 완전히 다르다.'},
 {a:'회귀 계수 $w$', b:'상관계수', d:'회귀 계수는 다른 특징을 고정했을 때 이 특징이 한 단위 늘어나면 타깃이 얼마나 변하는지를 나타내고, 스케일에 의존한다. 상관계수는 두 변수 사이의 선형 관계 강도를 -1~1 로 정규화한 값이다. 계수가 크다고 그 특징이 "더 중요하다"고 바로 해석하면 안 된다 — 스케일부터 맞춰야 비교가 된다.'},
 {a:'최소제곱법', b:'[MLE](#/c/mle)', d:'오차가 정규분포를 따른다고 가정하면 최소제곱해와 최대우도추정(MLE) 해가 정확히 일치한다. 이 사실 때문에 최소제곱법이 "그냥 편의적인 방법"이 아니라 통계적으로 정당화된 방법임을 이해할 수 있다.'},
 {a:'릿지(L2)', b:'라쏘(L1)', d:'둘 다 계수 크기에 벌점을 줘 과적합을 억제하지만, 릿지는 모든 계수를 고르게 줄여 다중공선성이 있는 특징들을 함께 남기고, 라쏘는 일부 계수를 정확히 0으로 만들어 자동으로 특징을 골라낸다. 해석 가능한 소수의 특징이 필요하면 라쏘, 특징을 다 살리면서 안정성만 필요하면 릿지를 우선 고려한다.'}
],

pitfalls:[
 '특징 스케일이 크게 다르면(나이 0~100, 연봉 0~1억) 경사하강법으로 풀 때 수렴이 느려진다 — [특징 스케일링](#/c/scaling-features)이 먼저다.',
 '결정계수 $R^2$ 가 높다고 인과관계가 있다고 착각하면 안 된다 — 상관과 인과는 다르다.',
 '이상치 몇 개가 제곱 손실 때문에 전체 회귀선을 크게 왜곡시킬 수 있다 — 이상치 제거나 로버스트 회귀(Huber loss 등)를 고려해야 한다.',
 '특징 간 다중공선성이 있으면 계수의 부호나 크기가 데이터를 조금만 바꿔도 크게 흔들린다 — 계수를 인과적으로 해석할 때 특히 위험하다.',
 '정규화 없는 선형회귀를 "베이스라인이니 튜닝할 게 없다"고 생각하기 쉽지만, 특징 수가 데이터 수에 비해 많으면 정규화 항 없이는 계수가 과도하게 커지며 과적합한다 — 릿지·라쏘를 기본값으로 함께 검토하는 것이 안전하다.'
],

code:{lang:'python', d:'정규방정식과 경사하강법 두 가지로 같은 해를 구하는 비교',
 src:'import numpy as np\n\n# 닫힌 해 (정규방정식)\nXb = np.hstack([X, np.ones((len(X),1))])\nw_closed = np.linalg.inv(Xb.T @ Xb) @ Xb.T @ y\n\n# 경사하강법 (특징이 많을 때)\nw = np.zeros(Xb.shape[1])\nfor _ in range(1000):\n    grad = -2/len(y) * Xb.T @ (y - Xb @ w)\n    w -= 0.01 * grad   # lr=0.01\n# 둘은 수렴하면 같은 w 로 모인다'},

papers:['backprop'],
terms:['gradient-descent','logistic-regression','regularization','mle','eigen-svd']
});
