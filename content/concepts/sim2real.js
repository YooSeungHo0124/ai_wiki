WIKI.concept({
slug:'sim2real',

tldr:'시뮬레이터에서 학습한 정책이 물리 법칙·센서 노이즈·마찰 같은 미세한 차이 때문에 실제 로봇/환경에서는 성능이 크게 떨어지는 현상으로, 로봇 강화학습을 실전에 배치할 때 반드시 넘어야 하는 관문이다.',

why:'강화학습은 시행착오로 배우기 때문에 실제 로봇으로 수백만 스텝을 시도하는 것은 비용도, 시간도, 안전 문제도 감당이 안 된다. 그래서 거의 모든 로봇 강화학습은 시뮬레이터에서 학습한 뒤 실제로 옮기는데, 이 전이가 왜 실패하는지와 무엇으로 완화하는지를 모르면 "시뮬레이션에서는 잘 되는데 실제 로봇에서는 안 된다"는 로보틱스에서 가장 흔한 실패를 디버깅할 수 없다.',

sections:[
 {h:'격차가 생기는 이유', d:'시뮬레이터는 물리 법칙(마찰 계수, 질량 분포, 모터 지연), 센서 노이즈, 조명·질감 같은 시각적 특성을 근사할 뿐 완벽히 재현하지 못한다. 정책이 시뮬레이터의 이 미세한 부정확함(예: 시뮬레이터에서만 존재하는 특정 마찰값)에 과적합되면, 실제 환경으로 옮겼을 때 그 부정확함이 사라지면서 정책의 가정이 깨진다. 이는 근본적으로 [일반화](#/c/generalization) 문제의 한 형태다 — 학습 분포(시뮬레이션)와 배포 분포(현실)가 다르다.'},
 {h:'도메인 무작위화', d:'도메인 무작위화(domain randomization)는 시뮬레이터의 물리 파라미터(마찰, 질량, 중력, 조명, 텍스처)를 학습 중 무작위로 넓게 흔들어, 정책이 특정 시뮬레이터 설정 하나에 과적합되지 못하게 만드는 기법이다. 정책이 "이 넓은 범위의 물리 법칙 전부에서 그럭저럭 통하는" 행동을 배우도록 강제하면, 실제 물리 법칙도 그 넓은 범위 안 어딘가에 속할 가능성이 높아 전이가 잘 된다. OpenAI 의 로봇 손 조작 연구는 손가락 마찰·물체 질량·크기까지 광범위하게 무작위화해 실제 로봇 손의 복잡한 조작을 학습시킨 대표 사례다.'},
 {h:'시스템 식별', d:'시스템 식별(system identification)은 도메인 무작위화와 반대 방향의 접근이다 — 무작위로 넓게 흔드는 대신, 실제 로봇을 관측해 실제 물리 파라미터(정확한 마찰 계수, 모터 응답 특성 등)를 최대한 정확히 추정하고 시뮬레이터를 그 값에 맞게 보정(calibrate)한다. 시뮬레이터가 현실에 더 가까울수록 격차는 자연히 줄어든다. 실무에서는 두 방법을 함께 쓰는 경우가 많다 — 시스템 식별로 시뮬레이터를 최대한 현실에 맞춘 뒤, 그래도 남는 불확실성을 도메인 무작위화로 덮는다.'},
 {h:'실제 데이터로 마무리하기', d:'[RT-1](#/p/rt1)처럼 대규모 실제 로봇 시연 데이터로 직접 학습하거나, 시뮬레이션에서 사전학습한 정책을 소량의 실제 데이터로 미세조정하는 것도 sim2real 격차를 줄이는 실무적 해법이다. 순수 시뮬레이션 학습이 스케일(값싸게 수백만 스텝)의 이점을, 실제 데이터 미세조정이 정확성의 이점을 각각 제공하므로 두 단계를 조합하는 파이프라인이 흔하다.'}
],

diagram:{type:'compare', cap:'sim2real 격차를 줄이는 두 접근.',
 left:{t:'도메인 무작위화', items:['물리 파라미터를 넓게 흔듦','특정 설정에 과적합 방지','현실이 범위 안에 있길 기대']},
 right:{t:'시스템 식별', items:['실제 파라미터 정밀 추정','시뮬레이터를 현실에 보정','격차 자체를 좁힘']}},

confuse:[
 {a:'sim2real 격차', b:'일반적인 일반화(generalization) 격차', d:'둘 다 학습 분포와 평가 분포의 차이에서 온다는 점은 같지만, sim2real 은 그 차이가 "물리 시뮬레이션 대 실제 물리 법칙"이라는 구체적이고 구조적인 형태를 띤다는 점이 다르다 — 그래서 도메인 무작위화·시스템 식별 같은 로보틱스 고유의 대응책이 발달했다.'},
 {a:'도메인 무작위화', b:'데이터 증강([augmentation](#/c/augmentation))', d:'데이터 증강은 이미 가진 데이터에 변형(회전, 자르기 등)을 가해 다양성을 늘리는 것이고, 도메인 무작위화는 시뮬레이터의 생성 파라미터 자체를 흔들어 매번 다른 물리 조건의 새 경험을 만들어낸다는 점에서 더 능동적이다. 목적(과적합 방지, 강건성 확보)은 비슷하다.'},
 {a:'시스템 식별', b:'모델 기반 강화학습(model-based RL)', d:'시스템 식별은 시뮬레이터(물리 엔진)의 파라미터를 실제 값에 맞추는 것이고, 모델 기반 강화학습은 환경의 전이 함수 자체를 신경망 등으로 학습해 그 학습된 모델 안에서 계획하거나 정책을 학습하는 것이다. 둘 다 "환경을 더 정확히 안다"는 목적은 같지만 대상(물리 파라미터 대 전이 함수 전체)이 다르다.'}
],

pitfalls:[
 '도메인 무작위화 범위를 너무 넓게 잡으면 정책이 어느 하나의 물리 조건에도 특화되지 못해 시뮬레이션 안에서조차 성능이 떨어질 수 있다 — 범위는 "실제로 있을 법한 불확실성"을 반영해야지 무작정 넓히는 게 능사가 아니다.',
 '시각적 sim2real 격차(렌더링이 실제 카메라 이미지와 다름)를 물리적 격차와 혼동하기 쉽다 — 물리 도메인 무작위화만 하고 시각 도메인 무작위화(질감·조명·카메라 노이즈)를 빠뜨리면 시각 입력에 의존하는 정책은 여전히 실패한다.',
 '시뮬레이션에서의 높은 성공률을 실제 배포 가능성의 증거로 그대로 받아들이면 안 된다 — 반드시 소규모라도 실제 환경에서 검증하는 단계를 거쳐야 한다.'
],

papers:['rt1','dexterity'],
terms:['generalization','augmentation','rl-basics']
});
