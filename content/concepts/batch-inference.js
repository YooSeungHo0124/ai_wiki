WIKI.concept({
slug:'batch-inference',

tldr:'여러 요청을 한 번의 GPU 연산에 묶어 처리해 하드웨어 활용률을 높이는 서빙 기법으로, 정적 배칭과 연속 배칭(continuous batching)으로 나뉜다.',

why:'GPU 는 요청 하나를 처리하든 서른 개를 처리하든 대부분의 연산이 메모리에서 가중치를 읽어오는 데 들어간다. 요청을 하나씩 순서대로 처리하면 이 비용을 매번 다시 치르는 셈이라 GPU 가 대부분 놀고 있는 상태가 된다. 배칭을 어떻게 하느냐가 같은 GPU 로 감당할 수 있는 동시 사용자 수, 즉 서빙 비용을 직접 좌우한다.',

sections:[
 {h:'왜 배칭이 이득인가', d:'[KV 캐시](#/c/kv-cache) 문서에서 다루듯 decode 단계는 메모리 대역폭이 병목이다 — 토큰 하나를 만들기 위해 모델 가중치 전체(수십 GB)를 GPU 메모리에서 읽어와야 하는데, 이 읽기 비용은 요청이 하나든 여러 개든 거의 동일하다. 여러 요청의 decode 스텝을 한 번의 행렬 연산으로 묶으면 가중치를 한 번 읽어 여러 요청의 토큰을 동시에 계산할 수 있어, 읽기 비용을 요청 수만큼 나눠 갖는 효과가 난다. 이것이 배칭이 처리량(throughput)을 크게 올리는 이유다.'},
 {h:'정적 배칭의 한계', d:'가장 단순한 방식은 몇 개의 요청을 모아 배치를 고정하고, 배치 안의 모든 요청이 끝날 때까지 기다렸다가 다음 배치를 시작하는 것이다. 문제는 요청마다 생성 길이가 다르다는 점이다 — 배치 안에서 가장 긴 응답이 끝날 때까지 이미 끝난 짧은 요청의 자리는 비워둔 채 기다려야 해서(패딩과 유휴 슬롯), GPU 가 다시 놀게 된다.'},
 {h:'연속 배칭', d:'[vLLM](#/p/vllm), [SGLang](#/p/sglang) 같은 서빙 엔진이 쓰는 방식으로, 배치를 고정된 묶음으로 보지 않고 매 decode 스텝마다 끝난 요청은 배치에서 빼고 새로 도착한 요청을 그 자리에 즉시 채워 넣는다. 요청 하나하나가 서로 다른 시점에 들어오고 나가도 배치의 GPU 활용률은 계속 높게 유지된다. 이 방식으로 같은 하드웨어에서 처리량이 몇 배 늘어나는 것이 실제로 보고된 이유다.'},
 {h:'처리량 vs 지연', d:'배치를 키울수록 처리량은 늘지만, 개별 요청이 배치 안 다른 요청들과 연산을 나눠 쓰므로 [TPOT](#/c/streaming-output)(토큰당 시간)는 늘어나기 쉽다. 즉 서버 전체가 처리하는 총 토큰 수는 늘어도, 한 사용자가 느끼는 토큰 생성 속도는 느려질 수 있다. 실무에서는 최대 배치 크기나 동시 처리 요청 수에 상한을 둬서 지연이 일정 수준을 넘지 않도록 제어한다.'}
],

diagram:{type:'compare', cap:'정적 배칭은 가장 긴 요청을 기다리고, 연속 배칭은 끝난 자리를 바로 채운다.',
 left:{t:'정적 배칭', items:['배치 고정 후 시작','짧은 요청도 끝까지 대기','유휴 슬롯 발생']},
 right:{t:'연속 배칭', items:['스텝마다 배치 갱신','끝난 요청 즉시 교체','GPU 활용률 유지']}},

confuse:[
 {a:'배치 추론(서빙)', b:'[배치·에폭](#/c/batch-epoch)(학습)', d:'학습에서의 배치는 경사 하나를 계산하는 데 묶는 샘플 수이고, 서빙에서의 배치는 동시에 처리하는 서로 다른 사용자 요청 묶음이다. 목적(학습 안정성 vs 처리량)과 구성 방식이 전혀 다르다.'},
 {a:'처리량(throughput)', b:'지연시간(latency)', d:'처리량은 서버가 단위 시간에 처리하는 총 토큰·요청 수, 지연시간은 한 요청이 끝나기까지 걸리는 시간이다. 배치를 키우면 대개 처리량은 오르고 개별 지연은 나빠진다 — 실시간 채팅과 대량 배치 작업이 이 트레이드오프를 반대로 최적화한다.'},
 {a:'연속 배칭', b:'[스트리밍](#/c/streaming-output)', d:'연속 배칭은 서버가 여러 요청을 GPU 에서 어떻게 나눠 처리하는지의 문제이고, 스트리밍은 한 요청의 결과를 클라이언트에 어떻게 순차 전달하는지의 문제다. 서버는 연속 배칭으로 요청들을 처리하면서 각각을 스트리밍으로 내보낸다.'}
],

pitfalls:[
 '"배치를 키우면 무조건 빠르다"고 생각하기 쉽지만, 처리량은 올라가도 개별 요청의 체감 속도(TPOT)는 오히려 느려질 수 있다.',
 '정적 배칭 방식의 서버를 쓰면서 요청 길이가 들쭉날쭉하면 GPU 활용률이 생각보다 훨씬 낮게 나온다 — 연속 배칭 지원 여부를 서빙 엔진 선택의 기준으로 삼아야 한다.',
 '배치 크기를 무한정 늘리면 KV 캐시 메모리가 먼저 바닥나 OOM 이 난다 — 처리량은 배치 크기가 아니라 메모리 여유로도 제한된다.'
],

papers:['vllm','sglang'],
terms:['kv-cache','streaming-output','serving-arch']
});
