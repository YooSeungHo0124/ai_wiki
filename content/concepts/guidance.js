WIKI.concept({
slug:'guidance',

tldr:'확산 모델이 생성할 때 조건(텍스트 프롬프트 등)을 얼마나 강하게 따르게 할지 조절하는 손잡이로, 값을 올릴수록 조건 충실도는 오르지만 다양성과 사실성은 떨어진다.',

why:'이미지 생성 도구에서 가장 흔히 마주치는 숫자(CFG scale)인데, "높을수록 무조건 좋다"고 잘못 쓰면 프롬프트는 잘 따르지만 부자연스럽게 과포화되거나 망가진 이미지를 계속 만들게 된다.',

sections:[
 {h:'왜 유도가 필요한가', d:'조건 없이 학습된 확산 모델은 "그럴듯한 이미지 아무거나"를 생성하는 방향으로 잡음을 걷어낸다. 텍스트 조건을 주더라도 모델이 그 조건을 약하게만 반영하면, 프롬프트와 대충 관련은 있지만 세부 지시(색상·구도·개수)를 놓친 이미지가 나온다. 유도는 매 역방향 스텝에서 "조건을 따르는 방향"을 인위적으로 증폭시켜 이 반영 강도를 조절하는 장치다.'},
 {h:'CFG 의 구조', d:'초기 유도 방식은 별도의 분류기를 학습시켜 그 그래디언트로 조건 방향을 밀어주는 것이었지만, [CFG](#/p/cfg)는 분류기 없이 같은 확산 모델을 조건부(prompt 를 줌)와 무조건부(prompt 를 비움)로 각각 한 번씩 실행하고, 두 예측의 차이를 조건 방향으로 간주해 외삽(extrapolate)한다. 이 차이를 스케일 $w$ 만큼 증폭해 최종 잡음 예측에 반영하는 것이 CFG scale이다. 별도 분류기 학습이 필요 없어졌다는 점이 CFG 가 널리 쓰이게 된 실무적 이유다. 학습 시에는 일정 확률로 조건을 비운 채(무조건부로) 같은 모델을 학습시켜, 하나의 네트워크가 조건부와 무조건부 예측을 모두 할 수 있게 만든다.'},
 {h:'scale 의 부작용', d:'스케일 $w$ 를 올릴수록 조건부와 무조건부 예측의 차이가 더 크게 증폭되어, 모델이 프롬프트가 가리키는 방향으로 더 강하게 밀려간다. 프롬프트 충실도(요청한 대상·속성이 실제로 나타나는 정도)는 이 덕분에 올라간다. 그러나 이 외삽은 원래 모델이 학습한 확률분포 바깥으로 예측을 밀어내는 것이므로, 스케일이 과하면 색이 과포화되거나 대비가 비정상적으로 강해지고, 같은 프롬프트를 여러 번 생성해도 비슷비슷한 구도만 나오는(다양성 붕괴) 현상, 그리고 해부학적으로 이상한 형태 같은 사실성 저하가 함께 나타난다.'},
 {h:'실무에서', d:'CFG scale 은 보통 중간값(도구·모델마다 기본값이 다르며 이는 각 구현의 관행이지 보편 상수가 아니다)에서 시작해 프롬프트 충실도가 부족하면 올리고, 이미지가 부자연스럽거나 다양성이 필요하면 낮추는 식으로 조정한다. 복잡하고 구체적인 프롬프트(여러 객체·정확한 속성)는 더 높은 스케일이 필요한 경향이 있고, 스타일 위주의 느슨한 프롬프트는 낮은 스케일에서도 충분하다. 스케일을 극단으로 올려 문제를 해결하려 하기보다, 프롬프트 자체를 더 명확히 쓰는 쪽이 부작용 없이 충실도를 올리는 방법인 경우가 많다.'},
 {h:'텍스트 생성에도 있는 개념', d:'유도라는 개념 자체는 확산 모델에 국한되지 않는다. 언어모델의 [temperature](#/c/temperature)·[top-k-top-p](#/c/top-k-top-p) 도 넓게 보면 "생성이 조건(문맥)을 얼마나 결정적으로 따를지"를 조절하는 손잡이라는 점에서 역할이 비슷하다. 다만 구체적인 메커니즘은 다르다 — CFG 는 조건부/무조건부 예측의 차이를 외삽하는 것이고, temperature 는 확률분포 자체의 뾰족한 정도를 조절하는 것이다.'}
],

math:[
 {tex:'\\hat{\\epsilon}=\\epsilon_\\theta(x_t,\\varnothing)+w\\big(\\epsilon_\\theta(x_t,c)-\\epsilon_\\theta(x_t,\\varnothing)\\big)', expr:'classifier-free guidance', d:'$\\epsilon_\\theta(x_t,c)$ 는 조건 $c$(프롬프트)를 준 예측, $\\epsilon_\\theta(x_t,\\varnothing)$ 은 조건을 비운 예측이다. $w=1$ 이면 순수 조건부 예측 그대로이고, $w>1$ 이면 두 예측의 차이(조건이 만드는 방향)를 그 이상으로 증폭해 조건을 더 강하게 반영하도록 외삽한다.'}
],

diagram:{type:'compare', cap:'CFG scale 을 올릴 때의 트레이드오프.',
 left:{t:'낮은 scale', items:['프롬프트 충실도 낮음','다양한 구도·색감','상대적으로 자연스러운 사실성']},
 right:{t:'높은 scale', items:['프롬프트 충실도 높음','비슷한 구도로 수렴(다양성 저하)','과포화·왜곡 등 사실성 저하']}},

confuse:[
 {a:'유도(guidance) scale', b:'[확산](#/c/diffusion-basics) 샘플링 스텝 수', d:'스텝 수는 잡음을 몇 번에 나눠 걷어낼지를 정하는 속도-품질 축이고, guidance scale 은 매 스텝에서 조건을 얼마나 강하게 반영할지를 정하는 충실도-다양성 축이다. 둘은 독립적으로 조절되며 서로 다른 문제를 해결한다.'},
 {a:'classifier-free guidance', b:'classifier guidance(분류기 유도)', d:'분류기 유도는 별도로 학습된 분류기의 그래디언트를 빌려 조건 방향을 만들어야 해서 분류기 학습·유지 비용이 든다. CFG 는 같은 확산 모델을 조건부/무조건부로 두 번 실행하는 것만으로 분류기 없이 같은 효과를 낸다 — 지금 대부분의 텍스트-투-이미지 모델이 쓰는 방식이다.'}
],

pitfalls:[
 '"scale 을 최대한 올리면 프롬프트를 가장 잘 따른다"는 생각은 절반만 맞다 — 어느 지점을 넘으면 충실도 대신 과포화·왜곡만 늘어난다.',
 'CFG scale 의 "적당한 값"은 모델·해상도·프롬프트 길이에 따라 다르며, 한 모델에서 좋았던 값을 다른 모델에 그대로 옮기면 안 맞을 수 있다 — 이는 각 구현의 관행이지 보편 상수가 아니다.',
 '다양성이 떨어진 것을 프롬프트 문제로 오인하기 쉽지만, 원인이 지나치게 높은 guidance scale 인 경우가 흔하다.',
 'scale=1(순수 조건부 예측)이 "유도가 없는 상태"라고 착각하기 쉽지만, 실제로 유도 효과가 완전히 없는 지점은 무조건부 예측만 쓰는 scale=0 쪽에 더 가깝다 — scale=1도 이미 조건을 어느 정도 반영한 상태다.',
 '같은 scale 값이라도 스케줄러나 스텝 수가 바뀌면 체감 충실도가 달라질 수 있다 — scale 하나만 고정된 상수처럼 취급하면 다른 샘플링 설정을 바꿀 때 재조정이 필요하다는 것을 놓친다.'
],

papers:['cfg','ldm'],
terms:['diffusion-basics','temperature','top-k-top-p']
});
