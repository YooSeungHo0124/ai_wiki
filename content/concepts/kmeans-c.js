WIKI.concept({
slug:'kmeans-c',

tldr:'데이터를 k개의 군집으로 나누되, 각 군집 중심(centroid)까지의 거리 제곱합이 최소가 되도록 중심과 소속을 번갈아 갱신하는 비지도 군집화 알고리즘으로, k는 미리 정해야 한다.',

why:'라벨이 없는 데이터를 빠르게 탐색하고 구조를 파악하는 가장 단순한 도구다. 고객 세분화·이상 탐지의 정상 군집 정의·[VQ-VAE](#/p/vqvae) 류 벡터 양자화의 코드북 학습까지, "비슷한 것끼리 묶는다"는 기본 연산이 필요한 곳마다 지금도 실무 파이프라인의 한 조각으로 들어간다. 구현이 몇 줄이면 끝나고 결과 해석이 직관적이라, 본격적인 군집화 알고리즘을 고르기 전에 데이터가 어떤 구조인지 빠르게 감 잡는 탐색적 분석(EDA) 단계에서 거의 항상 먼저 시도해보는 도구이기도 하다. 대규모 데이터에도 잘 확장되어(미니배치 변형 포함) 수백만 건 규모에서도 실시간에 가깝게 돌릴 수 있다는 것도 실무 채택률이 높은 이유다.',

sections:[
 {h:'번갈아 갱신하는 두 단계', d:'k-평균은 두 단계를 수렴할 때까지 반복한다. (1) 할당 단계: 각 점을 가장 가까운 중심에 배정한다. (2) 갱신 단계: 각 군집에 배정된 점들의 평균으로 중심을 다시 계산한다. 이 두 단계를 번갈아 반복하면 군집 내 거리 제곱합이 단조 감소하며 결국 지역 최적점에 수렴한다. 이 구조는 기댓값 최대화(EM) 알고리즘의 특수한 경우로 볼 수 있다 — 할당이 E-step, 중심 갱신이 M-step에 대응한다.'},
 {h:'k는 사람이 정해야 한다', d:'군집 개수 $k$ 를 알고리즘이 스스로 정하지 않는다 — 미리 정해줘야 한다. 엘보우 방법(군집 수를 늘려가며 군집 내 거리 제곱합 감소가 꺾이는 지점을 찾음)이나 실루엣 계수로 적당한 $k$ 를 추정하지만, 이 역시 명확한 정답이 없는 휴리스틱이다. 실루엣 계수는 각 점이 자기 군집과 얼마나 가깝고 다른 군집과는 얼마나 먼지를 -1~1 로 재는 지표라, 엘보우가 애매할 때 보조 지표로 함께 본다.'},
 {h:'초기화에 민감하다', d:'k-평균은 볼록(convex)하지 않은 최적화 문제라 초기 중심 위치에 따라 다른 지역 최적점에 수렴한다. 무작위 초기화를 여러 번 시도해 가장 좋은 결과를 고르거나(n_init), 초기 중심들이 서로 멀리 떨어지도록 고르는 k-means++ 초기화를 쓰는 것이 실무 표준이다. sklearn 의 `KMeans` 는 기본값으로 이미 k-means++ 를 쓰고 n_init 을 여러 번 반복해 가장 낮은 관성(inertia)의 결과를 자동으로 고른다.'},
 {h:'구형 가정의 한계', d:'k-평균은 유클리드 거리를 쓰기 때문에 암묵적으로 군집이 대략 구형(원형)이고 크기·밀도가 비슷하다고 가정한다. 실제 데이터의 군집이 길쭉하거나, 크기가 크게 다르거나, 밀도가 다르면 k-평균은 잘못된 경계를 만든다 — 이런 경우 DBSCAN(밀도 기반)이나 가우시안 혼합 모델 같은 대안을 쓴다.'},
 {h:'실무에서', d:'고객 세분화(구매 패턴으로 고객 그룹 나누기), 이미지 색상 양자화(픽셀 색을 k개 대표색으로 압축), 이상 탐지의 전처리(정상 데이터의 군집 중심에서 먼 점을 이상치 후보로 표시)처럼 "먼저 대략적인 구조를 보고 싶다"는 모든 탐색 단계에서 기본값으로 쓴다. 구현이 간단하고 대규모 데이터에도 잘 확장되는(미니배치 k-평균 변형도 있음) 것이 실무에서 꾸준히 선택받는 이유다. 딥러닝 파이프라인 안에서도 [VQ-VAE](#/p/vqvae) 의 코드북 학습처럼 연속 벡터를 이산 코드로 양자화하는 서브루틴으로 k-평균이 그대로 들어가는 경우가 있다.'}
],

math:[
 {tex:'J=\\sum_{k=1}^{K}\\sum_{x_i\\in C_k}\\|x_i-\\mu_k\\|^2', expr:'군집 내 거리 제곱합(관성, inertia)', d:'$C_k$ 는 $k$ 번째 군집, $\\mu_k$ 는 그 군집의 중심. 각 점이 자기 군집 중심에서 얼마나 떨어져 있는지를 전부 제곱해 더한 값으로, k-평균은 할당과 중심 갱신을 반복하며 이 값을 줄여 나간다. $J$ 는 전역 최적을 보장하지 않는 비볼록 함수라, 초기화가 다르면 서로 다른 지역 최적값에 도달할 수 있다.'}
],

diagram:{type:'loop', cap:'할당과 중심 갱신을 번갈아 하며 관성을 줄인다.',
 steps:['중심 k개 무작위 초기화','각 점을 가장 가까운 중심에 할당','군집별 평균으로 중심 갱신','변화 없을 때까지 반복']},

confuse:[
 {a:'k-평균', b:'[k-NN](#/c/knn)', d:'k-평균은 라벨 없는 데이터를 스스로 k개 군집으로 나누는 비지도학습이고, k-NN 은 라벨이 있는 데이터에서 가까운 k개 이웃의 다수결로 예측하는 지도학습이다. 이름의 k 가 가리키는 대상도 다르다(군집 수 vs 이웃 수) — 이름의 유사성이 실무에서도 혼동의 원인이 된다.'},
 {a:'k-평균 군집화', b:'[PCA](#/c/pca-c)', d:'둘 다 비지도학습이고 "차원/데이터를 요약한다"는 인상을 주지만, k-평균은 데이터를 이산적인 군집으로 나누고(분류에 가까운 결과), PCA 는 데이터를 저차원 연속 공간으로 투영한다(압축에 가까운 결과). 둘을 함께 써서 PCA 로 차원을 줄인 뒤 k-평균을 돌리는 조합도 흔하다 — 고차원에서는 거리 개념 자체가 흐려지므로 차원을 먼저 줄이는 것이 군집 품질에 도움이 된다.'},
 {a:'군집 중심(centroid)', b:'실제 데이터 점(medoid)', d:'k-평균의 중심은 군집에 속한 점들의 평균이라 실제 데이터에 존재하지 않는 가상의 점일 수 있다. k-medoids 는 평균 대신 실제 데이터 점 중 하나를 대표로 고른다 — 이상치에 덜 민감하고 거리 함수를 유클리드가 아닌 것(예: 임의의 유사도 행렬)으로도 쓸 수 있다는 장점이 있지만 계산은 더 비싸다.'}
],

pitfalls:[
 '특징 스케일을 맞추지 않으면 값의 범위가 큰 특징이 거리 계산을 지배해 군집이 그 특징 위주로만 나뉜다 — [특징 스케일링](#/c/scaling-features)이 필수다. 예를 들어 나이(0~100)와 연봉(0~억 단위)을 그대로 쓰면 사실상 연봉만으로 군집이 갈린다.',
 '군집 개수 $k$ 를 임의로 정하고 결과를 해석하면 실제로는 존재하지 않는 구조를 봤다고 착각하기 쉽다 — 엘보우·실루엣 같은 근거를 같이 봐야 하고, 도메인 지식(예: "고객을 3등급으로 나누고 싶다")이 있으면 그것도 함께 반영해야 한다.',
 '이상치 하나가 군집 평균(중심)을 크게 끌어당길 수 있다 — 평균을 쓰는 알고리즘의 공통적인 약점이다. 이상치를 미리 제거하거나 중심 대신 실제 데이터 점을 대표로 쓰는 k-medoids 로 바꾸는 것이 대안이다.',
 '군집 결과가 매번 조금씩 다르게 나오는 것을 버그로 오해하는 경우가 있다 — 무작위 초기화 때문에 정상적인 현상이며, 여러 번 돌려 가장 좋은 결과를 고르는 것이 표준 관행이다.',
 '고차원 데이터에 그대로 k-평균을 돌리고 결과를 신뢰하는 경우가 있는데, [차원의 저주](#/c/curse-dimensionality) 때문에 고차원에서는 거리 기반 군집화가 원래 잘 작동하지 않는다 — 차원 축소를 먼저 검토해야 한다.'
],

code:{lang:'python', d:'엘보우와 실루엣 계수를 함께 보며 k 를 탐색하는 예',
 src:'from sklearn.cluster import KMeans\nfrom sklearn.metrics import silhouette_score\n\ninertias, sils = [], []\nfor k in range(2, 11):\n    km = KMeans(n_clusters=k, init="k-means++", n_init=10)\n    labels = km.fit_predict(X_scaled)   # 스케일링을 먼저 했다고 가정\n    inertias.append(km.inertia_)\n    sils.append(silhouette_score(X_scaled, labels))\n# inertias 로 엘보우, sils 로 실루엣을 함께 보고 k 를 정한다'},

papers:['vqvae','patchcore'],
terms:['knn','pca-c','curse-dimensionality','scaling-features']
});
