WIKI.concept({
slug:'few-shot',

tldr:'프롬프트 안에 예시 몇 개(퓨샷) 또는 전혀(제로샷) 넣지 않고 지시만으로 모델이 과제를 수행하게 하는 방식으로, 가중치를 전혀 바꾸지 않고 추론 시점의 입력만으로 행동을 바꾼다는 점이 핵심이다.',

why:'"이 모델 파인튜닝 없이 예시 몇 개만 넣어서 되던데요"라는 말을 들을 때, 그게 왜 가능한지(가중치는 안 바뀌는데 성능이 달라진다)를 설명 못 하면 프롬프트 엔지니어링과 미세조정 중 언제 뭘 써야 하는지 판단할 수 없다.',

sections:[
 {h:'제로샷·퓨샷·문맥 내 학습', d:'제로샷(zero-shot)은 과제 설명만 주고 예시 없이 바로 답을 요구한다. 퓨샷(few-shot)은 프롬프트 안에 (입력, 정답) 예시를 몇 개(보통 1~수십 개) 넣은 뒤 새 입력에 답하게 한다. 이렇게 프롬프트에 넣은 예시로부터 그 자리에서 패턴을 파악해 답을 내는 능력 전체를 문맥 내 학습(in-context learning, ICL)이라 부른다 — [GPT-3](#/p/gpt3)가 대규모로 이 능력을 보여주며 "학습(learning)"이라는 단어를 쓸 만큼 인상적인 현상으로 주목받았다.'},
 {h:'가중치를 바꾸지 않는다', d:'ICL 에서 모델은 프롬프트의 예시를 forward pass 동안 어텐션으로 "참고"만 할 뿐, 역전파도 경사 갱신도 일어나지 않는다 — 추론이 끝나면 그 예시가 모델에 남기는 흔적은 전혀 없다. 이것이 [미세조정](#/c/pretrain-finetune)·[PEFT](#/c/peft)와의 근본적 차이다: 미세조정은 가중치(또는 LoRA 같은 추가 파라미터)를 영구히 바꾸는 학습이고, ICL 은 매 요청마다 프롬프트라는 "일회성 문맥"으로만 행동을 조절한다. 왜 ICL 이 작동하는가에 대해서는 어텐션이 프롬프트 안에서 암묵적으로 그래디언트 하강과 비슷한 연산을 수행한다는 가설([Induction Heads](#/p/induction-heads) 계열 해석 연구)이 있지만, 실제 가중치가 갱신되지 않는다는 사실 자체는 불변이다.'},
 {h:'문맥 예시가 성능을 좌우한다', d:'ICL 성능은 예시의 개수뿐 아니라 [어떤 예시를 어떤 순서로 넣는가](#/p/good-in-context)에 민감하게 반응한다 — 같은 예시라도 순서를 바꾸면 정확도가 크게 흔들릴 수 있고, 쿼리와 의미적으로 가까운 예시를 고르는 것이 무작위 선택보다 대체로 낫다고 보고된다. 예시의 "라벨이 실제로 맞는가"보다 "입력-출력의 형식과 분포"가 더 중요하다는 관찰도 있어, ICL 이 미세조정처럼 새 지식을 정확히 학습하는 과정이라기보다 이미 가진 능력을 문맥으로 끌어내는 과정에 가깝다는 해석을 뒷받침한다.'},
 {h:'언제 ICL, 언제 미세조정', d:'과제가 자주 안 바뀌고 데이터가 충분하며 지연시간·비용에 민감하면(예시를 매번 프롬프트에 넣으면 입력 토큰 수가 늘어 비용·지연시간이 커진다) 미세조정이나 PEFT가 유리하다. 과제가 자주 바뀌거나, 라벨 데이터가 거의 없거나, 빠르게 실험해봐야 하는 상황에서는 ICL 이 훨씬 저렴하고 즉각적이다. 실무에서는 프롬프트(ICL)로 먼저 베이스라인을 잡고, 비용·정확도가 안 맞으면 미세조정으로 전환하는 순서가 흔하다.'}
],

diagram:{type:'compare', cap:'같은 "새 과제 적응"이지만 가중치를 바꾸는지가 갈린다.',
 left:{t:'문맥 내 학습 (제로샷/퓨샷)', items:['프롬프트에 지시/예시만 추가','가중치 불변, 매 요청마다 새로 적용','즉시 가능, 예시 늘수록 입력 비용↑']},
 right:{t:'미세조정/PEFT', items:['가중치(또는 LoRA 등) 영구 갱신','한 번 학습하면 추론은 짧고 저렴','데이터·학습 비용 필요']}},

confuse:[
 {a:'퓨샷 문맥 내 학습', b:'퓨샷 미세조정(few-shot fine-tuning)', d:'이름이 비슷해 자주 섞인다. 문맥 내 학습의 "퓨샷"은 프롬프트에 넣는 예시 개수를 말하며 가중치는 그대로다. 퓨샷 미세조정은 적은 수의 라벨 데이터로 실제 경사 하강을 돌려 가중치를 갱신하는 것으로, 예시가 적다는 것만 같고 가중치를 바꾼다는 점에서 근본적으로 다르다.'},
 {a:'제로샷', b:'사전학습 지식의 일반화', d:'제로샷이 되는 이유는 마법이 아니라, [사전학습](#/c/pretrain-finetune) 단계에서 이미 그와 비슷한 과제·형식을 방대하게 봤기 때문인 경우가 많다. 완전히 새로운 형식이나 사전학습 데이터에 거의 없던 도메인에서는 제로샷 성능이 급격히 떨어진다 — "제로샷이 된다"가 "아무 학습 없이도 이해한다"는 뜻은 아니다.'},
 {a:'문맥 내 학습', b:'[프리픽스 튜닝](#/p/prefix-tuning)', d:'둘 다 입력 쪽에 무언가를 덧붙이지만, ICL 의 예시는 사람이 읽을 수 있는 텍스트 토큰이고 경사 하강이 전혀 개입하지 않는다. 프리픽스 튜닝의 prefix 는 사람이 읽을 수 없는 연속 벡터이며 학습 데이터로 경사 하강을 돌려 최적화한, PEFT 에 속하는 방법이다.'}
],

pitfalls:[
 '"문맥에 예시를 넣었으니 모델이 배웠다"는 표현은 비유다 — 다음 요청에서 그 예시 없이 물으면 방금 "배운" 것처럼 보였던 행동이 사라진다. 지속적인 개선이 필요하면 미세조정이 맞는 도구다.',
 '예시 개수를 늘릴수록 항상 좋아지지는 않는다 — 컨텍스트 윈도우를 많이 차지해 정작 중요한 지시가 잘리거나, 예시 순서·분포 편향이 오히려 성능을 해칠 수 있다.',
 '제로샷/퓨샷 성능만 보고 모델 능력을 평가하면 프롬프트 설계 품질과 모델 자체의 능력을 혼동하기 쉽다 — 같은 모델도 프롬프트를 바꾸면 성능이 크게 달라진다.'
],

papers:['gpt3','good-in-context'],
terms:['pretrain-finetune','peft','supervision-types']
});
