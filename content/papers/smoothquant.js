WIKI.paper({
slug:'smoothquant',
venue:'ICML 2023 (MIT · NVIDIA)',
authors:'Guangxuan Xiao, Ji Lin, Mickael Seznec, Hao Wu, Julien Demouth, Song Han',
arxiv:'2211.10438',

tldr:'LLM을 가중치·활성값 모두 INT8(**W8A8**)로 양자화하면서 재학습 없이 정확도를 지키는 방법. 활성값의 이상치가 문제의 원인이라는 진단에서 출발해, 그 어려움을 **수학적으로 동일한 변환**을 통해 다루기 쉬운 가중치 쪽으로 옮긴다.',

context:'가중치만 INT8로 줄이면 메모리는 절반이 되지만, 실제 추론 속도를 올리려면 활성값도 INT8로 줄여 정수 GEMM 커널(`INT8 GEMM`)을 써야 한다. 문제는 LLM을 6.7B 파라미터 이상으로 키우면 **활성값에 체계적인 이상치(outlier)**가 나타난다는 점이다 — 특정 채널의 값이 나머지보다 100배 가까이 커서, 텐서 전체의 양자화 스텝을 그 이상치에 맞추면 나머지 대다수 값에는 유효 비트가 거의 남지 않는다. [LLM.int8()](#/p/llm-int8)은 이 이상치만 FP16으로 따로 빼서 처리하는 혼합 정밀도 분해로 정확도를 지켰지만, 이 방식은 하드웨어 가속기에서 효율적으로 구현하기 어렵다는 대가를 치른다. SmoothQuant는 "이상치를 예외로 취급"하는 대신 "애초에 이상치를 없애는" 접근을 택한다.',

ideas:[
 {h:'수학적으로 동등한 재배분: 활성값과 가중치는 수식적으로 교환 가능하다',
  lead:'채널별 스무딩 팩터로 활성값을 나누고 가중치에 곱해 곱셈 결과는 그대로 두고 난이도만 옮긴다.',
  d:'선형층 $Y=XW$ 에서 채널별 대각행렬 $s$ 를 끼워 넣어도 $Y=(X\\text{diag}(s)^{-1})(\\text{diag}(s)W)$ 로 수학적으로 완전히 동일하다. 활성값 $X$ 를 $s$ 로 나눠 채널별 크기 차이를 줄이는 대신, 가중치 $W$ 에는 같은 만큼 곱해 커진다. 결과값 $Y$ 는 전혀 바뀌지 않으면서, 양자화하기 어려운 쪽(활성값)의 난이도를 원래 양자화하기 쉬운 쪽(가중치)으로 넘기는 것이다.'},
 {h:'가중치는 원래 평평하다 — 옮겨 받을 여유가 있다',
  lead:'가중치 분포는 채널 간 크기 차이가 활성값보다 훨씬 작아 난이도를 더 받아도 여전히 쉽다.',
  d:'활성값의 특정 채널이 다른 채널보다 100배 큰 반면, 가중치는 채널마다 크기가 비교적 고르다. 그래서 활성값의 이상치 크기 일부를 가중치로 옮겨도 가중치 쪽은 여전히 양자화하기 쉬운 평평한 분포를 유지한다. 두 텐서의 **원래 난이도 격차** 자체가 이 이전을 가능하게 하는 전제다.'},
 {h:'이전 강도 α로 두 텐서 사이의 균형을 조절',
  lead:'하이퍼파라미터 α로 활성값·가중치 중 어느 쪽에 난이도를 더 줄지 정한다.',
  d:'$s_j = \\max(|X_j|)^\\alpha / \\max(|W_j|)^{1-\\alpha}$ 로 채널별 스무딩 팩터를 정한다. $\\alpha=0.5$가 기본값으로 대부분의 OPT·BLOOM 모델에 잘 맞지만, 활성값 이상치가 유난히 큰 모델(GLM-130B 등)은 $\\alpha$를 더 크게 줘서 난이도를 활성값 쪽에 더 많이 남기는 대신 가중치로 더 옮긴다. 스무딩 팩터는 소수의 보정(calibration) 샘플로 오프라인에 한 번만 계산하고, 이전 층의 파라미터에 흡수시켜 런타임 오버헤드가 없다.'},
 {h:'하드웨어 친화적: per-tensor/per-channel INT8 GEMM 그대로 사용',
  lead:'이상치를 따로 빼지 않으므로 표준 INT8 GEMM 커널을 수정 없이 그대로 쓸 수 있다.',
  d:'[LLM.int8()](#/p/llm-int8)처럼 일부 채널을 FP16으로 분리하면 커널 안에서 두 정밀도를 섞어 계산해야 해 구현이 복잡하고 느려진다. SmoothQuant는 스무딩 후 텐서 전체가 균일하게 INT8이므로, 표준 per-tensor 또는 per-channel INT8 GEMM 커널을 그대로 활용할 수 있어 `FasterTransformer` 같은 실제 서빙 프레임워크에 바로 통합된다.'}
],

diagram:{type:'compare', cap:'LLM.int8()은 이상치를 예외 처리하고, SmoothQuant는 애초에 이상치를 만들지 않는다.',
 left:{t:'LLM.int8(): 이상치 분리', items:['이상치 채널만 FP16 유지','나머지만 INT8','혼합정밀도 커널 필요/느림']},
 right:{t:'SmoothQuant: 난이도 이전', items:['수학적으로 동등한 재조정','활성값·가중치 모두 균일 INT8','표준 INT8 GEMM 그대로 사용']}},

math:[
 {expr:'Y = (X·diag(s)⁻¹) · (diag(s)·W) = X̂·Ŵ',
  tex:'Y=(X\\,\\text{diag}(s)^{-1})\\cdot(\\text{diag}(s)\\,W)=\\hat{X}\\hat{W}',
  d:'채널별 대각행렬 $s$ 를 끼워 넣어도 곱 $Y$ 는 그대로다. $\\hat{X}$ 는 양자화하기 쉬워지고 $\\hat{W}$ 는 약간 어려워지지만 여전히 쉬운 수준을 유지한다.'},
 {expr:'s_j = max(|X_j|)^α / max(|W_j|)^(1-α)',
  tex:'s_j=\\dfrac{\\max(|X_j|)^{\\alpha}}{\\max(|W_j|)^{1-\\alpha}}',
  d:'채널 $j$ 의 스무딩 팩터. $\\alpha$ 는 난이도를 활성값·가중치 중 어느 쪽에 얼마나 남길지 정하는 이전 강도(기본값 0.5).'}
],

numbers:[
 {k:'W8A8 정확도 손실', v:'OPT-175B 등 6개 벤치마크 평균 거의 무손실', d:'재학습 없이 가중치·활성값 모두 INT8, LAMBADA·HellaSwag·PIQA 등'},
 {k:'속도 향상', v:'최대 1.56×', d:'FasterTransformer에 통합, FP16 대비 (OPT-30B, 단일 GPU 기준)'},
 {k:'메모리 절감', v:'최대 2×(최대 1.96×)', d:'PyTorch 구현 기준 GPU 메모리 사용량, FP16 대비'},
 {k:'GPU 대수 절감', v:'OPT-175B: FP16 대비 절반', d:'8 GPU → 4 GPU로 동일 모델 서빙, 속도는 오히려 더 빠름'},
 {k:'초대형 모델 서빙', v:'MT-NLG 530B를 8-GPU 노드 1대에', d:'W8A8 양자화로 처음으로 530B 모델을 단일 노드에서 서빙'}
],

impact:'LLM 양자화의 핵심 난제가 "활성값 이상치"라는 것을 명확히 진단하고, 이상치를 예외로 처리하는 대신 **수학적으로 동일한 재배분**으로 근본 원인을 없애는 접근을 보여줬다. 추가 학습이나 특수 커널 없이 표준 INT8 GEMM만으로 OPT·BLOOM·GLM·MT-NLG급 초대형 모델을 무손실에 가깝게 양자화할 수 있음을 실증해, LLM 서빙 비용을 낮추는 실용적인 표준 기법이 됐다.',

legacy:[
 '**"난이도를 다른 텐서로 옮긴다"는 발상이 이후 양자화 연구의 공통 도구가 됨** — 활성값-가중치 재분배라는 틀 자체가 여러 후속 기법에 재사용',
 '**[GPTQ](#/p/gptq) 등 가중치 전용 양자화와 결합 가능한 전처리로 자주 함께 쓰임** — 서로 다른 층에 적용되는 기법이라 상호 배타적이지 않음',
 '**FP8 양자화([FP8](#/p/fp8))에도 같은 스무딩 아이디어가 이식**되며, 비트 포맷과 무관하게 통하는 일반적 원리임이 확인됨',
 '**대형 오픈소스 LLM 서빙 스택(vLLM, TensorRT-LLM 등)의 기본 양자화 옵션으로 채택**'
],

pitfalls:[
 '**α=0.5가 모든 모델에 최적은 아니다.** 활성값 이상치 정도가 모델마다 달라(GLM-130B 등은 더 크다) 적절한 이전 강도를 모델별로 조정해야 한다.',
 '**`LLM.int8()`과 문제의식은 같지만 해법은 정반대다.** LLM.int8()은 이상치를 "분리해서" FP16으로 남기고, SmoothQuant는 이상치를 "옮겨서" 애초에 만들지 않는다. 두 방법을 같은 계열로 뭉뚱그리면 안 된다.',
 '**속도 향상 수치는 특정 구현(FasterTransformer/PyTorch)과 하드웨어(A100) 기준이다.** 1.56×·2× 같은 숫자를 다른 프레임워크·GPU에 그대로 일반화할 수 없다.'
],

figures:[
 {f:'fig2-intuition.png',
  cap:'(a) 원본: 활성값 |X|는 이상치(빨강)가 양자화 범위를 늘려놔서 대부분 값의 유효 비트가 거의 남지 않는 반면(빨강, 어려움), 가중치 |W|는 평평해 쉽다(초록). (b) SmoothQuant 적용 후: 난이도를 옮겨받은 |Ŵ|도 여전히 쉬운 수준이고, |X̂|는 이상치가 눌려 스무딩되어 둘 다 쉬워진다.',
  src:'원문 Figure 2, p.2'}
],

quotes:[
 {t:'SmoothQuant smooths the activation outliers by offline migrating the quantization difficulty from activations to weights with a mathematically equivalent transformation.',
  src:'Abstract, p.1'}
],

links:[
 {t:'arXiv 2211.10438 — SmoothQuant', u:'https://arxiv.org/abs/2211.10438'},
 {t:'GitHub — mit-han-lab/smoothquant', u:'https://github.com/mit-han-lab/smoothquant'}
]
});
