WIKI.concept({
slug:'autoencoder',

tldr:'입력을 저차원 병목으로 압축했다가 다시 복원하도록 학습시켜, 복원 오차를 최소화하는 과정에서 데이터의 핵심 구조만 남긴 표현을 얻는 신경망이다.',

why:'오토인코더 자체를 최종 생성 모델로 쓰는 일은 드물지만, 압축·복원이라는 발상은 [VAE](#/c/vae-c)의 확률적 잠재변수, [LDM](#/p/ldm)의 잠재 공간, [MAE](#/p/mae)의 자기지도 사전학습까지 이어지는 공통 부품이다. "병목이 왜 압축을 강제하는가"를 모르면 이 계보 전체가 임의의 트릭 나열로만 보인다.',

sections:[
 {h:'병목이 하는 일', d:'인코더 $f_\\theta$ 가 입력 $x$ 를 차원이 훨씬 작은 잠재벡터 $z=f_\\theta(x)$ 로 줄이고, 디코더 $g_\\phi$ 가 $z$ 에서 $\\hat{x}=g_\\phi(z)$ 를 복원한다. $z$ 의 차원이 $x$ 보다 작다는 제약(병목) 때문에 모델은 모든 픽셀을 그대로 베끼지 못하고, 복원에 꼭 필요한 정보만 $z$ 에 눌러 담아야 한다 — 이것이 "압축을 학습한다"는 말의 실체다. 병목이 없으면(예: $z$ 차원이 $x$ 와 같거나 크면) 항등함수를 배워버려 아무 표현도 배우지 못한다.'},
 {h:'대칭 구조가 필수는 아니다', d:'인코더와 디코더는 흔히 서로 거울상 구조(같은 층 수, 반대 방향)로 설계하지만 이는 관행이지 필수 요건이 아니다. 인코더는 CNN, 디코더는 트랜스포머를 쓰는 식의 비대칭 구조도 가능하며, 실제로 [LDM](#/p/ldm)의 오토인코더나 [MAE](#/p/mae)는 인코더·디코더의 크기와 구조를 의도적으로 비대칭으로 설계해(인코더는 무겁게, 디코더는 가볍게) 학습·추론 비용을 조절한다.'},
 {h:'무엇을 위해 압축하는가', d:'복원 손실(대개 $\\lVert x-\\hat{x}\\rVert^2$)만 최소화하므로 지도 레이블이 필요 없는 자기지도 학습이다. 학습이 끝나면 $z$ 는 분류·검색·이상탐지 같은 다운스트림 작업에 쓸 수 있는 압축 표현이 되고, 원본 데이터보다 작은 용량으로 저장·전송할 수도 있다. 다만 일반 오토인코더의 $z$ 공간은 학습 데이터 근방에서만 의미가 있고, 중간의 임의의 점을 디코딩하면 뭉개진 결과가 나오기 쉽다 — 이 한계가 [VAE](#/c/vae-c)가 나온 이유다.'},
 {h:'디노이징 오토인코더', d:'입력에 일부러 잡음을 섞거나 일부를 가린 뒤, 원래의 깨끗한 $x$ 를 복원하도록 학습시키면 단순 항등함수를 흉내 낼 방법 자체가 없어져 병목 제약 없이도 견고한 표현을 배운다. 이미지의 큰 패치를 가리고 나머지로부터 복원하게 하는 [MAE](#/p/mae)가 이 아이디어를 비전 트랜스포머 규모로 확장한 대표적인 예다 — 마스킹 비율을 아주 높게(75%) 가져가야 단순 보간이 아니라 진짜 의미 구조를 배운다는 것이 핵심 관찰이다.'},
 {h:'실무에서', d:'순수 오토인코더를 이미지 생성기로 쓰는 경우는 거의 없다(생성 품질이 [GAN](#/c/gan-c)·확산 모델에 못 미친다). 대신 (1) [LDM](#/p/ldm)처럼 확산 과정을 돌릴 저차원 [잠재 공간](#/c/latent-space)을 만드는 전처리 단계, (2) [MAE](#/p/mae)류의 사전학습으로 표현을 배운 뒤 미세조정하는 백본, (3) 이상탐지(복원 오차가 큰 샘플을 이상치로 판단)로 쓰인다.'},
 {h:'과소완비와 과잉완비', d:'잠재 차원이 입력보다 작은 경우를 과소완비(undercomplete)라 부르며, 병목이 압축을 강제하는 일반적인 설정이다. 반대로 잠재 차원이 입력과 같거나 큰 과잉완비(overcomplete) 오토인코더도 있는데, 이때는 항등함수로 수렴하는 것을 막기 위해 희소성 벌점 같은 별도의 규제가 반드시 필요하다 — 차원만으로 압축을 강제할 수 없기 때문이다.'},
 {h:'선형 오토인코더와 PCA', d:'활성화 함수 없이 선형 변환만으로 인코더·디코더를 만들면, 그 최적해는 주성분분석(PCA)이 찾는 주성분 부분공간과 사실상 같아진다는 것이 알려져 있다 — 오토인코더는 이런 선형 압축을 비선형으로 일반화한 것으로 볼 수 있다. 다만 비선형 활성화가 들어가는 순간 이 등가성은 깨지고, 대신 PCA 로는 잡아내지 못하는 비선형적인 데이터 구조(예: 곡면 위에 놓인 데이터)까지 압축할 수 있게 된다.'},
 {h:'구조적 변형들', d:'병목을 어떻게 규제하느냐에 따라 여러 변형이 갈라진다. Sparse autoencoder 는 잠재 차원은 넉넉히 두되 활성화되는 뉴런 수를 벌점으로 제한해 압축을 유도한다. Contractive autoencoder 는 입력의 작은 변화에 $z$ 가 민감하게 반응하지 않도록 야코비안에 벌점을 준다. [VQ-VAE](#/p/vqvae)는 $z$ 를 연속값이 아니라 미리 정한 코드북의 이산 항목으로 양자화해, 이후 자기회귀 모델이나 트랜스포머로 그 이산 코드를 직접 모델링할 수 있게 한다 — 오토인코더의 병목 아이디어가 얼마나 다양한 방식으로 재활용되는지를 보여준다.'}
],

diagram:{type:'flow', cap:'인코더가 압축하고 디코더가 복원, 복원 오차만으로 학습.',
 nodes:[
  {t:'x', s:'입력'},
  {t:'인코더 f_θ'},
  {t:'z', s:'저차원 병목'},
  {t:'디코더 g_φ'},
  {t:'x̂ vs x', s:'복원 오차'}
 ]},

confuse:[
 {a:'autoencoder', b:'[VAE](#/c/vae-c)', d:'오토인코더는 $z$ 를 결정론적인 한 점으로 뽑아 복원 오차만 최소화한다. VAE 는 $z$ 를 확률분포로 뽑고 그 분포를 정규분포에 가깝게 규제하는 항을 추가로 둬서, 잠재 공간에서 샘플링만으로 새 데이터를 생성할 수 있게 만든다 — 오토인코더는 압축기, VAE 는 생성 모델이다.'},
 {a:'디노이징 오토인코더', b:'확산 모델', d:'디노이징 오토인코더는 잡음 섞인 입력에서 깨끗한 원본을 한 번에 복원하도록 학습하는 표현학습 기법이다. [확산 모델](#/c/diffusion-basics)은 여러 스텝에 걸쳐 조금씩 잡음을 제거하며, 목표도 원본 복원이 아니라 각 스텝에 섞인 잡음 자체를 예측하는 것이다. 이름의 "잡음"이 겹칠 뿐 목적과 구조가 다르다.'},
 {a:'오토인코더의 압축', b:'JPEG 같은 고전 압축', d:'JPEG 은 주파수 변환·양자화라는 고정된 수학 규칙으로 모든 이미지에 동일하게 작동하는 범용 압축이다. 오토인코더의 압축은 특정 학습 데이터 분포에 맞춰 그 규칙 자체를 학습한 것이라, 학습 분포와 동떨어진 입력(예: 자연 이미지로 학습한 오토인코더에 의료 영상을 넣는 것)에서는 압축·복원 품질이 크게 떨어질 수 있다.'}
],

pitfalls:[
 '병목 차원을 줄이기만 하면 무조건 좋은 표현이 나온다고 생각하기 쉽지만, 너무 작으면 복원 자체가 불가능해지고 너무 크면 항등함수로 수렴해 아무것도 배우지 못한다 — 적절한 병목 크기는 데이터와 과제에 따라 다르다.',
 '오토인코더의 잠재 공간이 [VAE](#/c/vae-c)처럼 매끄럽게 보간 가능하다고 가정하면 안 된다 — 규제 항이 없으므로 학습 데이터 사이의 빈 영역을 디코딩하면 예측하기 어려운 결과가 나온다.',
 '오토인코더 하나로 압축·생성·표현학습을 전부 잘한다고 기대하면 안 된다 — 병목 크기·정규화 항·학습 목표(단순 복원 vs 디노이징)를 어떻게 설정하느냐에 따라 어느 한 쪽 용도에 특화되고 나머지는 희생되는 경향이 있다.',
 '복원 손실이 낮다고 해서 다운스트림 과제(분류 등)에 유용한 표현을 배웠다고 단정하면 안 된다 — 픽셀 단위 복원과 의미적으로 유용한 표현은 항상 같이 가지 않는다. [MAE](#/p/mae) 류가 마스킹 비율을 극단적으로 높이는 것도, 단순 보간으로 복원 손실만 낮추는 지름길을 막아 의미 표현을 강제로 배우게 하려는 설계다.',
 '"오토인코더"라는 이름 때문에 인코더·디코더가 항상 같은 종류의 신경망이어야 한다고 생각하기 쉽지만 그런 제약은 없다 — CNN 인코더에 트랜스포머 디코더를 붙이는 식으로 목적에 맞게 자유롭게 조합할 수 있다.',
 '학습 손실이 안정적으로 내려간다고 해서 인코더·디코더가 서로 "협력"하고 있다고 안심하기 쉽지만, 사실 둘은 그저 같은 손실을 함께 최소화하는 것일 뿐 인코더가 만든 $z$ 가 얼마나 의미 있게 조직됐는지는 별도로 확인해야 한다 — 복원 손실만으로는 그 내부 구조를 보장하지 않는다.'
],

papers:['mae','vqvae'],
terms:['vae-c','latent-space','diffusion-basics','gan-c']
});
