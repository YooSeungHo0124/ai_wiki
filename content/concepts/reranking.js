WIKI.concept({
slug:'reranking',

tldr:'대량의 문서에서 빠르게 후보를 추리는 1단계 검색(재현율)과, 그 후보만 정밀하게 다시 채점하는 2단계 리랭킹(정밀도)으로 역할을 나눈 검색 구조.',

why:'검색 품질을 올리고 싶을 때 임베딩 모델을 바꾸거나 청킹을 손보는 것만으로는 한계가 있다. 1단계 검색과 2단계 리랭킹이 애초에 서로 다른 것을 최적화하는 단계라는 걸 모르면, 상위 결과 몇 개의 순서가 미묘하게 틀린 문제를 엉뚱하게 임베딩 모델 탓으로 돌리게 된다. [RAG](#/c/rag-c) 파이프라인에서 답변 품질이 안 오를 때 가장 먼저 손대볼 만한 것이 리랭커인 경우가 많고, 재학습·재색인 없이 비교적 적은 비용으로 시도해 볼 수 있다는 것도 실무에서 매력적인 이유다.',

sections:[
 {h:'왜 2단계인가', d:'문서가 수백만 개면 질의 하나마다 모든 문서를 정밀하게 비교하는 것은 비용상 불가능하다. 그래서 1단계는 속도를 위해 정확도를 일부 희생한다 — [의미 검색](#/c/semantic-search)의 벡터 검색이나 [BM25](#/p/bm25) 같은 방법으로 전체 문서에서 후보 수십~수백 개를 빠르게 추린다. 이 단계의 목표는 "정답이 후보 안에 들어 있는가"(재현율)이지 "1등이 정답인가"(정밀도)가 아니다. 리랭킹은 그렇게 좁혀진 후보만 대상으로, 더 무겁지만 훨씬 정확한 모델로 순위를 다시 매겨 "진짜 1등"을 가려낸다(정밀도).'},
 {h:'교차 인코더가 정확한 이유', d:'1단계에서 쓰는 bi-encoder(듀얼 인코더)는 질의와 문서를 각각 따로 인코딩해 벡터로 만든 뒤 그 벡터끼리 거리를 잰다 — 그래서 문서 벡터를 미리 계산·저장해 둘 수 있고 검색이 빠르다. 반면 cross-encoder(교차 인코더)는 질의와 문서를 **하나로 이어 붙여 함께** 인코딩하면서, 모델 내부의 어텐션이 질의 토큰과 문서 토큰 사이를 직접 오가며 상호작용을 본다. 이 직접적인 상호작용 덕분에 "이 문서가 이 질의에 정말 답하는가"를 훨씬 정교하게 판단하지만(같은 데이터에서 검색 품질 지표가 오르는 경우가 흔히 보고된다), 질의-문서 쌍마다 매번 처음부터 다시 인코딩해야 해서 미리 계산해 둘 수 없고 그만큼 느리다 — 그래서 전체 문서가 아니라 1단계가 추린 top-k에만 적용한다.'},
 {h:'대표 아키텍처', d:'[monoBERT](#/p/monobert)·[monoT5](#/p/monot5)는 질의-문서 쌍을 통째로 넣어 "관련 있음/없음" 점수 하나를 뽑는 전형적인 point-wise cross-encoder다. [ColBERT](#/p/colbert)는 그 중간 지점을 노린다 — 질의와 문서를 따로 인코딩하되(bi-encoder처럼 사전 계산 가능) 토큰 단위 벡터를 그대로 남겨 두고, 검색 시점에 토큰끼리 세밀하게 비교하는 late interaction으로 cross-encoder에 준하는 정밀도를 bi-encoder에 가까운 속도로 얻으려 한다.'},
 {h:'top-k와 지연시간 예산', d:'리랭킹에 넣는 후보 수(top-k)는 정밀도와 지연시간의 트레이드오프다. k를 늘리면 1단계에서 놓칠 뻔한 정답을 리랭커가 건져낼 기회는 늘지만, cross-encoder 연산이 그만큼 늘어 응답 시간이 길어진다. 실무에서는 서비스가 감당할 수 있는 지연시간 예산 안에서 k를 정하고(흔히 수십 개 단위), 1단계 검색 자체의 재현율을 올려 애초에 정답이 후보 안에 들어오게 하는 것도 함께 신경 쓴다 — 리랭킹은 후보에 없는 문서를 되살릴 수 없다. 사용자가 체감하는 전체 응답 시간에는 1단계 검색·리랭킹·LLM 생성이 모두 순차적으로 더해지므로, 리랭킹에 배정할 수 있는 지연시간은 이 전체 예산에서 역산해 정하는 것이 실무적으로 안전하다.'}
],

math:[
 {tex:'\\text{cost}_{\\text{cross}} = O(k),\\qquad \\text{cost}_{\\text{cross,전체}} = O(N)',
  expr:'리랭킹 계산 비용', d:'$N$ 은 전체 문서 수, $k$ 는 리랭킹 대상 후보 수다. cross-encoder를 top-$k$ 에만 적용하면 비용이 $k$ 에 비례해 감당할 만하지만, 전체 문서 $N$ 에 바로 적용하면 $N$ 이 수백만 단위일 때 비용이 감당 불가능한 수준이 된다. 이것이 2단계 구조가 필요한 산술적인 이유이고, 서비스가 감당할 지연시간에 맞춰 $k$ 를 조정하는 것이 리랭킹 튜닝의 핵심이다.'}
],

diagram:{type:'flow', cap:'대량 후보에서 빠르게 추린 뒤, 좁혀진 후보만 무거운 모델로 재평가한다.',
 nodes:[
  {t:'전체 문서', s:'수백만 개'},
  {t:'1단계 검색', s:'BM25/벡터, 재현율'},
  {t:'top-k 후보', s:'수십~수백 개'},
  {t:'cross-encoder', s:'질의-문서 쌍마다 계산'},
  {t:'최종 순위', s:'정밀도 우선'}
 ]},

confuse:[
 {a:'bi-encoder', b:'cross-encoder', d:'bi-encoder는 질의와 문서를 독립적으로 인코딩해 문서 벡터를 미리 계산·저장할 수 있어 빠르지만, 둘 사이 상호작용을 못 본다. cross-encoder는 질의-문서를 함께 인코딩해 훨씬 정확하지만 매번 새로 계산해야 해 느리다 — 그래서 각각 1단계·2단계를 맡는다.'},
 {a:'reranking', b:'[hybrid-search](#/c/hybrid-search)', d:'하이브리드 검색은 BM25와 벡터 검색처럼 **서로 다른 1단계 검색 방법**의 결과를 결합해 애초에 더 나은 후보 집합을 만드는 것이고, 리랭킹은 그렇게 만들어진 후보(하이브리드든 아니든)를 **2단계에서** 더 정확한 모델로 재정렬하는 것이다. 순서상 하이브리드 검색 다음에 리랭킹을 이어붙이는 조합이 실무에서 흔히 쓰인다.'},
 {a:'[ColBERT](#/p/colbert)', b:'monoBERT/monoT5', d:'monoBERT·monoT5는 질의-문서를 완전히 합쳐 인코딩하는 순수 cross-encoder라 가장 정확하지만 가장 느리다. ColBERT는 토큰 벡터를 따로 계산해두고 비교만 나중에 하는 late interaction 방식이라, 정확도와 속도 사이에서 cross-encoder 쪽에 더 가깝게 절충한 중간 지점이다. 어느 쪽을 쓸지는 결국 지연시간 예산과 감당 가능한 인프라 복잡도(ColBERT는 토큰 단위 벡터를 저장해야 해 색인 용량이 커진다) 사이의 선택이다.'}
],

pitfalls:[
 '리랭킹은 1단계가 아예 놓친 문서를 구해내지 못한다 — top-k 후보 안에 정답이 없으면 아무리 좋은 리랭커를 써도 소용없다. 리랭커 도입 전에 1단계 재현율부터 점검해야 한다.',
 '"리랭커를 넣으면 무조건 품질이 오른다"고 기대하고 지연시간을 안 재보면, 실제 서비스에서 응답이 눈에 띄게 느려진 뒤에야 문제를 알아차리게 된다.',
 'cross-encoder 리랭커를 전체 문서 색인에 직접 적용하려는 시도는 계산량이 문서 수에 비례해 폭발해 현실적으로 불가능하다 — 반드시 1단계로 후보를 좁힌 뒤에만 적용한다.',
 '리랭커도 학습된 모델이라, 학습 데이터의 도메인(예: 일반 웹 텍스트)과 실제 서비스 도메인(예: 사내 법률 문서)이 크게 다르면 기대만큼 정밀도가 오르지 않을 수 있다 — 자체 데이터로 평가해 보지 않고 도입하면 착시에 빠지기 쉽다.',
 '[청킹](#/c/chunking)이 엉망이면 리랭커가 아무리 정확해도 애초에 재정렬할 후보 자체가 맥락을 잃은 조각들이라 효과가 제한적이다 — 리랭킹은 검색 파이프라인의 다른 결함을 대신 고쳐주지 않는다.'
],

code:{lang:'text', d:'1단계 검색과 2단계 리랭킹이 서로 다른 입력·출력 크기로 동작한다는 것을 정리.',
 src:'1단계(bi-encoder/BM25):  질의 1개 vs 문서 N개(수백만)  → top-k(예: 50개)\n2단계(cross-encoder):    질의-문서 쌍 k개를 각각 새로 인코딩 → 최종 top-n(예: 5개)\n# k를 키우면 재현율 기회는 늘지만 2단계 지연시간이 k에 비례해 늘어난다'},

papers:['monobert','monot5','colbert'],
terms:['semantic-search','similarity-metric','hybrid-search','rag-c','chunking']
});
