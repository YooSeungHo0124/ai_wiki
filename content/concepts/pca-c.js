WIKI.concept({
slug:'pca-c',

tldr:'데이터의 분산이 가장 큰 방향(주성분)을 순서대로 찾아, 그 방향들로 데이터를 투영해 정보 손실을 최소화하며 차원을 줄이는 선형 기법으로 공분산 행렬의 고유벡터로 구한다.',

why:'차원 축소·시각화·노이즈 제거·특징 압축의 기본 도구로, 딥러닝 시대에도 임베딩을 2~3차원으로 눌러 훑어보거나 특징 수를 줄여 다운스트림 모델의 계산량을 낮출 때 여전히 첫 번째로 시도하는 방법이다. [고윳값·SVD](#/c/eigen-svd) 를 실제로 손에 쥐고 쓰는 가장 흔한 자리이기도 하다. 선형대수 수업에서 배운 고유벡터·특이값이 "그래서 실무에서 어디에 쓰이는가"라는 질문에 대한 가장 직접적인 답이 PCA다.',

sections:[
 {h:'분산 최대 방향', d:'PCA 는 데이터를 원점 중심으로 옮긴 뒤(평균을 뺀 뒤), 데이터가 가장 넓게 퍼져 있는 방향(1주성분)을 찾고, 그 방향과 직교하면서 그다음으로 넓게 퍼진 방향(2주성분)을 찾는 식으로 반복한다. "정보가 많다"를 "분산이 크다"로 정의하는 것이 PCA 의 핵심 가정이다.'},
 {h:'공분산 행렬의 고유벡터', d:'이 방향들은 데이터의 공분산 행렬을 [고윳값 분해](#/c/eigen-svd)했을 때 나오는 고유벡터와 정확히 같다 — 고윳값이 클수록 그 방향의 분산이 크다. 실제 구현에서는 공분산 행렬을 직접 고윳값 분해하기보다, 수치적으로 더 안정적인 데이터 행렬의 [특이값 분해(SVD)](#/c/eigen-svd)로 같은 결과를 얻는 경우가 많다. 공분산 행렬을 명시적으로 계산하면 특징 수의 제곱에 비례해 메모리가 늘어나므로, 특징이 매우 많을 때는 SVD 경로가 실무적으로도 더 선호된다.'},
 {h:'백색화와 시각화', d:'주성분 상위 몇 개만 남기면 차원 축소가 되지만, 각 주성분을 그 표준편차로 나눠 모든 방향의 분산을 1로 맞추면 백색화(whitening)가 된다 — 특징 간 상관을 없애고 스케일을 통일해 이후 알고리즘의 수렴을 돕는다. 또는 반대로 상위 2~3개 주성분만 남겨 사람이 눈으로 볼 수 있게 시각화하는 용도로도 흔히 쓴다 — 다만 이 경우 [t-SNE·UMAP](#/c/tsne-umap) 이 군집 구조를 더 잘 드러내는 경우가 많다. 이미지 전처리에서 인접 픽셀 간 상관을 없애는 ZCA whitening 도 PCA whitening 의 변형이다.'},
 {h:'선형이라는 한계', d:'PCA 는 선형 변환만 다룬다 — 데이터가 곡면(비선형 다양체) 위에 놓여 있으면 주성분 몇 개로는 그 구조를 제대로 못 담는다. 이럴 때는 커널 PCA, 오토인코더, [t-SNE·UMAP](#/c/tsne-umap) 같은 비선형 기법이 필요하다. 하지만 계산이 빠르고 결과가 결정적(초기화에 의존하지 않음)이라는 점에서 PCA 는 여전히 첫 시도로 가장 무난하다.'},
 {h:'몇 개를 남길 것인가', d:'설명된 분산 비율(explained variance ratio)의 누적 그래프(scree plot)를 그려 원하는 보존 비율(예: 95%)에 도달하는 지점까지의 주성분 개수를 고르는 것이 표준 관행이다. 시각화가 목적이면 무조건 2~3개로 고정하고, 다운스트림 모델의 입력 차원 축소가 목적이면 보존 비율을 기준으로 고르는 것이 실무에서 흔히 갈리는 두 갈래다.'}
],

math:[
 {tex:'\\Sigma=\\dfrac1n X^TX', expr:'공분산 행렬(중심화된 X)', d:'$X$ 는 평균을 뺀 $n\\times d$ 데이터 행렬. $\\Sigma$ 는 $d\\times d$ 공분산 행렬로, 특징들이 서로 얼마나 같이 변하는지를 담는다.'},
 {tex:'\\Sigma v_i=\\lambda_i v_i', expr:'고유벡터·고윳값', d:'$v_i$ 는 $i$ 번째 주성분 방향(고유벡터), $\\lambda_i$ 는 그 방향의 분산(고윳값). $\\lambda_1\\ge\\lambda_2\\ge\\cdots$ 순으로 정렬해 상위 $k$ 개 $v_i$ 를 남기면 $k$ 차원으로 축소된다.'},
 {tex:'Z=XV_k', expr:'투영(차원 축소)', d:'$V_k$ 는 상위 $k$ 개 고유벡터를 열로 쌓은 $d\\times k$ 행렬. $X$ 를 $V_k$ 에 투영한 $Z$ 가 $k$ 차원으로 압축된 표현이다. 원래 공간으로 되돌리려면(복원) $\\hat X=ZV_k^T$ 를 계산하면 되고, 버린 주성분만큼 정보가 손실된 근사값이 나온다.'}
],

diagram:{type:'flow', cap:'분산이 큰 방향부터 순서대로 축을 새로 잡는다.',
 steps:['데이터 평균 제거(중심화)','공분산 행렬 계산','고유벡터·고윳값 분해','상위 k개 방향 선택','원 데이터를 투영']},

confuse:[
 {a:'PCA', b:'[t-SNE·UMAP](#/c/tsne-umap)', d:'PCA 는 전역적인 분산 구조를 보존하는 선형 투영이라 축의 의미(각 주성분이 분산을 얼마나 설명하는지)가 명확하고 재현 가능하다. t-SNE·UMAP 은 국소적인 이웃 관계 보존에 집중하는 비선형 기법으로 시각적으로 군집이 더 뚜렷해 보이지만 축에 의미가 없고 전역 거리를 신뢰할 수 없다. 실무에서는 PCA 로 먼저 노이즈를 줄인 뒤 그 결과를 t-SNE·UMAP 에 입력으로 넣는 조합도 흔하다.'},
 {a:'차원 축소', b:'특징 선택(feature selection)', d:'PCA 같은 차원 축소는 기존 특징들을 조합해 새로운 축(주성분)을 만들어내고, 특징 선택은 기존 특징 중 일부를 그대로 골라낸다. 차원 축소는 원래 특징의 해석 가능성을 잃는 대신 정보를 더 조밀하게 담을 수 있고, 특징 선택은 해석 가능성을 유지하는 대신 여러 특징에 걸쳐 있는 정보를 놓칠 수 있다.'},
 {a:'설명된 분산 비율(explained variance ratio)', b:'정확도', d:'PCA 에서 "주성분 3개가 분산의 90%를 설명한다"는 것은 그 3개 축이 데이터의 퍼짐 정도를 90% 보존한다는 뜻이지, 어떤 다운스트림 예측 작업의 정확도와 직결되는 것은 아니다 — 분산이 크다고 그 방향이 타깃 예측에 항상 유용한 것은 아니다.'}
],

pitfalls:[
 '특징 스케일을 맞추지 않고 PCA 를 돌리면 값의 범위가 큰 특징이 분산을 지배해 주성분이 사실상 그 특징 하나만 반영하게 된다 — [특징 스케일링](#/c/scaling-features)(표준화)이 사실상 필수다.',
 '주성분을 몇 개 남길지(k)를 임의로 정하고 넘어가는 경우가 있다 — 설명된 분산 비율의 누적 그래프(scree plot)를 보고 근거 있게 정해야 한다.',
 '주성분이 원래 특징의 물리적 의미를 그대로 갖는다고 착각하기 쉽다 — 주성분은 여러 특징의 선형 조합이라 "1주성분 = 특징 A" 식의 직접적 해석이 안 되는 경우가 대부분이다.',
 'PCA 로 차원을 줄인 뒤 지도학습을 하면 타깃과 무관한 분산 방향까지 보존하려다 유용한 정보를 버릴 수 있다 — 지도 학습이 목적이면 PLS 같은 타깃을 고려하는 대안도 검토해야 한다.',
 '테스트 데이터에 학습 데이터와 다른 PCA(별도로 fit한 변환)를 적용하는 실수가 있다 — 학습 데이터로 학습한 변환(고유벡터)을 테스트 데이터에도 그대로 적용해야 비교가 공정하다.',
 '분산이 큰 방향이 항상 유용한 정보라고 단정하면 안 된다 — 센서 노이즈 자체가 분산이 클 수도 있어, 상위 주성분이 오히려 노이즈만 담고 있을 가능성도 배제할 수 없다.'
],

code:{lang:'python', d:'설명된 분산 비율을 보고 몇 개 주성분을 남길지 정하는 예',
 src:'from sklearn.decomposition import PCA\nimport numpy as np\n\npca = PCA(n_components=0.95)   # 분산의 95%를 보존하는 k를 자동 선택\nZ = pca.fit_transform(X_scaled)  # 스케일링을 먼저 했다고 가정\nprint(pca.n_components_, pca.explained_variance_ratio_.cumsum())'},

papers:['vqvae'],
terms:['eigen-svd','tsne-umap','kmeans-c','scaling-features']
});
