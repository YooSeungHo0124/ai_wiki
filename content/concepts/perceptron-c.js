WIKI.concept({
slug:'perceptron-c',

tldr:'입력 벡터의 가중합에 편향을 더해 임계값과 비교하는 가장 단순한 선형 분류기이며, 오늘날 신경망 한 층의 뉴런 하나가 하는 계산과 같은 형태다.',

why:'퍼셉트론을 모르면 "뉴런 하나가 뭘 계산하는가"가 안 잡힌다. MLP·CNN·Transformer 할 것 없이 모든 층은 결국 이 가중합 연산을 병렬로 쌓은 것이고, 퍼셉트론이 **선형** 모델이라는 한계를 이해해야 왜 활성 함수와 다층 구조가 필요한지가 보인다.',

sections:[
 {h:'무엇을 계산하는가', d:'입력 $x=(x_1,...,x_n)$ 에 가중치 $w$ 를 곱해 더하고 편향 $b$ 를 더한 뒤, 그 값의 부호(또는 임계값 통과 여부)로 0/1을 출력한다. 기하적으로는 $w\\cdot x+b=0$ 이라는 **초평면(hyperplane)**으로 공간을 둘로 가르는 선형 분류기다. 1958년 Rosenblatt 가 제안했을 때는 하드웨어(Mark I Perceptron)로 구현된 실제 기계였다.'},
 {h:'학습 규칙', d:'퍼셉트론 학습 규칙은 경사 하강법이 정착되기 전의 방식이다 — 오분류된 샘플마다 $w \\leftarrow w + \\eta\\,y\\,x$ 로 가중치를 직접 밀어준다. 이 규칙은 데이터가 **선형 분리 가능**할 때만 유한 스텝 안에 수렴한다는 것이 증명되어 있다(퍼셉트론 수렴 정리).'},
 {h:'XOR 이 왜 못 풀리나', d:'단일 퍼셉트론은 초평면 하나로만 공간을 가르므로 XOR 처럼 선형 분리가 안 되는 문제를 절대 못 푼다. Minsky·Papert 의 1969년 책이 이 한계를 지적하면서 1차 신경망 붐이 꺼졌다. 해법은 층을 쌓는 것(MLP)인데, 그러려면 [역전파](#/c/backprop-c)로 여러 층의 가중치를 동시에 학습시킬 방법이 있어야 했다.'},
 {h:'지금의 퍼셉트론', d:'실무에서 "퍼셉트론 하나만 써서 뭔가를 푼다"는 일은 없다. 대신 이 계산 단위($z=w\\cdot x+b$)가 완전연결층(nn.Linear)의 뉴런 하나, CNN 필터의 한 위치, Transformer의 QKV 투영 하나하나로 그대로 살아 있다. 다른 점은 딱 하나 — 임계값 함수 대신 미분 가능한 [활성 함수](#/c/activation)를 쓴다는 것이다.'}
],

math:[
 {tex:'z=\\sum_{i=1}^n w_i x_i + b,\\qquad \\hat y=\\begin{cases}1 & z\\ge 0\\\\0 & z<0\\end{cases}',
  expr:'perceptron output', d:'$w$ 는 가중치, $b$ 는 편향, $z$ 는 사전활성값(pre-activation)이다. 임계 함수는 미분이 0이거나 정의되지 않아 경사 기반 학습에 직접 쓸 수 없다 — 그래서 나중에 sigmoid 같은 매끄러운 함수로 대체됐다.'},
 {tex:'w \\leftarrow w + \\eta\\,(y-\\hat y)\\,x',
  expr:'perceptron update rule', d:'맞힌 샘플은 $y-\\hat y=0$ 이라 갱신이 없고, 틀린 샘플만 결정 경계를 그 샘플 쪽으로 민다. 이 규칙이 확률적 경사 하강의 특수 사례처럼 보이지만, 손실 함수의 경사에서 유도된 것이 아니라 독립적으로 고안된 규칙이다.'}
],

diagram:{type:'flow', cap:'퍼셉트론의 계산 순서: 가중합 → 임계 함수 → 이진 출력.',
 nodes:[
  {t:'입력 x', s:'벡터'},
  {t:'가중합 z=w·x+b'},
  {t:'임계 함수'},
  {t:'출력 0/1'}
 ]},

confuse:[
 {a:'퍼셉트론', b:'로지스틱 회귀', d:'퍼셉트론은 임계 함수로 0/1을 직접 뱉고 오분류 시에만 갱신한다. 로지스틱 회귀는 sigmoid 로 확률을 출력하고 교차 엔트로피의 경사로 매 샘플마다 갱신한다 — 형태는 닮았지만 학습 원리가 다르다.'},
 {a:'퍼셉트론', b:'MLP(다층 퍼셉트론)', d:'"다층 퍼셉트론"이라는 이름과 달리 내부 뉴런은 임계 함수가 아니라 [활성 함수](#/c/activation)를 쓴다. 진짜 퍼셉트론을 그대로 쌓으면 [역전파](#/c/backprop-c)로 학습할 수 없다(미분값이 0).'}
],

pitfalls:[
 '"퍼셉트론 = 신경망"이 아니다. 퍼셉트론은 은닉층이 없는 선형 분류기 하나일 뿐이고, 딥러닝이 강력한 이유는 이걸 비선형 활성과 함께 여러 층 쌓았기 때문이다.',
 '퍼셉트론 수렴 정리는 선형 분리 가능한 데이터에서만 성립한다. 현실 데이터는 대부분 선형 분리가 안 되므로 이 정리를 실무 보장으로 오해하면 안 된다.'
],

papers:['perceptron','backprop'],
terms:['activation','backprop-c','gradient-descent']
});
