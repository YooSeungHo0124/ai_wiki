WIKI.paper({
slug:'maskformer',
venue:'NeurIPS 2021',
authors:'Cheng, Schwing, Kirillov (FAIR · UIUC)',
arxiv:'2107.06278',

tldr:'제목이 그대로 논문의 주장이다 — **픽셀 하나하나를 분류하는 것이 전부는 아니다.** 의미 분할을 "픽셀마다 클래스를 맞히는 문제"가 아니라 **"이진 마스크 집합을 예측하고 각 마스크에 클래스 하나를 붙이는 문제"**로 다시 정의하면, 모델·손실·학습 절차를 하나도 바꾸지 않고 의미·인스턴스·파놉틱 분할을 전부 풀 수 있다.',

context:'2021년까지 의미 분할은 [FCN](#/p/fcn) 이후 거의 예외 없이 **픽셀별 분류**로 풀렸다 — $H \\times W$ 개 픽셀마다 $K$개 클래스 중 하나를 맞히는 문제다. 반면 인스턴스 수준 분할은 [Mask R-CNN](#/p/mask-rcnn)이나 [DETR](#/p/detr)처럼 **마스크 분류**를 썼다. 바운딩박스 혹은 쿼리마다 마스크 하나와 클래스 하나를 예측하는 방식이다. 이 둘은 [파놉틱 분할](#/p/panoptic)이 하나로 묶으려 했던 것과 달리 여전히 서로 다른 태스크로 취급돼 각자 다른 모델이 필요했다. 문제는 픽셀별 분류가 클래스 수 $K$가 커질수록 힘들어진다는 점이다 — 출력이 고정된 $K$차원 분포라서, 세밀하게 갈린 클래스일수록 경계가 흐려진다. 이 논문의 질문은 단순하다 — **애초에 왜 의미 분할만 픽셀별로 풀어야 하는가?**',

ideas:[
 {h:'마스크 분류로 재정의: N개 마스크 + 클래스 하나씩',
  lead:'K차원 픽셀 분포 대신 N개의 (마스크, 클래스) 쌍을 예측한다.',
  d:'출력을 $H \\times W \\times K$의 픽셀별 확률맵 대신, $N$개의 이진 마스크와 각 마스크에 대한 클래스 확률 $p_i \\in \\Delta^{K+1}$ 쌍으로 바꾼다. $K+1$인 이유는 "해당 없음"(∅) 라벨이 추가되기 때문이다. $N$은 클래스 수 $K$와 같을 필요가 없다 — 같은 클래스를 여러 마스크가 나눠 가질 수도 있다. 이 표현이 의미·인스턴스 분할 모두를 같은 틀로 담는다.'},
 {h:'[DETR](#/p/detr)의 집합 예측을 분할로 가져온다',
  lead:'Transformer 디코더가 N개 쿼리로 마스크 임베딩을 병렬로 뽑는다.',
  d:'백본 특징을 **픽셀 디코더**가 점진적으로 업샘플링해 픽셀별 임베딩 $\\mathcal{E}_{pixel}$을 만든다. 동시에 **Transformer 디코더**가 $N$개의 학습 가능한 쿼리로 이미지 특징에 attention해 $N$개의 세그먼트 임베딩 $\\mathcal{Q}$를 병렬로 뽑는다. [DETR](#/p/detr)과 달리 바운딩박스가 전혀 없다 — 매칭 비용도 박스가 아니라 클래스·마스크 유사도로만 계산한다.'},
 {h:'마스크는 내적 하나로 만든다',
  lead:'세그먼트 임베딩과 픽셀 임베딩을 내적한 뒤 sigmoid만 씌운다.',
  d:'세그먼트 임베딩 $\\mathcal{Q}$를 2층 MLP로 변환해 $N$개의 마스크 임베딩 $\\mathcal{E}_{mask}$를 얻는다. 각 마스크는 $\\mathcal{E}_{mask}$와 픽셀 임베딩 $\\mathcal{E}_{pixel}$의 내적에 sigmoid를 씌운 것이 전부다. 마스크끼리 배타적일 필요가 없다는 점을 확인했다 — softmax 대신 sigmoid를 써서 겹치는 마스크를 허용하는 편이 오히려 낫다.'},
 {h:'의미 분할은 추론 방식만 바꾼다',
  lead:'N개 마스크·클래스 쌍을 행렬곱 한 번으로 K×H×W 픽셀맵으로 합친다.',
  d:'모델도 손실도 그대로 두고, 추론 단계에서만 두 전략을 고른다. 일반 추론은 각 픽셀을 가장 그럴듯한 마스크 하나에 고정 배정하고(인스턴스 구분에 유리), 의미 분할 전용 추론은 $\\sum_i p_i(c) \\cdot m_i[h,w]$로 **모든 마스크에 대해 주변화**한다. 논문은 후자가 픽셀 하나를 마스크 하나에 강제로 묶는 것보다 성능이 좋다는 것을 확인했다.'},
 {h:'손실은 DETR과 똑같은 focal + dice',
  lead:'추가 보조 손실 없이 분류 손실과 focal+dice 마스크 손실만 쓴다.',
  d:'헝가리안 매칭으로 예측과 정답을 짝지은 뒤, 클래스에는 교차 엔트로피, 마스크에는 [DETR](#/p/detr)과 동일한 focal loss + dice loss 조합을 쓴다. Max-DeepLab처럼 인스턴스 판별 손실이나 마스크-ID 교차 엔트로피 같은 보조 손실이 전혀 없다 — 이 단순함이 논문이 강조하는 지점이다.'}
],

diagram:{type:'flow', cap:'백본 특징을 픽셀 디코더와 트랜스포머 디코더로 각각 흘려보낸 뒤 내적으로 합친다. 인스턴스·의미·파놉틱 추론 모두 이 출력에서 갈라진다.',
 nodes:[
  {t:'백본', s:'이미지 특징 F'},
  {t:'픽셀 디코더', s:'→ 픽셀별 임베딩'},
  {t:'Transformer', s:'N개 쿼리 병렬', acc:true},
  {t:'마스크+클래스', s:'N×(K+1), N×H×W'},
  {t:'태스크별 추론', s:'의미/인스턴스/파놉틱'}
 ]},

math:[
 {expr:'L_mask-cls(z, z_gt) = Σ_j [ -log p_σ(j)(c_j^gt) + 1[c_j^gt≠∅] L_mask(m_σ(j), m_j^gt) ]',
  tex:'\\mathcal{L}_{\\text{mask-cls}}(z,z^{gt})=\\sum_{j=1}^{N}\\Big[-\\log p_{\\sigma(j)}(c_j^{gt})+\\mathbb{1}_{c_j^{gt}\\neq\\varnothing}\\,\\mathcal{L}_{\\text{mask}}(m_{\\sigma(j)},m_j^{gt})\\Big]',
  d:'헝가리안 매칭 $\\sigma$로 예측과 정답을 짝지은 뒤, 짝지어진 각 쌍마다 분류 손실과(있는 경우) 마스크 손실을 더한다. $N \\geq N^{gt}$라서 남는 예측은 "해당 없음"(∅)에 매칭된다.'},
 {expr:'m_i[h, w] = sigmoid( E_mask[:, i]^T · E_pixel[:, h, w] )',
  tex:'m_i[h,w]=\\text{sigmoid}\\big(\\mathcal{E}_{\\text{mask}}[:,i]^{\\top}\\cdot\\mathcal{E}_{\\text{pixel}}[:,h,w]\\big)',
  d:'마스크 예측 전체가 이 내적 한 줄이다. 세그먼트별 벡터 하나와 픽셀별 벡터 하나를 곱해 그 자리에 그 세그먼트가 있을 확률을 낸다.'}
],

numbers:[
 {k:'mIoU · ADE20K (Swin-L)', v:'55.6', d:'동일 백본 기존 최고(53.5) 대비 **+2.1**, 파라미터는 10%↓ FLOPs 40%↓'},
 {k:'PQ · COCO panoptic', v:'52.7', d:'Swin-L 백본, 당시 SOTA(Max-DeepLab) 대비 **+1.6 PQ**'},
 {k:'PQ · DETR 대비', v:'46.5 vs 43.4', d:'R50 백본 동일 조건, 박스 없이 마스크만으로 **+2.2~3.1 PQ**'},
 {k:'ADE20K-Full 개선폭', v:'mIoU +3.5', d:'847개 클래스로 클래스 수가 클수록 픽셀별 분류 대비 격차가 커짐'},
 {k:'Cityscapes 개선폭', v:'mIoU +0.0', d:'19개 클래스뿐인 데이터셋에서는 픽셀별 분류와 사실상 동일 — 클래스 수 가설의 반증 사례'}
],

impact:'분할 연구의 프레임이 바뀌었다. **의미 분할과 인스턴스 분할이 별개 태스크가 아니라 같은 마스크 분류 문제의 두 인스턴스**라는 것을 하나의 모델·손실·학습 절차로 증명했다. 특히 클래스 수가 많을수록(ADE20K-Full의 847개 클래스) 픽셀별 분류 대비 이득이 커진다는 결과는, 세밀한 어휘로 갈수록 마스크 분류가 유리하다는 실증 근거를 남겼다. [DETR](#/p/detr)의 집합 예측을 검출을 넘어 분할 전반의 표준 틀로 확장한 첫 논문이기도 하다.',

legacy:[
 '**[Mask2Former](#/p/mask2former)** — 같은 저자들이 바로 다음 해 마스크 어텐션으로 수렴 속도와 성능을 함께 올린 후속작을 냄',
 '**k-Net, Max-DeepLab 계열과의 수렴** — 검출 없이 마스크만으로 파놉틱을 푸는 접근이 표준이 됨',
 '**[Grounding DINO](#/p/grounding-dino)류 개방형 어휘 분할의 전제** — 클래스를 고정된 K개가 아니라 쿼리-마스크 쌍으로 다루는 발상이 텍스트 조건부 분할로 이어짐',
 '**"쿼리가 곧 객체"라는 관용구 확산** — 이후 검출·분할·추적 전반에서 학습 가능한 쿼리 집합으로 가변 개수 출력을 다루는 설계가 일반화됨'
],

pitfalls:[
 '**"항상 픽셀별 분류보다 낫다"가 아니다.** Cityscapes(19개 클래스)에서는 mIoU 개선폭이 거의 0이었다 — 이득은 클래스 어휘가 클 때 두드러진다.',
 '**바운딩박스가 전혀 없다는 점이 [DETR](#/p/detr)과의 핵심 차이다.** 매칭 비용도 박스 IoU가 아니라 클래스·마스크로만 계산해, "box-free DETR"로 불린다.',
 '**N(쿼리 수)은 클래스 수 K와 다른 개념이다.** 고정 매칭을 쓰려면 N=K가 필요하지만, 논문은 헝가리안 매칭 기반 가변 N이 더 낫다는 것을 확인했다.'
],

figures:[
 {f:'fig2-architecture.png',
  cap:'왼쪽 회색 블록이 백본→픽셀 디코더로 이어지는 픽셀 레벨 모듈, 위쪽이 N개 쿼리를 받는 트랜스포머 모듈. 두 갈래에서 나온 E_pixel과 E_mask를 우측 ⊗ 지점에서 내적해 N개 마스크 예측을 만들고, 의미 분할일 때만 우측 노란 박스에서 ∅ 클래스를 버리고 K×H×W로 합친다.',
  src:'원문 Figure 2, p.4'}
],

quotes:[
 {t:'MaskFormer, for the first time, unifies semantic- and instance-level segmentation with the exact same model, loss, and training pipeline.',
  src:'Section 4.3, p.7'}
],

links:[
 {t:'arXiv 2107.06278 — Per-Pixel Classification is Not All You Need for Semantic Segmentation', u:'https://arxiv.org/abs/2107.06278'},
 {t:'MaskFormer 프로젝트 페이지', u:'https://bowenc0221.github.io/maskformer'}
]
});
