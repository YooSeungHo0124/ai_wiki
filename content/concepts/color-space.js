WIKI.concept({
slug:'color-space',

tldr:'색공간은 픽셀 색을 몇 개의 숫자(채널)로 표현하는 좌표계로, RGB·그레이스케일·HSV처럼 목적에 따라 다른 좌표계를 골라 쓴다.',

why:'"채널 3개"라고 다 같은 RGB가 아니고, 색 기반 처리(피부색 검출, 조명 강건성)를 RGB에서 하려다 실패한 뒤에야 HSV 같은 대안이 있다는 걸 알게 되는 경우가 많다. 색공간 선택은 전처리 단계에서 조용히 성능을 좌우한다.',

sections:[
 {h:'RGB: 딥러닝의 기본값', d:'빨강·초록·파랑 세 채널의 밝기를 각각 표현하는 가장 흔한 색공간이다. 대부분의 이미지 분류·검출 모델은 RGB 세 채널 입력을 기본으로 한다. 그러나 RGB는 사람이 색을 지각하는 방식과 잘 안 맞는다 — 예를 들어 조명이 어두워지면 R, G, B 값이 모두 비선형적으로 함께 변해, "색상은 같은데 밝기만 다르다"는 직관적 조작을 RGB 채널 각각을 건드려서는 하기 어렵다.'},
 {h:'그레이스케일: 밝기만', d:'색 정보를 버리고 밝기(휘도) 하나만 남긴 1채널 표현이다. RGB에서 단순 평균을 내는 대신, 사람 눈이 초록에 더 민감하다는 것을 반영해 보통 $0.299R+0.587G+0.114B$ 같은 가중합으로 변환한다. 색 정보가 필요 없는 과제(예: 문서 스캔, 일부 고전 특징 기반 검출기)에서 채널 수를 줄여 계산량을 아끼는 데 쓰지만, 딥러닝 검출·분류에서는 색 정보도 유용한 신호라 그레이스케일로 바꾸면 보통 성능이 떨어진다.'},
 {h:'HSV 색공간', d:'HSV는 색상(Hue, 0~360도 색환), 채도(Saturation, 색의 순도), 명도(Value, 밝기)로 나눈 좌표계다. RGB와 달리 "조명이 바뀌어 밝기만 변한다"는 상황이 V 채널 하나만 바뀌는 것으로 깔끔하게 표현돼, 색 기반 세그멘테이션(예: 특정 색 물체 검출)이나 데이터 증강에서 밝기·채도만 랜덤하게 흔드는 색상 지터(color jitter)에 자주 쓰인다. 딥러닝 모델 입력 자체는 여전히 RGB로 넣는 경우가 많지만, 증강 단계에서 HSV로 바꿔 조작한 뒤 다시 RGB로 되돌리는 식으로 섞어 쓴다.'},
 {h:'채널 수와 순서 변환', d:'실무에서 자주 마주치는 채널 관련 함정은 두 가지다. 하나는 앞서 다룬 [이미지 전처리](#/c/image-preprocess)의 RGB/BGR 순서 문제이고, 다른 하나는 채널 수 불일치다 — 흑백 X-ray나 4채널(알파 포함) PNG를 3채널 RGB 모델에 그대로 넣으면 에러가 나거나(채널 수 불일치) 알파 채널이 색 채널로 잘못 해석되는 조용한 버그가 난다. 의료 영상처럼 원래 1채널인 데이터를 RGB 사전학습 백본에 넣을 때는 채널을 3번 복제하는 방식이 흔히 쓰인다.'}
],

diagram:{type:'compare', cap:'RGB는 색상과 밝기가 섞여 있고, HSV는 분리돼 있다.',
 left:{t:'RGB', items:['R·G·B 밝기 각각 표현','조명 변화가 세 채널에 분산','딥러닝 입력 기본값']},
 right:{t:'HSV', items:['색상·채도·명도 분리','밝기 변화 = V 채널만','색 기반 증강·검출에 유리'], acc:true}
},

confuse:[
 {a:'RGB', b:'HSV', d:'RGB는 색을 세 원색의 밝기 조합으로 표현해 딥러닝 모델의 기본 입력 형식이지만, 조명 변화가 세 채널에 얽혀 나타난다. HSV는 색상과 밝기를 분리해 색 기반 처리나 조명 강건 증강에 유리하지만 대부분의 모델 입력 형식은 아니다.'},
 {a:'채널(channel)', b:'차원(dimension)', d:'이미지 맥락에서 채널은 RGB처럼 색·특징을 표현하는 축(보통 3개 또는 특징 맵에서는 수백~수천 개)을 가리키고, 차원은 텐서 전체의 축 개수(높이·너비·채널 등)를 가리키는 더 일반적인 말이다. "3차원 이미지"라는 말은 보통 (H, W, C) 세 축을 뜻하지 채널이 3개라는 뜻이 아니다.'},
 {a:'그레이스케일', b:'단채널(single-channel) 특징 맵', d:'그레이스케일은 원본 이미지의 밝기 표현이라는 구체적 의미가 있지만, 신경망 중간층의 단채널 특징 맵은 밝기와 무관한 추상적 활성값이다. 둘 다 "채널이 1개"라는 점만 같을 뿐 의미는 다르다.'}
],

pitfalls:[
 'HSV로 색상 필터링을 할 때 Hue가 0~360도(또는 OpenCV처럼 0~180)로 순환하는 값이라는 것을 잊고 선형 범위처럼 다루면 빨간색 근처(0도 부근/360도 부근)에서 필터링이 깨진다.',
 '흑백 의료 영상을 3채널로 단순 복제해 RGB 사전학습 백본에 넣는 방식이 항상 최선은 아니다 — 도메인에 맞게 처음부터 1채널로 학습하는 편이 나을 때도 있다.',
 'PNG의 알파 채널을 무심코 4번째 색 채널로 읽어 들이면 모델 입력 채널 수가 어긋나거나 투명도가 색 정보로 잘못 섞인다.'
],

papers:['alexnet'],
terms:['image-preprocess','augmentation']
});
