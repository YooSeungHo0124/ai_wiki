WIKI.concept({
slug:'experiment-tracking',

tldr:'모델을 학습할 때마다 하이퍼파라미터·코드 버전·데이터·결과를 자동으로 기록해, "지난주에 성능이 제일 좋았던 그 모델이 정확히 어떤 설정으로 나왔는지"를 나중에도 재구성할 수 있게 하는 실무 습관이다.',

why:'모델 하나를 만드는 데 수십~수백 번의 실험이 들어간다. 기록 없이 실험하면 "어제 돌린 게 batch size 32였나 64였나", "그 좋았던 체크포인트가 어느 커밋에서 나왔나"를 아무도 답할 수 없는 상태가 된다. 이건 단순 편의가 아니라 [재현성](#/c/reproducibility)의 최소 전제조건이다 — 기록이 없으면 재현을 시도할 출발점 자체가 없다.',

sections:[
 {h:'무엇을 기록해야 하는가', d:'하이퍼파라미터(학습률, 배치 크기, 시드), 코드 버전(git 커밋 해시), 데이터 버전(어느 스냅샷을 썼는지), 환경(라이브러리 버전, 하드웨어), 그리고 학습 곡선(손실·검증 지표 시계열)과 최종 체크포인트까지 — 이 다섯 축을 실험마다 자동으로 남기는 것이 목표다. 수동으로 스프레드시트에 옮겨 적는 방식은 반드시 빠뜨리는 항목이 생기고 오래가지 못한다.'},
 {h:'왜 자동화해야 하는가', d:'사람이 기억에 의존해 기록하면 "성공한 실험"만 기록하고 "실패한 실험"은 빠뜨리는 편향이 생긴다. 실패한 설정도 "이미 해봤고 안 됐다"는 정보로서 가치가 있다 — 같은 실패를 반복하지 않게 해준다. 학습 스크립트가 시작할 때 자동으로 설정을 스냅샷 찍고 끝날 때 결과를 기록하도록 파이프라인에 내장하면, 사람의 기억력이나 성실함에 의존하지 않고 100% 커버리지를 얻는다.'},
 {h:'비교 가능성이 핵심 가치다', d:'실험 추적의 진짜 가치는 개별 기록이 아니라 **비교**에 있다 — "learning rate 를 1e-4 에서 3e-4 로 바꿨더니 검증 손실이 어떻게 바뀌었는가"를 여러 실행에 걸쳐 나란히 보는 것이다. 그러려면 기록 형식이 실험마다 일관돼야 한다 — 어떤 실행은 지표 이름을 "val_loss"로, 다른 실행은 "validation_loss"로 남기면 자동 비교가 깨진다. 실무에서는 지표 이름과 로깅 스키마를 팀 차원에서 통일하는 것이 도구 선택보다 더 중요하다.'},
 {h:'모델 레지스트리와의 경계', d:'실험 추적은 "학습 과정에서 무슨 일이 있었는가"를 기록하는 것이고, [모델 레지스트리](#/c/model-registry)는 그 결과물 중 실제로 쓸 만한 것을 골라 "배포 가능한 자산"으로 승격·버전 관리하는 것이다. 실무 파이프라인에서는 실험 추적 도구에서 가장 좋은 실행을 찾은 뒤, 그 체크포인트만 레지스트리로 올리는 흐름이 일반적이다 — 모든 실험이 레지스트리까지 갈 필요는 없다.'}
],

diagram:{type:'flow', cap:'실험 추적이 학습 파이프라인에 끼는 지점.',
 steps:['설정·코드 버전 스냅샷','학습 중 지표 자동 로깅','체크포인트 저장','여러 실행 나란히 비교','최선 실행만 레지스트리로']},

confuse:[
 {a:'실험 추적', b:'모델 레지스트리', d:'실험 추적은 학습 과정 전체(성공·실패 모두)를 기록하는 로그에 가깝고, 모델 레지스트리는 그중 선별된 결과물을 배포 후보로 관리하는 저장소다. 전자가 "무엇을 시도했는가", 후자가 "무엇을 쓸 것인가"에 답한다.'},
 {a:'로깅(logging)', b:'모니터링([drift](#/c/drift) 탐지 등)', d:'실험 추적의 로깅은 학습 단계에서 일어나는 일회성 기록이고, 모니터링은 배포된 모델이 실제 트래픽에서 계속 잘 동작하는지 지속적으로 관찰하는 것이다. 둘 다 지표를 기록한다는 점은 같지만 시점(학습 대 서빙)과 목적이 다르다.'},
 {a:'하이퍼파라미터', b:'아티팩트(artifact)', d:'하이퍼파라미터는 학습을 시작하기 전에 정하는 설정값이고, 아티팩트는 학습이 끝난 뒤 만들어지는 산출물(체크포인트, 로그 파일, 시각화)이다. 실험 추적은 이 둘을 하나의 실행(run) 단위로 묶어 함께 남긴다.'}
],

pitfalls:[
 '"나중에 정리해서 기록해야지"는 거의 지켜지지 않는다 — 기록을 학습 스크립트 안에 자동화해 넣지 않으면 결국 재현 불가능한 실험이 쌓인다.',
 '지표 이름·단위를 실험마다 다르게 쓰면(정확도를 0~1 로 쓰다가 다른 실행에서는 0~100 으로 쓰는 식) 자동 비교·정렬이 조용히 틀린 결론을 낸다.',
 '학습 코드는 기록하면서 그 코드가 사용한 데이터 스냅샷 버전은 빠뜨리는 경우가 흔하다 — 코드가 같아도 데이터가 바뀌면 결과가 재현되지 않는다([재현성](#/c/reproducibility) 참고).'
],

papers:['nnunet'],
terms:['reproducibility','model-registry','drift']
});
