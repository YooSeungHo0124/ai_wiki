WIKI.concept({
slug:'entropy-kl',

tldr:'엔트로피는 한 분포가 가진 불확실성의 양이고, 교차 엔트로피는 진짜 분포를 잘못된 분포로 표현하는 데 드는 평균 비용이며, KL 발산은 두 분포가 얼마나 다른지를 재는 비대칭 거리다.',

why:'분류 손실(cross-entropy loss), [VAE](#/p/vae)의 KL 항, [RLHF](#/p/instructgpt)의 KL 벌점, [TENT](#/p/tent) 같은 시험시 적응 기법의 엔트로피 최소화 — 이름도 등장 맥락도 다 다른 이 항들이 **전부 같은 KL 발산에서 나온 것**이라는 걸 알면, 새 논문에서 낯선 손실 항을 봐도 "이건 결국 어떤 분포를 어떤 분포에 가깝게 당기는 항이구나"라고 즉시 읽을 수 있다.',

sections:[
 {h:'엔트로피: 불확실성', d:'분포 $p$ 의 엔트로피 $H(p)=-\\sum_x p(x)\\log p(x)$ 는 "이 분포에서 뽑은 값을 맞히기가 평균적으로 얼마나 어려운가"를 잰다. 균등분포(모든 결과가 똑같이 그럴듯함)는 엔트로피가 최대이고, 한 값에 확률이 몰려 있으면(확신) 엔트로피는 0에 가깝다. [TENT](#/p/tent)가 시험 시점에 예측 분포의 엔트로피를 최소화하는 것은 "모델이 더 확신 있게 답하도록" 만드는 것이다.'},
 {h:'교차 엔트로피', d:'진짜 분포 $p$(정답 레이블, 원-핫)를 예측 분포 $q$(모델 출력, softmax)로 표현할 때 드는 평균 비용이 교차 엔트로피 $H(p,q)=-\\sum_x p(x)\\log q(x)$ 다. 분류 문제의 정답이 원-핫이면 이 식은 정답 클래스의 $-\\log q(\\text{정답})$ 하나만 남는다 — "정답 클래스에 확률을 얼마나 높게 줬는가"에만 페널티를 매기는 익숙한 cross-entropy loss 가 바로 이것이다.'},
 {h:'KL 발산', d:'KL 발산 $D_{KL}(p\\Vert q)=H(p,q)-H(p)$ 는 교차 엔트로피에서 $p$ 자체의 엔트로피(더 이상 줄일 수 없는 몫)를 뺀 것 — 즉 "$q$ 를 쓰는 데서 오는 **순수한 낭비**"만 남긴 값이다. $p=q$ 면 0이고, 두 분포가 다를수록 커진다. 정답 레이블이 원-핫으로 고정돼 $H(p)=0$ 인 일반 분류 학습에서는 cross-entropy 를 최소화하는 것과 KL 을 최소화하는 것이 완전히 같은 일이 된다.'},
 {h:'어디서나 같은 KL', d:'[VAE](#/p/vae)의 손실은 $D_{KL}(q(z\\mid x)\\Vert p(z))$ 항을 가진다 — 인코더가 만든 잠재 분포를 사전 분포(보통 표준정규분포)에 가깝게 당기는 정칙화다. [RLHF](#/p/instructgpt)의 KL 벌점 $D_{KL}(\\pi_\\theta\\Vert\\pi_{\\text{ref}})$ 는 강화학습으로 갱신되는 정책이 원래 SFT 모델의 분포에서 너무 멀어지지 않게 붙잡는 항이다. 형태(분포 두 개, "가깝게 당긴다")는 똑같고, 무엇을 분포로 삼는지만 매번 바뀐다.'}
],

math:[
 {tex:'H(p)=-\\sum_x p(x)\\log p(x)',
  expr:'엔트로피', d:'로그의 밑은 보통 자연로그(단위: nats) 또는 2(단위: bits)를 쓴다. 확실할수록(확률이 한 곳에 몰릴수록) 작고, 불확실할수록(균등할수록) 크다.'},
 {tex:'D_{KL}(p\\Vert q)=\\sum_x p(x)\\log\\frac{p(x)}{q(x)}=H(p,q)-H(p)',
  expr:'KL 발산', d:'$p$ 는 기준(진짜/타깃) 분포, $q$ 는 근사(예측/현재) 분포. $H(p,q)=-\\sum_x p(x)\\log q(x)$ 는 교차 엔트로피. $D_{KL}(p\\Vert q)\\ge 0$ 이고 $p=q$ 일 때만 0(Gibbs 부등식) — 항상 $p$ 를 기준으로 $q$ 가 얼마나 낭비하는지를 잰다는 점에서 대칭이 아니다.'}
],

diagram:{type:'compare', cap:'교차 엔트로피에서 p의 엔트로피를 빼면 KL 이 남는다.',
 left:{t:'교차 엔트로피 H(p,q)', items:['q로 p를 표현하는 비용','정답이 원-핫이면','= -log q(정답)']},
 right:{t:'KL 발산', items:['H(p,q) - H(p)','q가 p에서 멀수록 큼','p=q면 정확히 0']}},

confuse:[
 {a:'KL 발산', b:'거리(distance)', d:'KL 은 "거리"라고 부르는 경우가 많지만 수학적 거리(metric)의 조건인 대칭성($D_{KL}(p\\Vert q)\\neq D_{KL}(q\\Vert p)$ 인 경우가 흔함)과 삼각부등식을 만족하지 않는다. VAE·RLHF 모두 어느 쪽을 기준($p$)으로 놓느냐에 따라 결과가 달라지므로 순서를 항상 확인해야 한다.'},
 {a:'cross-entropy loss', b:'KL 발산', d:'정답이 원-핫으로 고정된 일반적인 분류 학습에서는 $H(p)=0$ 이라서 cross-entropy 를 최소화하는 것과 KL 을 최소화하는 것이 같은 결과를 낸다. 하지만 정답 자체가 분포(레이블 스무딩, distillation 의 soft label)라면 $H(p)\\neq 0$ 이 되어 두 손실이 상수만큼 달라진다 — 그래프에 찍히는 손실 값의 절대적 크기가 다르게 보이는 이유다.'},
 {a:'엔트로피(불확실성)', b:'[분산](#/c/expectation-variance)(퍼짐)', d:'둘 다 "얼마나 흩어져 있는가"를 재는 느낌이지만 엔트로피는 확률 분포의 모양(균등한가 뾰족한가) 자체를, 분산은 값들이 평균에서 얼마나 멀리 떨어져 있는가(단위가 있는 거리)를 잰다 — 같은 분산이라도 분포 모양에 따라 엔트로피는 다를 수 있다.'}
],

pitfalls:[
 'KL 발산을 대칭이라고 가정하고 $D_{KL}(p\\Vert q)$ 와 $D_{KL}(q\\Vert p)$ 를 바꿔 써도 된다고 착각하는 경우가 흔하다 — RLHF 에서 정책과 참조 모델 중 어느 쪽을 기준으로 놓는지가 실제로 학습 거동을 바꾼다.',
 'cross-entropy loss 값이 0에 가까워야 "완벽하다"고 생각하기 쉬운데, 레이블 노이즈나 애매한 샘플이 섞여 있으면 이론적 최솟값 자체가 0보다 크다 — 이론적 하한($H(p)$)을 모르고 0을 목표로 삼으면 과적합 신호를 놓친다.',
 '[temperature](#/c/temperature)의 $T$ 와 엔트로피를 혼동하기 쉬운데, temperature 는 분포를 바꾸는 손잡이고 엔트로피는 그 결과 분포의 성질(측정값)이다 — T 를 올리면 결과적으로 엔트로피가 커지는 것이지 둘이 같은 것은 아니다.',
 'VAE 의 KL 항과 RLHF 의 KL 벌점을 완전히 같은 크기 감각으로 다루면 안 된다 — 무엇을 사전/참조로 삼는지, 계수(β, 벌점 강도)를 얼마로 두는지에 따라 학습 거동이 크게 달라지므로 각 논문에서 실제로 어떤 분포 쌍을 쓰는지 확인해야 한다.'
],

papers:['vae','instructgpt','tent'],
terms:['probability-dist','mle','temperature']
});
