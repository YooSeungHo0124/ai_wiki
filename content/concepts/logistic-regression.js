WIKI.concept({
slug:'logistic-regression',

tldr:'입력의 가중합을 시그모이드에 통과시켜 클래스 확률을 예측하는 선형 분류기로, 이름과 달리 회귀가 아니라 분류에 쓰고 로그 오즈가 선형이라고 가정한다.',

why:'가장 단순한 분류기이자 신경망의 출력층(이진 분류 시)과 정확히 같은 구조다. 로그 오즈가 특징의 선형결합이라는 가정 하나로 해석 가능한 확률과 계수를 동시에 얻기 때문에, 의료·금융처럼 "왜 이렇게 예측했는가"를 설명해야 하는 분야에서 지금도 표준 베이스라인이다. 딥러닝 모델의 마지막 층에 시그모이드나 softmax 를 붙이는 관행 자체가 로지스틱 회귀에서 왔다는 것을 알면, "신경망은 결국 학습된 특징 위에 로지스틱 회귀를 얹은 것"이라는 시각으로 복잡한 모델을 훨씬 단순하게 이해할 수 있다.',

sections:[
 {h:'선형 결합 뒤에 시그모이드', d:'[선형회귀](#/c/linear-regression)처럼 $w^Tx+b$ 를 계산하지만, 이 값을 그대로 쓰지 않고 시그모이드 함수 $\\sigma(z)=1/(1+e^{-z})$ 를 통과시켜 0~1 사이의 확률로 만든다. 이 확률이 0.5(또는 정해진 임계값)를 넘으면 양성 클래스로 판단한다. 시그모이드는 $z$ 가 커질수록 완만하게 1에, 작아질수록 완만하게 0에 다가가는 S자 곡선이라 중간 영역에서는 입력이 조금만 변해도 확률이 크게 바뀌지만 양 극단에서는 거의 변하지 않는다(포화).'},
 {h:'로그 오즈가 선형이라는 뜻', d:'시그모이드의 역함수를 취하면 $\\log\\frac{p}{1-p}=w^Tx+b$ 가 된다 — 즉 로지스틱 회귀는 "확률이 선형"이 아니라 "오즈의 로그(log-odds)가 특징의 선형결합"이라고 가정하는 모델이다. 계수 $w_j$ 가 1 늘면 오즈가 $e^{w_j}$ 배 된다는 뜻이라 의료 통계에서 오즈비(odds ratio)로 바로 해석된다. 이 성질 때문에 확률 $p$ 자체는 특징에 대해 S자 곡선을 그리지만, 로그 오즈라는 다른 축으로 보면 여전히 직선이라는 점이 이 모델을 "선형" 모델로 부르는 근거이며, 결정 경계 자체도 여전히 하나의 직선(초평면)이다.'},
 {h:'교차 엔트로피 학습', d:'제곱오차가 아니라 [교차 엔트로피](#/c/entropy-kl) 손실을 쓴다 — 확률 예측이 틀릴수록 로그 손실이 급격히 커지게 설계되어, 잘못된 확신(정답이 1인데 0.01이라 말하는 것)에 큰 벌점을 준다. 닫힌 해가 없어 [경사하강법](#/c/gradient-descent) 류의 반복 최적화로 푼다.'},
 {h:'실무에서', d:'특징이 수십~수백 개고 선형 경계로 어느 정도 분리가 되는 문제(신용평가, 이탈 예측, A/B 테스트 반응 예측)에서 여전히 1순위 베이스라인이다. 클릭률 예측 같은 대규모 희소 특징 문제에서도 계산이 빨라 실시간 서빙에 쓰인다. 경계가 비선형이면 [결정 트리](#/c/decision-tree)·[부스팅](#/c/boosting)·신경망이 정확도에서 앞선다.'},
 {h:'다중 클래스로 확장', d:'이진 분류를 넘어 클래스가 셋 이상이면 두 가지 방식이 있다. one-vs-rest 는 클래스마다 "이 클래스인가 아닌가"의 이진 로지스틱 회귀를 따로 학습해 확률이 가장 높은 클래스를 고른다. 다항(multinomial) 로지스틱 회귀는 시그모이드 대신 [softmax](#/c/softmax)를 써서 모든 클래스 확률을 한 번에 정규화한다 — 후자가 이론적으로 더 일관되고 sklearn 등 현대 구현의 기본값이며, 신경망 분류기의 출력층도 대부분 이 다항 로지스틱 회귀 형태를 그대로 쓴다.'}
],

math:[
 {tex:'p(y=1|x)=\\sigma(w^Tx+b)=\\dfrac{1}{1+e^{-(w^Tx+b)}}', expr:'로지스틱 함수', d:'$w^Tx+b$ 가 매우 크면 $p\\to1$, 매우 작으면(음수로 크면) $p\\to0$, 0이면 $p=0.5$ 이다. 시그모이드는 임의의 실수를 확률처럼 보이는 0~1 값으로 눌러주는 역할을 한다.'},
 {tex:'L=-\\dfrac1n\\sum_i \\big[y_i\\log \\hat p_i+(1-y_i)\\log(1-\\hat p_i)\\big]', expr:'이진 교차 엔트로피(로그 손실)', d:'$y_i\\in\\{0,1\\}$ 는 실제 라벨, $\\hat p_i$ 는 예측 확률. 정답이 1인데 $\\hat p_i$ 가 0에 가까우면 $\\log\\hat p_i$ 항이 무한대로 발산해 손실이 급격히 커진다 — 확신에 찬 오답을 강하게 벌준다.'}
],

diagram:{type:'flow', cap:'선형 결합을 시그모이드로 눌러 확률로 바꾼다.',
 steps:['x → w^Tx+b','시그모이드 σ 적용','확률 p ∈ (0,1)','임계값 0.5 비교','클래스 0 또는 1']},

confuse:[
 {a:'로지스틱 회귀', b:'[선형회귀](#/c/linear-regression)', d:'이름에 둘 다 "회귀"가 들어가지만 로지스틱 회귀는 분류에 쓴다. 선형회귀는 연속값을 그대로 출력하고, 로지스틱 회귀는 선형결합을 시그모이드로 눌러 확률을 출력한 뒤 임계값으로 클래스를 정한다.'},
 {a:'로지스틱 회귀', b:'[SVM](#/c/svm-c)', d:'둘 다 선형 결정 경계를 찾는 분류기이지만, 로지스틱 회귀는 모든 점의 확률적 손실(로그 손실)을 최소화하고 SVM 은 결정 경계에서 가장 가까운 점들(서포트 벡터)과의 마진을 최대화한다. 로지스틱 회귀는 확률을 직접 주지만 SVM 은 기본적으로 확률을 주지 않는다.'},
 {a:'시그모이드', b:'[softmax](#/c/softmax)', d:'시그모이드는 이진 분류에서 클래스 하나의 확률을 독립적으로 계산하고(각 클래스별 확률 합이 1일 필요 없음), softmax 는 다중 클래스에서 모든 클래스 확률의 합이 1이 되도록 정규화한다. 다중 클래스 로지스틱 회귀(multinomial logistic regression)는 사실상 softmax 회귀다.'},
 {a:'로지스틱 회귀', b:'[나이브 베이즈](#/c/naive-bayes)', d:'둘 다 텍스트 분류 같은 문제에서 기본으로 시도해보는 선형에 가까운 분류기이지만, 로지스틱 회귀는 $P(y|x)$ 를 직접 학습하는 판별 모델이고 나이브 베이즈는 $P(x|y)$ 를 학습해 베이즈 정리로 뒤집는 생성 모델이다. 데이터가 많으면 로지스틱 회귀가, 데이터가 적으면 나이브 베이즈가 유리한 경향이 알려져 있다.'}
],

pitfalls:[
 '계수 부호와 크기를 스케일링 없이 바로 "중요도"로 해석하면 안 된다 — 특징 스케일이 다르면 계수 크기가 왜곡된다. 표준화된 특징으로 다시 학습한 뒤 계수를 비교해야 공정하다.',
 '클래스가 완벽히 선형 분리되는 데이터에서는 계수가 무한대로 발산하며 수렴하지 않는다 — 정규화 항을 추가해야 한다.',
 '임계값 0.5 를 당연하게 쓰지만 클래스 불균형이 있으면 이 값이 최선이 아니다 — [정밀도·재현율](#/c/precision-recall) 트레이드오프를 보고 조정해야 한다.',
 '정규화(L2 가 sklearn 기본값) 없이 그대로 쓰다가 특징이 많은 데이터에서 계수가 튀는 것을 모델의 결함으로 오해하는 경우가 있다 — 릿지·라쏘와 같은 원리의 정규화가 로지스틱 회귀에도 기본으로 필요하다.'
],

code:{lang:'python', d:'sklearn 로 학습하고 계수를 오즈비로 해석하는 예',
 src:'from sklearn.linear_model import LogisticRegression\nimport numpy as np\n\nclf = LogisticRegression(max_iter=1000).fit(X_train, y_train)\nproba = clf.predict_proba(X_test)[:,1]   # 양성 클래스 확률\n\n# 계수를 오즈비로 변환\nodds_ratio = np.exp(clf.coef_[0])\n# odds_ratio[j] > 1 이면 그 특징이 늘수록 양성 오즈가 커진다는 뜻'},

papers:['perceptron'],
terms:['linear-regression','svm-c','entropy-kl','precision-recall','naive-bayes']
});
