WIKI.concept({
slug:'similarity-metric',

tldr:'벡터 사이 거리를 재는 코사인·내적·유클리드는 벡터를 정규화하면 서로 같은 순위를 내지만, 정규화하지 않으면 벡터 크기(norm)를 반영하느냐에서 갈린다.',

why:'벡터 DB에 임베딩을 넣을 때 지표를 잘못 고르면 색인은 문제없이 만들어지고 검색도 결과를 돌려주는데, 그 결과의 순위 자체가 조용히 틀려 있는 상황이 생긴다. 임베딩 모델이 학습될 때 어떤 지표를 기준으로 최적화됐는지와 실제 검색에 쓰는 지표가 다르면 [의미 검색](#/c/semantic-search) 품질이 원인을 알기 어려운 방식으로 떨어지므로, 세 지표가 정확히 언제 같고 언제 다른지 알아야 한다. 디버깅할 때도 "임베딩이 나쁘다"와 "지표 설정이 틀렸다"를 구분하지 못하면 엉뚱한 곳(재학습, 재색인)에 시간을 쓰게 된다.',

sections:[
 {h:'세 지표의 정의', d:'내적(dot product)은 두 벡터 성분을 곱해 더한 값으로, 방향이 비슷할수록 그리고 벡터 크기(norm)가 클수록 커진다. 코사인 유사도는 내적을 두 벡터의 크기로 나눠 **방향만** 비교하도록 만든 값이다($-1$~$1$ 범위). 유클리드 거리는 두 벡터 사이의 실제 직선 거리로, 값이 작을수록 가깝다는 뜻이라 "유사도"가 아니라 "거리"이므로 부호 방향(작을수록 좋음)이 앞의 둘과 반대다. 실무 코드나 라이브러리 옵션에서는 이를 각각 dot, cosine, L2(또는 euclidean)라는 이름으로 부르는 경우가 많다.'},
 {h:'정규화하면 하나다', d:'모든 벡터의 크기를 1로 맞추는 정규화를 거치면, 내적은 크기 영향이 사라져 코사인과 정확히 같은 값이 된다. 또한 정규화된 두 벡터 $u,v$ 사이의 유클리드 거리 제곱은 $\\lVert u-v\\rVert^2 = 2-2\\cos(u,v)$ 로 코사인 유사도만의 함수가 된다 — 즉 정규화된 벡터에서는 유클리드 거리로 순위를 매기든 코사인 유사도로 순위를 매기든 **정렬 순서가 완전히 같다**. 반대로 정규화하지 않은 벡터에서는 세 지표가 서로 다른 순위를 낼 수 있다. 그래서 많은 벡터 DB가 "코사인 인덱스"를 내부적으로는 "벡터를 정규화한 뒤 내적 인덱스"로 구현한다 — 정규화만 한 번 해두면 더 빠른 내적 연산으로 코사인과 동일한 결과를 얻을 수 있기 때문이다.'},
 {h:'모델마다 권장 지표', d:'임베딩 모델은 학습 시 특정 지표를 기준으로 유사·비유사 쌍을 구분하도록 손실 함수가 설계된다. [Sentence-BERT](#/p/sentence-bert) 계열처럼 코사인 유사도를 기준으로 대조 학습된 모델에 유클리드 거리를 쓰면 학습 목표와 어긋나 성능이 떨어질 수 있고, [DPR](#/p/dpr)처럼 내적을 기준으로 학습된 모델(벡터 크기 자체에 의미를 담도록 학습된 경우)에 억지로 정규화를 적용하면 오히려 그 정보를 지워버릴 수 있다. 그래서 "이 임베딩 모델은 어떤 지표로 평가·학습됐는가"를 모델 카드에서 확인하는 것이 첫 단계이며, 잘 모르겠다면 정규화 후 코사인을 기본 선택으로 삼는 것이 가장 무난하다.'},
 {h:'벡터 DB 실무 함정', d:'[FAISS](#/p/faiss) 같은 벡터 색인 라이브러리는 인덱스를 만들 때 거리 지표(코사인/내적/L2)를 명시적으로 지정해야 한다. 임베딩 모델이 코사인 기준으로 학습됐는데 색인은 내적으로 만들거나, 벡터를 정규화하지 않은 채 내적 인덱스를 쓰면 검색 결과 순위가 미묘하게 또는 크게 틀어진다. 특히 정규화를 빼먹고 내적 인덱스를 쓰면 벡터 크기가 큰 문서(예: 긴 문서, 특정 토큰이 반복된 문서)가 실제 의미 관련성과 무관하게 계속 상위에 뜨는 편향이 생길 수 있다. 대부분의 임베딩 API·라이브러리가 코사인을 기본값으로 두는 것도, 이런 크기 편향을 사용자가 신경 쓰지 않아도 되게 하려는 실용적 선택이다.'}
],

math:[
 {tex:'\\cos(u,v)=\\dfrac{u\\cdot v}{\\lVert u\\rVert \\lVert v\\rVert},\\qquad \\lVert u-v\\rVert^2=\\lVert u\\rVert^2+\\lVert v\\rVert^2-2\\,u\\cdot v',
  expr:'코사인 유사도와 유클리드 거리의 관계', d:'$u\\cdot v$ 는 내적, $\\lVert u\\rVert,\\lVert v\\rVert$ 는 각 벡터의 크기(norm)다. $\\lVert u\\rVert=\\lVert v\\rVert=1$ 로 정규화하면 오른쪽 식이 $2-2\\cos(u,v)$ 로 단순해져, 유클리드 거리 제곱이 코사인 유사도에만 의존하는 단조 감소 함수가 된다 — 정규화된 공간에서 두 지표가 항상 같은 순위를 낸다는 근거다.'}
],

diagram:{type:'compare', cap:'정규화 전에는 세 지표가 다른 순위를 낼 수 있고, 정규화 후에는 코사인·내적·유클리드가 같은 순위로 수렴한다.',
 left:{t:'정규화 전', items:['내적: 크기+방향 모두 반영','코사인: 방향만 반영','유클리드: 크기+방향 모두 반영','세 지표 순위가 다를 수 있음']},
 right:{t:'정규화 후(‖v‖=1)', items:['내적 = 코사인','유클리드² = 2-2·코사인','세 지표 순위가 항상 일치','벡터 크기 정보는 사라짐']}},

confuse:[
 {a:'코사인 유사도', b:'내적(dot product)', d:'정규화된 벡터에서는 완전히 같은 값이지만, 정규화하지 않으면 다르다. 내적은 벡터 크기가 클수록 값도 커져 "얼마나 강한 신호인가"까지 반영하고, 코사인은 크기를 나눠 없애 순수하게 "방향이 얼마나 비슷한가"만 본다.'},
 {a:'코사인 유사도', b:'유클리드 거리', d:'정규화된 벡터에서는 둘이 단조 관계라 순위가 같지만, 정규화하지 않은 벡터에서는 유클리드 거리가 방향과 크기 차이를 모두 반영해 코사인과 다른 결과를 낼 수 있다. 또 코사인은 "클수록 유사", 유클리드는 "작을수록 유사"로 방향이 반대다.'},
 {a:'벡터 정규화(normalize)', b:'배치 정규화(batch normalization)', d:'여기서 말하는 정규화는 벡터 하나의 크기를 1로 맞추는 L2 정규화이고, 신경망의 배치/레이어 정규화는 학습 중 활성값 분포를 안정시키는 별개의 기법이다. 이름은 같은 "정규화"지만 목적과 적용 위치가 다르다.'}
],

pitfalls:[
 '"코사인이 항상 정답"이라고 단정하면 안 된다. 벡터 크기 자체에 의미가 있도록 학습된 임베딩(예: 인기도·강도를 크기로 인코딩)에서는 정규화가 오히려 유용한 정보를 지운다.',
 '벡터 DB의 기본 지표 설정을 확인하지 않고 그대로 쓰면, 임베딩 모델의 학습 지표와 어긋난 채로 운영되고 있을 수 있다 — 검색이 "그럭저럭 동작"하는 것처럼 보여도 최적 성능은 아닐 수 있다.',
 '고차원 임베딩에서는 [차원의 저주](#/c/curse-dimensionality) 때문에 벡터들 사이 거리가 전반적으로 비슷해지는 경향이 있어, 지표를 바꿔도 체감 차이가 크지 않게 느껴질 수 있다 — 그렇다고 지표 선택이 무의미하다는 뜻은 아니다.',
 '서로 다른 임베딩 모델로 만든 벡터를 같은 인덱스에 섞어 넣고 하나의 지표로 비교하면 안 된다. 모델이 다르면 임베딩 공간 자체가 다르므로 거리·유사도 값 자체에 비교 가능한 의미가 없다.'
],

code:{lang:'python', d:'정규화 전후로 코사인·내적 순위가 어떻게 달라질 수 있는지 확인하는 아주 단순한 예.',
 src:'import numpy as np\na = np.array([1.0, 0.0])\nb = np.array([2.0, 0.0])   # a와 방향은 같고 크기만 2배\nc = np.array([0.9, 0.9])  # a와 방향은 조금 다르지만 크기가 더 큼\n\n# 정규화 전 내적: c가 b보다 크게 나올 수 있다 (방향보다 크기 영향)\n# 정규화 후 코사인: b는 a와 코사인 1.0(방향 동일), c는 그보다 낮다'},

papers:['sentence-bert','faiss','dpr'],
terms:['embedding','curse-dimensionality','semantic-search','reranking']
});
