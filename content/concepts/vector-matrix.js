WIKI.concept({
slug:'vector-matrix',

tldr:'벡터는 숫자를 순서 있게 늘어놓은 1차원 배열, 행렬은 그것을 2차원으로 쌓은 것이며, 내적은 두 벡터를 하나의 스칼라로 압축하는 연산이다.',

why:'딥러닝의 모든 텐서 연산 — 임베딩, 선형층, 어텐션 — 은 결국 벡터와 행렬 위에서 정의된다. 논문의 수식에서 $x\\in\\mathbb{R}^d$, $W\\in\\mathbb{R}^{d\\times k}$ 같은 표기가 무엇을 뜻하는지 즉시 읽지 못하면 나머지 수학 개념(야코비안, 노름, 고윳값)은 전부 그 위에서 미끄러진다.',

sections:[
 {h:'벡터: 특징 묶음', d:'벡터 $x\\in\\mathbb{R}^d$ 는 $d$ 개의 숫자를 순서대로 묶은 것이다. 딥러닝에서는 토큰 하나의 [임베딩](#/c/embedding), 이미지 한 장의 특징, 문장 하나의 표현이 전부 벡터다. 차원 $d$ 는 "이 개체를 몇 개의 숫자로 표현하는가"이고, 흔히 128~4096 사이를 쓴다.'},
 {h:'행렬: 표 또는 변환', d:'행렬 $A\\in\\mathbb{R}^{m\\times n}$ 은 두 가지로 읽을 수 있다 — (1) $n$ 차원 벡터 $m$ 개를 쌓아놓은 표(미니배치, 시퀀스), 또는 (2) $n$ 차원 벡터를 $m$ 차원 벡터로 바꾸는 선형 변환(가중치 행렬 $W$). 신경망의 선형층 $y=Wx+b$ 는 후자다 — $W$ 의 각 행이 출력 뉴런 하나가 입력을 어떻게 조합할지를 담고 있다.'},
 {h:'내적: 유사도 연산', d:'두 벡터 $u,v\\in\\mathbb{R}^d$ 의 내적 $u\\cdot v=\\sum_i u_i v_i$ 는 두 벡터가 "같은 방향을 얼마나 공유하는가"를 하나의 숫자로 압축한다. 방향이 같을수록 크고, 직교하면 0, 반대 방향이면 음수다. [self-attention](#/c/self-attention)의 $QK^\\top$ 이 바로 모든 query-key 쌍의 내적을 한 번에 구하는 것이고, word2vec 류 임베딩에서 "의미가 비슷한 단어는 내적(또는 코사인 유사도)이 크다"는 것도 같은 원리다.'},
 {h:'전치와 형태 맞추기', d:'행렬곱 $AB$ 가 성립하려면 $A$ 의 열 수와 $B$ 의 행 수가 같아야 한다. 코드에서 shape mismatch 에러가 나는 대부분의 원인은 이 규칙을 놓친 것이다. 전치 $A^\\top$ 는 행과 열을 뒤바꾸는 연산으로, $QK^\\top$ 처럼 "내적을 모든 쌍에 대해 한 번에 계산"할 때 형태를 맞추는 용도로 흔히 쓰인다.'}
],

math:[
 {tex:'u\\cdot v=\\sum_{i=1}^{d} u_i v_i=\\lVert u\\rVert\\lVert v\\rVert\\cos\\theta',
  expr:'벡터 내적', d:'왼쪽은 성분별 곱의 합(계산 방법), 오른쪽은 두 벡터의 크기와 사잇각 $\\theta$ 로 표현한 것(기하적 의미)이다. $\\theta=0$(같은 방향)이면 $\\cos\\theta=1$ 로 내적이 최대, $\\theta=90°$ 면 0이다.'},
 {tex:'(AB)_{ij}=\\sum_{k} A_{ik}B_{kj}',
  expr:'행렬곱의 성분 정의', d:'결과 행렬의 $(i,j)$ 원소는 $A$ 의 $i$ 번째 행과 $B$ 의 $j$ 번째 열의 내적이다. 즉 행렬곱은 "모든 행-열 쌍에 대한 내적을 한 번에 계산"하는 연산이다.'}
],

diagram:{type:'compare', cap:'벡터·행렬·내적을 무엇으로 읽는가.',
 left:{t:'벡터', items:['한 개체의 특징 d개','토큰 임베딩 하나','방향과 크기를 가짐']},
 right:{t:'행렬', items:['벡터 m개를 쌓은 표','또는 선형 변환 W','내적을 모든 쌍에 확장']}},

confuse:[
 {a:'벡터의 내적', b:'원소별 곱(elementwise)', d:'내적($u\\cdot v$)은 결과가 스칼라 하나다. 원소별 곱($u\\odot v$, Hadamard product)은 같은 크기의 벡터가 나온다. LSTM 의 게이트 연산은 원소별 곱이고, 어텐션 점수는 내적이다 — 코드에서 `torch.dot`과 `*` 를 헷갈리면 완전히 다른 연산이 된다.'},
 {a:'행 벡터', b:'열 벡터', d:'수학 표기에서 벡터는 관행상 열 벡터($d\\times 1$)로 취급한다. 코드(numpy, PyTorch)의 1차원 배열은 이 구분이 없어서 종종 행/열 어느 쪽으로 해석되는지가 broadcasting 규칙에 암묵적으로 숨어 있다 — shape 을 항상 명시적으로 찍어보는 습관이 필요하다.'}
],

pitfalls:[
 '$Ax$ 는 되는데 $xA$ 는 안 되는(또는 그 반대) 경우가 흔하다 — 행렬곱은 교환법칙이 성립하지 않고, 애초에 형태가 안 맞으면 정의조차 안 된다.',
 '"차원이 크다"를 벡터의 길이(성분 개수)와 행렬의 크기(행×열)를 혼동해서 쓰는 경우가 있다 — $d$ 차원 벡터와 $d\\times d$ 행렬은 전혀 다른 크기의 대상이다.',
 '코사인 유사도(내적을 벡터 크기로 나눈 것)와 순수 내적을 같은 것으로 착각하기 쉽다 — 벡터 크기가 다르면 내적만으로는 방향이 비슷해도 값이 크게 다를 수 있다. 임베딩 검색에서 어떤 것을 쓰는지 반드시 확인해야 한다.'
],

code:{lang:'python', d:'shape 을 항상 명시적으로 찍어 행렬곱 형태 오류를 미리 잡는 습관.',
 src:'import numpy as np\nX = np.random.randn(32, 768)   # [batch, d]\nW = np.random.randn(768, 3072) # [d, k]\nprint(X.shape, W.shape)        # 안쪽 차원 768 이 일치해야 함\nY = X @ W                      # [32, 3072]\nprint(Y.shape)'},

papers:['backprop'],
terms:['tensor','matmul','norm']
});
