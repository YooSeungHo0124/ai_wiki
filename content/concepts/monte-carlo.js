WIKI.concept({
slug:'monte-carlo',

tldr:'복잡하거나 계산 불가능한 기대값·적분을 직접 풀지 않고 무작위 샘플을 많이 뽑아 그 평균으로 근사하는 방법으로, 샘플 수가 늘수록 추정이 참값에 가까워진다.',

why:'"정확히 계산할 수 없으면 샘플을 뽑아 평균 내라"는 이 발상 하나가 [MC 드롭아웃](#/p/mc-dropout)의 불확실성 추정, MuZero 의 트리 탐색, 확산 모델의 샘플링 과정에 공통으로 깔려 있다. 몬테카를로를 모르면 이 세 가지가 서로 무관한 기법처럼 보이지만, 사실은 전부 "기대값을 샘플 평균으로 근사한다"는 같은 원리의 응용이다.',

sections:[
 {h:'적분 대신 평균', d:'확률 분포 $p$ 아래에서 함수 $g$ 의 [기대값](#/c/expectation-variance) $\\mathbb{E}_{x\\sim p}[g(x)]$ 을 직접(해석적으로) 계산하기 어려운 경우가 많다 — 분포가 복잡하거나 차원이 높으면 적분식 자체를 풀 수 없다. 몬테카를로는 이 기대값을 $p$ 에서 뽑은 샘플 $x_1,\\ldots,x_N$ 의 평균 $\\frac{1}{N}\\sum_i g(x_i)$ 으로 근사한다 — 대수의 법칙에 의해 $N$ 이 커지면 이 평균이 참 기대값에 확률적으로 수렴한다.'},
 {h:'수렴 속도와 분산', d:'몬테카를로 추정의 오차(표준편차)는 대략 $O(1/\\sqrt{N})$ 로 줄어든다 — 샘플을 4배 늘리면 오차는 절반이 된다. 이는 차원 수에 거의 무관하다는 것이 장점이지만(그래서 고차원 적분에서도 쓸 수 있다), 수렴이 느려서(제곱근 비례) 정밀한 추정에는 아주 많은 샘플이 필요하다는 것이 단점이다 — 이 분산을 줄이려는 기법들(중요도 샘플링, 분산 감소 등)이 실무 몬테카를로의 큰 부분을 차지한다.'},
 {h:'MC 드롭아웃', d:'[MC 드롭아웃](#/p/mc-dropout)은 추론 시에도 dropout 을 켠 채 같은 입력을 여러 번(수십~수백 번) 순전파한다 — 매번 다른 뉴런이 꺼지므로 사실상 파라미터의 [사후 분포](#/c/bayes)에서 샘플을 뽑는 효과를 낸다. 그 출력들의 평균은 예측값, 퍼짐(분산)은 불확실성 추정치가 된다 — "분포에서 직접 적분 못 하니 여러 번 샘플링해서 평균·분산을 근사한다"는 전형적인 몬테카를로 적용이다.'},
 {h:'MuZero·확산모델', d:'[MuZero](#/p/muzero)의 몬테카를로 트리 탐색(MCTS)은 가능한 모든 수순을 다 계산할 수 없으니 유망해 보이는 경로를 확률적으로 반복 시뮬레이션해 가치를 추정한다 — 이름 그대로 몬테카를로다. 확산 모델의 생성 과정 역시 노이즈에서 데이터로 가는 확률적 역과정을 매 스텝 샘플링으로 구현한다 — [diffusion-basics](#/c/diffusion-basics)에서 다루는 노이즈 제거 과정 자체가 확률적 미분방정식을 몬테카를로로 수치 적분하는 것에 해당한다.'}
],

math:[
 {tex:'\\mathbb{E}_{x\\sim p}[g(x)] \\approx \\frac{1}{N}\\sum_{i=1}^{N} g(x_i),\\quad x_i \\overset{iid}{\\sim} p',
  expr:'몬테카를로 추정량', d:'$p$ 에서 독립적으로 뽑은 $N$ 개의 샘플에 대해 $g$ 를 평균 낸 것이 기대값의 불편(unbiased) 추정량이다. $N\\to\\infty$ 이면 대수의 법칙에 의해 이 값은 참 기대값으로 수렴한다.'},
 {tex:'\\text{Std}\\!\\left(\\frac{1}{N}\\sum_i g(x_i)\\right)=\\frac{\\sigma}{\\sqrt{N}}',
  expr:'몬테카를로 오차의 수렴 속도', d:'$\\sigma$ 는 $g(x)$ 의 표준편차. 오차가 샘플 수 $N$ 의 제곱근에 반비례해 줄어든다는 것이 몬테카를로의 대표적 성질이다 — 차원에 무관하다는 장점과 느린 수렴이라는 단점이 동시에 여기서 나온다.'}
],

diagram:{type:'flow', cap:'적분을 못 풀면 샘플을 뽑아 평균으로 근사한다.',
 nodes:[
  {t:'복잡한 기대값'},
  {t:'분포 p에서 샘플링'},
  {t:'각 샘플에 g 적용'},
  {t:'평균 = 추정값'}
 ]},

confuse:[
 {a:'몬테카를로', b:'[MLE](#/c/mle)', d:'몬테카를로는 이미 알고 있는(또는 시뮬레이션 가능한) 분포에서 기대값을 근사하는 방법이고, MLE 는 관측된 데이터로부터 분포의 파라미터 자체를 추정하는 방법이다 — 둘은 종종 함께 쓰이지만(예: MLE 로 얻은 분포에서 몬테카를로로 샘플링) 목적이 다르다.'},
 {a:'몬테카를로 방법', b:'몬테카를로 vs TD 학습', d:'강화학습 맥락의 "몬테카를로"([mc-vs-td](#/c/mc-vs-td))는 에피소드 전체가 끝날 때까지 기다렸다가 실제 보상 합으로 가치를 추정하는 구체적 기법을 가리킨다 — 이 문서에서 다루는 일반적인 "샘플로 기대값 근사"라는 개념의 한 특수 사례다.'}
],

pitfalls:[
 '샘플 수를 늘리면 항상 선형으로 정확도가 좋아진다고 착각하기 쉬운데, 오차는 $\\sqrt{N}$ 에 반비례해 줄어들 뿐이라 정밀도를 한 자릿수 더 얻으려면 샘플을 100배 늘려야 한다.',
 '샘플들이 서로 독립이 아니면(예: MCMC 의 연속된 샘플처럼 상관관계가 있으면) 분산 추정 자체가 왜곡된다 — "많이 뽑았으니 안정적이다"라고 안심하기 전에 샘플의 독립성·다양성을 확인해야 한다.',
 'MC 드롭아웃의 순전파 횟수를 늘리면 늘릴수록 무조건 더 정확한 불확실성을 얻는다고 생각하기 쉬운데, 이는 몬테카를로 추정의 분산만 줄일 뿐 근사 자체의 편향(dropout 이 진짜 베이즈 사후 분포와 얼마나 다른가)은 줄이지 못한다.'
],

code:{lang:'python', d:'원의 넓이(pi)를 몬테카를로로 추정해 N에 따른 오차 감소를 확인.',
 src:'import numpy as np\nfor N in [100, 10000, 1000000]:\n    pts = np.random.rand(N, 2)\n    inside = (pts[:,0]**2 + pts[:,1]**2) < 1\n    pi_est = inside.mean() * 4\n    print(N, pi_est, abs(pi_est - np.pi))  # 오차가 sqrt(N)에 반비례'},

papers:['mc-dropout','muzero'],
terms:['probability-dist','expectation-variance','bayes']
});
