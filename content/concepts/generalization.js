WIKI.concept({
slug:'generalization',

tldr:'학습 데이터에서 배운 것이 한 번도 보지 않은 새 데이터에서도 통하는 능력.',

why:'모델을 배포하기 전 실제로 궁금한 것은 학습셋 성능이 아니라 이것이다. 그런데 현대 신경망은 파라미터 수가 학습 샘플 수를 훨씬 넘고, 이론상 학습 데이터를 완전히 외워버릴 수 있는데도 실제로는 잘 일반화된다 — 이 수수께끼를 모르면 "왜 되는지 모르지만 일단 된다"는 감으로만 모델을 다루게 되고, 벤치마크 성능과 실제 배포 환경 성능이 갈리는 이유(분포 이동)도 설명하지 못한다.',

sections:[
 {h:'무엇인가', d:'모델이 학습 데이터의 분포에서 뽑힌, 학습에 쓰이지 않은 새로운 샘플에 대해서도 비슷한 성능을 내는 정도다. 학습 loss 와 (같은 분포에서 뽑힌) 시험 loss 의 차이를 **일반화 격차(generalization gap)** 라 부르고, 이 격차가 작을수록 일반화가 잘 된 것이다. [과적합](#/c/overfitting)은 이 격차가 벌어지는 과정을, [편향-분산](#/c/bias-variance)은 그 격차를 수학적으로 분해하는 틀을 제공한다.'},
 {h:'파라미터가 데이터보다 많은데 왜 되는가', d:'고전적 통계학습 이론은 모델의 표현력(파라미터 수, VC 차원)이 커질수록 일반화 격차의 상한도 커진다고 예측한다. 그런데 대형 신경망은 파라미터가 학습 샘플보다 몇 배, 몇십 배 많아 이론상 학습 데이터를 무작위 라벨로 바꿔도 외워버릴 수 있는데도, 실제 라벨로 학습하면 잘 일반화된다. [일반화 재고](#/p/rethinking-generalization) 는 실제로 CIFAR-10 라벨을 완전히 무작위로 섞어도 모델이 학습 데이터를 100% 외울 수 있음을 보여 "표현력이 크면 일반화가 나쁘다"는 고전 이론의 설명력이 딥러닝에는 부족함을 실험으로 드러냈다. 무엇이 진짜 일반화를 이끄는지(암묵적 정칙화, 손실 지형의 평탄한 최소점, SGD 자체의 편향 등)는 아직도 활발한 연구 주제다.'},
 {h:'분포 이동과의 관계', d:'지금까지 말한 일반화는 **학습 데이터와 같은 분포**에서 뽑힌 새 샘플에 대한 것이다(in-distribution, i.i.d. 일반화). 하지만 실제 배포 환경은 학습 데이터와 분포가 조금씩 다른 경우가 대부분이다(distribution shift) — 카메라 기종이 바뀌거나, 시간이 지나 트렌드가 바뀌거나. 벤치마크에서 성능이 좋아도 분포가 이동한 환경에서는 뚝 떨어질 수 있다. 즉 "일반화가 잘 된다"는 주장은 반드시 "어떤 분포에 대해"라는 단서를 달아야 정확하다.'},
 {h:'어떻게 측정하는가', d:'일반화는 학습에 전혀 쓰이지 않은 데이터로만 정직하게 측정할 수 있다. 그래서 데이터를 [학습·검증·시험](#/c/train-val-test)으로 나눈다 — 검증셋은 하이퍼파라미터를 고르는 데 쓰이므로 엄밀히는 완전히 독립이 아니고, 최종 일반화 성능은 그 과정에 전혀 관여하지 않은 시험셋으로만 보고해야 한다. 벤치마크를 몇 년간 같은 시험셋으로 반복 평가하면 커뮤니티 전체가 암묵적으로 그 시험셋에 과적합될 수 있다는 것도 실측된 바 있다.'}
],

diagram:{type:'split', cap:'"일반화가 잘 된다"는 말은 항상 어떤 분포에 대한 것인지 명시해야 한다.',
 from:{t:'학습된 모델', s:'학습 분포에서 학습'},
 branches:[
  {t:'같은 분포 새 샘플', s:'i.i.d. 일반화'},
  {t:'분포 이동 샘플', s:'OOD, 성능 하락 흔함'}
 ],
 join:'벤치마크 점수는 보통 전자만 잰다 — 후자는 별도로 확인해야 한다'},

confuse:[
 {a:'일반화(generalization)', b:'[과적합](#/c/overfitting)', d:'일반화는 "새 데이터에서 얼마나 잘 되는가"라는 능력 자체를 가리키고, 과적합은 그 능력이 망가지는 과정(학습 loss 와 검증 loss 가 갈라지는 것)을 가리킨다. 일반화가 나쁜 원인 중 하나가 과적합이다.'},
 {a:'i.i.d. 일반화', b:'분포 이동(distribution shift)에 대한 강건성', d:'전자는 같은 분포에서 뽑은 새 샘플에 대한 성능이고, 후자는 분포 자체가 바뀐 데이터에 대한 성능이다. 대부분의 벤치마크 수치는 전자만 잰다.'},
 {a:'학습 데이터 암기(memorization)', b:'일반화', d:'대형 모델은 이 둘을 동시에 할 수 있다 — 학습 데이터 일부를 그대로 재현할 수 있으면서도 새 입력에 대해 잘 일반화되기도 한다. 암기가 있다는 것만으로 일반화가 안 된다고 단정할 수 없다.'}
],

pitfalls:[
 '"파라미터가 데이터보다 많으니 과적합될 것"이라는 예측은 실제 딥러닝에서 자주 틀린다 — 왜 그런지는 아직 이론이 완전하지 않다는 것 자체를 알아둘 필요가 있다.',
 '검증셋 성능만 보고 "잘 일반화된다"고 말하는 것은 부정확하다 — 검증셋은 하이퍼파라미터 선택에 쓰였으므로 완전히 독립된 시험이 아니다.',
 '벤치마크에서 SOTA 를 찍었다고 실제 배포 환경에서도 그럴 것이라 가정하면 안 된다 — 분포 이동이 있으면 성능이 크게 달라질 수 있다.',
 '같은 시험셋으로 수년간 모델을 비교하며 그 시험셋 성능을 최적화하다 보면, 커뮤니티 전체가 그 시험셋에 암묵적으로 과적합될 수 있다.'
],

papers:['rethinking-generalization','double-descent'],
terms:['overfitting','bias-variance','train-val-test','regularization']
})
