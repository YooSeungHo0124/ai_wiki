WIKI.concept({
slug:'naive-bayes',

tldr:'특징들이 클래스가 주어졌을 때 서로 조건부 독립이라고(순진하게) 가정하고 베이즈 정리로 클래스 확률을 계산하는 생성 모델 분류기다.',

why:'가정이 거의 항상 틀림에도 불구하고 텍스트 분류(스팸 필터, 감성 분석)에서 놀랄 만큼 잘 동작하고, 학습이 극도로 빠르며(카운트만 세면 됨) 데이터가 적어도 안정적이다. 왜 "틀린 가정"의 모델이 실전에서 통하는지를 보여주는 대표 사례라 확률 모델의 실무 감각을 기르는 데 좋은 교재이기도 하다. [베이즈 정리](#/c/bayes)를 실제로 손에 쥐고 계산해보는 몇 안 되는 실전 알고리즘이라, 베이즈 추론이 실무에서 어떻게 굴러가는지 보여주는 입문 사례로도 자주 쓰인다.',

sections:[
 {h:'베이즈 정리로 거꾸로 푼다', d:'우리가 알고 싶은 건 "이 문서가 주어졌을 때 스팸일 확률" $P(y|x)$ 이지만, 직접 추정하기보다 [베이즈 정리](#/c/bayes)로 뒤집어 "스팸이라면 이런 문서가 나올 확률" $P(x|y)$ 과 스팸의 사전 확률 $P(y)$ 을 곱해 계산한다. 분모 $P(x)$ 는 모든 클래스에서 공통이라 클래스 비교(어느 쪽이 더 큰가)에는 계산할 필요가 없다. "베이즈"라는 이름이 붙은 이유가 바로 이 정리를 학습의 핵심 도구로 쓰기 때문이다.'},
 {h:'"순진한" 독립 가정', d:'$P(x|y)$ 를 직접 추정하려면 특징들의 모든 조합에 대한 결합분포가 필요해 특징이 많아질수록 지수적으로 데이터가 필요해진다. 나이브 베이즈는 클래스 $y$ 가 주어졌을 때 각 특징(단어)이 서로 독립이라고 가정해 $P(x|y)=\\prod_j P(x_j|y)$ 로 쪼갠다 — 이 가정은 현실에서 거의 항상 틀리지만(문장 속 단어들은 서로 관련이 있다), 계산을 극단적으로 단순하게 만들어준다.'},
 {h:'틀려도 잘 되는 이유', d:'이런 단순화된 분류의 최종 목적은 확률값 자체를 정확히 맞히는 게 아니라 "어느 클래스가 더 그럴듯한가"라는 순서를 맞히는 것이다. 독립 가정이 깨져 있어도 그 왜곡이 모든 클래스에 비슷하게 걸리면 최댓값의 순서는 보존되는 경우가 많다 — 그래서 확률 추정치 자체는 부정확해도 분류 결정은 종종 정확하다.'},
 {h:'실무에서', d:'단어 개수 기반의 텍스트 분류(스팸 필터, 뉴스 카테고리 분류)에서 특징이 극히 많고(어휘 크기) 희소한 상황에 특히 강하다 — 카운트만 세면 학습이 끝나 초 단위로 재학습이 가능하다. 특징 간 상호작용이 클래스 판단에 결정적인 문제(예: XOR 스타일 패턴)에는 근본적으로 약하다. 실무에서는 빠른 베이스라인으로 먼저 돌려보고, 본격적인 정확도가 필요하면 [로지스틱 회귀](#/c/logistic-regression)나 [부스팅](#/c/boosting)으로 넘어가는 경우가 많다.'},
 {h:'변형: 어떤 분포를 쓰는가', d:'특징의 성격에 따라 세 가지 변형을 쓴다. 다항(Multinomial) 나이브 베이즈는 단어 개수 같은 정수 카운트 특징에, 베르누이(Bernoulli) 나이브 베이즈는 "단어가 있는지 없는지"의 이진 특징에, 가우시안(Gaussian) 나이브 베이즈는 연속형 실수 특징(키, 몸무게 등)에 쓴다 — 각 특징이 클래스 조건부로 정규분포를 따른다고 가정한다. 표 데이터에 특징 종류가 섞여 있으면 특징마다 다른 변형을 혼합해 쓰는 구현도 있다.'}
],

math:[
 {tex:'P(y|x)\\propto P(y)\\prod_{j=1}^d P(x_j|y)', expr:'나이브 베이즈 분류 규칙', d:'$P(y)$ 는 클래스 사전 확률(학습 데이터에서 그 클래스의 비율), $P(x_j|y)$ 는 클래스 $y$ 에서 $j$ 번째 특징이 나올 확률. 모든 클래스에 대해 이 값을 계산해 가장 큰 클래스를 예측으로 고른다.'},
 {tex:'P(x_j|y)=\\dfrac{\\text{count}(x_j,y)+\\alpha}{\\text{count}(y)+\\alpha V}', expr:'라플라스(가산) 평활화', d:'학습 데이터에 한 번도 안 나온 단어가 등장하면 $P(x_j|y)=0$ 이 되어 곱 전체가 0이 되어버린다. 분자·분모에 작은 상수 $\\alpha$ 를 더해(보통 $\\alpha=1$) 이 문제를 막는다. $V$ 는 어휘(가능한 특징값) 크기. $\\alpha$ 를 키울수록 평활화가 강해져 극단적인 확률을 줄이는 정규화 효과도 함께 낸다.'}
],

diagram:{type:'flow', cap:'클래스별로 조건부 확률을 곱해 최댓값을 고른다.',
 steps:['문서를 단어로 분해','클래스별 사전확률 P(y)','단어별 P(word|y) 곱',"클래스마다 점수 계산",'최댓값 클래스 선택']},

confuse:[
 {a:'나이브 베이즈', b:'[로지스틱 회귀](#/c/logistic-regression)', d:'나이브 베이즈는 $P(x|y)$ 를 먼저 모델링하고 베이즈 정리로 $P(y|x)$ 를 유도하는 생성 모델(generative model)이고, 로지스틱 회귀는 $P(y|x)$ 를 바로 모델링하는 판별 모델(discriminative model)이다. 데이터가 적을 때는 나이브 베이즈가, 데이터가 많고 독립 가정이 심하게 깨질 때는 로지스틱 회귀가 유리한 경향이 있다는 것이 알려진 경험칙이다. 흥미롭게도 나이브 베이즈의 결정 함수를 전개하면 로지스틱 회귀와 같은 선형 형태가 나온다는 것도 두 모델의 깊은 연결점이다.'},
 {a:'조건부 독립', b:'독립', d:'나이브 베이즈가 가정하는 것은 "클래스가 주어졌을 때"의 조건부 독립이지, 특징들이 무조건 서로 독립이라는 뜻이 아니다. 예컨대 "무료"와 "당첨"이라는 단어는 전체적으로는 상관이 있어도, "스팸이라는 조건 하에서"는 독립에 가깝다고 가정하는 것이다. 이 구분을 놓치면 "단어들이 서로 관련 있으니 나이브 베이즈는 아예 못 쓴다"는 과도한 결론으로 이어지기 쉽다.'},
 {a:'나이브 베이즈의 확률 출력', b:'잘 보정된(calibrated) 확률', d:'나이브 베이즈가 내놓는 $P(y|x)$ 수치는 독립 가정이 깨진 만큼 실제 확률과 어긋나는(대개 너무 극단적인 0 또는 1에 가까운) 경우가 많다. 분류 결정(순위)은 믿을 만해도 확률값 자체를 그대로 신뢰도로 쓰려면 별도의 [보정](#/c/calibration)이 필요하다 — Platt scaling 같은 사후 보정을 나이브 베이즈 출력에 적용하는 것도 흔한 실무 패턴이다.'}
],

pitfalls:[
 '평활화(smoothing) 없이 그대로 쓰면 학습 데이터에 없던 단어 하나 때문에 전체 확률이 0이 되어버리는 사고가 난다 — 라플라스 평활화는 거의 항상 필요하고, 이 실수는 배포 후 실제 서비스 로그에서야 드러나는 경우가 많다.',
 '특징 간 상관이 클래스 판단에 핵심적인 문제(강하게 상호작용하는 특징들)에는 독립 가정이 성능을 크게 깎아먹는다.',
 '확률 출력값을 그대로 "모델이 몇 % 확신한다"고 보고하면 과장된 수치를 전달하게 된다 — 순위(어느 클래스가 더 그럴듯한지)만 신뢰하는 것이 안전하다.',
 '특징 성격(카운트인지, 이진인지, 연속인지)에 맞지 않는 변형을 고르는 실수가 흔하다 — 예를 들어 정수 카운트 특징에 가우시안 나이브 베이즈를 쓰면 분포 가정 자체가 어긋나 성능이 떨어진다.'
],

code:{lang:'python', d:'라플라스 평활화가 적용된 다항 나이브 베이즈로 텍스트를 분류하는 예',
 src:'from sklearn.feature_extraction.text import CountVectorizer\nfrom sklearn.naive_bayes import MultinomialNB\n\nvec = CountVectorizer()\nX = vec.fit_transform(texts)            # 단어 개수 행렬\nclf = MultinomialNB(alpha=1.0)          # alpha: 라플라스 평활화 강도\nclf.fit(X, labels)\npred = clf.predict(vec.transform(["free money now"]))'},

papers:['bm25'],
terms:['bayes','logistic-regression','calibration','mle']
});
