WIKI.concept({
slug:'cot-c',

tldr:'"단계적으로 생각하자"처럼 정답 전에 중간 추론 과정을 명시적으로 생성하게 해, 여러 단계를 거쳐야 하는 문제의 정확도를 올리는 프롬프팅 기법이다.',

why:'CoT 는 지금도 추론이 필요한 프롬프트의 기본 구성요소지만, 모델 크기와 벤치마크에 따라 오히려 해가 되는 경우가 있다는 것을 모르면 무조건 "생각해보자"를 붙이는 습관이 성능을 깎아먹는다. 언제 켜고 언제 꺼야 하는지 아는 것이 CoT 를 실무에서 쓰는 능력의 핵심이다.',

sections:[
 {h:'왜 통하는가', d:'언어모델은 한 번의 순전파로 다음 토큰 하나를 내놓는다 — 최종 답을 바로 요구하면 모델이 쓸 수 있는 "계산 스텝"은 사실상 그 한 토큰만큼뿐이다. [CoT](#/p/cot)는 중간 추론을 텍스트로 먼저 생성하게 강제함으로써, 이후 토큰(최종 답)이 앞서 생성된 중간 결과를 조건으로 참조할 수 있게 만든다. 즉 추론 과정 자체를 문맥에 풀어놓아 모델의 유효 계산량을 늘리는 효과를 낸다. 이는 사람이 암산으로 큰 수를 곱하기보다 종이에 자릿수를 적어가며 계산하는 것과 비슷한 이치다 — 계산을 외부(텍스트)에 풀어놓으면 한 번에 기억해야 할 정보의 양이 줄어든다.'},
 {h:'예시 없이도 통하는 이유', d:'원래 [CoT](#/p/cot) 논문은 few-shot 예시에 추론 과정을 손수 적어 넣는 방식이었지만, [Zero-shot CoT](#/p/zero-shot-cot)는 예시 없이 "단계적으로 생각해보자(Let us think step by step)" 한 문장만 붙여도 비슷한 효과가 난다는 것을 보였다. 이는 CoT 효과의 핵심이 정교한 예시 설계가 아니라 "중간 단계를 생성하라"는 지시 자체에 있음을 시사한다.'},
 {h:'자기일관성으로 더 안정화', d:'[자기일관성](#/p/self-consistency)(self-consistency)은 CoT 를 온도를 올려 여러 번 샘플링한 뒤 최종 답을 다수결로 정한다. 단일 CoT 경로는 중간에 한 번 잘못 추론하면 그대로 틀린 결론에 도달하지만, 여러 경로를 뽑아 투표하면 서로 다른 실수를 한 경로들이 상쇄돼 정답률이 오른다 — 대신 추론 비용이 샘플 수만큼 커진다. 몇 개를 샘플링할지(예: 5개, 20개)는 정확도 이득과 비용의 트레이드오프이며, 태스크 난이도가 높을수록 더 많은 샘플에서 이득이 커지는 경향이 보고된다.'},
 {h:'작은 모델엔 오히려 해롭다', d:'CoT 의 이득은 창발적(emergent)이다 — 충분히 큰 모델에서만 뚜렷하게 나타나고, 작은 모델에서는 중간 추론이 오히려 부정확한 경로를 만들어 최종 답을 그 잘못된 경로에 고정시켜버리는 역효과가 보고된다. 작은 모델은 애초에 올바른 중간 단계를 생성할 능력이 부족한데, 일단 텍스트로 뱉어진 잘못된 단계는 이후 토큰 생성의 조건이 되어 오류를 정정하지 못하고 그대로 이어간다. 이 현상은 "능력이 일정 규모를 넘어야만 나타난다"는 의미에서 창발적이라 불리며, 실무에서는 "우리가 쓰는 모델 크기에서 CoT 가 실제로 이득인가"를 프롬프트 설계 전에 직접 확인해야 하는 이유가 된다.'},
 {h:'벤치마크에 따라 정반대', d:'[MMLU-Pro](#/p/mmlu-pro) 류의 평가에서 확인된 것처럼 CoT 효과는 태스크 성격에 크게 좌우된다. 다단계 산술·논리처럼 명시적 중간 계산이 필요한 문제에서는 CoT 가 크게 도움되지만, 단순 사실 회상이나 분류처럼 한 번에 답이 나오는 문제에서는 불필요한 추론 텍스트가 오히려 정답에서 벗어날 여지를 늘려 정확도를 떨어뜨리는 경우가 보고된다. "CoT 는 항상 이득"이 아니라 태스크별로 검증해야 하는 이유다.'},
 {h:'비용과 지연 시간', d:'CoT 는 최종 답 전에 추론 토큰을 추가로 생성하므로, 자기회귀 생성 특성상 토큰 수에 비례해 지연 시간과 비용이 늘어난다. 단순 태스크에 CoT 를 습관적으로 붙이면 정확도 이득 없이 비용만 늘어나는 경우가 실무에서 흔하다. 반대로 정말 다단계 추론이 필요한 태스크에서는 이 추가 비용이 정확도 개선으로 상쇄되고도 남는다 — 태스크별로 CoT 를 켤지 끌지 결정하는 것이 비용 관리의 실질적인 손잡이가 된다.'}
],

diagram:{type:'flow', cap:'CoT 가 유효 계산량을 늘리는 경로.',
 steps:['질문 입력','중간 추론 토큰 생성(계산 스텝 확보)','중간 결과를 조건으로 다음 토큰 예측','최종 답 생성']},

code:{lang:'text', d:'Zero-shot CoT의 최소 형태 — 질문 뒤에 한 문장만 붙인다.', src:
'질문: 한 상자에 사과가 12개씩 들어있다. 상자 7개와 낱개 사과 5개가 있으면 사과는 총 몇 개인가?\n\n'+
'단계적으로 생각해봅시다.\n\n'+
'(모델이 이어서: 12*7=84, 84+5=89 처럼 중간 계산을 텍스트로 생성한 뒤 최종 답 89를 낸다)'},

confuse:[
 {a:'[CoT](#/p/cot)', b:'[자기일관성](#/p/self-consistency)', d:'CoT 는 한 번의 생성에서 추론 과정을 드러내는 것이고, 자기일관성은 그 CoT 생성을 여러 번 반복해 다수결로 답을 고르는 상위 기법이다. 자기일관성은 CoT 없이는 성립하지 않는다.'},
 {a:'CoT', b:'[프롬프트 엔지니어링](#/c/prompt-engineering)', d:'CoT 는 프롬프트 엔지니어링의 한 하위 기법이다. 프롬프트 엔지니어링은 역할 부여·예시·형식 지정 등을 포괄하는 더 큰 범주다.'},
 {a:'few-shot CoT', b:'[Zero-shot CoT](#/p/zero-shot-cot)', d:'few-shot CoT 는 예시 자체에 추론 과정을 사람이 손으로 적어 넣어 형식을 보여주고, zero-shot CoT 는 예시 없이 지시 문장 하나로 같은 효과를 유도한다. few-shot 이 더 안정적으로 형식을 지키게 하지만 프롬프트가 길어지고 예시 작성 비용이 든다.'},
 {a:'CoT', b:'모델이 실제로 내부에서 하는 계산', d:'CoT 로 보이는 텍스트는 모델이 다음 토큰을 예측하며 생성한 결과물일 뿐, 모델 내부의 실제 계산 경로를 그대로 보여준다는 보장은 없다. 텍스트 상의 추론과 모델의 실제 내부 표현이 다를 수 있다는 점은 CoT 를 해석 가능성(interpretability) 도구로 과신하면 안 되는 이유다.'}
],

pitfalls:[
 '"단계적으로 생각하자"를 모든 프롬프트에 습관적으로 붙이면, 작은 모델이나 단순 태스크에서는 오히려 정확도가 떨어질 수 있다.',
 'CoT 로 생성된 추론 텍스트가 그럴듯해 보인다고 해서 그 추론이 실제로 최종 답을 도출한 논리라는 보장은 없다 — 모델이 답을 먼저 정하고 설명을 사후에 꾸며내는 경우도 있다.',
 '자기일관성은 정확도를 올리지만 샘플 수에 비례해 추론 비용이 커진다 — 지연 시간이 중요한 서비스에서는 트레이드오프를 따져야 한다.',
 '"이 모델은 추론에 강하다"는 평가를 CoT 프롬프트를 쓴 결과로만 내리면, zero-shot 순수 답변 능력과 CoT 로 끌어낸 능력을 혼동하게 된다.',
 '긴 CoT 출력을 그대로 사용자에게 노출하면 장황해 보일 수 있다 — 실무에서는 추론 과정을 내부적으로만 생성시키고 최종 답만 파싱해 보여주는 방식을 쓰는 경우가 많다.'
],

papers:['cot','zero-shot-cot','self-consistency'],
terms:['prompt-engineering','hallucination','perplexity']
});
