WIKI.concept({
slug:'image-preprocess',

tldr:'이미지 전처리는 리사이즈·정규화·채널 순서 변환처럼 원본 이미지를 모델 입력 텐서로 바꾸는 고정된 절차이며, 학습과 추론에서 한 글자라도 다르면 조용히 성능이 무너진다.',

why:'모델 아키텍처보다 전처리 불일치가 실무 버그의 더 흔한 원인이다. 정확도가 학습 때는 잘 나왔는데 배포 후 뚝 떨어지는 사고의 상당수는 리사이즈 방식이나 정규화 값이 학습·추론 사이에서 미묘하게 달라진 것이 원인이다.',

sections:[
 {h:'리사이즈 방식', d:'대부분의 모델은 고정 크기 입력(예: 640×640)을 요구하는데, 원본 이미지 비율은 제각각이다. stretch(단순 리사이즈)는 가로세로 비율을 무시하고 강제로 목표 크기에 맞춰 늘리거나 줄인다 — 구현이 간단하지만 물체가 눌리거나 늘어나 형태가 왜곡되고, 이 왜곡에 맞춰 학습하지 않은 모델은 성능이 떨어진다. letterbox는 원본 비율을 유지한 채 리사이즈하고 남는 공간을 회색 등 고정값으로 패딩한다 — [YOLO](#/p/yolov3) 계열 검출기가 흔히 쓰는 방식으로, 물체 형태는 보존되지만 패딩 영역만큼 실효 해상도를 낭비하고, 좌표를 원본 이미지로 되돌릴 때 패딩·스케일을 정확히 역산해야 한다. 학습 때 letterbox를 썼는데 추론에서 stretch를 쓰면(또는 그 반대) 박스 좌표가 체계적으로 어긋난다.'},
 {h:'정규화 상수의 근거', d:'픽셀 값(보통 0~255)을 그대로 신경망에 넣으면 초기 층의 활성값 스케일이 커 학습이 불안정하다. 그래서 채널별로 평균을 빼고 표준편차로 나누는 정규화를 거친다. ImageNet 사전학습 모델들은 관행적으로 RGB 평균 (0.485, 0.456, 0.406), 표준편차 (0.229, 0.224, 0.225) (0~1 스케일 기준)를 쓰는데, 이는 ImageNet 학습 데이터 전체에서 실측한 채널별 통계다. 이 값 자체에 특별한 이론적 근거는 없고, "이 통계로 사전학습된 백본을 가져다 쓸 거면 입력도 같은 통계로 맞춰야 그 백본이 기대하는 분포가 유지된다"는 것이 핵심이다. 자기 데이터로 처음부터 학습한다면 ImageNet 값을 그대로 쓸 이유가 없고, 자체 데이터의 평균·표준편차를 다시 재는 것이 원칙적으로 맞다.'},
 {h:'채널 순서와 스케일', d:'OpenCV는 이미지를 BGR 순서로 읽고 PIL·대부분의 딥러닝 프레임워크는 RGB를 기대한다. 이 순서를 안 바꾸고 그대로 텐서로 만들면 모델은 빨강과 파랑이 뒤바뀐 이미지를 입력받는 셈이라 성능이 크게 떨어지는데, 완전히 망가지진 않아 (색이 반전된 채로도 어느 정도 특징이 통계적으로 비슷해서) 원인 파악이 오래 걸리는 경우가 많다. 마찬가지로 0~255 정수 범위를 0~1로 나눴는지 안 나눴는지, HWC(높이·너비·채널) 순서인지 CHW 순서인지도 프레임워크마다 관례가 달라 흔한 실수 지점이다.'},
 {h:'학습-추론 불일치', d:'학습 파이프라인(데이터 증강 포함)과 배포용 추론 파이프라인은 보통 서로 다른 코드로 짜여 있다. 학습 쪽 리사이즈·정규화 로직을 추론 서버에 그대로 옮기지 않으면, 모델 자체는 멀쩡한데 입력 분포가 학습 때와 달라 정확도가 떨어지는 현상이 생긴다. 이런 버그는 에러를 던지지 않고 그냥 결과가 나빠지기만 해서 찾기 어렵다. 실무에서는 전처리 함수를 학습·추론이 공유하는 단일 모듈로 만들거나, [ONNX](#/c/deploy-onnx) 변환 시 전처리까지 그래프에 포함시켜 이 불일치 가능성 자체를 없애는 방법을 쓴다.'}
],

diagram:{type:'compare', cap:'리사이즈 방식에 따라 같은 원본이 다른 입력이 된다.',
 left:{t:'stretch', items:['비율 무시, 강제 리사이즈','물체 형태 왜곡','구현 단순']},
 right:{t:'letterbox', items:['비율 유지, 여백 패딩','형태 보존, 해상도 손실','좌표 역산 필요'], acc:true}
},

confuse:[
 {a:'letterbox', b:'stretch(단순 resize)', d:'letterbox는 비율을 지키고 남는 공간을 패딩해 형태 왜곡이 없지만 좌표 역변환이 필요하고, stretch는 구현이 단순한 대신 물체 형태가 눌려 학습·추론 방식이 다르면 성능이 크게 떨어질 수 있다.'},
 {a:'정규화(normalization, 이미지)', b:'정칙화(regularization)', d:'이미지 전처리의 정규화는 픽셀 값 분포를 평균 0·표준편차 1 근방으로 맞추는 절차이고, [정칙화](#/c/regularization)는 과적합을 막기 위해 손실 함수나 학습 과정에 제약을 거는 것이다. 한국어로 둘 다 "정규화"로 번역되는 경우가 있어 원어(normalization vs regularization)를 확인해야 한다.'},
 {a:'RGB', b:'BGR', d:'둘 다 같은 세 채널을 담지만 저장 순서가 반대다. OpenCV(BGR)로 읽은 이미지를 RGB를 기대하는 모델에 그대로 넣으면 에러 없이 색이 뒤바뀐 채 추론이 진행돼 원인 파악이 어렵다.'}
],

pitfalls:[
 '학습에서 letterbox를 쓰고 추론 서버에서 단순 resize를 쓰는 식의 불일치는 에러 없이 정확도만 조용히 떨어뜨려 가장 찾기 어려운 버그 중 하나다.',
 'ImageNet 평균·표준편차를 "표준값"이라 여기고 전혀 다른 도메인 데이터(의료 영상, 위성 사진 등)에 그대로 쓰면 사전학습 백본의 이점이 줄어들 수 있다.',
 'BGR/RGB 순서 실수는 모델이 완전히 망가지지 않고 성능만 애매하게 낮아지는 경우가 많아, "학습이 덜 됐나?"로 오진하기 쉽다.',
 '정규화 상수를 하드코딩해두고 전처리 라이브러리 버전이 바뀌면서 기본 동작(예: 자동 스케일링)이 달라진 것을 놓치는 경우가 있다 — 버전 고정과 출력값 샘플 검증이 필요하다.'
],

papers:['alexnet','vit'],
terms:['color-space','augmentation','deploy-onnx']
});
