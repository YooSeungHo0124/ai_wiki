WIKI.concept({
slug:'sampling',

tldr:'다음 토큰의 확률분포에서 매번 가장 높은 값을 고르는 대신 그 분포를 따라 확률적으로 하나를 뽑는 디코딩 방식.',

why:'언어모델은 다음 토큰에 대해 "정답 하나"가 아니라 어휘 전체에 걸친 확률분포를 뱉는다. 그 분포에서 토큰을 어떻게 고르느냐(디코딩 전략)가 같은 모델로도 매번 똑같은 지루한 문장을 낼지, 다채롭지만 가끔 이상한 문장을 낼지를 가른다. [temperature](#/c/temperature)·[top-k·top-p](#/c/top-k-top-p) 는 전부 이 선택 방식을 조절하는 손잡이다.',

sections:[
 {h:'출력은 확률분포다', d:'모델의 마지막 층은 어휘 크기만큼의 실수 점수(logit)를 내고, 이를 softmax 로 정규화하면 "다음 토큰이 무엇일지"에 대한 확률분포 $p(x_t\\mid x_{<t})$ 가 된다. 여기서 실제로 어떤 토큰을 고를지 결정하는 절차가 디코딩(decoding)이고, 샘플링은 그 디코딩 방법 중 하나다.'},
 {h:'탐욕적 디코딩 vs 샘플링', d:'탐욕적 디코딩(greedy decoding)은 매 스텝에서 확률이 가장 높은 토큰을 무조건 고른다 — 같은 입력이면 항상 같은 출력(결정론적)이 나온다. 샘플링은 그 확률에 비례해 무작위로 하나를 뽑는다 — 같은 입력이라도 호출마다 다른 출력이 나올 수 있다(확률적). 탐욕은 재현 가능성이 필요한 분류·태깅류 작업에, 샘플링은 다양성이 필요한 대화·창작에 어울린다는 게 실무 감각이다.'},
 {h:'왜 최대우도는 지루한가', d:'[뉴클리어스 샘플링](#/p/nucleus-sampling) 논문은 탐욕적 디코딩이나 빔 서치처럼 확률을 최대화하는 디코딩이 사람 글보다 반복적이고 밋밋한 텍스트("neural text degeneration")를 만든다는 것을 실증했다. 사람이 쓰는 문장은 매 시점에서 확률이 가장 높은 단어만 고르지 않는다 — 오히려 사람의 실제 텍스트를 모델에 넣고 각 단어의 순위를 재 보면 최고 확률 단어가 아닌 경우가 흔하다. 확률을 맹목적으로 최대화하는 디코딩은 이 분포와 어긋나서 "정답이지만 단조로운" 글을 만든다는 것이 이 논문의 핵심 관찰이다.'},
 {h:'실무에서', d:'대부분의 LLM API 는 기본값이 순수 탐욕도 순수 무작위 샘플링도 아니고, [temperature](#/c/temperature)와 [top-p](#/c/top-k-top-p)로 다듬은 샘플링이다. 재현 가능한 결과가 필요하면 temperature 를 0에 가깝게 낮춰 사실상 탐욕적으로 만들고(seed 를 지원하는 API 라면 seed 도 고정), 다양한 초안이 필요하면 temperature 를 올리고 같은 프롬프트를 여러 번 호출한다.'}
],

diagram:{type:'compare', cap:'다음 토큰을 고르는 두 가지 방식.',
 left:{t:'탐욕적 디코딩', items:['매번 최고 확률 토큰 선택','결정론적 — 항상 같은 출력','반복적·밋밋해지기 쉬움']},
 right:{t:'샘플링', items:['확률에 비례해 무작위 선택','확률적 — 호출마다 다를 수 있음','다양하지만 일관성 낮아질 위험']}},

confuse:[
 {a:'샘플링', b:'[temperature](#/c/temperature)', d:'샘플링은 "분포를 따라 무작위로 뽑는다"는 디코딩 방식 자체이고, temperature 는 그 분포의 모양(얼마나 뾰족한지)을 바꾸는 파라미터다. temperature=0 이면 샘플링을 켜 놔도 사실상 탐욕적 디코딩과 같아진다.'},
 {a:'샘플링', b:'[Top-k·Top-p](#/c/top-k-top-p)', d:'샘플링은 확률분포에서 뽑는 행위 자체이고, top-k·top-p 는 그 뽑는 대상을 어휘 전체가 아니라 일부 후보로 미리 좁히는 필터다. 필터링 후 남은 후보 안에서 다시 확률에 비례해 샘플링한다.'}
],

pitfalls:[
 '"확률이 가장 높은 문장을 만드는 것"이 곧 "가장 사람다운 문장을 만드는 것"이 아니다 — [뉴클리어스 샘플링](#/p/nucleus-sampling)이 이 둘의 괴리를 실증한 지점이다.',
 'temperature=0 요청이 API 문서상 "결정론적"이라고 적혀 있어도, 부동소수점 연산 순서나 배치 구성에 따라 완전히 똑같은 출력이 보장되지 않는 경우가 있다.',
 '샘플링을 켜 둔 채로 평가·회귀 테스트를 돌리면 실패 원인이 코드 변경 때문인지 무작위성 때문인지 구분이 안 된다 — 재현성이 필요하면 temperature 를 낮추고 seed 를 고정해야 한다.'
],

papers:['nucleus-sampling','gpt2'],
terms:['temperature','top-k-top-p','beam-search']
});
