WIKI.concept({
slug:'speculative',

tldr:'작고 빠른 초안 모델이 여러 토큰을 미리 생성하고 크고 느린 모델이 그것들을 한 번의 순전파로 검증·확정해, 최종 출력 분포는 그대로 두면서 생성 속도만 높이는 디코딩 기법이다.',

why:'같은 모델을 그대로 쓰면서 지연 시간만 줄일 수 있는 몇 안 되는 방법이라 서빙 비용에 직결된다. "출력이 바뀌지 않는다"는 보장이 왜 성립하는지 이해하지 못하면 왜 이 기법이 품질 저하 없는 순수한 속도 최적화로 취급되는지 설명할 수 없다.',

sections:[
 {h:'구조', d:'작은 초안(draft) 모델이 다음 몇 개 토큰(예: 4~8개)을 순차적으로 빠르게 예측해 하나의 후보 시퀀스를 만든다. 그다음 원래 쓰려던 큰(target) 모델이 이 후보 시퀀스 전체를 한 번의 순전파로 병렬 검증한다 — 자기회귀 생성은 토큰마다 순전파가 필요하지만, 이미 만들어진 시퀀스를 채점하는 것은 한 번의 순전파로 모든 위치를 동시에 계산할 수 있기 때문이다. 큰 모델의 확률과 비교해 초안 토큰을 앞에서부터 받아들이다가 처음 불일치하는 지점에서 멈추고, 그 지점은 큰 모델 자신의 확률로 다시 뽑는다.'},
 {h:'분포가 안 바뀌는 이유', d:'핵심은 초안 모델의 토큰을 그냥 채택하는 것이 아니라 거부 표본추출(rejection sampling)로 받아들인다는 점이다. 초안 모델의 확률 $q$ 와 큰 모델의 확률 $p$ 를 비교해 $p(x)/q(x)$ 비율로 accept/reject 를 결정하고, 거부된 위치에서는 $p$ 와 $q$ 의 차이만큼 보정된 분포에서 다시 샘플링한다. 이 절차를 거치면 최종적으로 채택되는 토큰의 분포는 큰 모델 혼자 생성했을 때의 분포 $p$ 와 수학적으로 동일하다 — 초안 모델은 후보를 "제안"만 할 뿐 최종 확률을 결정하지 않는다.'},
 {h:'왜 빨라지는가', d:'자기회귀 생성의 병목은 연산량이 아니라 토큰마다 큰 모델 가중치를 메모리에서 읽어와야 하는 메모리 대역폭이다. 검증 단계는 여러 토큰을 한 번의 순전파로 동시에 채점하므로, 같은 가중치를 한 번만 읽고 여러 토큰 몫의 계산을 한다 — 초안이 자주 맞을수록(초안 모델과 큰 모델의 분포가 비슷할수록) 한 번의 큰 모델 순전파로 여러 토큰이 한꺼번에 확정되어 평균 지연 시간이 줄어든다.'},
 {h:'실무에서', d:'초안 모델은 같은 계열의 더 작은 모델을 쓰는 것이 일반적이며, 초안과 타깃의 분포가 비슷할수록 수락률이 높아 가속 효과가 크다. [Medusa](#/p/medusa) 는 별도 초안 모델 없이 타깃 모델 자체에 여러 개의 예측 헤드를 달아 병렬로 후보를 만들고, [EAGLE](#/p/eagle) 은 타깃 모델의 은닉 상태를 초안 생성에 활용해 초안 품질(수락률)을 더 끌어올린다. 실제 가속 배율은 태스크 난이도에 따라 크게 갈린다 — 예측 가능한 텍스트(코드 자동완성 등)는 초안이 잘 맞아 배율이 크고, 창의적 생성은 초안 적중률이 낮아 이득이 작다.'},
 {h:'서빙에서의 위치', d:'추측 디코딩은 배치 크기가 작은 저지연 서빙(단일 사용자 대화형 응답)에서 이득이 가장 크다 — 이때 병목이 연산량이 아니라 가중치를 읽어오는 메모리 대역폭이기 때문이다. 반대로 이미 여러 요청을 한 배치로 묶어 GPU 연산을 꽉 채운 대규모 서빙 환경에서는 검증 단계의 추가 연산이 배치 처리량 자체를 깎아먹을 수 있어, 무조건 도입하기보다 실제 트래픽 패턴에서 처리량과 지연 시간을 함께 측정해야 한다.'}
],

math:[
 {tex:'\\alpha(x)=\\min\\!\\Big(1,\\dfrac{p(x)}{q(x)}\\Big)', expr:'수락 확률', d:'$q$ 는 초안 모델, $p$ 는 큰(타깃) 모델의 확률. 초안 모델이 과대평가한 토큰($q>p$)일수록 거부 확률이 커지고, 거부된 자리는 $p$ 와 $q$ 의 차이에서 다시 뽑아 전체 분포가 $p$ 하나만 썼을 때와 같아지도록 보정한다.'}
],

diagram:{type:'flow', cap:'추측 디코딩 한 라운드.',
 steps:['초안 모델이 토큰 N개 순차 생성','타깃 모델이 N개를 한 번에 병렬 검증','앞에서부터 일치하는 토큰 채택','첫 불일치 지점에서 타깃 확률로 재샘플링']},

code:{lang:'text', d:'한 라운드의 진행을 문자로 도식화한 예. N=4로 초안을 만들고 타깃이 검증한다.', src:
'초안 모델 제안: [A, B, C, D]\n'+
'타깃 모델 검증(1회 병렬 순전파):\n'+
'  A: 확률 비교 -> 채택\n'+
'  B: 확률 비교 -> 채택\n'+
'  C: 확률 비교 -> 거부 (타깃 확률로 재샘플링 -> C\')\n'+
'  D: C에서 이미 멈췄으므로 버려짐, 다음 라운드는 C\'부터\n'+
'결과: 이번 라운드에 A, B, C\' 세 토큰이 확정됨 (자기회귀 3스텝 분을 1회 검증으로)'},

confuse:[
 {a:'추측 디코딩', b:'양자화·distillation', d:'추측 디코딩은 최종 출력 분포를 수학적으로 그대로 유지하면서 속도만 올리는 무손실(lossless) 기법이다. 양자화나 [지식 증류](#/c/distillation-c) 은 모델 자체(가중치·크기)를 바꿔 정확도와 속도를 맞바꾸는 유손실 기법이라는 점에서 근본적으로 다르다.'},
 {a:'초안 모델의 채택률', b:'초안 모델의 정확도', d:'초안 모델이 "정답"을 맞히는 게 목표가 아니라 타깃 모델과 확률분포가 얼마나 비슷한가가 중요하다. 초안이 타깃과 다른 토큰을 제안해도 거부-재샘플링 과정에서 항상 타깃 분포로 보정되므로, 채택률이 낮아도 출력 품질 자체는 떨어지지 않고 속도만 손해를 본다.'}
],

pitfalls:[
 '추측 디코딩이 출력을 "근사"한다고 오해하기 쉽지만, 거부 표본추출을 정확히 구현하면 타깃 모델 단독 생성과 분포가 동일한 무손실 기법이다.',
 '초안 모델이 타깃 모델과 너무 다른 스타일로 학습됐으면 수락률이 낮아 오히려 초안 생성 + 매 라운드 검증 오버헤드 때문에 순수 자기회귀보다 느려질 수 있다.',
 '가속 배율은 하드웨어의 메모리 대역폭 대비 연산 여유(배치가 작을수록 유리)에 크게 의존한다 — 이미 배치가 큰 서빙 환경에서는 이득이 줄어든다.',
 '탐욕적(greedy) 디코딩에서는 검증 절차가 단순히 "초안 토큰이 타깃의 argmax 와 같은가"로 줄어드는데, 이를 확률적 샘플링에도 그대로 적용하면 분포가 틀어진다 — 샘플링 모드에서는 반드시 거부 표본추출 절차를 써야 무손실 보장이 성립한다.',
 '초안 모델을 키우면 채택률은 오르지만 초안 생성 자체의 비용도 커지므로, 초안 모델 크기는 무작정 키운다고 항상 더 빨라지지 않는다 — 실측으로 최적점을 찾아야 한다.'
],

papers:['speculative','medusa','eagle'],
terms:['perplexity','hallucination']
});
