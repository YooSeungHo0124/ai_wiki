WIKI.concept({
slug:'calibration',

tldr:'모델이 90% 확신한다고 말한 예측들이 실제로도 약 90% 맞는지를 보정이라 하며, ECE 는 이 어긋남의 크기를 숫자로 잰다.',

why:'정확도가 높은 모델도 확률 출력은 엉망일 수 있다. 의료 진단이나 자율주행처럼 "얼마나 확신하는가"가 실제 의사결정에 쓰이는 상황에서는 정확도만으로는 부족하고, 신뢰도 수치 자체를 믿을 수 있는지 따로 확인해야 한다.',

sections:[
 {h:'보정이 뜻하는 것', d:'분류기가 어떤 샘플에 대해 "80% 확률로 양성"이라고 말했다면, 그렇게 80% 근방의 확신을 준 샘플들을 모아봤을 때 실제로 그중 약 80% 가 양성이어야 한다. 이것이 맞으면 잘 보정된(calibrated) 모델이고, 신뢰도가 항상 실제 정확도보다 높게 나오면 과신(overconfident), 낮게 나오면 과소신(underconfident)이라 부른다.'},
 {h:'신뢰도 다이어그램으로 시각화', d:'예측 신뢰도를 여러 구간(bin, 예: 0~10%, 10~20%, ...)으로 나누고, 각 구간에 속한 샘플들의 평균 신뢰도와 실제 정확도를 막대로 비교한다. 완벽히 보정된 모델은 대각선(신뢰도=정확도) 위에 막대가 정확히 얹히고, 막대가 대각선 아래에 있으면 그 구간에서 모델이 과신하고 있다는 뜻이다.'},
 {h:'정확도와 보정은 별개다', d:'정확도가 같은 두 모델도 보정 상태는 전혀 다를 수 있다. [보정](#/p/temperature-scaling) 연구가 보인 것처럼, 현대의 깊고 큰 신경망(특히 배치 정규화·큰 용량을 쓰는 모델)은 과거의 얕은 모델보다 정확도는 높아졌지만 오히려 체계적으로 더 과신하는 경향이 있다 — 모델 용량이 커지고 학습이 길어질수록 softmax 출력이 정답 클래스로 지나치게 뾰족하게 쏠리기 때문이다. 정확도 개선이 자동으로 보정 개선을 의미하지 않는다는 것이 핵심이다.'},
 {h:'보정을 고치는 법과 앙상블', d:'가장 간단한 교정법은 temperature scaling — 검증셋에서 스칼라 $T$ 하나만 학습해 softmax 입력(logit)을 나누는 것으로, 클래스 순위(정확도)는 그대로 두면서 신뢰도 곡선만 눌러 편다. [Deep Ensembles](#/p/deep-ensembles) 처럼 여러 모델의 예측을 평균 내는 방법도 개별 모델의 과신을 완화하는 효과가 있다고 보고된다. 어느 쪽이든 보정은 학습이 끝난 뒤 검증셋으로 사후에 점검·교정해야 하는 별도 단계라는 인식이 필요하다.'}
],

math:[
 {tex:'\\text{ECE}=\\sum_{m=1}^M \\dfrac{|B_m|}{n}\\big|\\text{acc}(B_m)-\\text{conf}(B_m)\\big|', expr:'Expected Calibration Error', d:'신뢰도를 $M$ 개 구간 $B_m$ 으로 나눠, 구간별 실제 정확도 $\\text{acc}(B_m)$ 과 평균 신뢰도 $\\text{conf}(B_m)$ 의 차이를 구간 크기 $|B_m|/n$ 으로 가중 평균한다. 0 이면 완벽한 보정, 값이 클수록 신뢰도와 실제 정확도가 어긋난다는 뜻.'},
 {tex:'\\sigma(z/T)', expr:'temperature scaling', d:'학습된 logit $z$ 를 스칼라 $T>0$ 으로 나눈 뒤 softmax($\\sigma$)를 취한다. $T>1$ 이면 분포가 평평해져 과신이 줄고, $T$ 는 argmax(예측 클래스)를 바꾸지 않으므로 정확도는 그대로 유지된다.'}
],

confuse:[
 {a:'정확도(accuracy)', b:'보정(calibration)', d:'정확도는 예측 클래스가 맞았는지만 본다. 보정은 맞았는지가 아니라 모델이 내세운 확신 수준이 실제 적중률과 일치하는지를 본다 — 정확도 95% 모델이 항상 99% 확신을 내뱉는다면 정확도는 좋아도 심하게 과신된 것이다.'},
 {a:'ECE', b:'AUROC/AUPR', d:'[AUROC](#/c/auroc)는 점수의 순위(양성이 음성보다 높은 점수를 받는가)만 보고 절대적인 확률값의 정확성은 신경 쓰지 않는다. 같은 순위를 유지한 채로도 보정은 완전히 다를 수 있어 두 지표는 서로 대체할 수 없다.'},
 {a:'과신(overconfident)', b:'과소신(underconfident)', d:'과신은 신뢰도가 실제 정확도보다 체계적으로 높은 것(대각선 아래), 과소신은 반대(대각선 위)다. 현대 신경망은 대체로 과신 쪽으로 치우친다고 보고된다.'}
],

pitfalls:[
 '정확도가 높으니 확률 출력도 믿을 만하다고 가정하면 안 된다 — 둘은 독립적으로 측정하고 개선해야 하는 별개의 성질이다.',
 'ECE 는 구간 수·구간 나누는 방식(균등 폭 vs 균등 표본 수)에 따라 값이 달라질 수 있어, 서로 다른 논문의 ECE 숫자를 그대로 비교하면 위험하다.',
 'temperature scaling 은 검증셋 분포에서 학습한 보정이라, 배포 후 입력 분포가 바뀌면(분포 이동) 다시 어긋날 수 있다.'
],

papers:['temperature-scaling','deep-ensembles'],
terms:['auroc','precision-recall','data-leakage']
});
