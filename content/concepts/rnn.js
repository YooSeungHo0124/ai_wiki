WIKI.concept({
slug:'rnn',

tldr:'매 타임스텝마다 같은 가중치를 재사용하며 이전 상태를 다음 스텝으로 넘기는 신경망으로, 가변 길이 시퀀스를 다룰 수 있지만 긴 의존성을 학습하기 어렵다.',

why:'왜 LSTM·GRU가 필요했는지, 왜 Transformer가 RNN을 대체했는지를 이해하려면 먼저 순수 RNN이 시퀀스를 어떻게 처리하고 어디서 무너지는지부터 알아야 한다. 시계열·스트리밍처럼 지금도 RNN류가 쓰이는 자리를 판단하는 데도 필요하다.',

sections:[
 {h:'기본 발상', d:'각 타임스텝 $t$에서 입력 $x_t$와 이전 은닉 상태 $h_{t-1}$을 같은 가중치 행렬로 조합해 새 은닉 상태 $h_t$를 만든다. 같은 가중치를 모든 타임스텝에 재사용하므로(파라미터 공유) 시퀀스 길이가 바뀌어도 모델 크기는 그대로다.'},
 {h:'시간 전개와 BPTT', d:'학습할 때는 RNN을 타임스텝 수만큼 펼쳐(unroll) 마치 아주 깊은 순전파 신경망처럼 취급하고, 역전파를 시간 축으로 거슬러 올라가며 적용한다 — 이를 BPTT(Backpropagation Through Time)라 부른다. 시퀀스가 길수록 펼쳐진 깊이도 깊어진다.'},
 {h:'왜 긴 의존성에 실패하는가', d:'BPTT는 같은 가중치 행렬을 시퀀스 길이만큼 반복해서 곱하는 것과 같다. 이 행렬의 고유값이 1보다 작으면 그래디언트가 지수적으로 줄어들어 먼 과거의 정보가 거의 반영되지 못하고, 1보다 크면 반대로 지수적으로 커져 학습이 발산한다. 이것이 [경사 소실·폭주](#/c/gradient-problem) 문제이며, 순수 RNN이 수십 스텝 이상 떨어진 의존성을 사실상 학습하지 못하는 근본 원인이다.'},
 {h:'변형들', d:'단방향 RNN은 과거만 보지만, 양방향(bidirectional) RNN은 순방향·역방향을 각각 돌려 이어 붙여 양쪽 문맥을 모두 활용한다. 여러 RNN 층을 쌓은 stacked RNN은 CNN처럼 계층적 표현을 만든다. 하지만 경사 소실 문제 자체는 이런 구조 변형만으로는 해결되지 않고, 게이트를 도입한 [LSTM](#/c/lstm-c)이 실질적인 해법이 됐다.'}
],

math:[
 {tex:'h_t = \\tanh(W_{xh}x_t + W_{hh}h_{t-1} + b_h)',
  expr:'기본 RNN 상태 전이', d:'입력 가중치 $W_{xh}$와 상태 가중치 $W_{hh}$가 모든 타임스텝에서 공유된다. 출력이 필요하면 $y_t = W_{hy}h_t$처럼 별도 층을 얹는다.'},
 {tex:'\\dfrac{\\partial h_t}{\\partial h_0} = \\prod_{i=1}^{t} \\dfrac{\\partial h_i}{\\partial h_{i-1}} \\approx \\prod_{i=1}^{t} W_{hh}^{\\top}\\,\\text{diag}(1-\\tanh^2)',
  expr:'BPTT의 연쇄 곱', d:'그래디언트가 같은 행렬 $W_{hh}$를 $t$번 곱한 형태가 되어, 그 스펙트럴 노름이 1보다 작으면 지수적으로 소실하고 크면 폭주한다. 이것이 시퀀스가 길수록 초기 타임스텝의 정보가 학습에 반영되기 어려운 이유다.'}
],

diagram:{type:'loop', cap:'같은 가중치를 매 타임스텝 재사용하며 은닉 상태를 다음 스텝으로 넘긴다.', center:'타임스텝마다 반복',
 nodes:[
  {t:'x_t 입력', s:'현재 타임스텝 입력'},
  {t:'h_t 갱신', s:'h_{t-1}과 x_t를 결합'},
  {t:'y_t 출력', s:'필요시 출력층 통과'}
 ]},

confuse:[
 {a:'RNN', b:'[LSTM](#/c/lstm-c)', d:'RNN은 은닉 상태 하나만 단순 비선형 결합으로 갱신해 경사 소실에 취약하고, LSTM은 별도의 셀 상태와 게이트로 정보를 선택적으로 보존·차단해 긴 의존성을 훨씬 잘 학습한다. LSTM은 RNN의 한 종류이지 별개의 패러다임이 아니다.'},
 {a:'RNN', b:'1D CNN', d:'RNN은 순서를 상태 전이로 처리해 이론상 임의 길이의 과거를 볼 수 있지만 순차 계산이라 병렬화가 어렵고, 1D CNN은 고정된 커널 크기만큼만 지역적으로 보지만 병렬로 빠르게 계산된다. 두 접근 모두 [수용 영역](#/c/receptive-field) 개념으로 "얼마나 먼 과거를 보는가"를 설명할 수 있다.'},
 {a:'시간 전개(unroll)', b:'가중치 공유', d:'시간 전개는 학습·추론 시 RNN을 타임스텝 수만큼 펼쳐 그리는 계산 그래프 표현이고, 가중치 공유는 그 펼쳐진 각 스텝이 서로 다른 파라미터가 아니라 동일한 파라미터를 쓴다는 설계 원칙이다. 전자는 표현 방식, 후자는 모델의 성질이다.'}
],

pitfalls:[
 '"RNN은 무한히 긴 문맥을 기억한다"는 이론과 실제가 다르다 — 이론적으로는 은닉 상태가 모든 과거 정보를 압축해 가진다고 볼 수 있지만, 경사 소실 때문에 실제 학습되는 것은 최근 수십 스텝 정도다.',
 '순수 RNN이 요즘 거의 쓰이지 않는다고 해서 "순환 구조 자체가 쓸모없다"는 뜻은 아니다 — 게이트가 달린 LSTM/GRU는 여전히 소형 모델·스트리밍·시계열 예측에서 실용적으로 쓰인다.',
 'BPTT를 전체 시퀀스에 대해 그대로 하면 메모리·연산이 시퀀스 길이에 비례해 커지므로, 실무에서는 일정 구간마다 잘라 역전파하는 truncated BPTT를 쓰는 경우가 많다 — 이때는 그 구간을 넘는 의존성을 애초에 학습할 기회가 없다.'
],

papers:['lstm','seq2seq'],
terms:['lstm-c','gradient-problem','receptive-field']
});
