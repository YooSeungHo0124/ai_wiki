WIKI.concept({
slug:'deploy-onnx',

tldr:'PyTorch로 학습한 모델을 ONNX라는 프레임워크 중립 그래프 형식으로 내보낸 뒤 TensorRT 같은 추론 엔진으로 최적화·실행하는 것이 실무 배포의 표준 경로다.',

why:'학습 코드 그대로 서비스에 올리면 파이썬·PyTorch 런타임 오버헤드 때문에 느리고 무겁다. ONNX·TensorRT 변환은 그 병목을 없애 실제 지연시간(latency)을 몇 배 줄이지만, 변환 과정 자체가 새로운 버그의 원천이 되기 때문에 흐름과 함정을 알아야 한다.',

sections:[
 {h:'배포 파이프라인', d:'표준 배포 흐름은 세 단계다. 먼저 학습이 끝난 PyTorch 모델을 `torch.onnx.export`로 ONNX(Open Neural Network Exchange) 형식으로 내보낸다 — 이는 연산자와 가중치를 프레임워크에 얽매이지 않는 그래프로 직렬화한 것이다. 다음으로 이 ONNX 그래프를 TensorRT 같은 추론 엔진에 넣으면, 엔진이 실행될 GPU에 맞춰 연산을 융합(fusion)하고 정밀도를 낮추는 등 하드웨어 특화 최적화를 거쳐 훨씬 빠른 실행 엔진(TensorRT의 경우 `.engine` 파일)을 만든다. 마지막으로 이 최적화된 엔진을 서비스 코드에서 로드해 추론에 쓴다. ONNX Runtime처럼 ONNX를 직접 실행하는 런타임도 있어, TensorRT까지 안 가고 ONNX 단계에서 배포를 끝내는 경우도 흔하다.'},
 {h:'동적 축 설정', d:'학습 때는 배치 크기가 고정이지만 서비스에서는 요청마다 배치 크기나 이미지 크기가 달라질 수 있다. ONNX export 시 어떤 축(보통 배치 차원, 때로는 높이·너비)을 고정값이 아니라 "가변"으로 표시하지 않으면, 내보낸 그래프는 그 학습 시점의 크기로만 작동하는 하드코딩된 그래프가 된다. `dynamic_axes` 인자로 이를 명시해야 하는데, 이걸 빠뜨리고 export하면 추론 서버에서 배치 크기가 다른 요청이 들어올 때마다 에러가 나거나, 매번 새로 컴파일해야 해서 속도 이점이 사라진다.'},
 {h:'양자화 배포', d:'TensorRT는 FP32(원본 정밀도) 대신 FP16이나 INT8로 낮춰 실행하는 [양자화](#/c/quantization)를 지원해 속도와 메모리를 크게 아낀다. FP16은 보통 정확도 손실이 거의 없이 적용 가능하지만, INT8은 활성값의 범위를 미리 추정하는 보정(calibration) 데이터셋이 필요하고 잘못 보정하면 정확도가 눈에 띄게 떨어질 수 있다. 실무에서는 FP16으로 먼저 배포하고, 더 공격적인 속도가 필요할 때만 INT8 보정 파이프라인을 추가로 구축하는 순서를 흔히 밟는다.'},
 {h:'변환 후 수치 오차', d:'ONNX·TensorRT 변환은 수학적으로 완전히 동일한 연산이 아니다. 연산자 융합, 정밀도 변경, 프레임워크 간 구현 차이(예: 보간·패딩의 경계 처리 방식) 때문에 변환 전후 출력값이 소수점 몇 자리에서 미세하게 달라지는 것이 정상이다. 대부분의 과제에서는 무시할 만한 차이지만, 분류 경계값 근처의 샘플이나 회귀 출력을 그대로 쓰는 과제에서는 이 미세한 차이가 최종 결정을 뒤집을 수 있다. 그래서 배포 전에는 PyTorch 원본과 변환된 엔진의 출력을 같은 입력에 대해 비교해 최대 오차가 허용 범위 안에 있는지 반드시 검증해야 한다 — "변환됐으니 끝"이 아니라 "변환됐으니 검증"이 순서다.'}
],

diagram:{type:'flow', cap:'PyTorch 모델이 배포 가능한 최적화 엔진이 되기까지.',
 nodes:[
  {t:'PyTorch 모델', s:'학습 완료'},
  {t:'ONNX export', s:'그래프+가중치 직렬화'},
  {t:'TensorRT 빌드', s:'연산 융합, 정밀도 선택'},
  {t:'추론 엔진', s:'서비스에서 로드'}
 ]},

confuse:[
 {a:'ONNX', b:'TensorRT', d:'ONNX는 프레임워크 중립적인 모델 표현 형식(그래프+가중치)이고, TensorRT는 NVIDIA GPU에 특화된 추론 최적화·실행 엔진이다. ONNX는 여러 런타임에서 쓸 수 있는 "중간 언어"이고, TensorRT는 그 중 하나를 골라 GPU에 맞춰 컴파일하는 도구다.'},
 {a:'FP16 변환', b:'INT8 [양자화](#/c/quantization)', d:'FP16은 대개 보정 없이 바로 적용해도 정확도 손실이 미미하지만, INT8은 활성값 범위를 추정할 보정 데이터셋이 필요하고 잘못하면 정확도가 눈에 띄게 떨어진다. "양자화"라고 뭉뚱그리기보다 어느 정밀도인지 구분해야 한다.'},
 {a:'export 성공', b:'수치 일치 검증', d:'ONNX export가 에러 없이 끝났다는 것은 그래프 변환이 구조적으로 성공했다는 뜻일 뿐, 출력값이 원본과 수치적으로 같다는 보장은 아니다. 별도로 입력 샘플에 대한 출력 오차를 비교해야 한다.'}
],

pitfalls:[
 '동적 축을 지정하지 않고 export하면 학습 시점 배치 크기로 고정돼, 배포 후 다른 배치 크기 요청에서 에러가 나거나 매번 재컴파일이 일어난다.',
 '변환 후 출력이 원본과 완벽히 똑같을 거라 기대하고 수치 검증을 생략하면, 나중에야 미세한 차이가 실제 결정을 뒤집는 사례를 뒤늦게 발견하게 된다.',
 'PyTorch에서는 지원하지만 ONNX 연산자 집합(opset)에는 아직 없는 연산(커스텀 레이어, 일부 동적 제어 흐름)을 쓰면 export 자체가 실패하거나 우회 구현이 필요하다.',
 'INT8 보정 데이터셋이 실제 서비스 입력 분포를 대표하지 못하면, 보정은 성공했는데도 실제 트래픽에서 정확도가 떨어지는 경우가 생긴다.'
],

papers:['mobilenetv2','rtmpose'],
terms:['image-preprocess','quantization']
});
