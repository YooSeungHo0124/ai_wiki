WIKI.concept({
slug:'feature-store',

tldr:'학습 때 쓴 피처(입력 변수)를 서빙 때도 똑같이, 그것도 빠르게 계산해서 쓸 수 있게 관리하는 중앙 저장소로, 학습-서빙 스큐(train-serving skew)를 막는 것이 존재 이유다.',

why:'모델은 학습 때 본 피처와 서빙 때 계산되는 피처가 조금이라도 다르면 조용히 성능이 나빠진다 — 에러도 안 나고 그냥 예측이 나빠질 뿐이라 원인 찾기가 특히 어렵다. "사용자의 최근 7일 구매 횟수" 같은 피처를 학습 파이프라인과 서빙 파이프라인에서 각자 다시 구현하면, 둘의 계산 로직이 미묘하게 어긋나는 것은 시간 문제다.',

sections:[
 {h:'학습-서빙 스큐가 왜 생기는가', d:'학습 때는 보통 대량의 과거 데이터를 배치로 긁어(SQL, Spark 등) 피처를 계산하고, 서빙 때는 요청이 들어온 순간 실시간으로 최신 값을 조회해 계산한다. 같은 정의("최근 7일 구매 횟수")라도 두 파이프라인이 서로 다른 코드로 구현되면, 시간 경계 처리·결측치 처리·집계 방식이 미세하게 달라지기 쉽다. 이 어긋남이 학습-서빙 스큐이고, 피처 스토어는 계산 로직을 한 곳에 두어 이 스큐를 원천적으로 막으려는 시도다.'},
 {h:'오프라인 스토어와 온라인 스토어', d:'피처 스토어는 보통 두 개의 저장소로 나뉜다. **오프라인 스토어**는 대량의 과거 피처 값을 저장해 학습 데이터셋을 만드는 데 쓴다(처리량 우선, 지연시간은 신경 안 씀). **온라인 스토어**는 최신 피처 값만 저장해 서빙 시점에 밀리초 단위로 조회하는 데 쓴다(지연시간 우선). 같은 피처 정의(코드)로 두 스토어를 채우는 것이 핵심 — 정의는 하나, 저장소는 용도에 따라 둘로 나눈다.'},
 {h:'포인트-인-타임 정합성', d:'학습 데이터를 만들 때 "이 예측을 하던 시점에 실제로 알 수 있었던 값"만 써야 한다 — 미래 정보가 학습 데이터에 섞이면([데이터 누수](#/c/data-leakage)) 오프라인 성능은 비현실적으로 좋게 나오지만 실제 서빙에서는 그 정보를 알 수 없어 성능이 뚝 떨어진다. 피처 스토어는 각 피처 값에 타임스탬프를 붙여, 특정 과거 시점 기준으로 "그때 유효했던" 값만 정확히 뽑아주는 포인트-인-타임(point-in-time) 조회를 지원하는 것이 핵심 기능 중 하나다.'},
 {h:'피처 재사용', d:'여러 모델(추천, 이탈 예측, 사기 탐지)이 같은 사용자 피처("최근 30일 평균 구매액" 등)를 공유하는 경우가 많다. 피처 스토어가 있으면 한 팀이 정의한 피처를 다른 팀이 재계산 없이 그대로 가져다 쓸 수 있어, 조직 전체의 중복 계산 비용과 피처 정의 불일치를 줄인다 — [피처 엔지니어링](#/c/feature-engineering) 결과물을 회사 차원의 공유 자산으로 만드는 것이 목적이다.'}
],

diagram:{type:'split', cap:'하나의 피처 정의가 두 스토어로 흘러가는 구조.',
 from:{t:'피처 정의(코드 1곳)'},
 branches:[
  {t:'오프라인 스토어', s:'학습용, 대량·배치'},
  {t:'온라인 스토어', s:'서빙용, 저지연 조회'}
 ]},

confuse:[
 {a:'피처 스토어', b:'[모델 레지스트리](#/c/model-registry)', d:'피처 스토어는 모델의 **입력**(피처 값)을 버전 관리하고 학습-서빙 간 일관성을 보장하는 시스템이고, 모델 레지스트리는 모델의 **결과물**(학습된 가중치)을 버전 관리하는 시스템이다. 두 시스템 모두 "학습 때와 서빙 때가 일치해야 한다"는 같은 원칙을 다른 대상에 적용한다.'},
 {a:'학습-서빙 스큐', b:'[데이터 드리프트](#/c/drift)', d:'학습-서빙 스큐는 피처를 계산하는 **코드/로직이 달라서** 생기는 불일치이고(같은 시점이라도 발생), 데이터 드리프트는 코드는 동일해도 **시간이 지나며 입력 분포 자체가 달라져서** 생기는 불일치다. 스큐는 파이프라인 설계 문제, 드리프트는 세상이 바뀌는 문제로 원인이 다르다.'},
 {a:'오프라인 스토어', b:'[실험 추적](#/c/experiment-tracking)', d:'오프라인 스토어는 "어떤 피처 값을 썼는가"를 저장하고, 실험 추적은 "그 피처로 어떤 모델을 어떤 설정으로 학습했는가"를 저장한다. 둘 다 재현성에 기여하지만 관리하는 대상(입력 데이터 대 학습 실행)이 다르다.'}
],

pitfalls:[
 '온라인 스토어 조회가 느리면 실시간 추론 전체 지연시간에 그대로 더해진다 — 피처 스토어를 도입해도 온라인 조회 경로의 지연시간을 따로 측정하고 최적화해야 한다.',
 '포인트-인-타임 조회를 제대로 구현하지 않으면, 학습 데이터셋에 미래 시점의 피처 값이 섞여 들어가는 데이터 누수가 조용히 발생한다 — 오프라인 성능만 보고는 이 문제를 알아채기 어렵다.',
 '작은 팀이나 피처 수가 적은 초기 단계에서 피처 스토어부터 도입하면, 인프라 복잡도가 실제로 얻는 이득(재사용, 스큐 방지)보다 커질 수 있다 — 피처가 여러 모델·팀에 걸쳐 재사용되기 시작할 때 도입을 고려하는 것이 실무적이다.'
],

papers:['wide-deep'],
terms:['data-leakage','model-registry','drift','feature-engineering']
});
