WIKI.concept({
slug:'mlp',

tldr:'입력층-은닉층(들)-출력층을 완전연결(fully-connected)로 쌓고 각 층 사이에 비선형 활성 함수를 끼운 가장 기본적인 신경망 구조.',

why:'CNN·트랜스포머 안의 FFN(feed-forward network) 블록, 분류기 헤드, 강화학습의 정책망까지 — 실무에서 마주치는 거의 모든 신경망은 어딘가에 MLP를 부품으로 품고 있다. MLP가 왜 비선형 활성 함수 없이는 아무 의미가 없는지 이해하지 못하면, 활성 함수를 실수로 빼먹거나 층 수만 늘리면 표현력이 는다고 착각하기 쉽다. MLP의 동작 원리를 정확히 알아야 더 복잡한 구조를 읽을 때도 "결국 이 부분은 완전연결+비선형이구나"라고 분해해서 이해할 수 있다.',

sections:[
 {h:'왜 비선형이 필수인가', d:'선형 층을 아무리 쌓아도 $W_2(W_1x)=(W_2W_1)x$ 는 결국 하나의 선형 변환과 같다 — 층을 100개 쌓아도 1개 층짜리 [선형 회귀](#/c/linear-regression)와 표현력이 같다. 각 층 사이에 ReLU·GELU 같은 [활성 함수](#/c/activation)를 넣어야만 비로소 곡선·경계를 표현할 수 있는 비선형 함수가 된다. MLP를 "완전연결층 + 비선형"의 반복이라고 정의하는 이유가 이것이다.'},
 {h:'보편 근사와 현실', d:'은닉층이 충분히 넓은 2층 MLP는 임의의 연속 함수를 원하는 정밀도로 근사할 수 있다는 보편 근사 정리(universal approximation theorem)가 있다. 하지만 이는 존재성만 보장할 뿐, 그 폭이 데이터 차원에 지수적으로 커질 수 있고 학습으로 그 파라미터를 실제로 찾을 수 있다는 보장은 없다. 실무에서 폭 하나를 늘리기보다 층을 늘려 깊게 만드는(depth) 쪽이 같은 파라미터 수로 더 효율적인 경우가 많다는 것이 딥러닝이 "깊다"고 불리는 이유다.'},
 {h:'구조', d:'한 층의 연산은 $h=\\sigma(Wx+b)$ 로, 가중치 행렬 $W$ 와 편향 $b$ 로 선형 변환한 뒤 활성 함수 $\\sigma$ 를 적용한다. 이를 여러 층 이어 붙이면 앞 층의 출력이 다음 층의 입력이 된다. 은닉층의 활성 함수는 보통 ReLU·GELU처럼 계산이 값싸고 경사가 잘 흐르는 함수를 쓰고, 출력층의 활성 함수는 과제에 따라 다르다 — 회귀는 항등함수, 이진 분류는 시그모이드, 다중 분류는 [소프트맥스](#/c/softmax)를 쓴다.'},
 {h:'실무에서', d:'순수 MLP를 이미지·텍스트에 직접 쓰는 일은 드물다 — 픽셀·토큰의 국소·순서 구조를 무시하고 모든 입력을 동등하게 완전연결하기 때문에 파라미터가 낭비되고 [CNN](#/c/cnn)·[셀프 어텐션](#/c/self-attention)보다 데이터 효율이 떨어진다. 대신 트랜스포머 블록 안의 FFN, 분류기의 마지막 헤드, 임베딩 뒤의 프로젝션처럼 "이미 구조화된 특징을 섞는" 역할로 거의 모든 아키텍처 안에 살아있다.'},
 {h:'파라미터와 계산량', d:'완전연결층 하나의 파라미터 수는 입력·출력 차원의 곱 $d_{in}\\times d_{out}$ 에 비례한다 — 국소 연결·가중치 공유가 있는 CNN·어텐션과 달리, 입력이 커질수록 파라미터가 매우 빠르게 늘어난다. 이 때문에 고해상도 이미지를 MLP 하나로 바로 처리하려 하면 입력층 파라미터 수만으로도 감당하기 어려운 크기가 되며, 이것이 이미지에 CNN이 먼저 자리 잡은 이유 중 하나다.'}
],

math:[
 {tex:'h^{(l)}=\\sigma\\!\\left(W^{(l)}h^{(l-1)}+b^{(l)}\\right)', expr:'MLP forward pass', d:'$h^{(l)}$ 은 $l$ 번째 층의 출력, $h^{(0)}=x$ 는 입력. 각 층은 이전 층 출력을 선형 변환한 뒤 비선형 $\\sigma$ 를 씌워 다음 층에 넘긴다 — 이 반복이 MLP의 전부다.'}
],

code:{lang:'python', d:'PyTorch로 2층 MLP를 정의하는 최소 예. 활성 함수를 층 사이에 반드시 끼워야 한다는 점을 보여준다.', src:'import torch.nn as nn\n\nclass MLP(nn.Module):\n    def __init__(self, d_in, d_hidden, d_out):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(d_in, d_hidden),\n            nn.ReLU(),                 # 비선형 — 이게 없으면 선형 변환 1개와 동일\n            nn.Linear(d_hidden, d_out)\n        )\n    def forward(self, x):\n        return self.net(x)'},

diagram:{type:'flow', cap:'완전연결과 비선형이 번갈아 반복되는 구조.',
 nodes:[
  {t:'입력 x', s:'벡터'},
  {t:'선형 Wx+b', s:'완전연결'},
  {t:'활성 함수', s:'비선형'},
  {t:'출력층', s:'과제별 활성'}
 ]},

confuse:[
 {a:'퍼셉트론(Perceptron)', b:'MLP', d:'[퍼셉트론](#/c/perceptron-c)은 은닉층 없이 입력을 바로 계단함수로 이진 분류하는 1층짜리 선형 분류기다. MLP는 은닉층을 하나 이상 두고 비선형 활성을 끼워 선형 분리 불가능한 문제(XOR 등)까지 풀 수 있게 확장한 것이다.'},
 {a:'은닉층 넓이', b:'은닉층 개수(깊이)', d:'넓이(width)는 한 층의 뉴런 수, 깊이(depth)는 층의 개수다. 같은 파라미터 예산이면 얕고 넓은 것보다 깊고 좁은 쪽이 계층적 특징을 잘 뽑는 경향이 있지만, 너무 깊으면 [경사 소실](#/c/gradient-problem)이 문제가 되어 [잔차 연결](#/c/residual)이 필요해진다.'},
 {a:'MLP', b:'FFN(트랜스포머 안의)', d:'트랜스포머의 FFN 블록은 사실상 2층 MLP(확장→활성→축소)다. 토큰마다 독립적으로 같은 가중치를 적용한다는 점에서 위치 간 정보 교환은 [셀프 어텐션](#/c/self-attention)이 담당하고 FFN은 각 위치의 표현을 비선형으로 변환하는 역할만 한다.'},
 {a:'MLP', b:'[CNN](#/c/cnn)', d:'MLP는 입력의 모든 위치를 서로 독립적인 좌표로 취급해 완전연결하지만, CNN은 커널이 국소 영역만 보고 같은 가중치를 모든 위치에 공유(파라미터 공유)한다. 이미지처럼 국소 패턴이 위치와 무관하게 반복되는 데이터에서 CNN이 MLP보다 훨씬 적은 파라미터로 더 잘 일반화하는 이유가 이 가정 차이에 있다.'},
 {a:'넓이(width)', b:'모델 용량(capacity)', d:'넓이는 용량을 구성하는 한 축일 뿐이다. 용량은 넓이·깊이·정칙화 강도가 함께 결정하는, 모델이 표현할 수 있는 함수 집합의 크기를 가리키는 더 넓은 개념이다. 넓이만 키우고 깊이·정칙화를 그대로 두면 용량이 늘어난 만큼 과적합 위험도 함께 커질 수 있다.'}
],

pitfalls:[
 '활성 함수를 빼먹으면 층을 아무리 쌓아도 선형 변환 하나와 동등해진다 — 성능이 안 오르는 원인을 구조 문제로 오해하기 쉽다.',
 '은닉층 폭을 무작정 넓히면 파라미터와 과적합 위험만 커질 뿐, 이미지·시퀀스처럼 구조가 있는 데이터에서는 CNN·트랜스포머보다 데이터 효율이 낮다.',
 '가중치를 모두 같은 값(특히 0)으로 초기화하면 같은 층의 뉴런들이 항상 같은 경사를 받아 서로 구분되지 않는다 — [가중치 초기화](#/c/weight-init)가 따로 다루는 문제다.',
 '이미지·시퀀스 데이터를 평탄화(flatten)해 MLP에 그대로 넣으면 공간적·순서적 인접 관계 정보가 사라진다 — 픽셀 (0,0)과 (0,1)이 이웃이라는 사실을 모델이 데이터만 보고 처음부터 다시 배워야 한다.'
],

papers:['perceptron','backprop'],
terms:['perceptron-c','activation','backprop-c','weight-init','self-attention','cnn']
})
