WIKI.concept({
slug:'chunking',

tldr:'청킹은 긴 문서를 검색 가능한 작은 조각으로 나누는 작업으로, 조각 크기·겹침·분할 기준을 잘못 정하면 검색이 성공해도 답에 필요한 맥락이 통째로 잘려나간다.',

why:'RAG 파이프라인이 실패하는 원인의 상당수는 임베딩 모델이나 LLM이 아니라 청킹 설계에 있다. 검색 점수는 높은데 답변 품질이 나쁘다면, 열에 아홉은 청크가 문맥을 반토막 낸 상태로 인덱싱된 것이다.',

sections:[
 {h:'왜 나누는가', d:'임베딩 모델과 [컨텍스트 윈도](#/c/context-window)에는 한 번에 처리할 수 있는 길이 제한이 있고, 문서 전체를 하나로 임베딩하면 문서 안의 여러 주제가 뭉개져 벡터가 뭉뚱그려진 평균처럼 흐려진다. 그래서 문서를 의미 있는 단위(chunk)로 쪼개 각각을 따로 임베딩하고 [벡터 DB](#/c/semantic-search)에 색인한다. 목표는 "질문과 관련된 조각만 정확히 찾아 꺼내는 것"이고, 청크 경계를 어디에 긋느냐가 이 정확도를 좌우한다.'},
 {h:'청크 크기와 겹침', d:'청크가 너무 작으면(예: 문장 하나) 검색은 정밀해지지만 그 조각만으로는 맥락이 부족해 LLM이 오독하거나 [환각](#/c/hallucination)을 일으키기 쉽다. 청크가 너무 크면(예: 문서 전체) 맥락은 풍부하지만 서로 무관한 내용이 섞여 임베딩이 흐려지고, [컨텍스트 윈도](#/c/context-window)를 불필요하게 많이 차지해 [Lost in the Middle](#/p/lost-in-the-middle) 현상처럼 중간에 묻힌 정보를 LLM이 놓치기 쉬워진다. 그래서 인접 청크끼리 일부 겹치게(overlap) 잘라, 경계에 걸친 문장이 양쪽 청크 모두에 온전히 남도록 하는 것이 실무 관행이다 — 겹침이 없으면 문장 하나가 청크 경계에서 반으로 잘려 양쪽 다 불완전한 정보만 갖게 된다.'},
 {h:'의미 단위 분할', d:'가장 단순한 방법은 고정 글자 수·토큰 수로 기계적으로 자르는 것이지만, 이는 문장이나 문단 중간을 예사로 끊는다. 더 나은 방법은 문단·절·헤딩 같은 문서의 자연스러운 구조 경계를 따라 자르는 것이다 — 마크다운의 헤딩, 코드의 함수 경계, 계약서의 조항 번호처럼 문서 자체가 가진 의미 단위를 존중하면 각 청크가 그 자체로 완결된 의미를 갖는다. 문장 임베딩 유사도가 급격히 떨어지는 지점을 경계로 잡는 의미 기반(semantic) 청킹도 쓰이지만, 계산 비용이 더 들고 항상 고정 크기 방식보다 낫다고 단정할 수는 없다 — 문서 구조가 뚜렷한 데이터에는 구조 기반 분할이 더 안정적인 경우가 많다.'},
 {h:'메타데이터 보존', d:'청크를 원본 문서에서 잘라내는 순간 "이 조각이 어느 문서, 어느 섹션, 몇 번째 페이지에서 왔는지"라는 맥락 정보를 잃기 쉽다. 실무에서는 각 청크에 출처 문서 ID·섹션 제목·페이지 번호 같은 메타데이터를 함께 저장해, 검색된 청크만으로는 부족한 맥락을 보완하거나(예: 프롬프트에 "이 내용은 [계약서 3조]에서 발췌" 식으로 표기) 답변에 출처를 표시하는 데 쓴다. 메타데이터 없이 청크 텍스트만 저장하면 나중에 "이 답이 어디서 나왔는지" 검증할 방법이 없어진다.'}
],

diagram:{type:'flow', cap:'문서가 검색 가능한 청크로 쪼개지는 과정.',
 nodes:[
  {t:'원본 문서', s:'구조·메타데이터 포함'},
  {t:'경계 결정', s:'구조/의미/고정크기'},
  {t:'겹침 적용', s:'경계 문장 보존'},
  {t:'청크+메타데이터', s:'임베딩·색인 대상'}
 ]},

confuse:[
 {a:'청크 크기', b:'[컨텍스트 윈도](#/c/context-window)', d:'청크 크기는 색인 단계에서 문서를 얼마나 잘게 자르는지의 문제이고, 컨텍스트 윈도는 LLM이 한 번에 읽을 수 있는 최대 토큰 수라는 추론 단계의 제약이다. 청크를 아무리 작게 잘라도 검색된 청크 여러 개를 합친 길이가 컨텍스트 윈도를 넘으면 잘라내거나 개수를 줄여야 한다.'},
 {a:'고정 크기 청킹', b:'의미 기반 청킹', d:'고정 크기는 구현이 단순하고 속도가 빠르지만 문장·문단을 예사로 끊고, 의미 기반은 자연스러운 경계를 찾아주지만 계산 비용이 더 들고 항상 우월한 것은 아니다. 문서 구조가 명확하면(마크다운, 계약서 조항 등) 구조 기반 분할이 의미 기반보다 더 안정적일 수 있다.'},
 {a:'겹침(overlap)', b:'중복 청크', d:'겹침은 경계에 걸친 문장을 보존하려고 인접 청크의 일부를 의도적으로 공유시키는 설계이고, 중복 청크는 같은 내용이 실수로 두 번 색인된 것이다. 겹침 비율을 과하게 높이면(예: 50% 이상) 사실상 중복 청크를 양산하는 것과 다를 바 없어진다.'}
],

pitfalls:[
 '청크 크기를 한 번 정해두고 모든 문서 유형(짧은 FAQ, 긴 계약서, 코드)에 똑같이 적용하면 어느 한쪽에서는 반드시 문맥이 잘린다 — 문서 유형별로 전략을 다르게 가져가는 것이 실무 정석이다.',
 '겹침 없이 고정 크기로만 자르면 검색 점수가 높은 청크를 찾았는데도 정작 필요한 문장이 청크 경계에서 잘려 반쪽만 들어 있는 경우가 잦다.',
 '메타데이터(출처)를 청크에 저장하지 않으면 나중에 환각 여부를 검증하거나 사용자에게 근거를 보여줄 방법이 없어진다.',
 '의미 기반 청킹이 항상 더 좋다고 믿고 무비판적으로 도입하면, 계산 비용만 늘고 구조가 뚜렷한 문서에서는 오히려 구조 기반 분할보다 나쁜 경계를 만들 수 있다.'
],

papers:['rag','lost-in-the-middle'],
terms:['semantic-search','rag-c','context-management']
});
