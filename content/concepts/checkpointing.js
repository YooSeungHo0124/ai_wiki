WIKI.concept({
slug:'checkpointing',

tldr:'역전파에 필요한 중간 활성값을 순전파 때 저장해 두는 대신 버리고, 역전파 시점에 필요한 구간만 다시 순전파해 계산함으로써 GPU 메모리를 추가 계산량과 맞바꾸는 기법.',

why:'모델·배치 크기가 커질수록 GPU 메모리를 가장 많이 먹는 것은 가중치가 아니라 역전파를 위해 저장해 둔 순전파 중간 활성값(activation)이다. 경사 체크포인팅(gradient checkpointing, activation checkpointing)은 같은 GPU로 몇 배 더 큰 배치·모델을 올릴 수 있게 해 주는, 대규모 학습에서 사실상 필수적인 트릭이다. 이름에 "체크포인트"가 들어가 모델 저장 파일(checkpoint file)과 자주 혼동된다는 점도 알아 둬야 한다. "OOM(메모리 부족)이 뜨면 가장 먼저 시도해 볼 옵션"으로 기억해 둘 만하다.',

sections:[
{h:'활성값과 메모리', d:'역전파는 연쇄 법칙에 따라 각 층의 경사를 계산하는데, 이때 순전파에서 계산했던 중간 활성값(각 층의 출력)이 필요하다. 그래서 표준 학습은 순전파를 하며 모든 층의 활성값을 메모리에 쌓아 두고, 역전파가 끝날 때까지 이를 유지한다. 층 수 $L$, 레이어당 활성값 크기가 배치·시퀀스 길이에 비례해 커지는 트랜스포머에서는 이 저장량이 층 수에 비례해 선형으로 쌓여, 긴 시퀀스·큰 배치에서 가중치보다 훨씬 큰 메모리를 차지하는 경우가 흔하다.'},
 {h:'무엇을 교환하는가', d:'경사 체크포인팅은 전체 $L$ 개 층의 활성값을 다 저장하는 대신, 일부 지점(체크포인트)의 활성값만 저장하고 나머지는 버린다. 역전파가 특정 층의 경사를 계산할 차례가 되면, 가장 가까운 체크포인트에서 그 지점까지 순전파를 **다시** 실행해 필요한 활성값을 즉석에서 만들어낸다. 즉 메모리를 아끼는 대가로 순전파 연산을 한 번 더 하게 되어 전체 학습 시간이 늘어난다.'},
{h:'구체적인 절약량', d:'층을 대략 $\\sqrt{L}$ 개 간격으로 체크포인트를 두면, 저장 메모리는 $O(L)$ 에서 $O(\\sqrt{L})$ 로 줄고 추가 연산은 순전파를 한 번 더 하는 정도(약 33% 안팎의 시간 증가)로 그친다는 것이 체크포인팅 기법의 고전적 결과다. 실무 구현은 트랜스포머 블록 단위로 체크포인트를 두는 경우가 많아, 블록 수만큼 저장이 줄고 순전파가 한 번 더 반복된다.'},
 {h:'실무에서', d:'PyTorch의 `torch.utils.checkpoint`, HuggingFace `gradient_checkpointing=True` 처럼 한 줄로 켤 수 있는 표준 기능이다. 켜면 배치 크기를 2~4배 키울 수 있는 대신 스텝당 시간이 20~30% 늘어나는 트레이드오프가 흔하다 — GPU 메모리가 병목이면 이득이 시간 손해보다 크다. [ZeRO](#/p/zero)·[FSDP](#/p/fsdp) 같은 분산 학습의 메모리 절약 기법(옵티마이저 상태·가중치 샤딩)과는 별개의 축이라 함께 켜는 것이 일반적이다.'},
{h:'다른 기법과 조합', d:'대규모 학습에서는 경사 체크포인팅과 [혼합 정밀도](#/c/mixed-precision-c), [ZeRO/FSDP](#/p/zero) 세 가지를 함께 쓰는 것이 사실상 표준 레시피다. 체크포인팅은 활성값을, 혼합 정밀도는 값 하나당 바이트 수를, ZeRO/FSDP는 여러 GPU에 상태를 나눠 담아 GPU 1장당 부담을 각각 다른 축에서 줄인다 — 세 기법이 서로 겹치지 않는 자원을 절약하기 때문에 함께 켜도 이득이 중첩된다.'}
],

math:[
 {tex:'\\text{mem}_{\\text{full}}=O(L),\\quad \\text{mem}_{\\text{ckpt}}=O(\\sqrt{L}),\\quad \\text{extra compute}\\approx 1\\times\\text{forward}', expr:'checkpointing tradeoff', d:'$L$ 은 층 수. 체크포인트를 $\\sqrt{L}$ 간격으로 두면 저장 메모리가 층 수의 제곱근에 비례하는 수준으로 줄고, 대가로 순전파 연산량이 대략 한 번 더 늘어난다.'}
],

code:{lang:'python', d:'PyTorch의 checkpoint 유틸을 트랜스포머 블록 단위로 감싸는 아주 흔한 구현 패턴이다.', src:'from torch.utils.checkpoint import checkpoint\n\nclass TransformerLayer(nn.Module):\n    def forward(self, x):\n        return self.block(x)  # 어텐션+FFN\n\nclass Model(nn.Module):\n    def forward(self, x):\n        for layer in self.layers:\n            # 활성값을 저장하지 않고, 역전파 때 layer(x)를 다시 계산\n            x = checkpoint(layer, x, use_reentrant=False)\n        return x'},

diagram:{type:'compare', cap:'모든 값을 저장할지, 일부만 남기고 나중에 다시 계산해 둘지.',
 left:{t:'일반 학습', items:[
  '모든 층 활성값 저장',
  '메모리 사용량 큼',
  '역전파 추가연산 없음']},
 right:{t:'경사 체크포인팅', items:[
  '일부 지점만 저장',
  '메모리 크게 절약',
  '역전파 때 순전파 재실행']}},

confuse:[
 {a:'경사 체크포인팅(activation checkpointing)', b:'모델 체크포인트 파일(checkpoint file)', d:'이름은 같지만 전혀 다른 개념이다. 후자는 학습 중간 가중치를 디스크에 저장해 재개·평가에 쓰는 스냅샷이고, 전자는 학습 **중** 메모리를 아끼려고 활성값을 버렸다가 다시 계산하는 기법이다.'},
 {a:'경사 체크포인팅', b:'[혼합 정밀도](#/c/mixed-precision-c)', d:'둘 다 메모리를 아끼지만 방식이 다르다 — 혼합 정밀도는 값 하나하나가 차지하는 바이트 수를 줄이고, 체크포인팅은 저장하는 값의 **개수** 자체를 줄인다. 실무에서는 두 기법을 함께 쓰는 것이 일반적이다.'},
 {a:'체크포인팅', b:'[ZeRO](#/p/zero)/[FSDP](#/p/fsdp) 샤딩', d:'체크포인팅은 GPU 한 장 안에서 활성값 메모리를 줄이는 기법이고, ZeRO/FSDP는 여러 GPU에 가중치·경사·옵티마이저 상태를 나눠 담아(sharding) 메모리를 줄이는 분산 기법이다. 층위가 다른 문제라 함께 쓰인다.'},
{a:'재계산(recompute)', b:'캐싱(caching)', d:'체크포인팅의 발상은 일반적인 캐싱 전략과 정확히 정반대다 — 캐싱은 나중에 다시 쓸 값을 미리 저장해 재계산을 피하는 것이고, 체크포인팅은 반대로 저장해 두면 쓸 값을 일부러 버리고 필요할 때 다시 계산한다. 메모리가 계산보다 더 귀한 자원일 때 이 역전된 선택이 이득이 된다.'}
],

pitfalls:[
 '연산이 거의 없는(메모리는 크지만 계산은 가벼운) 층까지 체크포인팅하면 절약 대비 재계산 비용만 늘어 손해가 될 수 있다 — 보통 트랜스포머 블록처럼 계산량이 큰 단위로 체크포인트를 둔다.',
 '드롭아웃처럼 무작위성이 있는 층을 재계산할 때 순전파를 다시 실행하면서 난수 시드가 어긋나면, 저장했던 순전파와 재계산된 순전파의 결과가 달라질 수 있다 — 구현체는 보통 난수 상태를 저장해 이를 방지한다.',
 '체크포인팅을 켜면 스텝당 시간이 늘어나므로, GPU 메모리 여유가 이미 충분한 상황에서 무조건 켜면 오히려 전체 처리량(throughput)만 손해 본다.',
 '체크포인트 간격을 너무 촘촘하게(층마다) 잡으면 재계산 오버헤드가 커지고, 너무 성기게 잡으면 메모리 절약 효과가 줄어든다 — 블록 단위로 적당한 간격을 찾는 실측 튜닝이 필요하다.'
],

papers:['zero','fsdp'],
terms:['mixed-precision-c','param-count','gradient-problem','batch-epoch','grad-clipping','residual']
})
