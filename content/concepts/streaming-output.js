WIKI.concept({
slug:'streaming-output',

tldr:'생성이 끝날 때까지 기다리지 않고 토큰이 만들어지는 대로 순차적으로 클라이언트에 내려보내는 방식으로, 체감 지연을 좌우한다.',

why:'같은 모델, 같은 응답 길이라도 스트리밍을 켜느냐 끄느냐에 따라 사용자가 느끼는 속도가 완전히 다르다. 500 토큰짜리 답을 다 만든 뒤에야 한 번에 보여주면 사용자는 몇 초간 빈 화면을 본다. 이 지연을 이루는 두 구간 — 첫 토큰까지의 시간과 이후 토큰 하나하나가 나오는 간격 — 은 병목의 성격이 전혀 달라서, 어느 쪽이 문제인지 구분하지 못하면 엉뚱한 곳을 최적화하게 된다.',

sections:[
 {h:'TTFT 와 TPOT', d:'TTFT(Time To First Token)는 요청을 보낸 뒤 첫 토큰이 나오기까지의 시간이고, TPOT(Time Per Output Token)는 그 이후 토큰 하나가 추가로 나오는 데 걸리는 평균 시간이다. 전체 체감 지연은 대략 TTFT + (출력 토큰 수 − 1) × TPOT 로 나뉜다. 같은 총 응답 시간이라도 TTFT 가 길고 TPOT 가 짧은 경우와, TTFT 는 짧지만 TPOT 가 긴 경우는 사용자 체감이 다르다 — 전자는 "느리게 시작해서 빨리 끝나는" 느낌이고 후자는 "바로 시작하지만 질질 끄는" 느낌이다.'},
 {h:'prefill·decode', d:'TTFT 를 좌우하는 prefill 단계는 입력 프롬프트 전체를 한 번에 모델에 통과시켜 [KV 캐시](#/c/kv-cache)를 채우는 과정으로, 토큰 여러 개를 한꺼번에 행렬곱하므로 GPU 연산량(compute-bound)이 병목이다. 프롬프트가 길수록 TTFT 가 늘어나는 이유가 이것이다. 반면 TPOT 를 좌우하는 decode 단계는 토큰을 하나씩만 생성하는데, 이때마다 이미 쌓인 KV 캐시 전체를 메모리에서 읽어야 해서 연산량 자체는 작아도 메모리 대역폭(memory-bandwidth-bound)이 병목이 된다. 두 단계의 병목 성격이 다르기 때문에 최적화 방법도 다르다 — prefill 은 연산을 줄이거나 병렬화하는 쪽([FlashAttention](#/p/flashattention) 계열), decode 는 캐시 읽기 효율이나 투기적 디코딩([EAGLE](#/p/eagle))으로 접근한다.'},
 {h:'배치와의 상호작용', d:'서버가 여러 요청을 동시에 처리하는 [배치 추론](#/c/batch-inference)을 쓰면 처리량(throughput)은 올라가지만 개별 요청의 TPOT 는 다른 요청들과 GPU 를 나눠 쓰느라 늘어날 수 있다. 동시 요청이 몰리는 시간대에 TTFT·TPOT 가 같이 나빠지는 것은 이 트레이드오프 때문이다.'},
 {h:'실무에서', d:'사용자 대면 채팅 제품은 TTFT 를 체감 반응성의 핵심 지표로 삼고, 긴 배치 작업(요약 파이프라인 등)은 총 처리 시간과 처리량을 더 중요하게 본다 — 같은 제품이라도 용도에 따라 최적화 목표를 다르게 잡아야 한다. 스트리밍을 켜면 사용자는 답이 끝나기 전에 읽기 시작할 수 있어 TTFT 만 낮아도 체감 만족도가 크게 오르는 경우가 많다. 다만 스트리밍 중 함수 호출이나 구조화 출력을 섞으면 부분적으로 깨진 JSON 이 중간에 노출될 수 있어 파싱 로직을 별도로 다뤄야 한다.'}
],

math:[
 {tex:'T_{total}\\approx T_{TTFT}+(n-1)\\cdot T_{TPOT}', expr:'총 응답 시간 근사', d:'$n$ 은 생성된 출력 토큰 수. TTFT 는 prefill(연산 병목) 에, TPOT 는 decode(메모리 대역폭 병목) 에 지배된다 — 같은 하드웨어에서도 두 값을 각각 따로 줄여야 한다.'}
],

diagram:{type:'flow', cap:'prefill 은 한 번에, decode 는 토큰마다 반복.',
 steps:['프롬프트 전체 입력','prefill: 연산 병목 → 첫 토큰(TTFT)','decode: 캐시 읽기 병목','토큰 1개씩 생성(TPOT)','스트림으로 순차 전송']},

confuse:[
 {a:'TTFT', b:'TPOT', d:'TTFT 는 시작까지의 대기시간, TPOT 는 시작 이후 토큰 하나당 간격이다. 프롬프트가 길면 TTFT 가, 출력이 길면 TPOT 의 누적 효과가 전체 체감 시간을 더 좌우한다.'},
 {a:'스트리밍', b:'[배치 추론](#/c/batch-inference)', d:'스트리밍은 한 요청의 결과를 어떻게 전달하느냐(순차적 전송)의 문제이고, 배치 추론은 여러 요청을 서버가 어떻게 동시에 처리하느냐의 문제다. 서버는 배치로 여러 요청을 처리하면서 각 요청의 결과는 개별적으로 스트리밍할 수 있다 — 둘은 동시에 적용된다.'},
 {a:'지연시간(latency)', b:'처리량(throughput)', d:'지연시간은 한 요청이 끝나기까지 걸리는 시간이고 처리량은 서버가 단위 시간에 처리하는 전체 토큰·요청 수다. 배치를 키우면 처리량은 늘지만 개별 요청의 지연시간(특히 TPOT)은 나빠질 수 있다.'}
],

pitfalls:[
 '"응답이 느리다"는 불만이 TTFT 문제인지 TPOT 문제인지 구분하지 않고 무작정 모델을 바꾸는 경우가 많다 — 원인이 다르면 처방도 달라야 한다.',
 '프롬프트를 줄이면 TPOT 가 아니라 주로 TTFT 가 개선된다 — 긴 출력이 문제라면 프롬프트를 줄여도 체감이 크게 안 나아질 수 있다.',
 '스트리밍 중간에 구조화 출력이 부분적으로만 도착한 상태를 그대로 파싱하려 하면 에러가 난다 — 완결된 청크 단위로 처리해야 한다.'
],

papers:['vllm','eagle'],
terms:['kv-cache','batch-inference','serving-arch']
});
