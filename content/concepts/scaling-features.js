WIKI.concept({
slug:'scaling-features',

tldr:'서로 다른 단위·범위를 가진 입력 특징들을 비슷한 스케일로 맞춰, 값의 크기 차이만으로 학습이 왜곡되지 않도록 하는 전처리.',

why:'"소득(수백만 단위)"과 "나이(두 자릿수)"를 스케일 맞춤 없이 그대로 선형 모델·신경망에 넣으면 값이 큰 특징이 손실·경사에 부당하게 큰 영향을 미쳐, 실제로는 더 중요한 정보를 가진 나이 같은 특징이 무시된다. 경사 하강법이 잘 수렴하지 않을 때 학습률부터 의심하기 전에 입력 스케일부터 확인하는 것이 순서이며, 이 기본 하나를 놓쳐 며칠을 낭비하는 경우도 드물지 않다.',

sections:[
 {h:'스케일과 학습', d:'경사 하강법은 모든 파라미터에 같은 학습률을 적용하는데, 입력 특징의 스케일이 파라미터가 놓인 손실 지형의 굴곡(각 축 방향의 곡률)을 결정한다. 한 특징의 범위가 다른 특징보다 1000배 크면 그 방향의 손실 지형이 길쭉하게 늘어나, 최적화가 지그재그로 튀며 느리게 수렴하거나 발산하기 쉽다. 거리 기반 모델([k-NN](#/c/knn), [k-평균](#/c/kmeans-c))은 더 직접적이다 — 유클리드 거리 계산 자체가 값이 큰 특징에 지배된다.'},
 {h:'표준화', d:'각 특징에서 평균을 빼고 표준편차로 나눠 평균 0, 표준편차 1로 맞춘다: $x\'=(x-\\mu)/\\sigma$. 정규분포에 가까운 데이터, 그리고 [배치 정규화](#/c/normalization) 계열과 궁합이 좋아 딥러닝 입력 전처리에서 가장 흔히 쓰이는 기본 선택지다.'},
 {h:'Min-Max 정규화', d:'특징을 최솟값·최댓값 기준으로 $[0,1]$(또는 $[-1,1]$) 범위로 선형 변환한다: $x\'=(x-x_{min})/(x_{max}-x_{min})$. 값의 범위가 확실히 정해져 있는 경우(픽셀값 0~255 등)에 적합하지만, 이상치 하나가 최댓값을 크게 밀어 올리면 나머지 정상값들이 좁은 구간에 몰려버리는 약점이 있다.'},
 {h:'언제 어느 쪽을 쓰는가', d:'이상치가 있는 데이터에는 표준화가 min-max보다 덜 흔들린다(이상치가 평균·표준편차에 영향을 주긴 하지만 범위 자체를 결정하진 않는다). 이상치가 아주 심하면 중앙값·사분위범위를 쓰는 로버스트 스케일링(RobustScaler)이 더 안전하다. 트리 기반 모델([결정 트리](#/c/decision-tree), [부스팅](#/c/boosting))은 각 특징을 독립적으로 임계값 비교만 하므로 스케일에 영향을 받지 않아 스케일링이 필요 없다는 것도 실무에서 자주 쓰는 판단 기준이다. 반대로 거리·내적 기반 모델은 스케일링 없이는 사실상 작동하지 않는다고 봐도 무방하다.'},
 {h:'실무에서', d:'스케일 통계(평균·표준편차 또는 최소·최댓값)는 반드시 **학습셋에서만** 계산하고, 검증·시험셋·운영 데이터에는 그 통계를 그대로 적용해야 한다 — 전체 데이터로 통계를 낸 뒤 나누면 시험셋 정보가 학습 과정에 새어 들어가는 데이터 누수가 된다. 배포 시에는 이 스케일 통계 자체를 모델과 함께 저장·버전관리해야, 서빙 시점에 학습 때와 다른 스케일을 적용하는 사고(training-serving skew)를 막을 수 있다.'},
 {h:'딥러닝 입력에서', d:'이미지 입력은 흔히 픽셀값을 [0,1]로 나누거나 ImageNet 평균·표준편차로 표준화하고, 정형 데이터를 다루는 MLP·[임베딩](#/c/embedding) 이전 단계의 수치 특징도 표준화를 거치는 것이 일반적이다. 반면 [정규화 층](#/c/normalization)이 이미 각 층 활성값을 다시 정규화해 주므로, 입력 스케일링이 다소 부정확해도 학습이 완전히 실패하지는 않는 경우가 많다 — 다만 초반 수렴 속도에는 여전히 영향을 준다.'}
],

math:[
 {tex:'x\'=\\dfrac{x-\\mu}{\\sigma}', expr:'standardization (z-score)', d:'$\\mu,\\sigma$ 는 학습셋에서 계산한 평균·표준편차. 결과는 평균 0, 표준편차 1인 분포로 재배치된다.'},
 {tex:'x\'=\\dfrac{x-x_{min}}{x_{max}-x_{min}}', expr:'min-max normalization', d:'$x_{min},x_{max}$ 도 학습셋 기준. 결과는 $[0,1]$ 범위로 선형 압축된다.'}
],

code:{lang:'python', d:'scikit-learn 스케일러는 학습셋에서만 fit하고, 검증·시험·운영 데이터에는 transform만 적용해야 정보 누수를 막을 수 있다.', src:'from sklearn.preprocessing import StandardScaler\n\nscaler = StandardScaler()\nX_train_scaled = scaler.fit_transform(X_train)   # 학습셋 통계로 fit\nX_val_scaled = scaler.transform(X_val)           # 같은 통계 재사용\nX_test_scaled = scaler.transform(X_test)\n\n# 배포 시 scaler 자체를 모델과 함께 저장해야 한다\nimport joblib\njoblib.dump(scaler, "scaler.pkl")'},

diagram:{type:'compare', cap:'이상치에 대한 민감도가 둘을 가르는 갈림길이다.',
 left:{t:'표준화(Standardization)', items:[
  '평균 0·표준편차 1',
  '이상치에 비교적 강함',
  '정규분포 가정 데이터에 적합']},
 right:{t:'Min-Max 정규화', items:[
  '범위 [0,1]로 압축',
  '이상치에 취약',
  '범위가 명확한 데이터에 적합']}},

confuse:[
 {a:'특징 스케일링(Feature Scaling)', b:'[정규화 층(BatchNorm 등)](#/c/normalization)', d:'특징 스케일링은 학습 **전** 입력 데이터에 딱 한 번 적용하는 전처리이고, BatchNorm·LayerNorm은 학습 **중** 매 순전파마다 각 층의 중간 활성값을 다시 정규화하는 신경망 층이다. 이름의 "정규화"가 같아 자주 혼동되지만 적용 시점과 대상이 다르다.'},
 {a:'정규화(Normalization, 스케일링의 의미)', b:'정규화(Regularization, 정칙화의 의미)', d:'한국어로 둘 다 "정규화"로 번역되는 경우가 있어 특히 헷갈린다. 전자(scaling)는 값의 범위를 맞추는 전처리이고, 후자([regularization](#/c/regularization))는 과적합을 억제하는 학습 기법이다 — 원어를 병기해서 구분하는 습관이 필요하다.'},
 {a:'표준화(Standardization)', b:'화이트닝(whitening)', d:'표준화는 각 특징을 독립적으로 평균 0·분산 1로 맞추지만 특징 간 상관관계는 그대로 남는다. 화이트닝은 여기서 더 나아가 [PCA](#/c/pca-c) 등으로 특징 간 상관관계까지 제거해 공분산 행렬을 단위 행렬로 만드는, 한 단계 더 강한 전처리다.'}
],

pitfalls:[
 '전체 데이터(학습+검증+시험)로 스케일 통계를 계산한 뒤 나누면, 시험셋의 정보가 학습 과정에 누수된다 — 반드시 학습셋만으로 통계를 구해야 한다.',
 '학습 때 쓴 스케일러를 저장하지 않고 배포 시 운영 데이터에 다른 기준으로 스케일링하면, 모델은 학습 때와 다른 분포의 입력을 받게 되어 성능이 소리 없이 떨어진다.',
 '트리 기반 모델에는 스케일링이 불필요한데도 관행적으로 모든 파이프라인에 일괄 적용하면 불필요한 전처리 비용과 해석 어려움만 늘어난다.',
 '이상치가 매우 큰 컬럼 하나를 min-max로 정규화하면 나머지 정상값들이 0에 가까운 좁은 구간에 몰려, 사실상 그 컬럼의 변별력이 사라질 수 있다 — 이상치를 먼저 다듬거나 로버스트 스케일링을 검토해야 한다.',
 '범주형 변수를 [원-핫 인코딩](#/c/one-hot)한 0/1 컬럼까지 다른 수치 특징과 함께 표준화하면 그 의미(있음/없음)가 흐려진다 — 범주형 컬럼은 보통 스케일링 대상에서 제외한다.'
],

papers:['batchnorm'],
terms:['normalization','regularization','feature-engineering','knn','embedding','pca-c']
})
