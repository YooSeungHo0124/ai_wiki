WIKI.concept({
slug:'weight-init',

tldr:'학습 시작 전 가중치를 어떤 분포에서 뽑을지 정하는 것으로, 층을 통과해도 활성값·경사의 분산이 폭주하거나 소실하지 않도록 분산을 맞춰 뽑는다.',

why:'초기화를 잘못하면 학습이 시작되기도 전에 실패가 결정된다 — 깊은 네트워크에서 활성값이 층마다 커지거나 작아지면 몇 층만 지나도 값이 발산하거나 0에 가까워져 [경사 소실·폭주](#/c/gradient-problem)가 일어난다. 프레임워크가 기본값으로 알아서 해 준다고 넘어가기 쉽지만, 커스텀 층·활성 함수를 쓰거나 매우 깊은 모델을 직접 설계할 때는 어떤 초기화를 골라야 하는지 원리로 알아야 한다.',

sections:[
 {h:'대칭성 붕괴', d:'모든 가중치를 0이나 같은 상수로 초기화하면 같은 층의 모든 뉴런이 정확히 같은 입력을 받고 같은 출력을 내며, 역전파 때도 정확히 같은 경사를 받는다. 그러면 학습이 아무리 진행돼도 뉴런들이 서로 다른 특징을 배우지 못하고 영원히 동일하게 움직인다 — 이를 대칭성(symmetry)이 깨지지 않는다고 한다. 그래서 초기화는 반드시 뉴런마다 다른 무작위값이어야 하며, 남은 질문은 "그 무작위값의 분산을 얼마로 할 것인가"다.'},
 {h:'분산을 맞춘다는 것', d:'층을 통과하며 활성값의 분산이 유지되려면, 입력 $x$ 의 각 성분이 독립이고 평균 0이라 할 때 출력 분산 $\\mathrm{Var}(Wx)=n_{in}\\mathrm{Var}(W)\\mathrm{Var}(x)$ 가 성립한다. 이 값이 층마다 그대로 유지되려면 $\\mathrm{Var}(W)=1/n_{in}$ 이어야 한다 — 입력 노드 수 $n_{in}$ 이 클수록 가중치 하나하나는 더 작게 뽑아야 총 분산이 커지지 않는다는 뜻이다.'},
 {h:'Xavier 초기화', d:'선형·tanh·시그모이드처럼 좌우 대칭인 활성 함수를 가정하고, 순전파(입력 $n_{in}$ 기준)와 역전파(출력 $n_{out}$ 기준) 양쪽의 분산을 동시에 보존하려고 둘의 평균을 쓴다. 균등분포로는 $W\\sim U(-\\sqrt{6/(n_{in}+n_{out})},\\ \\sqrt{6/(n_{in}+n_{out})})$ 를 쓴다.'},
 {h:'He 초기화', d:'ReLU는 입력의 절반(음수 부분)을 0으로 죽이기 때문에 순전파를 지날 때마다 분산이 대략 절반이 된다. 이를 보정하기 위해 Xavier의 분산을 2배로 키운 $\\mathrm{Var}(W)=2/n_{in}$, 즉 $W\\sim\\mathcal{N}(0,\\ 2/n_{in})$ 를 쓴다. ReLU 계열 활성 함수를 쓰는 [CNN](#/c/cnn)·MLP에서 사실상 표준값이다.'},
 {h:'실무에서', d:'프레임워크 기본값(PyTorch의 `nn.Linear`, `nn.Conv2d` 등)은 대개 He 계열을 이미 적용하므로 직접 손댈 일은 드물다. 다만 [잔차 연결](#/c/residual)이 매우 깊이 쌓이는 모델(ResNet, 트랜스포머)에서는 마지막 프로젝션 층의 가중치를 0에 가깝게 또는 층 수의 제곱근에 반비례하게 축소 초기화(scaled init)해, 잔차 경로의 분산이 층이 늘수록 폭주하지 않게 조정하는 기법을 쓴다. 임베딩 층은 보통 작은 표준편차(0.01~0.02)의 정규분포로 별도 초기화하는 경우가 많다 — 어휘 크기가 매우 커서 $n_{in}$ 기준 공식을 그대로 적용하면 지나치게 작은 값이 나오기 때문이다.'}
],

math:[
 {tex:'\\mathrm{Var}(W)=\\dfrac{1}{n_{in}}\\quad\\text{(선형 근사 보존)}', expr:'variance preservation', d:'입력 분산이 층을 지나도 유지되려면 가중치 분산이 입력 노드 수에 반비례해야 한다.'},
 {tex:'W\\sim U\\!\\left(-\\sqrt{\\dfrac{6}{n_{in}+n_{out}}},\\ \\sqrt{\\dfrac{6}{n_{in}+n_{out}}}\\right)', expr:'Xavier/Glorot uniform init', d:'순전파·역전파 분산을 함께 고려한 균등분포 초기화. $n_{in},n_{out}$ 은 해당 층의 입력·출력 노드 수.'},
 {tex:'W\\sim\\mathcal{N}\\!\\left(0,\\ \\dfrac{2}{n_{in}}\\right)', expr:'He/Kaiming init', d:'ReLU가 활성값의 절반을 죽이는 것을 보정해 분산을 2배로 키운 정규분포 초기화.'}
],

code:{lang:'python', d:'PyTorch에서 He/Xavier 초기화를 명시적으로 적용하는 예. 활성 함수 종류에 따라 어떤 것을 골라야 하는지 이름에서부터 드러난다.', src:'import torch.nn as nn\n\nlinear_relu = nn.Linear(512, 512)\nnn.init.kaiming_normal_(linear_relu.weight, nonlinearity="relu")  # He\nnn.init.zeros_(linear_relu.bias)\n\nlinear_tanh = nn.Linear(512, 512)\nnn.init.xavier_uniform_(linear_tanh.weight)  # Xavier/Glorot\nnn.init.zeros_(linear_tanh.bias)'},

diagram:{type:'compare', cap:'활성 함수의 좌우 대칭성 여부가 계수를 가른다.',
 left:{t:'Xavier/Glorot', items:[
  'tanh·시그모이드용',
  'Var(W)=1/n or 2/(nin+nout)',
  '좌우 대칭 활성 가정']},
 right:{t:'He/Kaiming', items:[
  'ReLU 계열용',
  'Var(W)=2/n_in',
  'ReLU의 분산 반토막 보정']}},

confuse:[
 {a:'가중치 초기화', b:'[정규화 층](#/c/normalization)', d:'초기화는 학습 시작 시점 한 번만 정하는 값이고, BatchNorm·LayerNorm 같은 정규화 층은 학습 내내 매 순전파마다 활성값 분포를 다시 맞춘다. 정규화 층을 쓰면 초기화 선택에 덜 민감해지지만, 초기화가 아예 필요 없어지는 것은 아니다.'},
 {a:'무작위 초기화', b:'0 초기화', d:'편향(bias)은 보통 0으로 초기화해도 무방하다 — 같은 층 뉴런들 사이의 비대칭은 가중치가 이미 만들어 주기 때문이다. 반면 가중치를 0으로 하면 대칭성이 깨지지 않아 학습이 진행되지 않는다.'},
 {a:'분산이 층마다 유지된다', b:'평균이 층마다 유지된다', d:'초기화 이론은 활성값의 분산(퍼짐 정도)이 폭주·소실하지 않는 것을 목표로 한다. 평균을 0 근처로 두는 것은 활성 함수·정규화 층이 별도로 담당하는 문제다.'},
 {a:'가중치 초기화', b:'[전이 학습](#/c/transfer-learning)의 사전학습 가중치', d:'무작위 초기화는 학습을 "0에서" 시작하는 것이고, 사전학습된 가중치를 불러와 시작하는 것은 이미 다른 데이터로 학습된 지점에서 출발하는 것이다. 후자는 일종의 아주 정교한 초기화라고 볼 수도 있지만, 목적(대칭성 붕괴 vs 지식 재사용)이 다르므로 보통 별개 개념으로 다룬다.'}
],

pitfalls:[
 '가중치를 아주 작은 상수(0.01 같은)로 통일해서 초기화하면 0으로 초기화한 것과 마찬가지로 대칭성 문제가 거의 그대로 남는다 — 반드시 뉴런마다 독립적인 무작위값이어야 한다.',
 'tanh/시그모이드용 Xavier 초기화를 ReLU 네트워크에 그대로 쓰면 층을 지날수록 분산이 계속 줄어 깊은 층에서 활성값이 죽는 경향이 생긴다.',
 '사전학습된 가중치를 불러와 일부 층만 새로 초기화(예: 분류기 헤드 교체)할 때, 나머지 사전학습 층과 스케일이 안 맞으면 학습 초반이 불안정해질 수 있다.',
 '초기화 이론은 활성 함수가 원점 근처에서 선형에 가깝다는 근사(선형화)에 기대고 있다 — 활성 함수·층 구조가 이 가정에서 크게 벗어나면(예: 매우 깊은 네트워크의 초반 몇 스텝) 이론값이 최적이 아닐 수 있어 실측으로 미세조정하기도 한다.'
],

papers:['resnet','batchnorm'],
terms:['mlp','gradient-problem','normalization','activation','residual']
})
