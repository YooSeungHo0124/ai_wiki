WIKI.concept({
slug:'supervision-types',

tldr:'모델이 정답 신호를 어디서 얻는가에 따른 구분으로, 자기지도는 사람이 라벨을 달지 않고 데이터 자체의 구조에서 정답을 만들어낸다는 점이 비지도와 다르다.',

why:'논문이나 모델 카드에 "self-supervised pretraining" 이라고 쓰여 있을 때 이게 비지도(클러스터링류)와 뭐가 다른지 구분 못 하면 [BERT](#/p/bert)·[SimCLR](#/p/simclr)·[MAE](#/p/mae) 가 왜 라벨 없는 데이터로 지도학습처럼 손실을 계산하는지 이해할 수 없다. 데이터에 라벨이 얼마나 있는지가 어떤 학습 방식을 쓸지를 결정하는 첫 질문이다.',

sections:[
 {h:'지도학습', d:'입력 $x$ 와 사람(또는 신뢰할 수 있는 소스)이 단 정답 $y$ 의 쌍으로 학습한다. 분류·회귀·탐지가 대표적이며 손실은 예측과 $y$ 의 차이로 직접 계산된다. 라벨링 비용이 성능의 상한을 좌우하는 가장 직관적인 방식이다.'},
 {h:'비지도학습', d:'라벨이 전혀 없는 데이터에서 구조(군집, 밀도, 저차원 표현)를 찾는다. k-means·GMM·PCA·오토인코더 재구성이 여기 속한다. 목적함수가 "정답 맞히기"가 아니라 "데이터 분포 자체를 설명하기"라서 지도학습과 평가 방식도 다르다 — 정답이 없으니 정확도가 아니라 군집 응집도 같은 대리 지표를 쓴다.'},
 {h:'자기지도학습이 다른 이유', d:'자기지도(self-supervised)는 사람이 단 라벨은 없지만, 데이터의 일부를 가리거나 변형해서 **모델이 풀 수 있는 지도학습 문제를 자동으로 만든다.** [BERT](#/p/bert)의 마스크 토큰 예측, [MAE](#/p/mae)의 패치 복원, [SimCLR](#/p/simclr)의 "같은 이미지의 두 증강본은 가깝게"가 전부 이 패턴이다. 손실 계산 방식은 지도학습과 똑같이 예측-정답 차이지만, 정답이 사람이 아니라 데이터 파이프라인에서 나온다는 것이 핵심이다. 그래서 비지도가 아니라 별도 범주로 부른다.'},
 {h:'준지도와 약지도', d:'준지도학습(semi-supervised)은 라벨 있는 소량 데이터와 라벨 없는 대량 데이터를 함께 쓴다 — 라벨 없는 데이터에 모델 자신의 예측을 의사 라벨(pseudo-label)로 달아 재학습하는 self-training이 대표적이다. 약지도학습(weakly-supervised)은 라벨이 있긴 하지만 부정확하거나 성긴 라벨을 쓴다 — 예를 들어 픽셀 단위 분할 마스크 대신 이미지 단위 태그만으로 분할을 학습시키는 식이다. 둘 다 "라벨이 부족한 현실"에 대응하는 절충안이라는 공통점이 있다.'}
],

diagram:{type:'compare', cap:'정답 신호가 어디서 오는가로 구분한다.',
 left:{t:'비지도', items:['라벨 없음','목표: 데이터 구조 발견','예: k-means, PCA']},
 right:{t:'자기지도', items:['라벨 없음, 그러나 데이터에서 자동 생성','목표: 지도학습처럼 예측-정답 손실','예: 마스크 예측, 증강 쌍 매칭']}},

confuse:[
 {a:'비지도학습', b:'자기지도학습', d:'둘 다 사람이 라벨을 달지 않지만, 비지도는 애초에 "맞혀야 할 정답"이라는 개념이 없고(구조 발견이 목표), 자기지도는 데이터 자체를 변형·은닉해서 정답을 인위적으로 만든 뒤 지도학습과 동일한 손실을 쓴다. 최근 딥러닝에서 "self-supervised pretraining"이 압도적으로 많이 쓰이는 이유는 지도학습의 최적화 도구(교차 엔트로피, 역전파)를 그대로 재사용할 수 있어서다.'},
 {a:'준지도학습', b:'전이학습', d:'준지도는 "같은 과제 안에서" 라벨 있는/없는 데이터를 함께 쓰는 것이고, [전이학습](#/c/transfer-learning)은 다른 과제나 데이터셋에서 학습한 모델을 가져와 새 과제에 맞춘다. 사전학습-미세조정 파이프라인은 보통 자기지도(사전학습) + 지도학습(미세조정) + 전이학습의 조합이다.'},
 {a:'약지도학습', b:'노이즈 있는 라벨(noisy label)', d:'약지도는 라벨의 "해상도"가 낮은 것(이미지 태그로 분할 학습)이고, 노이즈 라벨은 해상도는 맞지만 라벨 자체가 틀린 경우다. 둘 다 라벨 신뢰도가 낮다는 공통점이 있지만 대응 기법(약지도는 약한 라벨을 강한 라벨로 정제, 노이즈 라벨은 손실 재가중·필터링)이 다르다.'}
],

pitfalls:[
 '"라벨이 없다 = 비지도"가 아니다. 자기지도는 라벨이 없어도 지도학습과 동일한 손실 구조를 쓴다는 점에서 실무적으로는 지도학습에 훨씬 가깝게 다룬다.',
 '자기지도 사전학습이 잘 됐다고 해서 다운스트림 성능이 자동 보장되지 않는다 — 사전학습 과제(pretext task)가 실제 과제와 얼마나 관련 있는 특징을 배우게 하는지가 관건이다.',
 '준지도의 의사 라벨링은 모델이 이미 틀리고 있는 방향으로 확신을 강화하는 확증 편향(confirmation bias) 위험이 있다 — 임계값과 검증 없이 그대로 재학습에 쓰면 성능이 오히려 나빠질 수 있다.'
],

papers:['simclr','bert','mae'],
terms:['transfer-learning','pretrain-finetune','contrastive']
});
