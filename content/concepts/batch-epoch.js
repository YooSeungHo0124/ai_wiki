WIKI.concept({
slug:'batch-epoch',

tldr:'배치는 한 스텝에 쓰는 샘플 묶음, 이터레이션은 파라미터 갱신 1회, 에폭은 전체 학습 데이터를 한 바퀴 다 본 단위 — 셋은 서로 다른 것을 세는 서로 다른 단위다.',

why:'학습 로그의 "epoch 3/50, step 1200/1500" 같은 표기를 정확히 못 읽으면 학습 시간을 잘못 추정하고, 체크포인트를 잘못된 타이밍에 저장하고, 배치 크기를 바꿨는데 학습률은 그대로 둬서 성능이 떨어지는 실수를 한다. 이 셋의 관계는 한국어 사전에 꼭 필요한, 그러나 의외로 정확히 설명되는 일이 드문 항목이다.',

sections:[
 {h:'숫자 예제로 관계 잡기', d:'학습 데이터가 10,000개, 배치 크기가 100이라 하자. 한 에폭을 도는 데 필요한 이터레이션(=스텝) 수는 $10000/100=100$ 번이다. 이 데이터셋을 50 에폭 학습하면 총 이터레이션은 $100\\times 50=5000$ 번, 즉 파라미터가 5000번 갱신된다. 데이터가 100,000개로 10배 늘면 같은 배치 크기에서 에폭당 이터레이션도 10배(1000번)로 늘어난다 — 에폭 수는 그대로라도 총 학습 시간은 늘어난다.'},
 {h:'배치 크기가 학습률에 미치는 영향', d:'배치가 클수록 한 스텝의 경사 추정이 더 정확해지지만([경사 하강법](#/c/gradient-descent) 참고), 같은 에폭 수 동안 파라미터 갱신 **횟수**는 줄어든다. 배치를 $k$ 배로 키우면 학습률도 대략 $k$ 배로 키워야 갱신당 이동 거리가 보존된다는 것이 [대배치 SGD](#/p/lr-scaling)의 선형 스케일링 규칙(linear scaling rule)이다. 이 조정 없이 배치만 키우면 갱신 횟수가 줄어든 만큼 수렴이 느려지거나 덜 된 상태로 끝난다.'},
 {h:'배치 크기가 일반화에 미치는 영향', d:'배치가 클수록 경사 추정의 노이즈가 줄어드는데, 이 노이즈가 오히려 손실 지형의 평평한(flat) 최솟값 쪽으로 학습을 이끄는 역할을 한다는 관찰이 있다. 매우 큰 배치로 학습하면 날카로운(sharp) 최솟값에 수렴해 검증 성능이 떨어지는 "일반화 격차(generalization gap)"가 보고된 바 있다 — 그래서 배치를 무한정 키우는 것이 항상 이득은 아니다.'},
 {h:'실무 기본값', d:'이미지 분류는 배치 256~1024, LLM 사전학습은 (그래디언트 누적을 포함해) 유효 배치 수백만 토큰까지도 쓴다. 에폭 수는 데이터 크기·과적합 여부를 보며 정하는 경우가 많고, 최근 대규모 사전학습에서는 "에폭"보다 "총 학습 토큰 수"로 진행도를 표현하는 경우가 흔하다 — 데이터가 너무 커서 한 바퀴(1 에폭)도 못 채우고 학습이 끝나기 때문이다.'}
],

math:[
 {tex:'\\text{iterations per epoch}=\\left\\lceil \\dfrac{N}{B}\\right\\rceil',
  expr:'steps per epoch', d:'$N$ 은 전체 학습 샘플 수, $B$ 는 배치 크기. 나머지가 남으면(예: 10000/128) 올림 처리하거나 마지막 배치를 버리는(drop_last) 구현 선택이 갈린다.'},
 {tex:'\\text{total steps}=\\left\\lceil \\dfrac{N}{B}\\right\\rceil \\times E',
  expr:'total training steps', d:'$E$ 는 에폭 수. 학습률 스케줄(warmup·cosine decay)은 보통 에폭이 아니라 이 총 스텝 수를 기준으로 설계한다.'}
],

diagram:{type:'stack', cap:'가장 작은 단위(이터레이션)가 쌓여 에폭이 되는 계층 구조.',
 layers:[
  {t:'이터레이션 1회', note:'배치 1개로 파라미터 1회 갱신'},
  {t:'에폭 1회', note:'전체 데이터를 한 바퀴 (=여러 이터레이션)'},
  {t:'학습 전체', note:'에폭을 E번 반복'}
 ]},

confuse:[
 {a:'배치(batch)', b:'에폭(epoch)', d:'배치는 "한 스텝에 몇 개의 샘플을 보는가"이고 에폭은 "전체 데이터를 몇 바퀴 도는가"다. 배치 크기를 바꿔도 에폭 수는 그대로일 수 있지만, 에폭당 이터레이션 수는 배치 크기에 반비례해 바뀐다.'},
 {a:'이터레이션(iteration)', b:'스텝(step)', d:'실무에서는 거의 동의어로 쓰인다 — 둘 다 옵티마이저가 파라미터를 1회 갱신하는 단위를 가리킨다. 다만 그래디언트 누적(gradient accumulation)을 쓰면 "미니배치 forward 횟수"와 "실제 파라미터 갱신 횟수"가 갈라지므로, 그럴 때는 어느 쪽을 세는지 문서에서 확인해야 한다.'},
 {a:'배치 크기', b:'유효 배치 크기(effective batch size)', d:'GPU 메모리 제약으로 물리적 배치를 작게 쓰고 그래디언트 누적으로 여러 스텝의 경사를 모아 한 번에 갱신하면, 유효 배치 크기는 (물리 배치)×(누적 스텝 수)×(GPU 수)가 된다. 로그에 찍힌 "batch size"가 물리 배치인지 유효 배치인지 헷갈리면 학습률 설정이 어긋난다.'}
],

pitfalls:[
 '데이터가 매우 커서 1 에폭도 못 채우고 학습을 끝내는 LLM 사전학습에서는 "몇 에폭 돌렸다"는 질문 자체가 성립하지 않는 경우가 많다 — 이때는 총 학습 토큰 수로 진행도를 말해야 한다.',
 '배치 크기를 늘렸는데 학습률을 그대로 두면, 갱신 횟수가 줄어든 만큼 같은 에폭 수 안에서 수렴이 덜 된 상태로 끝날 수 있다 — [선형 스케일링 규칙](#/p/lr-scaling)을 함께 적용해야 한다.',
 '마지막 배치가 지정한 배치 크기보다 작게 남는 경우(drop_last=False)가 BatchNorm 통계나 평균 손실 계산에 미묘한 편차를 만들 수 있다 — 작은 데이터셋에서 특히 주의.'
],

papers:['lr-scaling'],
terms:['gradient-descent','learning-rate']
});
