WIKI.concept({
slug:'iou-map',

tldr:'IoU 는 두 박스가 얼마나 겹치는지, mAP 는 여러 IoU 임계값과 클래스에서 평균 낸 정밀도-재현율 곡선 아래 면적이다.',

why:'객체 탐지 논문의 숫자표를 읽으려면 AP50 과 COCO mAP 가 서로 다른 기준으로 계산된, 비교 불가능한 숫자라는 것부터 알아야 한다. 같은 모델도 어느 IoU 기준으로 보느냐에 따라 점수가 크게 달라지고, 이걸 모르면 논문끼리 숫자를 잘못 비교하게 된다.',

sections:[
 {h:'IoU: 겹침의 정도', d:'예측 박스와 정답 박스의 교집합 넓이를 합집합 넓이로 나눈 값이다. 0 이면 전혀 안 겹침, 1 이면 완전히 일치. 탐지 결과가 "맞았다"고 인정할지는 IoU 가 특정 임계값(가장 흔한 것은 0.5) 이상인지로 판단한다 — 임계값을 넘으면 그 예측은 TP 후보가 되고, 넘지 못하면 FP 다.'},
 {h:'AP: 정밀도-재현율 곡선 아래 면적', d:'탐지 결과를 신뢰도 점수 순으로 정렬하며 임계값을 낮춰가면 [정밀도와 재현율](#/c/precision-recall)이 각각 변하는 곡선이 그려진다. AP(Average Precision)는 이 곡선 아래 면적으로, 한 클래스에 대해 탐지기가 재현율을 늘려가면서도 정밀도를 얼마나 유지하는지를 하나의 숫자로 압축한다. mAP(mean AP)는 이 AP 를 모든 클래스에 대해 평균 낸 것이다.'},
 {h:'AP50 과 COCO mAP 는 다른 지표다', d:'PASCAL VOC 시절의 관행은 IoU 0.5 하나만 기준으로 AP 를 계산하는 것(AP50, 흔히 "mAP@0.5"로도 불림)이었다. COCO 는 이보다 훨씬 엄격하게, IoU 0.5 부터 0.95 까지 0.05 간격으로 10개 임계값 각각에서 AP 를 구한 뒤 그 10개를 평균 낸다(흔히 mAP 또는 AP 로 표기, "mAP@[.5:.95]"). 같은 모델이라도 AP50 은 위치가 조금 부정확해도 봐주지만 COCO mAP 는 박스 위치 정확도까지 엄격히 반영하므로, 두 숫자는 값 자체가 크게 다르고 서로 대체할 수 없다.'},
 {h:'실무에서', d:'논문 표에서 "mAP" 라고만 적혀 있으면 반드시 어느 정의(VOC AP50 인지 COCO AP인지)인지, 어느 데이터셋(VOC vs COCO)에서 잰 것인지 확인해야 한다. [YOLOv3](#/p/yolov3) 는 두 지표를 나란히 보고하며 AP50 에서는 경쟁력 있지만 COCO AP 에서는 뒤처진다는 것을 스스로 밝혔는데, 이는 작은 박스나 정확한 위치 맞추기에서 약했다는 뜻이다. 박스 좌표 자체의 손실 함수를 개선한 [GIoU](#/p/giou) 같은 연구는 바로 이 위치 정확도(높은 IoU 임계값에서의 AP) 문제를 겨냥한다.'}
],

math:[
 {tex:'\\text{IoU}=\\dfrac{|B_{pred}\\cap B_{gt}|}{|B_{pred}\\cup B_{gt}|}', expr:'Intersection over Union', d:'두 박스의 교집합 넓이를 합집합 넓이로 나눈 값, 0~1 범위. IoU 가 임계값(예: 0.5) 이상이어야 그 예측을 정답과 매치된 TP 로 센다.'},
 {tex:'\\text{AP}=\\int_0^1 p(r)\\,dr', expr:'Average Precision', d:'재현율 $r$ 을 0에서 1까지 움직일 때 정밀도 $p(r)$ 곡선 아래 면적(실제 계산은 이산적인 재현율 지점에서 보간해 근사). mAP 는 이 AP 를 클래스 전체에 대해 평균한 것.'},
 {tex:'\\text{AP}_{\\text{COCO}}=\\dfrac1{10}\\sum_{t\\in\\{.5,.55,\\dots,.95\\}}\\text{AP}_t', expr:'COCO mAP', d:'IoU 임계값 $t$ 를 0.5부터 0.95까지 0.05 간격으로 10개 잡아 각각 AP 를 구하고 평균한다. $\\text{AP}_{50}=\\text{AP}_{t=0.5}$ 하나만 쓰는 것과는 값의 성격이 다르다.'}
],

confuse:[
 {a:'AP50 (VOC 관행)', b:'COCO mAP (AP@[.5:.95])', d:'AP50 은 IoU 0.5 하나만 기준, COCO mAP 는 0.5~0.95 열 개 임계값의 평균이라 박스 위치 정확도에 훨씬 엄격하다. 같은 모델도 두 수치가 크게 다르니 논문 비교 시 반드시 어느 쪽인지 확인해야 한다.'},
 {a:'IoU', b:'AP/mAP', d:'IoU 는 박스 하나 대 박스 하나의 겹침 비율(매치 판정 기준)이고, AP/mAP 는 전체 데이터셋에 대한 탐지기 성능 요약 지표다. IoU 임계값은 AP 를 계산하는 "재료" 중 하나일 뿐이다.'}
],

pitfalls:[
 '"mAP" 라는 표기만 보고 VOC AP50 인지 COCO AP@[.5:.95] 인지 확인하지 않으면 서로 다른 모델을 잘못 비교하게 된다.',
 'AP50 이 높다고 해서 박스 위치가 정확하다는 뜻은 아니다 — IoU 0.5 는 상당히 느슨한 기준이다.',
 '데이터셋(VOC/COCO/자체 데이터)이 다르면 클래스 수·난이도가 달라 mAP 숫자 자체를 직접 비교할 수 없다.'
],

papers:['giou','yolov3','coco-captions'],
terms:['precision-recall','confusion-matrix','ablation']
});
