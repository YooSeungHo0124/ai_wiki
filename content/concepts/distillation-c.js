WIKI.concept({
slug:'distillation-c',

tldr:'크고 정확한 교사 모델의 출력 확률분포(소프트 타깃)를 작은 학생 모델이 모방하도록 학습시켜, 정답 라벨만으로 학습할 때보다 더 많은 정보를 전달하는 압축 기법이다.',

why:'모바일·엣지 배포나 서빙 비용 절감을 위해 큰 모델을 그대로 못 쓸 때, "작은 모델을 처음부터 학습"과 "큰 모델에서 증류"의 성능 차이는 크다. temperature 하나 잘못 잡으면 증류 효과가 거의 안 나는데 그 이유를 모르면 디버깅이 안 된다.',

sections:[
 {h:'왜 소프트 타깃이 하드 라벨보다 나은가', d:'하드 라벨(원-핫)은 "정답은 고양이, 나머지는 전부 0"이라는 정보만 준다. 교사 모델의 소프트 타깃(softmax 확률)은 "고양이 0.7, 개 0.25, 자동차 0.0001" 처럼 **정답이 아닌 클래스들 사이의 상대적 유사도**까지 담고 있다. [지식 증류](#/p/distillation) 논문은 이 클래스 간 관계 정보를 "암흑 지식(dark knowledge)"이라 불렀다 — 정답 라벨에는 전혀 없지만 교사가 학습 과정에서 습득한, 클래스들이 서로 얼마나 헷갈리는지에 대한 정보다. 학생은 이 정보 덕분에 하드 라벨만으로 학습할 때보다 더 매끄럽고 일반화되는 결정 경계를 배운다.'},
 {h:'temperature 의 역할', d:'교사의 logit $z_i$ 를 그냥 softmax 하면 정답 클래스 확률이 1에 가깝게 뾰족해져서 다른 클래스들의 상대적 정보가 거의 사라진다. 그래서 온도 $T>1$ 로 나눠 분포를 평평하게 편 뒤 학생에게 넘긴다. $T$ 가 클수록 작은 확률을 가진("헷갈렸던") 클래스들의 정보가 살아나 학생이 더 많은 암흑 지식을 받는다. 학생을 학습할 때도 같은 $T$ 로 softmax 한 뒤 [KL divergence](#/c/loss-function) 나 교차 엔트로피로 학생-교사 분포 차이를 줄이는데, 이 손실의 경사 크기가 $T^2$ 에 비례해 작아지므로 손실 앞에 $T^2$ 를 곱해 보정하는 것이 원 논문의 처방이다.'},
 {h:'학습 구성', d:'보통 두 손실을 섞는다 — ① 학생 출력과 교사의 소프트 타깃 사이의 증류 손실(위 온도 적용), ② 학생 출력과 실제 하드 라벨 사이의 표준 교차 엔트로피. 두 손실의 가중치 비율은 과제마다 튜닝 대상이다. 교사는 학생 학습 동안 가중치가 고정되며(추론만), 이 때문에 증류는 별도의 "정답 생성 단계"로 취급되기도 한다.'},
 {h:'어디까지 옮기는가', d:'가장 흔한 형태는 출력 로짓만 증류하지만, 중간 특징 맵이나 어텐션 패턴까지 맞추도록 확장한 방법들도 많다([DistilBERT](#/p/distilbert)는 층 수를 줄이면서 임베딩·은닉 상태 유사도까지 함께 맞춘다). [MobileSAM](#/p/mobile-sam)처럼 무거운 인코더만 증류해서 교체하고 나머지 파이프라인은 그대로 두는 부분 증류도 실무에서 흔히 쓰인다.'}
],

math:[
 {tex:'p_i=\\dfrac{\\exp(z_i/T)}{\\sum_j\\exp(z_j/T)}', expr:'temperature softmax', d:'$z_i$ 는 클래스 $i$ 의 logit. $T=1$ 이면 보통의 softmax, $T$ 가 커질수록 분포가 평평해져 정답이 아닌 클래스들의 확률 차이가 더 잘 드러난다.'},
 {tex:'L=\\alpha\\, T^2\\, \\text{KL}(p_T^{teacher}\\,\\|\\,p_T^{student}) + (1-\\alpha)\\,\\text{CE}(y,\\,p_1^{student})', expr:'distillation loss', d:'앞항은 온도 $T$ 로 부드럽게 만든 교사-학생 분포 차이(KL divergence), 뒷항은 $T=1$ 에서의 통상적 하드 라벨 교차 엔트로피. $\\alpha$ 는 두 손실의 가중치이고, $T^2$ 는 온도가 높을수록 작아지는 그래디언트 크기를 보정한다.'}
],

diagram:{type:'flow', cap:'교사의 확률분포가 학생에게 전달되는 경로.',
 nodes:[
  {t:'교사 모델(고정)', s:'큰 네트워크'},
  {t:'logit / T', s:'온도로 분포 평활화'},
  {t:'소프트 타깃', s:'암흑 지식 포함'},
  {t:'학생 모델', s:'작은 네트워크, 학습 대상'},
  {t:'KL + CE 손실', s:'소프트+하드 타깃 동시 맞춤'}
 ]},

confuse:[
 {a:'지식 증류', b:'[PEFT](#/c/peft)/[양자화](#/c/quantization)', d:'증류는 학생이라는 **별도의 더 작은 모델**을 새로 학습시켜 압축한다. 양자화는 같은 모델의 가중치 표현 정밀도를 줄이고, PEFT는 모델 크기는 그대로 두고 학습 파라미터만 줄인다. 셋 다 "효율화"로 묶이지만 손대는 대상이 아키텍처(증류)냐 수치 표현(양자화)이냐 학습 방식(PEFT)이냐가 다르며, 실무에서는 증류 후 양자화처럼 함께 쓰기도 한다.'},
 {a:'소프트 타깃', b:'라벨 스무딩(label smoothing)', d:'둘 다 원-핫이 아닌 부드러운 타깃을 쓰지만, 소프트 타깃은 교사 모델이 실제로 계산한 클래스 간 상대적 유사도를 담고 있고, 라벨 스무딩은 정답 클래스에서 일정 비율을 떼어 나머지 클래스에 **균등하게** 나눠주는 고정된 규칙이다. 라벨 스무딩은 클래스 간 관계 정보를 전혀 담지 못한다는 점이 소프트 타깃과의 핵심 차이다.'}
],

pitfalls:[
 'temperature 를 너무 높이면 분포가 지나치게 평평해져 정답 신호 자체가 희석된다 — 보통 2~10 사이에서 과제별로 탐색하는 관행이 있다.',
 '교사가 틀린 예측에 대해서도 높은 확신을 보이면 그 오류까지 학생에게 그대로 전달된다 — 증류는 교사 성능의 상한을 넘기 어렵다.',
 '학생 구조가 교사와 너무 다르면(예: 레이어 수·너비 차이가 극단적) 증류 손실만으로는 잘 따라가지 못해 중간 특징 매칭 같은 추가 장치가 필요할 수 있다.'
],

papers:['distillation','distilbert','mobile-sam'],
terms:['quantization','pruning','peft']
});
