WIKI.concept({
slug:'quantization',

tldr:'모델의 가중치·활성값을 32/16비트 부동소수점 대신 8비트·4비트 정수 등 더 적은 비트로 표현해 메모리와 연산량을 줄이는 기법으로, 정밀도를 잃는 대가로 속도와 용량을 얻는다.',

why:'7B~70B 파라미터 LLM을 개인 GPU나 엣지 기기에 올리는 것은 양자화 없이는 사실상 불가능하다. INT8·INT4·PTQ·QAT 같은 용어가 모델 카드에 나올 때 무엇을 얼마나 잃는지 가늠하지 못하면 배포 결정을 잘못 내리기 쉽다.',

sections:[
 {h:'정수로 표현한다는 것', d:'FP16 가중치 값의 범위 $[\\min,\\max]$ 를 정해진 정수 구간(INT8이면 -128~127, INT4면 -8~7)에 선형으로 매핑한다. 스케일 $s$ 와 영점(zero-point) $z$ 를 구해 $q=\\text{round}(x/s)+z$ 로 양자화하고, 추론 때는 $x\\approx s(q-z)$ 로 역양자화하거나 정수 연산을 그대로 활용한다. 비트 수가 줄수록 표현 가능한 값의 개수가 줄어 원래 값과의 오차(양자화 오차)가 커진다.'},
 {h:'대칭 vs 비대칭', d:'대칭(symmetric) 양자화는 영점을 0으로 고정해 $[-c,c]$ 범위를 대칭적으로 매핑한다 — 계산이 단순하고 가중치처럼 분포가 0 중심에 가까운 값에 적합하다. 비대칭(asymmetric) 양자화는 영점을 따로 두어 $[\\min,\\max]$ 를 그대로 매핑한다 — ReLU 이후 활성값처럼 0 이상으로 치우친 분포를 표현할 때 오차가 작다. 활성값은 비대칭, 가중치는 대칭을 쓰는 조합이 흔하다.'},
 {h:'PTQ vs QAT', d:'사후 양자화(Post-Training Quantization, PTQ)는 이미 학습이 끝난 FP 모델을 가져와 소량의 보정(calibration) 데이터로 스케일만 추정해 변환한다 — 재학습이 필요 없어 빠르고 싸지만 저비트(4비트 이하)로 갈수록 정확도 손실이 커진다. 양자화 인식 학습(Quantization-Aware Training, QAT)은 학습(또는 추가 미세조정) 중에 순전파에서 양자화-역양자화를 시뮬레이션해 모델이 양자화 오차를 감안한 가중치를 배우게 한다 — 정확도는 더 잘 보존되지만 학습 파이프라인과 시간이 추가로 든다. LLM은 재학습 비용이 커서 PTQ 계열([GPTQ](#/p/gptq)·[AWQ](#/p/awq))이 주류다.'},
 {h:'그룹 단위 양자화', d:'가중치 전체에 스케일 하나를 쓰면(per-tensor) 값 분포가 넓을 때 오차가 크다. 그래서 채널 단위(per-channel)나 더 세밀하게 128개 값씩 묶은 그룹 단위(per-group/block)로 각기 다른 스케일을 둔다. 세밀할수록 정확도는 좋아지지만 스케일 값 자체를 저장하는 오버헤드가 늘어나므로, 압축률과 정확도의 절충으로 그룹 크기(예: 128)를 정한다.'},
 {h:'대표 기법', d:'[LLM.int8()](#/p/llm-int8)은 대부분의 값을 INT8로 양자화하되 극단적으로 큰 값(outlier)이 있는 채널만 FP16 그대로 남겨 정확도를 지킨다. [GPTQ](#/p/gptq)는 레이어별로 2차 정보(헤시안 근사)를 이용해 양자화 오차를 다음 가중치로 보정해가며 순차적으로 4비트까지 정확도를 유지한다. [AWQ](#/p/awq)는 모든 가중치가 똑같이 중요하지 않다는 관찰에서 출발해, 활성값 크기 기준으로 중요한 소수의 채널을 스케일링으로 보호하고 나머지를 공격적으로 양자화한다.'}
],

math:[
 {tex:'q=\\text{clip}\\Big(\\text{round}\\big(\\tfrac{x}{s}\\big)+z,\\;q_{min},\\,q_{max}\\Big)', expr:'linear quantization', d:'$x$ 는 원래 값, $s$ 는 스케일(구간 폭), $z$ 는 영점. 대칭 양자화는 $z=0$ 으로 고정한다. $q_{min},q_{max}$ 는 비트 수로 정해지는 정수 범위(INT8이면 -128, 127).'}
],

diagram:{type:'compare', cap:'같은 4비트라도 학습 파이프라인 참여 여부가 다르다.',
 left:{t:'PTQ (사후)', items:['학습 완료된 모델에 적용','소량 보정 데이터로 스케일만 추정','빠르고 저렴, 저비트에서 손실 큼']},
 right:{t:'QAT (학습 중)', items:['학습/미세조정 중 양자화 시뮬레이션','모델이 오차를 감안해 학습','정확도 우수, 비용·시간 더 듦']}},

confuse:[
 {a:'양자화', b:'[가지치기](#/c/pruning)', d:'양자화는 값의 **정밀도**(비트 수)를 줄이고, 가지치기는 값(가중치)의 **개수**를 줄인다. 서로 독립적이라 함께 적용 가능하다 — 가지치기로 구조를 줄인 뒤 양자화로 남은 가중치의 비트를 더 줄이는 식이다.'},
 {a:'PTQ', b:'QAT', d:'PTQ는 재학습 없이 이미 학습된 가중치에 사후 처리로 스케일을 씌운다. QAT는 학습 루프 안에 양자화 연산을 넣어 그래디언트가 양자화 오차를 반영하며 흐르게 한다(직선 관통 추정기straight-through estimator로 미분 불가능한 round 를 우회). 저비트로 갈수록 QAT 의 이점이 커진다.'},
 {a:'INT8 양자화', b:'FP16/BF16 캐스팅', d:'FP16/BF16은 여전히 부동소수점이라 표현 방식(지수+가수)이 FP32와 같고 정밀도만 낮춘 것이다. INT8/INT4 양자화는 아예 정수로 바꾸는 것이라 스케일·영점이라는 추가 정보가 필요하고, 정수 연산 하드웨어(텐서 코어 등)를 활용해 속도 이득이 더 크다.'}
],

pitfalls:[
 '"양자화해도 정확도는 거의 그대로"는 비트 수와 모델·과제에 따라 다르다 — INT8은 보통 손실이 미미하지만 INT4 이하로 가면 특정 벤치마크(특히 추론·수학)에서 눈에 띄는 저하가 보고된다.',
 '가중치만 양자화하고 활성값은 FP로 두는 weight-only 양자화(GPTQ·AWQ 계열)는 메모리는 줄이지만 연산 자체가 정수로 가속되지 않으면 속도 이득이 제한적일 수 있다 — 무엇을 줄이는지(메모리 vs 연산 속도)를 구분해야 한다.',
 '보정 데이터의 분포가 실제 서빙 데이터와 다르면 PTQ 의 스케일 추정이 틀어져, 벤치마크 상에서는 괜찮아 보여도 실제 입력에서 정확도가 떨어질 수 있다.'
],

papers:['llm-int8','gptq','bitnet'],
terms:['pruning','distillation-c','peft']
});
