WIKI.concept({
slug:'supervised-tasks',

tldr:'분류(classification)는 이산적인 범주를 맞히는 지도학습이고 회귀(regression)는 연속적인 실수값을 맞히는 지도학습이다 — 둘 다 정답 라벨이 있다는 점은 같다.',

why:'문제를 처음 정의할 때 "이건 분류인가 회귀인가"를 잘못 고르면 손실함수·출력층·평가지표가 통째로 어긋난다. 연속값을 구간으로 잘라 분류로 풀지, 그대로 회귀로 풀지는 데이터 성격과 요구되는 출력 형태에 따라 갈리는 실전 판단이다. 이 구분은 [고전 기계학습](#/c/decision-tree) 뿐 아니라 딥러닝에서도 그대로 이어진다 — 이미지 분류, 객체 검출의 클래스 예측, 언어모델의 다음 토큰 예측은 전부 분류이고, 바운딩 박스 좌표 예측이나 깊이 추정은 회귀다. 같은 신경망 안에서도 헤드에 따라 분류 손실과 회귀 손실이 함께 쓰이는 경우가 흔해, 이 구분을 명확히 못 하면 멀티태스크 모델의 손실 설계 자체를 못 한다.',

sections:[
 {h:'분류와 회귀의 경계', d:'분류는 출력이 유한한 범주 집합 중 하나(스팸/정상, 개/고양이/새) 이고, 회귀는 출력이 실수 전체 또는 구간(집값, 온도, 잔여 수명)이다. 이 둘은 출력의 형태로 구분하지 모델 종류로 구분하지 않는다 — 같은 [결정 트리](#/c/decision-tree)·[랜덤 포레스트](#/c/random-forest-c)·[k-NN](#/c/knn) 이 분류기로도 회귀기로도 쓰인다.'},
 {h:'손실함수가 갈리는 이유', d:'분류는 보통 [교차 엔트로피](#/c/entropy-kl) 손실을 쓰고 마지막 층에 [softmax](#/c/softmax)(다중 클래스) 또는 시그모이드(이진)를 둔다. 회귀는 평균제곱오차(MSE)나 평균절대오차(MAE)를 쓰고 마지막 층에 활성화 함수를 두지 않는 게 보통이다 — 값의 범위를 인위적으로 제한하면 안 되기 때문이다.'},
 {h:'평가지표도 다르다', d:'분류는 [정확도·정밀도·재현율](#/c/precision-recall), [AUROC](#/c/auroc) 처럼 맞았는지 틀렸는지를 세는 지표를 쓴다. 회귀는 RMSE, MAE, R² 처럼 "얼마나 가까웠는지" 거리를 재는 지표를 쓴다. 분류 지표를 회귀 문제에, 혹은 그 반대로 잘못 적용하면 숫자는 나오지만 의미가 없다. RMSE 는 MSE 에 제곱근을 씌운 것으로 타깃과 같은 단위로 오차를 볼 수 있어 MAE 보다 실무 보고에 더 자주 쓰이지만, 제곱을 거치는 만큼 MAE 보다 이상치에 더 민감하다는 점도 함께 알아둬야 한다.'},
 {h:'경계가 애매한 경우', d:'나이 예측처럼 본질적으로 연속값이지만 "20대/30대/40대" 처럼 구간으로 나눠 분류로 풀 수도 있다. 이렇게 하면 해석이 쉬워지고 클래스 불균형 도구를 쓸 수 있지만, 구간 경계에서 정보가 손실된다(39세와 41세가 다른 클래스로 갈린다). 순서가 있는 범주(평점 1~5)는 회귀와 분류 사이의 순서형 회귀(ordinal regression)로 따로 다루기도 한다.'},
 {h:'출력부터 정한다', d:'"이 문제에 [SVM](#/c/svm-c) 을 쓸까 [부스팅](#/c/boosting) 을 쓸까"는 분류·회귀 구분보다 한 단계 뒤의 결정이다 — 거의 모든 고전 알고리즘([결정 트리](#/c/decision-tree), [k-NN](#/c/knn), [랜덤 포레스트](#/c/random-forest-c))은 분류·회귀 버전을 둘 다 갖고 있고, sklearn 에서도 Classifier/Regressor 접미사로만 구분된다. 먼저 출력 형태(분류냐 회귀냐)를 정하고, 그다음에 어떤 알고리즘 계열을 쓸지 고르는 순서가 맞다.'}
],

math:[
 {tex:'L_{cls}=-\\sum_k y_k\\log \\hat p_k,\\quad L_{reg}=\\dfrac1n\\sum_i(y_i-\\hat y_i)^2', expr:'분류·회귀 손실 비교', d:'분류의 교차 엔트로피는 확률 벡터 $\\hat p$ 와 원-핫 정답 $y$ 사이의 "정보량 차이"를 재고, 회귀의 MSE 는 실수 예측 $\\hat y_i$ 와 실제값 $y_i$ 사이의 "거리"를 잰다. 두 손실은 애초에 다른 종류의 오차를 측정하므로 값 자체를 서로 비교하는 것은 의미가 없다.'}
],

diagram:{type:'compare', cap:'같은 문제도 출력 형태에 따라 다른 길로 간다.',
 left:{t:'분류 (classification)', items:['출력: 이산 범주','손실: 교차 엔트로피','지표: 정확도·F1·AUROC']},
 right:{t:'회귀 (regression)', items:['출력: 연속 실수','손실: MSE·MAE','지표: RMSE·MAE·R²']}},

confuse:[
 {a:'분류', b:'순서형 회귀', d:'분류는 범주 사이에 순서가 없다고 가정한다(개-고양이-새는 순서가 없다). 평점처럼 순서가 있는 범주를 일반 분류로 풀면 "1점과 5점을 헷갈린 것"과 "4점과 5점을 헷갈린 것"을 똑같이 취급하는 손실을 쓰게 되어 손해다.'},
 {a:'회귀', b:'구간을 나눈 분류', d:'연속값을 구간으로 잘라 분류로 풀면 클래스 불균형 기법을 쓸 수 있고 결과 해석이 쉬워지지만, 구간 경계 근처의 미세한 차이를 다 뭉갠다. 실제 오차가 중요하면(가격 예측 등) 회귀를 우선 고려해야 한다.'},
 {a:'다중 클래스 분류', b:'다중 레이블 분류', d:'다중 클래스는 여러 범주 중 정확히 하나를 고르는 문제(softmax, 확률 합=1)이고, 다중 레이블은 한 샘플이 여러 범주에 동시에 속할 수 있는 문제(각 클래스마다 독립 시그모이드, 확률 합이 1일 필요 없음)다. 태그 분류처럼 "동시에 여러 개가 맞을 수 있는" 문제에 softmax 를 쓰면 클래스들이 서로 확률을 뺏어가는 잘못된 가정이 들어간다.'},
 {a:'지도학습의 분류·회귀', b:'[지도의 종류](#/c/supervision-types)', d:'분류·회귀는 "출력이 무엇인가"를 가르는 축이고, 지도의 종류(완전지도·약지도·자기지도 등)는 "라벨을 어떻게 얻었는가"를 가르는 축이다. 두 축은 독립적이다 — 자기지도로 학습한 표현 위에도 최종적으로는 분류 또는 회귀 헤드가 붙는 경우가 대부분이다.'}
],

pitfalls:[
 '회귀 문제에 정확도(accuracy) 지표를 쓰려 하는 경우가 있다 — 연속값에는 애초에 "맞았다/틀렸다"가 정의되지 않는다.',
 '다중 클래스 분류에 회귀용 MSE 손실을 그대로 쓰면 클래스 번호 사이에 없는 순서·거리 관계를 모델이 학습해버린다(클래스 3이 클래스 1과 클래스 5의 중간이라고 착각).',
 '연속값을 구간으로 나눠 분류로 바꿀 때 구간 개수와 경계를 임의로 정하면 평가 결과가 그 선택에 크게 좌우된다.',
 '다중 레이블 문제에 다중 클래스용 softmax 를 그대로 쓰는 실수가 흔하다 — "이 샘플은 액션이면서 동시에 코미디"인 경우를 표현할 수 없게 된다.',
 '회귀 타깃의 분포가 한쪽으로 크게 치우쳐 있으면(예: 가격이 로그정규분포) MSE 를 원본 스케일에 그대로 적용하기보다 로그 변환 후 학습하는 것이 유리한 경우가 많은데, 이를 놓치고 원본 스케일로만 학습하는 경우가 있다.'
],

code:{lang:'python', d:'같은 sklearn 트리 모델이 분류·회귀로 이름만 바뀌는 예',
 src:'from sklearn.tree import DecisionTreeClassifier, DecisionTreeRegressor\n\n# 분류: 출력이 이산 범주\nclf = DecisionTreeClassifier(max_depth=5)\nclf.fit(X_train, y_class)          # y_class: 0/1/2 ...\n\n# 회귀: 출력이 연속값\nreg = DecisionTreeRegressor(max_depth=5)\nreg.fit(X_train, y_price)          # y_price: 37.5, 102.0 ...\n# 알고리즘(트리 분할 방식)은 같고 분할 기준과 잎 값만 다르다'},

papers:['lenet','xgboost'],
terms:['decision-tree','random-forest-c','precision-recall','entropy-kl','supervision-types']
});
