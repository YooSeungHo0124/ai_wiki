WIKI.concept({
slug:'bbox-nms',

tldr:'바운딩 박스는 좌표 네 개로 물체 위치를 표현하는 방식이고, 앵커는 미리 깔아둔 후보 박스이며, NMS는 겹치는 중복 예측을 IoU 기준으로 걸러내는 후처리다.',

why:'객체 탐지 코드를 읽으면 xyxy·xywh 변환, anchor 생성, nms_threshold 같은 하이퍼파라미터가 곳곳에 나온다. 이 셋의 역할을 모르면 왜 같은 모델이 설정 하나로 박스가 두 배로 뜨거나 절반이 사라지는지 디버깅할 수 없다.',

sections:[
 {h:'좌표 표현', d:'박스는 보통 두 가지 방식으로 저장한다. xyxy는 좌상단과 우하단 좌표$(x_1,y_1,x_2,y_2)$, xywh는 중심 좌표와 너비·높이$(c_x,c_y,w,h)$다. 손실 계산은 IoU 기반이라 xywh가 편하고, NMS나 크롭 연산은 xyxy가 편해서 파이프라인 안에서 두 표현을 계속 변환한다. 또 픽셀 절대값으로 쓸지, 이미지 크기로 나눈 0~1 정규화 좌표로 쓸지도 프레임워크마다 달라 좌표 버그의 절반은 이 변환 단계에서 난다.'},
 {h:'앵커란 무엇인가', d:'초기 탐지기는 이미지 전체를 슬라이딩 윈도로 훑으며 "여기 물체가 있나"를 일일이 물어야 해서 느렸다. 앵커(anchor)는 각 특징 맵 위치마다 미리 정해둔 크기·비율의 박스 여러 개(보통 3~9개)를 깔아두고, 모델은 "이 앵커를 정답 쪽으로 얼마나 옮기고 늘릴까"라는 상대적인 회귀 문제만 풀게 한다. [Faster R-CNN](#/p/faster-rcnn)이 이 RPN(Region Proposal Network) 앵커 방식을 정착시켰고, [YOLOv3](#/p/yolov3)는 데이터셋에서 k-means로 앵커 크기를 미리 뽑아 쓴다. 앵커가 물체 크기·비율 분포와 안 맞으면 회귀 부담이 커져 성능이 떨어지므로, 데이터셋이 바뀌면 앵커도 다시 튜닝해야 한다.'},
 {h:'NMS: 중복 제거', d:'앵커 기반이든 아니든 탐지기는 같은 물체 하나에 대해 신뢰도(confidence)가 조금씩 다른 박스를 여러 개 쏟아낸다. NMS(Non-Maximum Suppression)는 이걸 하나로 줄이는 절차다. (1) 모든 예측 박스를 클래스별로 신뢰도 점수 내림차순 정렬한다. (2) 점수가 가장 높은 박스를 최종 결과로 채택하고 목록에서 뺀다. (3) 남은 박스 중 방금 채택한 박스와 IoU 가 임계값(흔히 0.45~0.5) 이상인 것을 모두 버린다 — 같은 물체를 가리키는 중복으로 간주해서다. (4) 남은 박스가 없을 때까지 (2)~(3)을 반복한다. IoU 임계값을 낮추면 겹친 물체 두 개를 하나로 뭉개버리기 쉽고(재현율 손해), 높이면 같은 물체의 중복 박스가 살아남기 쉽다(정밀도 손해) — 이 트레이드오프를 데이터 특성(물체가 서로 얼마나 붙어 있는가)에 맞춰 조정한다.'},
 {h:'Soft-NMS와 변형', d:'표준 NMS는 임계값을 넘는 박스를 아예 삭제해, 군중처럼 물체가 심하게 겹친 장면에서 옆 물체를 통째로 지워버릴 수 있다. Soft-NMS는 삭제 대신 IoU가 클수록 점수를 서서히 깎아 완전히 지우지 않고 살려둔다. 또 실무 구현은 보통 클래스별 NMS를 쓴다 — 사람 박스와 자전거 박스는 겹쳐도 서로 다른 물체이니 억제 대상에서 제외하고, 같은 클래스끼리만 비교한다. 클래스 구분 없이 전체를 한 번에 NMS 하면 겹쳐 있는 서로 다른 물체를 하나로 잘못 지우는 사고가 난다.'}
],

math:[
 {tex:'\\text{IoU}(B_1,B_2)=\\dfrac{|B_1\\cap B_2|}{|B_1\\cup B_2|}', expr:'Intersection over Union', d:'NMS가 "같은 물체의 중복"인지 판단하는 유일한 기준. [IoU·mAP](#/c/iou-map)에서 다룬 것과 같은 식이며, 여기서는 정답과의 비교가 아니라 예측 박스끼리의 비교에 쓰인다.'}
],

diagram:{type:'flow', cap:'NMS는 점수 최고 박스를 채택하고 겹치는 후보를 지우는 과정을 반복한다.',
 nodes:[
  {t:'후보 박스 전체', s:'클래스별 신뢰도 정렬'},
  {t:'최고 점수 채택', s:'결과에 추가'},
  {t:'IoU 임계 초과 제거', s:'중복으로 간주'},
  {t:'반복', s:'후보 소진까지'}
 ]},

confuse:[
 {a:'앵커(anchor)', b:'예측 박스(prediction)', d:'앵커는 학습 전에 미리 정해둔 고정된 후보 틀이고, 예측 박스는 모델이 앵커를 기준으로 얼마나 옮기고 크기를 바꿀지 회귀한 최종 출력이다. "앵커 개수"와 "예측 박스 개수"는 NMS 이전 단계에선 같지만 이후엔 크게 줄어든다.'},
 {a:'confidence threshold', b:'NMS IoU threshold', d:'confidence threshold는 애초에 후보로 볼지 말지를 점수로 거르는 문턱값이고, NMS IoU threshold는 살아남은 후보들끼리 겹침을 보고 중복을 거르는 문턱값이다. 박스가 아예 안 뜨면 전자를, 같은 물체가 중복으로 뜨면 후자를 먼저 의심한다.'},
 {a:'xyxy', b:'xywh', d:'같은 박스를 표현하는 두 좌표계일 뿐 정보량은 같다. 변환 코드 한 줄을 빠뜨리면 박스가 화면 밖으로 튀거나 크기가 반으로 줄어드는데, 겉보기엔 모델 버그처럼 보여 헷갈리기 쉽다.'}
],

pitfalls:[
 'NMS를 클래스 구분 없이 전체 박스에 적용하면 겹쳐 있는 서로 다른 클래스 물체(사람+가방 등)가 하나로 지워질 수 있다.',
 'IoU 임계값을 낮추면 정밀도는 오르지만 군중·밀집 장면에서 재현율이 크게 떨어진다 — 데이터의 겹침 정도를 보고 정해야 한다.',
 '정규화 좌표(0~1)와 픽셀 좌표를 섞어 쓰면 NMS의 IoU 계산 자체가 틀어져 조용히 잘못된 결과를 낸다.',
 '앵커 프리 모델([FCOS](#/p/fcos), [CenterNet](#/p/centernet))도 NMS는 대부분 여전히 필요하다 — 앵커가 없다고 후처리 중복 제거까지 없어지는 건 아니다.'
],

papers:['faster-rcnn','yolov3','diou'],
terms:['iou-map','anchor-free','multi-scale']
});
