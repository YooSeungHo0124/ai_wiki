WIKI.concept({
slug:'cost-optimization',

tldr:'추론 비용은 모델 크기·요청당 계산량·하드웨어 활용률의 곱으로 결정되며, 실무에서는 효과가 큰 순서대로 모델 선택 → 양자화/증류 → 배칭/캐싱 → 조기 종료를 검토한다.',

why:'학습 비용은 한 번 지불하지만 추론 비용은 서비스가 살아있는 한 요청마다 계속 청구된다 — 트래픽이 커질수록 추론 비용이 전체 비용의 대부분을 차지하는 경우가 흔하다. 어떤 최적화를 먼저 시도해야 하는지 순서를 모르면, 효과가 작은 기법에 몇 주를 쓰고도 정작 큰 비용은 그대로 남는 일이 생긴다.',

sections:[
 {h:'1순위: 모델 선택', d:'가장 큰 레버는 애초에 필요 이상으로 큰 모델을 쓰지 않는 것이다. 작업이 실제로 요구하는 성능 수준을 먼저 정하고, 그 수준을 만족하는 가장 작은 모델을 고른다. 큰 모델 하나로 모든 요청을 처리하는 대신, 쉬운 요청은 작은 모델로, 어려운 요청만 큰 모델로 넘기는 라우팅(모델 캐스케이드)도 같은 원리다 — 아키텍처 최적화보다 먼저 검토해야 할 만큼 비용 절감 폭이 크다.',},
 {h:'2순위: 양자화와 증류', d:'[양자화](#/c/quantization)는 가중치·활성값의 수치 정밀도를 낮춰(FP16 → INT8/INT4) 메모리와 연산량을 줄인다 — [GPTQ](#/p/gptq)처럼 정확도 손실을 최소화하며 4비트까지 낮추는 기법이 실무에서 널리 쓰인다. [지식 증류](#/p/distillation)는 큰 모델(교사)의 지식을 작은 모델(학생)로 옮겨, 아예 더 작은 모델로 비슷한 성능을 내게 만든다. 둘 다 모델 자체의 크기·연산량을 줄이는 접근이라 효과가 크고, 한 번 적용하면 이후 모든 요청에 계속 이득이 누적된다.'},
 {h:'3순위: 캐싱과 배칭', d:'캐싱은 같은(또는 비슷한) 요청에 대한 결과를 재사용해 계산 자체를 건너뛴다 — LLM 서빙의 [KV 캐시](#/c/kv-cache)는 매 토큰마다 이전 토큰들의 어텐션을 다시 계산하지 않도록 재사용하는 것으로, 자기회귀 생성 비용을 대폭 낮춘다. 배칭은 여러 요청을 묶어 GPU 활용률을 높인다 — [vLLM](#/p/vllm)의 연속 배칭처럼 서빙 엔진 수준의 최적화로, 모델을 전혀 바꾸지 않고도 처리량을 크게 늘릴 수 있다. 이미 양자화/증류된 모델 위에 적용하면 이득이 배가된다.'},
 {h:'4순위: 조기 종료와 적응적 연산', d:'입력의 난이도에 따라 계산량을 동적으로 줄이는 기법이다 — 쉬운 입력은 신경망의 앞쪽 레이어만 통과하고 조기에 출력을 내는 조기 종료(early exit), 생성이 뻔한 구간은 작은 모델이 초안을 만들고 큰 모델이 검증만 하는 [추측 디코딩](#/c/speculative) 등. 이런 기법들은 구현 복잡도가 높고 적용 가능한 모델 구조가 제한적이라, 앞선 세 단계로 충분하지 않을 때 추가로 고려하는 것이 실무적이다.'}
],

diagram:{type:'flow', cap:'추론 비용 최적화를 검토하는 순서(효과 순).',
 steps:['필요 이상 큰 모델 쓰지 않기','양자화·증류로 모델 자체 축소','캐싱·배칭으로 활용률 개선','조기 종료 등 적응적 연산']},

confuse:[
 {a:'양자화(quantization)', b:'[증류](#/c/distillation-c)', d:'양자화는 같은 모델의 가중치를 더 낮은 정밀도의 숫자로 표현하는 것이고, 증류는 아예 더 작은(레이어 수·차원이 다른) 모델을 새로 학습시켜 원래 모델을 흉내 내게 하는 것이다. 양자화는 학습이 거의 필요 없어 빠르게 적용할 수 있고, 증류는 별도 학습 과정이 필요하지만 더 근본적인 크기 축소가 가능하다. 둘은 함께 적용할 수 있다(증류한 작은 모델을 다시 양자화).'},
 {a:'캐싱', b:'배칭', d:'캐싱은 "같은 계산을 반복하지 않는 것"(계산량 자체를 줄임)이고, 배칭은 "여러 계산을 동시에 처리하는 것"(단위 시간당 처리량을 늘림)이다. 캐싱은 계산을 아예 생략하지만, 배칭은 계산은 그대로 하되 하드웨어를 더 효율적으로 쓴다는 점에서 메커니즘이 다르다.'},
 {a:'처리량(throughput) 최적화', b:'지연시간(latency) 최적화', d:'배칭은 대개 처리량을 늘리지만 개별 요청의 지연시간을 늘릴 수 있고, 캐싱·양자화·증류는 지연시간과 처리량을 동시에 개선하는 경우가 많다 — 비용 최적화 기법을 고를 때 어느 지표를 우선하는지(서비스 특성에 따라 [배치 vs 실시간](#/c/batch-vs-realtime))를 먼저 정해야 한다.'}
],

pitfalls:[
 '아키텍처 수준 최적화(조기 종료, 커스텀 커널)부터 시도하는 것은 흔한 순서 착오다 — 모델 크기를 먼저 줄이거나 서빙 엔진의 배칭을 먼저 개선하는 쪽이 노력 대비 효과가 훨씬 큰 경우가 대부분이다.',
 '양자화·증류로 속도는 빨라졌지만 정확도 하락을 검증 없이 그대로 배포하면 안 된다 — 비용 최적화는 항상 "정확도 대비 비용" 트레이드오프이며, 어느 정도의 정확도 손실까지 허용할지를 먼저 정해야 한다.',
 '캐시 히트율을 측정하지 않고 캐싱을 도입하면, 실제로는 캐시가 거의 재사용되지 않는데(요청이 매번 달라서) 캐시 인프라 유지비만 늘어나는 경우가 생긴다.'
],

papers:['gptq','vllm','distillation'],
terms:['quantization','kv-cache','serving-arch','batch-vs-realtime']
});
