WIKI.concept({
slug:'gradient-descent',

tldr:'손실 함수의 경사(gradient)가 가리키는 방향의 반대로 파라미터를 조금씩 옮겨가며 손실을 줄이는 최적화 절차로, 배치/미니배치/확률적(SGD)의 차이는 경사를 몇 개 샘플로 추정하느냐에 있다.',

why:'딥러닝 학습의 바깥 루프는 결국 이거 하나다 — 경사를 구하고, 그 반대 방향으로 조금 움직인다. 배치 크기가 왜 학습률·수렴 안정성·일반화에 영향을 주는지, Adam 같은 옵티마이저가 이 위에 무엇을 얹은 것인지 이해하려면 미니배치 경사 하강이 표준이 된 이유부터 잡아야 한다.',

sections:[
 {h:'배치 경사 하강 (Batch GD)', d:'전체 학습 데이터로 경사를 정확히 계산한 뒤 파라미터를 한 번 갱신한다. 경사 추정이 정확해 수렴 경로가 매끄럽지만, 데이터가 클수록 한 스텝의 비용이 커지고 GPU 메모리에 전체를 올려야 한다는 문제가 있다.'},
 {h:'확률적 경사 하강 (SGD)', d:'샘플 **하나**로만 경사를 추정해 즉시 갱신한다. 스텝이 매우 싸고 빠르지만 추정 경사의 분산이 커서 손실 곡선이 요동친다. 이 노이즈가 역설적으로 얕은 극솟값(local minima)을 빠져나오게 돕는다는 관찰도 있다.'},
 {h:'미니배치 경사 하강이 표준이 된 이유', d:'미니배치(보통 32~4096개)는 배치의 정확성과 SGD 의 속도 사이 절충이다. 결정적으로, 미니배치 안의 샘플별 경사 계산은 GPU 의 행렬 연산으로 **병렬화**된다 — 배치 크기를 2배로 늘려도 GPU 가 놀지 않는 한 벽시계 시간은 거의 그대로다. 그래서 지금은 "SGD"라고 부를 때도 사실상 미니배치 SGD 를 가리키는 경우가 대부분이다.'},
 {h:'경사가 실제로 하는 일', d:'경사 $\\nabla_\\theta \\mathcal{L}$ 는 손실이 가장 가파르게 **증가**하는 방향이다. 그 반대로 학습률 $\\eta$ 만큼 움직이면 손실이 (국소적으로) 줄어든다. $\\eta$ 가 너무 크면 최솟값을 지나쳐 발산하고, 너무 작으면 학습이 지나치게 느리다 — 이 둘 사이를 자동으로 조절하려는 시도가 [적응적 옵티마이저](#/c/adaptive-optimizer)들이다.'}
],

math:[
 {tex:'\\theta_{t+1}=\\theta_t-\\eta\\,\\nabla_\\theta \\mathcal{L}(\\theta_t)\\qquad(\\text{batch: 전체 }N\\text{개 평균})',
  expr:'batch GD update', d:'$\\nabla_\\theta \\mathcal{L}=\\tfrac1N\\sum_{i=1}^N \\nabla_\\theta \\ell_i$ — 전체 데이터의 손실 평균에 대한 경사.'},
 {tex:'\\theta_{t+1}=\\theta_t-\\eta\\,\\nabla_\\theta \\ell_i(\\theta_t)\\qquad(\\text{SGD: 샘플 1개})',
  expr:'SGD update', d:'매 스텝마다 다른 샘플 하나의 경사만 쓴다. $\\mathbb{E}[\\nabla_\\theta\\ell_i]=\\nabla_\\theta\\mathcal{L}$ 이라 기댓값은 배치 경사와 같지만 분산이 크다.'},
 {tex:'\\theta_{t+1}=\\theta_t-\\eta\\,\\dfrac1{|B|}\\sum_{i\\in B}\\nabla_\\theta \\ell_i(\\theta_t)',
  expr:'mini-batch update', d:'$B$ 는 미니배치(크기 $|B|$, 보통 32~4096). 경사 추정의 분산이 배치 크기에 반비례($\\propto 1/|B|$)로 줄어든다 — 배치가 클수록 경사가 정확해지지만 스텝당 비용도 커진다.'}
],

diagram:{type:'compare', cap:'경사 추정에 쓰는 샘플 수가 정확도와 속도를 맞바꾼다.',
 left:{t:'SGD (샘플 1개)', items:['스텝당 계산 최소','경사 추정 분산 큼','손실 곡선이 요동침']},
 right:{t:'미니배치 GD (32~4096개)', items:['GPU 병렬 연산에 최적','경사 추정 분산 작음','벽시계 시간 거의 동일하게 확장']}},

confuse:[
 {a:'SGD (알고리즘)', b:'SGD (옵티마이저 이름)', d:'PyTorch 의 `optim.SGD` 는 실제로는 미니배치 경사 하강(+선택적 모멘텀)을 구현한다. "확률적(stochastic)"이라는 이름은 샘플 하나짜리 원조 알고리즘에서 왔지만, 실무에서 SGD 라고 하면 거의 항상 미니배치 버전을 뜻한다.'},
 {a:'경사 하강법', b:'[역전파](#/c/backprop-c)', d:'경사 하강법은 "경사를 알면 어느 방향으로 얼마나 움직일지"를 정하는 최적화 규칙이고, 역전파는 그 경사를 **계산**하는 알고리즘이다. 역전파가 경사를 구해주면 경사 하강법이 그걸로 파라미터를 갱신한다 — 서로 다른 층위의 개념.'},
 {a:'학습률', b:'배치 크기', d:'둘 다 스텝의 "크기"에 관여하지만 다른 축이다. 학습률은 경사 방향으로 얼마나 이동하는지, 배치 크기는 그 경사를 얼마나 정확하게 추정하는지를 정한다. 배치를 키우면 경사 분산이 줄어드는 만큼 학습률도 함께 키우는 것이 관행이다([대배치 SGD](#/p/lr-scaling)의 선형 스케일링 규칙).'}
],

pitfalls:[
 '"배치 크기를 키우면 항상 더 빠르고 좋다"는 틀렸다 — 스텝 수가 줄어 총 갱신 횟수가 적어지고, 일반화 성능이 오히려 떨어질 수 있다는 보고가 있다(sharp minima 논쟁).',
 'SGD 의 손실 곡선이 요동친다고 발산이라 오판하지 말 것 — 미니배치 노이즈로 인한 정상적인 변동과 실제 발산(손실이 추세적으로 증가)을 구분해야 한다.',
 '경사 하강법은 손실의 **지역** 구조만 본다 — 전역 최솟값을 찾는다는 보장은 없고, 딥러닝에서는 그럴 필요도 별로 없다는 것이 현재의 경험적 이해다.'
],

papers:['backprop','adam'],
terms:['backprop-c','batch-epoch','gradient-problem']
});
