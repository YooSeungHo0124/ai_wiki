WIKI.concept({
slug:'vision-encoder',

tldr:'VLM에서 이미지는 비전 인코더로 특징 벡터가 된 뒤 프로젝터가 그 벡터를 언어모델의 토큰 임베딩 공간으로 옮겨 텍스트 토큰과 나란히 놓인다.',

why:'"이미지를 LLM에 넣는다"는 말이 정확히 무엇을 뜻하는지 모르면 VLM(비전-언어 모델)의 해상도 설정·토큰 예산·파인튜닝 전략을 판단할 수 없다. 이미지 하나가 소비하는 토큰 수는 답변 품질뿐 아니라 [컨텍스트 윈도우](#/c/context-window)와 비용을 직접 좌우하므로, 이 파이프라인의 각 단계가 무엇을 하는지 아는 것이 실무에서 모델을 고르고 튜닝하는 데 필수적이다. 특히 문서 이해·차트 해석처럼 세부 디테일이 중요한 응용에서는 이 파이프라인의 선택 하나하나가 정답률을 직접 좌우한다.',

sections:[
 {h:'이미지가 토큰이 되는 경로', d:'이미지는 먼저 [CLIP](#/p/clip) 같은 비전 인코더에 들어가 고정 개수의 패치(작은 사각형 조각) 단위 특징 벡터들로 바뀐다. 이 벡터들은 원래 비전 인코더 자신의 임베딩 공간에 있어서 LLM의 토큰 임베딩과는 차원도 의미 공간도 다르다. 프로젝터(projector)는 이 벡터들을 LLM의 토큰 임베딩과 같은 차원·같은 공간으로 옮기는 역할을 한다. 변환이 끝난 벡터들은 "이미지 토큰"이라는 이름으로 텍스트 토큰들 사이(대개 프롬프트 앞부분)에 그대로 끼워져 LLM에 들어가며, LLM 입장에서는 이미지 토큰이든 텍스트 토큰이든 똑같은 임베딩 시퀀스일 뿐이다.'},
 {h:'프로젝터의 두 갈래', d:'[LLaVA](#/p/llava)는 프로젝터를 아주 단순하게 만든다 — 비전 인코더가 뽑은 패치별 특징 벡터 각각에 선형층(또는 2층 MLP) 하나를 곱해 차원만 맞춰준다. 패치 수만큼 이미지 토큰이 그대로 생기므로 구현이 간단하지만 이미지 토큰 수가 해상도에 비례해 늘어난다. [BLIP-2](#/p/blip2)의 Q-Former는 반대로 학습 가능한 소수의 query 벡터를 두고, 이 query들이 cross-attention으로 비전 특징 전체를 압축해 고정된(대개 수십 개) 개수의 요약 토큰만 뽑아낸다. 선형 프로젝터는 정보 손실이 적지만 토큰을 많이 쓰고, Q-Former는 토큰을 아끼지만 압축 과정에서 세부 정보를 잃을 수 있다. 어느 쪽이든 프로젝터는 비전 인코더나 LLM에 비해 파라미터 수가 작아서, VLM을 처음 학습할 때 이 작은 프로젝터부터 먼저 정렬(align)시키고 그다음 단계에서 LLM 전체를 함께 미세조정하는 2단계 학습이 흔하다.'},
 {h:'해상도와 토큰 수', d:'비전 인코더는 이미지를 고정 크기 패치(예: 14×14픽셀)로 나눠 패치마다 벡터 하나를 뽑으므로, 입력 해상도가 커질수록 패치 개수 — 곧 이미지 토큰 개수 — 가 커진다. 그런데 초기 VLM들은 입력 이미지를 모두 정사각형의 고정 해상도(예: 224×224나 336×336)로 강제로 리사이즈해서 넣었다. 이러면 가로로 긴 표나 세로로 긴 문서 스캔본처럼 원본 비율이 다른 이미지는 찌그러지거나 중요한 부분이 뭉개져 작은 글씨·세부 디테일을 놓치기 쉽다. 반대로 아주 작은 아이콘 하나를 336×336으로 억지로 키워도 원래 없던 디테일이 생기지는 않으니, 고정 해상도는 이미지 크기와 무관하게 일률적인 토큰 비용을 치르게 만든다.'},
 {h:'동적 해상도가 나온 이유', d:'[Qwen2-VL](#/p/qwen2-vl) 같은 최신 모델은 이미지를 고정 크기로 찌그러뜨리지 않고, 원본 비율을 유지한 채 그때그때 다른 개수의 패치로 나누는 동적(native) 해상도를 쓴다. 큰 이미지나 가로로 넓은 스크린샷은 더 많은 패치(더 많은 토큰)를, 작은 이미지는 더 적은 패치를 쓰게 해 "화질과 토큰 예산" 사이 트레이드오프를 이미지마다 유연하게 조절한다. 그 대신 이미지마다 토큰 수가 달라지므로 배치 처리·위치 인코딩 설계가 고정 해상도 방식보다 복잡해지고, 서빙 시 요청마다 비용을 예측하기도 고정 해상도보다 까다로워진다.'}
],

math:[
 {tex:'\\#\\text{patches} = \\left(\\dfrac{H}{p}\\right)\\times\\left(\\dfrac{W}{p}\\right)',
  expr:'패치 수(≈이미지 토큰 수)', d:'$H,W$ 는 입력 이미지의 세로·가로 픽셀 수, $p$ 는 패치 한 변의 픽셀 크기(예: 14)다. 선형 프로젝터를 쓰면 이 패치 수가 거의 그대로 이미지 토큰 수가 된다. 해상도를 2배로 올리면 가로·세로 패치 수가 각각 2배가 되어 토큰 수는 4배로 늘어난다 — 해상도를 올릴수록 토큰 예산이 제곱으로 커지는 이유다.'}
],

diagram:{type:'flow', cap:'이미지가 특징 벡터를 거쳐 LLM이 이해하는 토큰 시퀀스로 합류하는 경로.',
 nodes:[
  {t:'입력 이미지'},
  {t:'비전 인코더', s:'패치별 특징 추출'},
  {t:'프로젝터', s:'선형층 또는 Q-Former'},
  {t:'이미지 토큰', s:'텍스트 토큰과 병합'},
  {t:'LLM', s:'하나의 시퀀스로 처리'}
 ]},

confuse:[
 {a:'선형 프로젝터(LLaVA)', b:'Q-Former([BLIP-2](#/p/blip2))', d:'선형 프로젝터는 패치 벡터 하나하나를 차원만 바꿔 그대로 전달해 이미지 토큰 수가 패치 수(해상도)에 비례한다. Q-Former는 학습된 query가 cross-attention으로 정보를 압축해 이미지 토큰 수를 고정된 소수로 만든다 — 토큰 예산과 세부 정보 보존 사이의 다른 선택이다.'},
 {a:'비전 인코더', b:'프로젝터', d:'비전 인코더는 이미지 자체에서 시각적 특징을 뽑는 역할이고([CLIP](#/p/clip) 등으로 별도 사전학습됨), 프로젝터는 그 특징을 LLM이 이해할 수 있는 임베딩 공간으로 옮기는 어댑터 역할이다. VLM을 학습할 때 흔히 비전 인코더는 고정(freeze)해 두고 프로젝터(와 LLM 일부)만 학습한다.'},
 {a:'고정 해상도', b:'동적 해상도', d:'고정 해상도는 모든 이미지를 같은 크기로 리사이즈해 토큰 수가 항상 일정하지만 원본 비율이 깨질 수 있다. 동적 해상도는 이미지마다 패치 수가 달라져 원본 디테일을 더 보존하지만, 구현과 배치 처리가 더 복잡해진다.'}
],

pitfalls:[
 '이미지 하나가 "토큰 몇 개"를 쓰는지는 모델·설정마다 크게 다르다. 고해상도 이미지를 그대로 넣으면 프롬프트 텍스트보다 이미지 토큰이 훨씬 많아져 [컨텍스트 윈도우](#/c/context-window)를 순식간에 채울 수 있다.',
 '"비전 인코더가 좋으면 VLM도 좋다"고 단순화하면 안 된다. 프로젝터가 정보를 얼마나 잘 옮기는지, LLM이 그 이미지 토큰을 텍스트 토큰과 얼마나 잘 통합해 추론하는지도 최종 품질을 크게 좌우한다.',
 '작은 글씨·표의 숫자처럼 세부 정보가 중요한 문서 이미지에서, Q-Former식 압축 프로젝터는 정보가 뭉개져 답을 틀리기 쉽다 — 문서 이해 태스크에는 토큰을 더 쓰더라도 세부 정보를 보존하는 방식이 유리한 경우가 많다.',
 '멀티 이미지·동영상 입력에서는 이미지 한 장당 토큰 수가 그대로 곱해져 늘어난다. 프레임 수나 해상도를 무심코 올리면 텍스트를 위한 토큰 예산이 눈에 띄게 줄어들 수 있다는 것을 실무에서 놓치기 쉽고, 동영상의 경우 프레임 샘플링 간격까지 함께 고려해야 전체 토큰 비용을 가늠할 수 있다.'
],

code:{lang:'text', d:'같은 이미지를 처리 방식에 따라 몇 개의 토큰으로 바꾸는지 비교.',
 src:'336×336 이미지, 패치 14px  →  24×24 = 576 패치\nLLaVA(선형 프로젝터):   이미지 토큰 576개 (패치 수 그대로)\nBLIP-2(Q-Former):       이미지 토큰 32개  (학습된 query로 압축)\nQwen2-VL(동적 해상도):  이미지 크기·비율에 따라 토큰 수가 매번 달라짐\n# 같은 이미지도 파이프라인 선택에 따라 토큰 비용이 20배 가까이 벌어질 수 있다'},

papers:['clip','llava','blip2','qwen2-vl'],
terms:['multimodal-c','embedding','tokenizer','context-window']
});
