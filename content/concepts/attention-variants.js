WIKI.concept({
slug:'attention-variants',

tldr:'멀티헤드 어텐션에서 Key·Value 를 얼마나 공유하거나 압축하느냐로 갈리는 계열로, MHA→MQA→GQA→MLA 순으로 KV 캐시가 줄어든다.',

why:'서빙 비용의 상당 부분이 [KV 캐시](#/c/kv-cache) 메모리에서 나오기 때문에, 최근 LLM은 거의 다 [멀티헤드](#/c/multi-head) 표준형이 아니라 이 변형들 중 하나를 쓴다. 모델 카드에 "GQA 8그룹"이나 "MLA" 라고 적혀 있을 때 그게 캐시 크기·서빙 처리량·품질에 각각 무엇을 의미하는지 구분하지 못하면 서빙 비용 산정과 모델 선택 모두에서 헛다리를 짚는다. 특히 같은 파라미터 수라도 어떤 변형을 쓰느냐에 따라 동시에 처리할 수 있는 요청 수나 감당 가능한 컨텍스트 길이가 몇 배씩 달라질 수 있어, 벤치마크 점수만 보고 서빙 비용을 넘겨짚으면 실제 운영에서 계산이 크게 틀어진다.',

sections:[
 {h:'무엇을 줄이는가', d:'표준 [멀티헤드](#/c/multi-head) 어텐션(MHA)은 헤드마다 Query·Key·Value 를 전부 따로 갖는다. 그런데 추론 시 캐시에 쌓이는 건 Key·Value 뿐이고 Query 는 매 스텝 새로 계산되니 캐시에 남지 않는다. 그래서 이 계열은 전부 **Query 헤드 수는 그대로 두고 Key·Value 헤드만 줄이거나 압축**하는 쪽으로 움직인다 — 표현력은 최대한 지키면서 캐시만 줄이려는 시도다. 학습 연산량(FLOPs)만 보면 Query 쪽이 그대로라 차이가 크지 않지만, 추론 시 메모리 대역폭과 캐시 용량에서는 Key·Value 헤드 수가 그대로 곱으로 들어가므로 그 부분만 줄여도 서빙 비용에는 직접적인 효과가 난다.'},
 {h:'MHA→MQA→GQA', d:'[MQA](#/p/mqa)는 극단이다. 모든 Query 헤드가 Key·Value 헤드 단 1개를 공유한다 — 캐시가 헤드 수 $H$ 분의 1로 줄어드는 대신, 헤드마다 다른 것에 주목하던 다양성을 잃어 품질 손실이 뚜렷하다. [GQA](#/p/gqa)는 그 중간이다. $H$ 개 헤드를 $g$ 개 그룹으로 묶어 그룹마다 Key·Value 를 하나씩만 두므로, 캐시는 $g/H$ 배로 줄고 품질 손실은 $g$ 를 몇 개(예: 8)로 두면 MHA에 거의 붙는다. 그래서 GQA가 실무 기본값으로 가장 널리 쓰인다. 새 모델을 처음부터 GQA로 학습할 수도 있지만, 이미 학습된 MHA 체크포인트의 헤드들을 평균 내 그룹으로 묶은 뒤 적은 추가 학습만으로 GQA로 전환하는 "업트레이닝" 방식도 실무에서 널리 쓰인다.'},
 {h:'MLA는 압축이다', d:'[DeepSeek-V2](#/p/deepseek-v2)의 MLA(Multi-head Latent Attention)는 헤드를 공유하는 대신 아예 다른 길을 택한다. Key·Value 를 저차원 잠재 벡터 하나로 압축해 캐시에는 이 압축 벡터만 저장하고, 어텐션을 계산할 때마다 그걸 다시 헤드별 Key·Value 로 복원한다. 캐시에 저장하는 건 저차원 벡터 하나뿐이라 GQA보다도 캐시를 더 크게 줄이면서, 복원 과정이 학습되기 때문에 헤드별 다양성을 MQA/GQA보다 덜 잃는다는 것이 보고된 이점이다. 대신 매 스텝 압축·복원 행렬 곱이 추가로 들어가므로 캐시 절감과 약간의 연산 오버헤드를 맞바꾸는 셈이다.'},
 {h:'실무에서 고르는 법', d:'모델을 고를 때는 "몇 그룹 GQA인가", "MLA를 쓰는가"가 곧 같은 파라미터 수에서 서빙 시 감당할 수 있는 동시 요청 수·컨텍스트 길이를 좌우한다. 그룹 수가 작을수록(1에 가까울수록) 캐시는 줄지만 미세한 품질 저하 위험이 커지므로, 파인튜닝 없이 그룹 수를 사후에 바꾸는 것은 표준적인 방법이 아니다 — 이는 사전학습 시점에 정해지는 아키텍처 선택이다. 긴 컨텍스트를 많이 다루는 서비스일수록 캐시 절감의 체감 효과가 크므로 GQA·MLA 채택 여부를 모델 선정 기준에 넣는 것이 좋다.'}
],

math:[
 {tex:'\\text{cache}_{\\text{GQA}} = \\dfrac{g}{H}\\cdot \\text{cache}_{\\text{MHA}}',
  expr:'GQA 캐시 절감 비율', d:'$H$ 는 전체 Query 헤드 수, $g$ 는 Key·Value 그룹 수다. $g=H$ 면 MHA와 같고 $g=1$ 이면 MQA가 된다. 그룹 수 $g$ 를 8 정도로 두면 캐시는 헤드 수 대비 크게 줄면서도 그룹 안에서 헤드들이 어느 정도 다양성을 유지해 품질 손실을 억제한다.'}
],

diagram:{type:'stack', cap:'아래일수록 원형(MHA)에 가깝고, 위로 갈수록 KV 캐시를 더 적극적으로 줄인다.',
 layers:[
  {t:'MHA', s:'헤드마다 K·V 따로', note:'캐시 100%, 품질 기준선'},
  {t:'MQA', s:'전 헤드가 K·V 1벌 공유', note:'캐시 1/H, 품질 손실 큼'},
  {t:'GQA', s:'헤드를 그룹으로 묶어 공유', note:'캐시 g/H, 실무 기본값', acc:true},
  {t:'MLA', s:'저차원 잠재로 압축·복원', note:'캐시 대폭 감소, 손실 적음'}
 ]},

confuse:[
 {a:'[MQA](#/p/mqa)', b:'[GQA](#/p/gqa)', d:'MQA는 그룹 수가 1인 GQA의 특수한 경우다. GQA는 그룹 수를 1(MQA)과 헤드 수 $H$(MHA) 사이에서 고를 수 있는 일반화된 형태이고, 실무에서 8 안팎의 그룹 수가 캐시 절감과 품질 사이 균형점으로 흔히 쓰인다.'},
 {a:'GQA(헤드 공유)', b:'MLA(잠재 압축)', d:'GQA는 헤드 여러 개가 물리적으로 같은 Key·Value 를 그대로 나눠 쓰는 방식이고, MLA는 헤드별로 다른 Key·Value 를 유지하되 캐시에는 압축된 저차원 벡터만 남기고 필요할 때 복원한다. 전자는 "공유", 후자는 "압축"이라 메커니즘이 다르다.'},
 {a:'attention-variants', b:'[KV 캐시](#/c/kv-cache)', d:'KV 캐시는 왜 메모리 문제가 생기는지를 다루고, 이 글은 그 캐시를 줄이기 위해 어텐션 구조 자체를 어떻게 바꿨는지 — 즉 캐시 절감의 아키텍처 쪽 해법을 비교한다.'},
 {a:'attention-variants', b:'[flash-attention-c](#/c/flash-attention-c)', d:'이 글의 변형들은 캐시에 저장하는 Key·Value 의 **개수·크기 자체**를 줄이는 아키텍처 설계다. FlashAttention은 그와 독립적인 축으로, 어텐션 연산을 수행하는 **방식**(메모리 이동 순서)을 바꿔 속도를 올린다 — MHA든 GQA든 MLA든 FlashAttention 커널로 계산할 수 있다.'}
],

pitfalls:[
 '"GQA를 쓰면 무조건 MHA와 품질이 같다"고 오해하기 쉽지만, 그룹 수가 너무 작으면 여전히 눈에 띄는 품질 손실이 있다 — 그룹 수는 캐시와 품질을 조정하는 다이얼이지 공짜 최적화가 아니다.',
 'MLA는 GQA보다 구현이 복잡하고(압축·복원 행렬을 따로 학습해야 함) 기존 MHA 체크포인트에서 사후 변환하기 어렵다 — 사전학습 단계부터 설계에 반영해야 하는 아키텍처 선택이다.',
 '벤치마크 점수만 보고 "MQA/GQA를 쓰는 모델은 품질이 나쁘다"고 단정하면 안 된다. 그룹 수·학습 데이터·학습 스케줄을 함께 조정하면 MHA에 근접한 품질을 내면서도 캐시를 크게 줄인 사례가 여럿 보고돼 있다 — 손실 정도는 설계와 학습에 따라 달라지는 변수다.'
],

code:{lang:'text', d:'같은 은닉 차원에서 헤드 수 H, 그룹 수 g 에 따라 캐시에 실제로 남는 K·V 벡터 수가 어떻게 달라지는지 정리.',
 src:'MHA:  H개 헤드 → 캐시에 K,V 각 H벌 저장          (기준 100%)\nGQA:  H개 헤드, g개 그룹 → 캐시에 K,V 각 g벌 저장  (g/H 배)\nMQA:  H개 헤드, 1개 공유 → 캐시에 K,V 각 1벌 저장  (1/H 배)\nMLA:  H개 헤드 → 캐시에 저차원 잠재벡터 1벌만 저장 (추가 압축)'},

papers:['mqa','gqa','deepseek-v2'],
terms:['multi-head','kv-cache','flash-attention-c']
});
