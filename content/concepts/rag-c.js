WIKI.concept({
slug:'rag-c',

tldr:'모델이 답을 생성하기 전에 외부 문서를 검색해 그 내용을 문맥으로 함께 넣어주는 방식으로, 모델 파라미터에 없는 최신·전용 지식을 답에 반영하게 한다.',

why:'RAG 는 실무에서 가장 많이 배포되는 LLM 패턴이지만, 파이프라인이 5단계로 쪼개져 있어 각 단계에서 조용히 실패할 수 있다. 어느 단계가 문제인지 구분하지 못하면 "RAG 가 안 되는데 이유를 모르겠다"는 상태에서 벗어나기 어렵다.',

sections:[
 {h:'파이프라인 개관', d:'[RAG](#/p/rag) 는 청킹(문서를 검색 단위로 자르기) → 임베딩(각 조각을 벡터로 변환) → 검색(질의와 가까운 조각을 찾기) → 리랭킹(후보를 더 정밀하게 재정렬, 선택적) → 생성(찾은 조각을 문맥에 넣어 답 생성) 순서로 이어진다. 각 단계는 이전 단계의 출력에 전적으로 의존하므로, 앞 단계가 나쁘면 뒤 단계가 아무리 잘 작동해도 복구되지 않는다.'},
 {h:'청킹에서 무엇이 잘못되나', d:'조각이 너무 크면 관련 없는 내용이 섞여 임베딩이 흐려지고, 너무 작으면 문맥이 잘려 문장 하나의 의미조차 파악이 안 된다. 표·코드처럼 구조가 있는 문서를 문자 수 기준으로 기계적으로 자르면 표의 헤더와 값이 다른 조각으로 갈라지는 식의 손상이 흔하다. 청킹 경계가 질문에 필요한 정보를 정확히 쪼개버리면 이후 어떤 단계도 그 정보를 되찾지 못한다.'},
 {h:'임베딩·검색 실패 지점', d:'임베딩 모델이 질의-문서 쌍으로 학습되지 않았으면([DPR](#/p/dpr) 이 보여준 것처럼 질의와 답이 되는 문서의 벡터가 가까워지도록 대조학습을 해야 한다) 표면적 단어 겹침에만 반응해 의미는 같지만 표현이 다른 문서를 놓친다. 사용자의 질의가 짧고 모호하면 벡터 검색 자체가 관련 문서를 못 찾는데, [HyDE](#/p/hyde) 는 질의로 가상의 답변을 먼저 생성해 그 답변으로 검색함으로써 이 간극을 줄이는 접근이다. top-k 를 너무 작게 잡으면 정답 문서가 순위 밖으로 밀려나고, 너무 크게 잡으면 리랭킹·생성 단계의 부담이 커진다.'},
 {h:'리랭킹에서 무엇이 잘못되나', d:'1차 검색은 속도를 위해 가벼운 유사도(벡터 내적 등)만 쓰므로 관련도 순위가 거칠다. [monoT5](#/p/monot5) 류의 리랭커는 질의-문서 쌍을 직접 읽고 더 정밀하게 재정렬하지만, 리랭커 자체가 1차 검색 후보 안에서만 고를 수 있으므로 애초에 후보에 정답 문서가 없으면 리랭킹으로 되살릴 수 없다. 리랭커를 생략하면 표면적으로 비슷하지만 실제로는 답과 무관한 문서가 상위에 남을 위험이 커진다.'},
 {h:'생성에서 무엇이 잘못되나', d:'검색된 문서를 넣어줘도 모델이 그 내용을 무시하고 원래 학습 때 알던(때로는 틀린) 지식으로 답하는 경우가 있다 — grounding 실패다. 여러 조각을 한 문맥에 욱여넣으면 서로 모순되는 내용이 섞여 모델이 어느 쪽을 따라야 할지 혼란스러워하기도 한다. [Self-RAG](#/p/self-rag) 는 모델이 스스로 "이 검색 결과가 필요한가", "이 답이 검색 결과에 근거하는가"를 판단하는 토큰을 생성하게 해 이 grounding 실패를 줄이려는 접근이다.'},
 {h:'실무에서', d:'RAG 가 잘못된 답을 낼 때는 파이프라인을 단계별로 분리해 디버깅한다 — 검색된 조각을 눈으로 직접 확인해 관련 문서가 애초에 들어왔는지(검색 실패)부터 보고, 들어왔는데도 답이 틀렸다면 생성 단계의 [환각](#/c/hallucination)을 의심한다. 청킹 전략·임베딩 모델 교체·리랭커 추가는 비용이 다르므로, 어느 단계가 병목인지 확인한 뒤 거기에 투자하는 것이 순서다.'},
 {h:'평가는 단계를 나눠서', d:'RAG 전체 품질을 하나의 숫자로 재면 어느 단계를 고쳐야 할지 알 수 없다. 검색 단계는 정답 조각이 top-k 안에 들었는지(recall@k)로, 생성 단계는 최종 답이 검색된 조각에 실제로 근거했는지(grounding)와 사실 정확도로 따로 측정하는 것이 진단에 유리하다. 두 지표를 분리해서 추적해야 "검색은 잘 되는데 생성이 무시한다"거나 "애초에 검색이 못 찾는다" 같은 원인을 구분할 수 있다.'}
],

diagram:{type:'flow', cap:'RAG 파이프라인 5단계와 각 단계의 실패 지점.',
 steps:['청킹(경계 손상)','임베딩(의미 불일치)','검색(관련 문서 누락)','리랭킹(후보 밖은 복구 불가)','생성(grounding 실패)']},

code:{lang:'text', d:'파이프라인 실패를 단계별로 분리해 디버깅하는 순서 — 실무에서 가장 먼저 할 일.', src:
'1. 질의를 그대로 검색기에 넣어 top-k 조각을 눈으로 확인\n'+
'   -> 정답 조각이 없다: 청킹/임베딩/검색 문제\n'+
'   -> 정답 조각이 있다: 3번으로\n'+
'2. 청킹 경계와 임베딩 모델의 질의-문서 정합성 점검\n'+
'3. 정답 조각을 최종 프롬프트에 실제로 넣었는지 로그 확인\n'+
'4. 넣었는데도 답이 틀리면: 생성 단계 grounding 실패(환각)'},

confuse:[
 {a:'검색 실패', b:'생성 단계 [환각](#/c/hallucination)', d:'검색된 조각 자체에 정답이 없었다면 검색 실패이고, 정답이 담긴 조각이 들어왔는데도 모델이 다르게 답했다면 생성 단계의 grounding 실패(환각)다. 검색 결과와 최종 답을 나란히 대조해야 구분된다.'},
 {a:'리랭킹', b:'검색(retrieval)', d:'검색은 전체 문서 집합에서 후보를 빠르게 추려내는 1차 단계이고, 리랭킹은 그 후보들만을 대상으로 더 정밀한 모델로 순서를 다시 매기는 2차 단계다. 리랭킹은 검색이 놓친 문서를 되살릴 수 없다.'},
 {a:'[RAG](#/p/rag)', b:'파인튜닝', d:'RAG 는 모델 가중치를 바꾸지 않고 외부 지식을 추론 시점에 주입하는 것이고, 파인튜닝은 가중치 자체를 갱신해 지식이나 스타일을 모델 안에 새긴다. 자주 바뀌는 지식(가격·정책)은 RAG 가, 말투나 형식처럼 안정적인 패턴은 파인튜닝이 더 적합한 관행으로 쓰인다.'}
],

pitfalls:[
 '"RAG 를 붙였으니 최신 정보를 안다"고 가정하지만, 검색 인덱스가 갱신되지 않았으면 여전히 오래된 정보로 답한다.',
 '문맥에 검색 결과를 많이 넣을수록 좋다고 생각하기 쉽지만, 관련 없는 조각이 섞이면 모델의 grounding 이 오히려 흐트러질 수 있다.',
 '벡터 검색 점수가 높다고 해서 그 문서가 질문에 실제로 답한다는 뜻은 아니다 — 의미적으로 비슷한 주제일 뿐 답이 없는 문서도 상위에 오를 수 있다.',
 '청킹 크기를 한 번 정해놓고 모든 문서 유형에 똑같이 적용하면, 표·FAQ·긴 산문처럼 구조가 다른 문서에서 손상 양상이 다르게 나타난다는 것을 놓치기 쉽다.',
 '리랭커를 추가하면 지연 시간이 늘어난다 — 정확도 개선폭이 작은데도 습관적으로 리랭커를 넣으면 비용 대비 이득이 낮을 수 있다.'
],

papers:['rag','dpr','hyde','self-rag','monot5'],
terms:['hallucination','perplexity','prompt-engineering']
});
