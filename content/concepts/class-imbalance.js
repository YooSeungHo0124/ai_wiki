WIKI.concept({
slug:'class-imbalance',

tldr:'클래스별 샘플 수가 크게 차이 나는 상황으로, 그대로 학습하면 모델이 다수 클래스만 찍어도 정확도가 높아 보이는 착시가 생긴다.',

why:'사기 탐지, 희귀 질환 진단, 이상 탐지처럼 실무에서 정말 중요한 문제일수록 양성 클래스가 1% 미만인 경우가 흔하다. "정확도 99%"라는 숫자에 속아 전부 음성으로만 찍는 모델을 배포하는 실수를 피하려면, 재샘플링·가중 손실·문턱값 조정이 각각 **무엇을 바꾸는지**와 평가 지표를 먼저 바꿔야 한다는 순서를 알아야 한다.',

sections:[
 {h:'정확도의 착시', d:'양성이 1%인 데이터에서 모든 샘플을 그냥 음성으로 예측해도 정확도는 99%가 나온다 — 숫자는 높지만 모델은 사실상 아무것도 배우지 않은 것과 같다. 그래서 클래스 불균형 문제에서는 [정확도 대신 정밀도·재현율·F1](#/c/precision-recall), [AUROC·AUPR](#/c/auroc) 같은 지표를 먼저 확인해야 한다. 무엇을 바꾸기 전에 무엇으로 측정할지부터 바로잡지 않으면, 다른 처방들이 실제로 도움이 됐는지조차 판단할 수 없다.'},
{h:'재샘플링', d:'오버샘플링(소수 클래스를 복제하거나 SMOTE처럼 보간해 늘림)과 언더샘플링(다수 클래스 일부를 줄임)은 모델이 실제로 보는 **배치 안의 클래스 비율**을 바꾼다. 오버샘플링은 같은 소수 샘플을 반복 학습시켜 과적합 위험이 있고, 언더샘플링은 다수 클래스의 정보를 버리는 대가가 있다 — 데이터 자체를 조작한다는 점이 다음 두 방법과 다르다.'},
{h:'가중 손실', d:'데이터는 그대로 두고, 손실 함수에서 소수 클래스의 오차에 더 큰 가중치를 곱해 그 클래스를 틀렸을 때 더 크게 벌준다(class-weighted cross-entropy, 가중 교차 엔트로피). [Focal Loss](#/p/focal-loss)는 한 걸음 더 나아가, 이미 잘 맞히고 있는 "쉬운" 샘플의 손실 기여도를 $(1-p_t)^\\gamma$ 로 깎아 학습이 어려운(소수 클래스에 몰려 있기 쉬운) 샘플에 자연스럽게 집중하도록 만든다 — 클래스 비율이 아니라 예측 난이도 기준으로 가중치를 준다는 점이 단순 클래스 가중치와 다르다.'},
 {h:'문턱값 조정', d:'앞의 두 방법은 모두 **학습**을 바꾸지만, 문턱값 조정은 이미 학습된 모델의 확률 출력을 놓고 "확률이 얼마 이상이면 양성으로 판단할지"의 **추론 시점** 기준만 옮기는 것이다. 기본값 0.5 대신 검증셋에서 원하는 정밀도-재현율 균형에 맞는 문턱값을 찾아 쓰면, 재학습 없이도 소수 클래스 재현율을 크게 끌어올릴 수 있다 — 가장 값싸고 먼저 시도해 볼 방법이다.'},
 {h:'실무 순서', d:'① 평가 지표를 정확도에서 정밀도·재현율·F1·AUPR로 바꾼다. ② 문턱값 조정만으로 충분한지 먼저 확인한다(재학습이 필요 없어 가장 빠르다). ③ 부족하면 가중 손실이나 Focal Loss로 학습 자체를 조정한다. ④ 그래도 부족하면 재샘플링을 데이터 파이프라인에 넣는다. 이 순서를 거꾸로 밟아 재샘플링부터 시도하면 원인 진단 없이 데이터만 복잡하게 만들기 쉽다.'},
 {h:'평가 지표 선택', d:'양성 클래스가 매우 희귀하면 [AUROC](#/c/auroc)조차 낙관적인 그림을 보여줄 수 있다 — 음성이 압도적으로 많아 거짓 양성률(FPR) 분모가 커지기 때문이다. 이런 극단적 불균형에서는 정밀도-재현율 곡선 아래 면적(AUPR)이 실제 운영 성능을 더 잘 반영하는 지표로 흔히 권장된다. 어떤 지표를 최적화 기준으로 삼을지는 실제 비즈니스에서 거짓 양성과 거짓 음성 중 어느 쪽 비용이 더 큰지에 따라 달라진다.'}
],

math:[
 {tex:'\\mathrm{FL}(p_t)=-(1-p_t)^\\gamma \\log(p_t)', expr:'Focal Loss', d:'$p_t$ 는 정답 클래스에 대한 모델의 예측 확률. $p_t$ 가 클수록(쉬운 샘플일수록) $(1-p_t)^\\gamma$ 가 작아져 손실 기여도가 줄어들고, $\\gamma$ 가 클수록 이 효과가 강해진다. $\\gamma=0$ 이면 일반 교차 엔트로피와 같다.'}
],

code:{lang:'python', d:'클래스 가중치를 손실에 반영하는 방법과, 학습된 모델의 문턱값을 검증셋에서 조정하는 방법을 함께 보여준다.', src:'import torch.nn as nn\nfrom sklearn.metrics import precision_recall_curve\n\n# 1) 가중 손실 — 클래스 빈도 역수로 가중치\nclass_weights = torch.tensor([1.0, 20.0])  # 소수 클래스에 20배 가중\nloss_fn = nn.CrossEntropyLoss(weight=class_weights)\n\n# 2) 문턱값 조정 — 검증셋에서 원하는 재현율에 맞는 문턱값 탐색\nprecisions, recalls, thresholds = precision_recall_curve(y_val, probs_val)\n# recall >= 0.9를 만족하는 가장 높은 정밀도의 문턱값 선택\nidx = next(i for i, r in enumerate(recalls) if r >= 0.9)\nbest_threshold = thresholds[idx]'},

diagram:{type:'compare', cap:'학습을 바꿀지 추론 기준을 바꿀지, 층위가 다르다.',
 left:{t:'학습을 바꾸는 방법', items:[
  '재샘플링(데이터 분포)',
  '가중 손실(오차 가중치)',
  'Focal Loss(난이도 가중치)']},
 right:{t:'추론을 바꾸는 방법', items:[
  '문턱값 조정',
  '재학습 불필요',
  '가장 먼저 시도할 방법']}},

confuse:[
 {a:'가중 손실', b:'Focal Loss', d:'가중 손실은 클래스 **비율**에 따라 고정된 가중치를 곱해 학습 내내 유지하고, Focal Loss는 각 샘플의 예측 **난이도**(현재 확률)에 따라 매 스텝 동적으로 가중치를 다시 조절한다. 클래스가 불균형하면서 동시에 쉬운 배경 샘플이 압도적으로 많은 [객체 검출](#/c/bbox-nms) 같은 문제에서 Focal Loss가 특히 효과적인 이유다.'},
 {a:'오버샘플링', b:'데이터 증강([Augmentation](#/c/augmentation))', d:'오버샘플링은 같은 소수 클래스 샘플을 그대로 반복하거나 보간(SMOTE)해 개수를 늘리는 것이고, 데이터 증강은 회전·색상 변환 등으로 **새로운 변형**을 만들어 다양성을 늘리는 것이다. 오버샘플링에 증강을 함께 쓰면 단순 복제만 할 때보다 과적합이 덜하다.'},
 {a:'정확도(accuracy)', b:'[정밀도·재현율](#/c/precision-recall)', d:'정확도는 클래스 비율을 무시하고 전체 맞힌 비율만 보므로 불균형 데이터에서 착시를 만든다. 정밀도·재현율은 클래스별로 따로 계산되어 소수 클래스 성능을 다수 클래스 뒤에 숨기지 않는다.'}
],

pitfalls:[
 '검증셋까지 오버샘플링해서 평가하면 실제 운영 환경의 클래스 비율과 달라져 지표가 실제 성능을 반영하지 못한다 — 재샘플링은 학습셋에만 적용해야 한다.',
 '가중 손실의 가중치를 클래스 빈도의 역수로 기계적으로 정하면 극단적 불균형에서 소수 클래스 가중치가 너무 커져 학습이 불안정해질 수 있다 — 역수의 제곱근 등 완화된 스케일을 쓰는 경우가 많다.',
 '문턱값을 학습셋이 아닌 검증셋에서 골라야 한다 — 학습셋 기준으로 고르면 그 학습셋에 과적합된 문턱값을 얻는다.',
 '극단적 불균형(양성 0.1% 미만)에서는 재샘플링만으로 균형을 맞추려 하면 소수 클래스를 수백~수천 배 복제하게 되어 다양성이 거의 없는 데이터를 과적합하기 쉽다 — 이런 경우 분류보다 정상 분포에서 벗어남을 탐지하는 이상 탐지 관점으로 문제를 재정의하는 것이 나을 때가 많다.'
],

papers:['focal-loss','msp-baseline'],
terms:['precision-recall','auroc','augmentation','bbox-nms','dataset-dataloader','stratified-split']
})
