WIKI.concept({
slug:'reproducibility',

tldr:'같은 코드·데이터·설정으로 학습을 다시 돌렸을 때 같은(또는 통계적으로 동등한) 결과가 나오는 성질로, 시드·라이브러리 버전·데이터 스냅샷·하드웨어까지 전부 고정해야 비로소 보장된다.',

why:'재현이 안 되는 연구·실험은 검증할 수 없다 — 논문의 결과가 우연인지 실제 효과인지, 배포한 모델의 성능이 정말 그 설정 덕분인지 알 방법이 없어진다. 실무에서는 장애가 났을 때 "그때 그 모델을 다시 만들어 원인을 재현"해야 하는 경우가 많은데, 재현성이 없으면 이 디버깅 자체가 불가능하다.',

sections:[
 {h:'시드 고정', d:'가중치 초기화, 데이터 셔플 순서, [드롭아웃](#/c/dropout-c) 마스크처럼 학습 곳곳에 무작위성이 개입한다. 난수 생성기의 시드(seed)를 고정하면 이 무작위 선택들을 매번 똑같이 재현할 수 있다. 다만 시드를 고정해도 GPU 병렬 연산의 특성상 완전히 똑같은 결과가 안 나올 수 있다는 것이 다음 항목의 함정이다.'},
 {h:'하드웨어 비결정성', d:'GPU 의 부동소수점 연산은 순서에 따라 결과가 미세하게 달라질 수 있다 — 덧셈이 수학적으로는 교환법칙이 성립하지만, 부동소수점 반올림 오차 때문에 $(a+b)+c$ 와 $a+(b+c)$ 가 정확히 같은 값이 아닐 수 있다. 여러 스레드가 비결정적 순서로 덧셈을 누적하는 병렬 커널(예: 일부 컨볼루션·어텐션 구현)에서는 시드를 고정해도 실행할 때마다 소수점 아래 자리가 미세하게 달라질 수 있다. 완전한 결정성을 원하면 프레임워크의 결정론적 연산 모드를 켜야 하는데, 이는 보통 속도 저하를 대가로 치른다 — 실무에서는 "완전히 동일한 값"이 아니라 "통계적으로 동등한 성능"을 재현성의 기준으로 삼는 경우가 많다.'},
 {h:'데이터·버전 고정', d:'같은 코드라도 데이터가 바뀌면 결과가 달라진다 — 데이터셋 스냅샷을 버전으로 고정해두지 않으면(계속 갱신되는 데이터베이스에서 매번 새로 긁는 식이면) 몇 주 전 실험을 다시 돌렸을 때 이미 다른 데이터를 보게 된다. 마찬가지로 라이브러리·CUDA·드라이버 버전이 조금만 달라져도 수치 결과가 미세하게, 때로는 크게 달라질 수 있어, 환경을 고정(컨테이너 이미지, requirements 잠금)해두는 것이 재현성의 전제조건이다. [실험 추적](#/c/experiment-tracking)이 이 모든 것을 자동 기록하는 이유가 바로 이것이다.'},
 {h:'체크포인트 공개의 의미', d:'[Pythia](#/p/pythia)와 [OLMo](#/p/olmo)는 최종 모델뿐 아니라 학습 중간 여러 시점의 체크포인트, 학습에 쓴 정확한 데이터 순서, 학습 코드까지 통째로 공개했다. 이는 단순한 투명성 제스처가 아니다 — 중간 체크포인트가 있어야 "학습이 진행되며 특정 능력이 언제 나타나는가" 같은 연구 질문에 다른 연구자들이 직접 답해볼 수 있고, 최종 결과만으로는 검증할 수 없는 주장(예: 특정 스케일에서 능력이 갑자기 생긴다)을 독립적으로 재현·반박할 수 있다. 최종 가중치만 공개하는 것과 전체 학습 과정을 재현 가능하게 공개하는 것은 과학적 가치가 다르다.'}
],

diagram:{type:'stack', cap:'재현성을 위해 고정해야 하는 층.',
 layers:['시드(초기화·셔플·드롭아웃)','코드 버전(커밋 해시)','데이터 스냅샷 버전','라이브러리·환경 버전','하드웨어·결정론 모드']},

confuse:[
 {a:'재현성(reproducibility)', b:'반복성(replicability)', d:'엄밀히 구분하면 재현성은 같은 데이터·코드로 같은 결과를 다시 얻는 것이고, 반복성은 다른 데이터·다른 구현으로도 같은 결론(효과)이 나오는 것을 가리킨다 — 후자가 더 강한 과학적 주장이다. 실무에서는 두 단어가 섞여 쓰이는 경우가 많지만, "완전히 같은 실험을 다시 돌릴 수 있는가"와 "다른 설정에서도 같은 결론이 유지되는가"는 별개의 질문이라는 점은 기억해둘 만하다.'},
 {a:'시드 고정', b:'완전한 결정성(determinism)', d:'시드를 고정하는 것은 재현성을 위한 필요조건 중 하나일 뿐, 그것만으로 완전히 동일한 결과를 보장하지 않는다 — GPU 병렬 연산의 부동소수점 비결정성 때문이다. "시드를 고정했는데 왜 결과가 다르지"라는 흔한 혼란이 여기서 나온다.'},
 {a:'재현성', b:'[데이터 버전 관리](#/c/data-versioning)', d:'데이터 버전 관리는 재현성을 달성하기 위한 여러 구성요소 중 하나(데이터 축)이고, 재현성은 시드·코드·데이터·환경·하드웨어를 모두 아우르는 상위 목표다. 데이터만 고정해도 다른 축이 안 고정되면 재현성은 깨진다.'}
],

pitfalls:[
 '시드만 고정하면 재현이 보장된다고 믿는 것이 가장 흔한 오해다 — 라이브러리 버전, GPU 종류, 병렬 연산의 비결정성까지 함께 고정하지 않으면 미세한(때로는 유의미한) 차이가 남는다.',
 '"평균적으로 비슷한 성능이 나오면 재현된 것"이라고 느슨하게 판단하면, 실제로는 실행마다 분산이 커서 어떤 실행에서는 우연히 좋은 결과가 나온 것을 안정적인 개선으로 착각할 수 있다 — 여러 시드로 반복 실행해 분산까지 함께 보고하는 것이 신뢰할 수 있는 관행이다.',
 '데이터를 계속 갱신되는 원본에서 매번 새로 긁어 쓰면서 "데이터셋 이름"만 기록해두는 것은 재현성을 보장하지 못한다 — 특정 시점의 스냅샷을 별도로 고정·보관해야 한다.'
],

papers:['pythia','olmo'],
terms:['experiment-tracking','data-versioning','benchmark-pitfall']
});
