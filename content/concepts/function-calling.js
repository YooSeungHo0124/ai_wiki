WIKI.concept({
slug:'function-calling',

tldr:'모델이 자연어 대신 미리 정의된 함수 이름과 인자를 구조화된 형식으로 출력하게 해, 실제 코드 실행·외부 API 호출과 연결하는 방식.',

why:'LLM 은 그 자체로는 계산기도, 캘린더도, 데이터베이스도 아니다. function calling 이 없으면 "내일 오후 3시 회의 잡아줘" 같은 요청을 모델이 말로만 대답하고 끝난다. 이 기능이 있어야 모델의 출력이 실제 시스템의 동작(API 호출, DB 조회, 코드 실행)으로 이어지는 에이전트형 제품을 만들 수 있다.',

sections:[
 {h:'동작 원리', d:'개발자가 함수의 이름·설명·인자 스키마(보통 JSON Schema)를 모델에게 미리 알려주면, 모델은 사용자 요청을 보고 어떤 함수를 어떤 인자로 불러야 할지를 텍스트가 아니라 정해진 구조(예: {"name":"get_weather","arguments":{"city":"Seoul"}})로 출력한다. 실제 함수 실행은 모델이 하지 않는다 — 모델은 "무엇을 어떻게 부를지"만 결정하고, 그 결과를 애플리케이션 코드가 실행한 뒤 결과를 다시 모델에게 넘겨 다음 응답을 만들게 한다.'},
 {h:'왜 별도 능력인가', d:'초기에는 프롬프트에 "함수 목록을 알려줄 테니 이 형식으로 답해"라고 지시하는 방식([Toolformer](#/p/toolformer), [ReAct](#/p/react))으로 흉내 냈지만, 형식을 깨거나 존재하지 않는 함수를 지어내는 문제가 잦았다. 이후 모델 자체가 도구 호출을 위한 전용 출력 모드로 미세조정되면서 형식 준수율과 "언제 도구를 부를지 안 부를지"의 판단이 크게 좋아졌다. 이는 [구조화 출력](#/c/structured-output)의 한 특수 사례로 볼 수 있다 — 다만 목표가 "형식이 맞는 JSON"이 아니라 "실행 가능한 올바른 호출"이라는 점이 다르다.'},
 {h:'멀티턴 도구 사용', d:'복잡한 작업은 한 번의 호출로 끝나지 않는다. 모델이 함수를 부르고, 결과를 받고, 그 결과를 보고 또 다른 함수를 부르는 루프가 이어질 수 있다([ReAct](#/p/react) 패턴). 이 루프가 길어질수록 각 단계의 결과가 대화 [문맥](#/c/context-management)에 계속 쌓이므로 토큰 비용과 지연이 같이 늘어난다.'},
 {h:'실무에서', d:'함수 설명(description)을 모호하게 쓰면 모델이 엉뚱한 함수를 고르거나 인자를 잘못 채운다 — 함수 하나하나를 "다른 엔지니어에게 API 문서를 쓰듯" 명확히 적는 것이 실무에서 가장 효과가 큰 개선이다. 비슷한 기능의 함수를 여러 개 늘어놓으면 모델이 헷갈리므로 도구 개수를 줄이고 통합하는 것도 흔한 처방이다. 또한 모델이 반환한 인자값은 사용자 입력이 그대로 흘러들어 온 것일 수 있으므로, 실제 실행 전에 권한·범위 검증을 반드시 애플리케이션 쪽에서 해야 한다 — 모델의 판단을 그대로 신뢰해 결제·삭제 같은 동작을 실행하면 위험하다.'}
],

diagram:{type:'flow', cap:'모델은 실행하지 않고 무엇을 부를지만 정한다.',
 steps:['사용자 요청 + 함수 목록','모델이 함수명·인자 JSON 출력','애플리케이션이 실제 함수 실행','실행 결과를 모델에 다시 전달','모델이 최종 답 생성']},

confuse:[
 {a:'function calling', b:'[구조화 출력](#/c/structured-output)', d:'구조화 출력은 "형식이 스키마와 맞는 JSON"을 얻는 것이 목적이고, function calling 은 그중에서도 "어떤 함수를 부를지 선택하고 실행까지 이어지는" 특수 목적의 구조화 출력이다. 모든 구조화 출력이 함수 호출은 아니다.'},
 {a:'function calling', b:'에이전트 프레임워크', d:'function calling 은 모델이 도구를 부르는 단위 능력이고, 에이전트 프레임워크는 그 호출을 반복하며 계획·기억·오류 복구까지 관리하는 상위 시스템이다. 에이전트는 내부적으로 function calling 을 여러 번 쓴다.'},
 {a:'도구 선택 오류', b:'[환각](#/c/hallucination)', d:'존재하지 않는 함수를 지어내거나 있지도 않은 인자를 채우는 것은 함수 호출 맥락에서의 환각이다 — 텍스트 생성의 환각과 원인(학습 데이터·불확실성 처리 부족)은 같지만 결과가 실제 시스템 오류로 이어진다는 점에서 더 위험하다.'}
],

pitfalls:[
 '모델이 반환한 함수 인자를 검증 없이 그대로 실행하면, 프롬프트 주입으로 조작된 값이 실제 시스템에 그대로 들어갈 수 있다.',
 '도구를 너무 많이 나열하면 모델이 고르는 정확도가 떨어진다 — 도구 설명의 품질과 개수 관리가 정확도에 직결된다.',
 '함수 호출이 실패했을 때(잘못된 인자, API 에러) 그 오류 메시지를 모델에게 그대로 돌려주지 않으면 모델이 같은 실수를 반복한다.'
],

papers:['toolformer','react'],
terms:['structured-output','system-prompt','context-management','hallucination']
});
