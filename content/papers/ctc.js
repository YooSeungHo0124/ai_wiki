WIKI.paper({
slug:'ctc',
venue:'ICML 2006',
authors:'Graves, Fernández, Gomez, Schmidhuber (IDSIA)',

tldr:'입력 프레임과 출력 라벨 사이의 **정렬을 몰라도** RNN을 학습시킬 수 있게 만든 손실 함수. "이 프레임이 어느 글자인가"를 사람이 미리 표시하던 관행을 없애고, 가능한 모든 정렬을 합쳐서 하나의 확률로 만들었다.',

context:'2006년 이전까지 음성·필기 인식에 RNN을 쓰려면 프레임 단위의 정답 라벨이 필요했다 — 프레임 100장짜리 오디오에 "이 프레임은 /a/, 저 프레임은 /b/"라는 정렬을 HMM 등으로 미리 만들어 붙여야 했다. 이 정렬 자체가 부정확하면 학습이 그 오차를 그대로 물려받았고, 정렬을 만드는 파이프라인이 따로 필요했다. 근본 문제는 입력 길이 $T$(프레임 수)와 출력 길이 $U$(글자 수)가 다르다는 것이다 — $T > U$인데 RNN 출력은 보통 프레임마다 하나씩 나온다. CTC는 "정렬을 미리 정하지 말고, 가능한 정렬을 전부 확률적으로 고려하자"는 질문에서 시작한다.',

ideas:[
 {h:'빈칸(blank) 토큰: "여기서는 아무 글자도 아니다"',
  lead:'출력 알파벳에 blank를 추가해 프레임 수 T와 라벨 수 U의 길이 차이를 흡수한다.',
  d:'라벨 집합에 특수 기호 blank(∅)를 추가한다. RNN은 매 프레임마다 실제 라벨이거나 blank인 하나의 기호를 출력하고, 이 길이-$T$ 시퀀스를 "① 연속으로 반복된 같은 라벨을 하나로 합치고 ② blank를 전부 지운다"는 규칙으로 축약하면 길이-$U$ 목표 시퀀스가 남는다. `aa∅bb∅∅c`와 `a∅ab∅bc∅`가 모두 `abc`로 축약되는 식이라, **하나의 목표 시퀀스에 대응하는 프레임별 정렬이 여러 개** 존재하게 된다.'},
 {h:'정렬을 하나 고르지 않고 전부 더한다',
  lead:'목표 시퀀스로 축약되는 모든 프레임별 경로의 확률을 합산해 학습 신호로 쓴다.',
  d:'목표 $l$에 대응하는 모든 프레임별 경로(정렬) $\\pi$의 집합을 $\\mathcal{B}^{-1}(l)$이라 하면, 조건부 확률은 이 집합 전체에 대한 합이다. 어떤 경로가 "맞는" 정렬인지 사람이 고를 필요가 없다 — 학습 초기에는 여러 정렬에 확률이 퍼져 있다가, 학습이 진행되면서 몇몇 경로에 확률이 몰리는 쪽으로 자연스럽게 수렴한다.'},
 {h:'경로가 지수적으로 많아도 동적계획법으로 계산한다',
  lead:'forward-backward 알고리즘으로 지수 개의 정렬 합을 O(T·U)로 계산한다.',
  d:'가능한 정렬의 수는 $T$가 커지면 지수적으로 늘어나 직접 나열은 불가능하다. HMM의 forward-backward와 같은 방식으로, "시점 $t$까지 목표의 앞 $u$개를 출력했을 확률"(forward)과 "시점 $t$부터 나머지를 출력할 확률"(backward)을 재귀식으로 계산해 두 값을 곱하면 $O(T \\cdot U)$ 시간에 전체 합을 얻는다. 이 트릭이 없으면 CTC는 계산적으로 불가능했다.'},
 {h:'디코딩은 여전히 근사가 필요하다',
  lead:'학습 때 쓴 정확한 합산과 달리 추론에서는 최적 경로를 근사적으로만 찾는다.',
  d:'학습 시에는 모든 정렬의 합을 정확히 계산할 수 있지만, 추론 시 "가장 그럴듯한 출력 시퀀스"를 찾는 문제는 다시 지수적으로 어렵다. 매 프레임에서 가장 확률 높은 기호를 그리디하게 고르는 best-path decoding이나, 여러 후보를 유지하는 beam search로 근사한다.'}
],

diagram:{type:'compare', cap:'프레임별 분류(정렬 필요) vs CTC(정렬을 몰라도 학습). CTC는 blank를 도입해 여러 정렬을 동시에 주변화한다.',
 left:{t:'기존: 프레임별 분류', items:['프레임마다 정답 라벨 필요','정렬을 HMM 등으로 미리 생성','정렬 오차가 학습에 그대로 전파']},
 right:{t:'CTC: 정렬을 주변화', items:['blank 토큰으로 T와 U 길이 차 흡수','가능한 모든 정렬의 확률을 합산','forward-backward로 O(T·U) 계산']}
},

math:[
 {expr:'p(l|x) = Σ_{π ∈ B⁻¹(l)} p(π|x)',
  tex:'p(l\\mid x)=\\sum_{\\pi\\,\\in\\,\\mathcal{B}^{-1}(l)} p(\\pi\\mid x)',
  d:'목표 라벨 시퀀스 $l$의 확률은, 반복 제거·blank 삭제로 $l$이 되는 모든 프레임별 경로 $\\pi$의 확률을 합한 것이다. $\\mathcal{B}$가 그 축약 규칙(many-to-one 사상)이다.'},
 {expr:'p(π|x) = Π_t y_t^{π_t}   (조건부 독립 가정)',
  tex:'p(\\pi\\mid x)=\\prod_{t=1}^{T} y_t^{\\pi_t}',
  d:'하나의 경로의 확률은 각 시점의 softmax 출력값을 그냥 곱한 것이다 — 즉 **각 프레임의 출력이 다른 프레임의 출력과 독립**이라고 가정한다. 이 조건부 독립 가정이 CTC의 가장 큰 한계이고, [RNN-T](#/p/rnn-t)가 정확히 이 지점을 깬다.'}
],

impact:'"정렬을 먼저 만들고 그 위에 분류기를 얹는다"는 음성·필기·시퀀스 인식의 오랜 관행을 뒤집었다. HMM-GMM 파이프라인의 강제 정렬(forced alignment) 단계를 없애고 RNN 하나를 end-to-end로 학습시킬 수 있다는 것을 보여, 이후 [Deep Speech](#/p/deep-speech)를 비롯한 종단간 음성 인식의 손실 함수로 채택되었다. blank 토큰과 정렬의 주변화라는 아이디어 자체는 음성을 넘어 필기 인식, 제스처 인식 등 입출력 길이가 다른 모든 시퀀스 문제에 재사용됐다.',

legacy:[
 '**[Deep Speech](#/p/deep-speech)** — 음향모델·발음사전·정렬을 통째로 걷어내고 CTC + RNN 하나로 음성 인식 파이프라인을 대체',
 '**[RNN-T](#/p/rnn-t)** — CTC의 조건부 독립 가정을 깨고 이전 출력에 의존하는 예측 네트워크를 추가, 스트리밍 인식의 표준이 됨',
 '**[wav2vec 2.0](#/p/wav2vec2)** 등 이후 음성 모델도 CTC를 미세조정 손실로 계속 사용 — 사전학습 표현 위에 얹는 가장 단순한 헤드로 남음',
 'attention 기반 [LAS](#/p/las) 계열은 반대로 CTC의 정렬 가정 자체를 버리는 방향으로 갈라져 나감'
],

pitfalls:[
 '**이 노트는 원문 PDF를 확보하지 못해 작성됐다.** CTC(Graves et al., ICML 2006)는 arXiv에 없고 별도로 구할 수 있는 공개 PDF도 이번 작업 범위에서 확보하지 못했다. 대신 같은 저자(Graves)가 2012년 [RNN-T](#/p/rnn-t) 논문에서 CTC를 상세히 재서술한 부분을 근거로 작성했다 — 그래서 인용문(quotes)과 원문 그림(figures)은 넣지 않았다.',
 '**CTC의 출력이 "정답 정렬"이라고 착각하기 쉽다.** 모델이 특정 프레임에 특정 글자를 배치하는 것은 학습 편의상 나온 부산물(보통 spike 형태로 나타남)이지, 그 프레임이 실제로 그 발음을 담고 있다는 보장은 아니다.',
 '**조건부 독립 가정은 공짜가 아니다.** 각 프레임의 출력이 서로 독립이라고 가정하기 때문에 CTC 자체는 언어모델 역할을 하지 못한다 — 실전에서는 항상 별도의 언어모델을 붙여 디코딩한다.'
],

links:[
 {t:'arXiv 목록에 없음 — ICML 2006 프로시딩', u:'https://dl.acm.org/doi/10.1145/1143844.1143891'},
 {t:'Distill — Sequence Modeling with CTC', u:'https://distill.pub/2017/ctc/'}
]
});
