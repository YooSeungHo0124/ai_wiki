WIKI.concept({
slug:'beam-search',

tldr:'매 스텝마다 확률 상위 $k$개(빔 폭) 시퀀스만 유지하며 전체 시퀀스의 결합 확률을 최대화하려는 탐색적 디코딩.',

why:'번역·요약처럼 "정답이 대체로 하나로 정해져 있는" 작업에서는 빔 서치가 탐욕적 디코딩보다 더 나은 문장을 찾아낸다. 하지만 자유 대화나 창작에 그대로 쓰면 [샘플링](#/c/sampling)보다 훨씬 단조롭고 반복적인 글이 나온다 — 언제 빔 서치를 쓰면 안 되는지 아는 것이 언제 쓸지 아는 것만큼 중요하다.',

sections:[
 {h:'무엇을 최적화하는가', d:'탐욕적 디코딩은 매 스텝에서 그 순간 확률이 가장 높은 토큰 하나만 고르고 되돌아보지 않는다 — 이는 종종 최적이 아닌 전체 시퀀스로 이어진다(당장은 좋아 보이는 선택이 이후 선택지를 나쁘게 만들 수 있다). 빔 서치는 매 스텝에서 상위 1개가 아니라 상위 $k$개(빔 폭, beam width) 후보 시퀀스를 동시에 유지하면서 다음 토큰을 이어 붙이고, 다시 전체 후보 중 상위 $k$개만 남기는 것을 반복한다. 이렇게 하면 전체 시퀀스의 결합 확률 $\\prod_t p(x_t\\mid x_{<t})$ 을 탐욕보다 더 높게 만드는 시퀀스를 찾을 확률이 커진다 — 다만 어휘 크기의 지수 폭발 때문에 모든 경우를 다 보는 완전 탐색은 아니고, 여전히 근사 탐색이다.'},
 {h:'번역엔 좋고 자유 생성엔 나쁜 이유', d:'번역·요약은 입력이 답의 내용을 강하게 제약한다 — "정답에 가까운 표현들"이 비교적 좁게 몰려 있고, 그 안에서 확률을 최대화하는 것이 실제로 더 정확하고 유창한 결과로 이어지는 경우가 많다. 반면 자유 생성(대화·소설)은 애초에 "정답"이 하나가 아니라 그럴듯한 전개가 무수히 많은 상황이라, 확률을 최대화하는 선택은 가장 안전하고 뻔한 표현으로 수렴하기 쉽다. [뉴클리어스 샘플링](#/p/nucleus-sampling) 논문이 지적한 "확률 최대화 디코딩이 반복적인 글을 만든다"는 문제가 빔 서치에서 특히 두드러진다 — 여러 빔이 결국 비슷한 안전한 문구로 수렴해버리는 현상도 흔히 보고된다.'},
 {h:'길이 벌점 문제', d:'결합 확률 $\\prod_t p(x_t\\mid x_{<t})$ 은 각 항이 1보다 작은 확률이라 토큰이 늘어날수록 값이 단조 감소한다. 그대로 두면 빔 서치는 구조적으로 짧은 문장을 선호하게 된다 — 심하면 한두 단어짜리 문장이 "가장 확률 높은 시퀀스"로 뽑힌다. 이를 막으려고 실무에서는 길이로 나누거나(length normalization) 길이에 따른 벌점 항을 곱하는 보정을 흔히 쓴다. 벌점 계수를 잘못 잡으면 이번엔 반대로 불필요하게 긴 문장을 선호하게 되므로, 이 계수 자체가 튜닝 대상이다.'},
 {h:'실무에서', d:'현대 챗봇형 LLM API 는 대부분 빔 서치를 기본으로 노출하지 않는다 — 대화형 생성에 빔 서치가 어울리지 않기 때문이다. 반면 기계번역 시스템이나 문서 요약 파이프라인, 음성인식 디코더에서는 여전히 표준적인 선택지다. 빔 폭을 키우면 품질이 항상 좋아질 거라 기대하기 쉽지만, 어느 지점을 넘으면 오히려 더 안전하고 짧은(때로 더 나쁜) 출력으로 수렴한다는 보고가 많다 — 무작정 빔 폭을 키우는 것이 답은 아니다.'}
],

math:[
 {tex:'\\text{score}(y)=\\dfrac{1}{|y|^{\\alpha}}\\log\\prod_{t}p(y_t\\mid y_{<t})', expr:'길이 정규화된 빔 서치 점수', d:'$|y|$ 는 생성된 시퀀스 길이, $\\alpha$ 는 길이 벌점 강도(보통 0.6~1.0 범위를 실무에서 관행적으로 시도). $\\alpha=0$ 이면 길이 보정이 없어 짧은 문장이 유리해지고, $\\alpha=1$ 이면 토큰당 평균 로그확률로 완전히 정규화된다.'}
],

diagram:{type:'flow', cap:'빔 폭 2로 탐욕적 디코딩보다 넓게 탐색하는 과정.',
 steps:['시작','빔 2개 유지: 각 스텝 상위 2 후보 확장','확률 낮은 나머지 가지는 버림','종료 토큰까지 반복 후 최종 상위 시퀀스 선택']},

confuse:[
 {a:'빔 서치', b:'[샘플링](#/c/sampling)', d:'빔 서치는 확률을 최대화하는 결정론적 탐색이고, 샘플링은 확률분포를 따라 무작위로 뽑는 확률적 절차다. 목적 자체가 다르다 — 전자는 "가장 그럴듯한 하나"를, 후자는 "그럴듯한 여러 가능성 중 하나"를 노린다.'},
 {a:'빔 폭(beam width)', b:'배치 크기', d:'빔 폭은 한 입력에 대해 동시에 유지하는 후보 시퀀스 개수이고, 배치 크기는 서로 다른 여러 입력을 한 번에 처리하는 개수다. 둘 다 "동시에 몇 개를 처리하는가"라 헷갈리기 쉽지만 완전히 다른 축이다.'}
],

pitfalls:[
 '빔 서치가 항상 탐욕적 디코딩보다 좋은 결과를 낸다고 오해하기 쉽지만, 자유 생성 작업에서는 오히려 반복적이고 안전한(재미없는) 텍스트로 수렴하는 경우가 많다.',
 '길이 정규화를 빼먹으면 빔 서치가 비정상적으로 짧은 문장을 최고 점수로 고르는 것을 그대로 겪게 된다.',
 '빔 폭을 무작정 키우면 계산량이 폭 $k$에 비례해 늘어나는데도 품질 향상은 어느 지점부터 정체되거나 오히려 나빠질 수 있다 — "크게 할수록 좋다"가 아니다.'
],

papers:['seq2seq','nucleus-sampling'],
terms:['sampling','top-k-top-p','kv-cache']
});
