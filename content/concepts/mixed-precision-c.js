WIKI.concept({
slug:'mixed-precision-c',

tldr:'행렬곱 같은 무거운 연산은 16비트(FP16/BF16)로, 경사 누적처럼 정밀도가 중요한 연산은 32비트로 섞어 써서 메모리·연산 속도를 함께 아끼는 학습 방식.',

why:'같은 GPU 메모리로 더 큰 배치·더 큰 모델을 학습하려면 혼합 정밀도는 사실상 기본 옵션이다. 하지만 FP16으로 학습하다 손실이 NaN이 되는 사고를 한 번은 겪게 되는데, 그 원인(표현 범위 부족)과 대응(손실 스케일링, 또는 애초에 BF16 사용)을 모르면 "혼합 정밀도는 불안정하다"고 잘못 결론 내리고 성능 이득을 포기하게 된다. 어떤 GPU 세대에서 무엇을 쓸 수 있는지 아는 것도 학습 설정을 고를 때 바로 필요한 지식이다.',

sections:[
 {h:'32비트 부동소수점 구조', d:'FP32는 부호 1비트, 지수부(exponent) 8비트, 가수부(mantissa) 23비트로 이뤄진다. 지수부는 표현 가능한 값의 **범위**(얼마나 크거나 작은 수까지 담을 수 있는가)를, 가수부는 그 범위 안에서의 **정밀도**(얼마나 세밀하게 구분하는가)를 결정한다. 딥러닝 학습은 오랫동안 이 FP32를 기본 정밀도로 써 왔고, 혼합 정밀도는 이 중 일부 연산만 더 가벼운 포맷으로 대체하는 것이다.'},
 {h:'FP16 vs BF16', d:'FP16(반정밀도)은 부호 1 + 지수 5 + 가수 10비트로, FP32보다 표현 범위가 훨씬 좁다(최대값이 약 65504) — 그래서 경사나 활성값이 이 범위를 벗어나면 무한대(`inf`)나 0으로 뭉개진다. BF16(bfloat16)은 부호 1 + 지수 8 + 가수 7비트로, 지수부를 FP32와 **똑같이** 8비트 유지하고 가수부만 줄였다 — 표현 범위는 FP32와 동일하게 넓고 정밀도만 낮다.'},
 {h:'손실 스케일링', d:'FP16으로 학습할 때 경사값 상당수가 FP16이 표현할 수 있는 최소 범위보다 작아 0으로 뭉개지는(underflow) 문제가 자주 생긴다. 이를 막기 위해 손실(loss)에 큰 상수(예: 1024, 65536)를 곱해 역전파 전체를 키운 뒤, 옵티마이저 갱신 직전에 경사를 다시 그 상수로 나눠 원래 크기로 되돌리는 것이 손실 스케일링(loss scaling)이다. 스케일 값을 학습 중 자동으로 늘렸다 줄였다 조정하는 동적 손실 스케일링(dynamic loss scaling)이 표준이며, `inf`/`NaN`이 감지되면 그 스텝을 건너뛰고 스케일을 낮춘다.'},
 {h:'BF16이 표준인 이유', d:'BF16은 FP32와 지수부 비트 수가 같아 표현 범위가 FP32와 같으므로, FP16에서 필요했던 손실 스케일링이 대부분 불필요하다 — 오버플로·언더플로 걱정 없이 FP32 코드를 거의 그대로 BF16으로 바꿔 쓸 수 있다. 대신 가수부가 FP16보다도 더 적어(7비트) 세밀한 정밀도는 FP16보다 떨어지지만, 실전에서는 범위 부족으로 인한 학습 붕괴보다 약간의 정밀도 손실이 다루기 훨씬 쉬운 문제라, A100 이후 세대 GPU·TPU를 쓰는 대규모 사전학습에서는 BF16이 사실상 기본값이 됐다.'},
 {h:'실무에서', d:'가중치의 마스터 사본(master copy)은 FP32로 유지하고, 순전파·역전파의 행렬곱만 FP16/BF16으로 계산한 뒤 옵티마이저 갱신은 FP32 마스터 가중치에 적용하는 것이 표준 패턴이다(PyTorch `torch.cuda.amp`, `bfloat16` autocast 등). 최신 GPU(H100 등)는 한 단계 더 나아가 [FP8](#/p/fp8)까지 지원하지만, FP8은 BF16보다도 표현력이 좁아 스케일링·보정 기법이 더 정교하게 필요하다.'},
{h:'TF32라는 중간 지대', d:'NVIDIA A100 이후 세대 GPU는 TF32(TensorFloat-32)라는 내부 연산 포맷도 지원한다 — 저장은 FP32처럼 32비트로 하되, 텐서 코어 행렬곱 내부에서만 지수 8비트·가수 10비트로 잘라 계산해 FP32보다 훨씬 빠르면서 코드 변경이 거의 필요 없다. FP16/BF16만큼 빠르진 않지만 "아무 것도 안 건드리고 어느 정도 가속만 얻고 싶다"는 경우의 손쉬운 선택지로 쓰인다.'}
],

code:{lang:'python', d:'PyTorch autocast로 BF16 혼합 정밀도를 켜는 표준 패턴 — FP16과 달리 GradScaler(손실 스케일링) 없이도 대개 안정적으로 동작한다.', src:'import torch\n\nfor x, y in loader:\n    optimizer.zero_grad()\n    with torch.autocast(device_type="cuda", dtype=torch.bfloat16):\n        out = model(x)\n        loss = loss_fn(out, y)\n    loss.backward()      # BF16: GradScaler 불필요\n    optimizer.step()\n\n# FP16을 쓸 경우엔 GradScaler로 손실 스케일링이 필요:\n# scaler = torch.cuda.amp.GradScaler()\n# scaler.scale(loss).backward(); scaler.step(optimizer); scaler.update()'},

diagram:{type:'compare', cap:'같은 16비트라도 지수부·가수부 배분이 완전히 다르다.',
 left:{t:'FP16', items:[
  '지수 5비트 · 가수 10비트',
  '표현 범위 좁음(오버플로↑)',
  '손실 스케일링 필수']},
 right:{t:'BF16', items:[
  '지수 8비트 · 가수 7비트',
  '표현 범위 FP32와 동일',
  '손실 스케일링 대개 불필요']}},

confuse:[
 {a:'혼합 정밀도(Mixed Precision)', b:'[양자화](#/c/quantization)', d:'혼합 정밀도는 **학습** 중 연산 정밀도를 16/32비트로 섞어 쓰는 기법이고, 양자화는 대개 **학습이 끝난 모델**을 INT8·INT4처럼 훨씬 낮은 정밀도로 변환해 추론을 가볍게 만드는 기법이다. 목적(학습 가속 vs 배포 경량화)과 적용 시점이 다르며, 부동소수점(FP) 계열과 정수(INT) 계열이라는 점도 다르다.'},
 {a:'FP16', b:'BF16', d:'비트 수(16)는 같지만 지수부·가수부 배분이 다르다. FP16은 정밀도가 높은 대신 범위가 좁아 손실 스케일링이 필요하고, BF16은 범위가 FP32와 같아 스케일링 없이도 안정적이지만 정밀도는 더 낮다. 이름의 "16"만 보고 둘을 같다고 넘기면 안 되는 이유다.'},
 {a:'손실 스케일링', b:'[학습률](#/c/learning-rate) 조정', d:'손실 스케일링은 역전파 중간의 경사가 FP16 표현 범위 안에 들어오도록 임시로 키웠다 되돌리는 수치적 트릭으로, 최종 파라미터 갱신량에는 영향을 주지 않는다. 학습률은 그 갱신량 자체의 보폭을 정하는 하이퍼파라미터로, 둘은 계산 파이프라인에서 서로 다른 단계를 담당한다.'}
],

pitfalls:[
 'FP16으로 학습하며 손실 스케일링 없이 그냥 순전파·역전파만 FP16으로 바꾸면 경사 언더플로로 학습이 멈추거나 NaN이 뜨기 쉽다.',
 'BF16은 범위 문제는 적지만 가수부가 짧아 정밀도가 중요한 연산(작은 값들의 누적 합 등)에서는 여전히 FP32 누산(accumulation)이 필요한 경우가 있다 — 프레임워크가 내부적으로 이를 처리해 주는지 확인해야 한다.',
 '오래된 세대 GPU(V100 이전)는 BF16 하드웨어 지원이 없어 FP16 손실 스케일링 경로를 그대로 써야 한다 — 최신 레시피를 그대로 옮기면 동작하지 않는다.',
 'BF16으로 학습한 체크포인트를 FP16 전용 배포 환경에 그대로 캐스팅하면, 좁아진 표현 범위 탓에 값이 잘려 나가 추론 품질이 떨어질 수 있다 — 배포 전 범위·정밀도를 다시 확인해야 한다.'
],

papers:['mixed-precision','fp8'],
terms:['quantization','learning-rate','grad-clipping','checkpointing','param-count','weight-init','softmax']
})
