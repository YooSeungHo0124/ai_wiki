WIKI.concept({
slug:'embedding',

tldr:'이산적인 기호(단어·토큰·위치)를 학습된 실수 벡터로 바꾼 것으로, 벡터 공간에서의 거리·방향이 의미적 관계를 나타내도록 학습된다.',

why:'검색·추천·RAG·분류 어디서든 "임베딩을 어떻게 만들었는가"가 결과 품질을 좌우한다. 단어 임베딩·문장 임베딩·위치 임베딩이 서로 다른 목적으로 학습된 별개의 물건이라는 것을 모르면 사전학습된 벡터를 엉뚱한 용도에 쓰는 실수를 하게 된다.',

sections:[
 {h:'원-핫에서 밀집 벡터로', d:'원-핫 벡터는 어휘 크기만큼 차원이 있고 그중 한 자리만 1인 표현이라, 모든 단어 쌍의 내적이 0이라서 "similarity" 개념 자체가 없다. 임베딩은 이 대신 훨씬 낮은 차원(예: 300, 4096)의 밀집 벡터를 각 기호에 대응시키고, 이 벡터를 학습 가능한 파라미터로 취급한다.'},
 {h:'왜 의미가 거리로 표현되는가', d:'임베딩은 보통 "비슷한 문맥에서 등장하는 기호는 비슷한 표현을 가져야 한다"는 목적함수로 학습된다. 이 목적함수를 만족시키려면 의미적으로 관련된 기호들이 벡터 공간에서 가까이 모이는 것이 손실을 줄이는 가장 쉬운 방법이므로, 학습이 끝나면 코사인 유사도나 유클리드 거리가 곧 의미적 유사도의 근사가 된다. 이것이 임베딩이 "학습된 부산물"이지 "설계된 규칙"이 아닌 이유다.'},
 {h:'단어 임베딩', d:'토큰 하나에 고정된 벡터 하나를 대응시키는 방식. [word2vec](#/p/word2vec)은 주변 단어를 예측하거나(skip-gram) 주변 단어로부터 예측되는(CBOW) 과제로 이 벡터를 학습해, "king - man + woman ≈ queen" 같은 벡터 연산이 의미적으로 성립하는 것을 보였다. 문맥에 따라 벡터가 바뀌지 않는다는 한계가 있어, 이후 문맥화된 임베딩(ELMo, BERT의 토큰 표현)으로 대체됐다.'},
 {h:'문장 임베딩', d:'단어가 아니라 문장·문서 전체를 하나의 벡터로 압축한 것. BERT의 [CLS] 토큰을 그대로 쓰면 문장 유사도 성능이 나쁘다는 것이 알려지며, [Sentence-BERT](#/p/sentence-bert)는 샴 네트워크 구조로 문장 쌍의 유사도를 직접 학습 목표로 삼아 의미 검색·클러스터링에 바로 쓸 수 있는 문장 벡터를 만들었다. RAG의 검색 단계가 기대는 것이 바로 이런 문장/문서 임베딩이다.'},
 {h:'위치 임베딩', d:'Transformer는 [셀프 어텐션](#/c/self-attention) 자체가 순서 정보를 갖지 않으므로, 토큰이 시퀀스의 몇 번째 위치인지를 따로 벡터로 더하거나 곱해 알려줘야 한다. 이는 단어의 "의미"가 아니라 "위치"를 표현으로 바꾼 것이라 단어 임베딩과 학습 목적이 전혀 다르다. 자세한 방식은 [위치 인코딩](#/c/positional-encoding)에서 다룬다.'}
],

diagram:{type:'compare', cap:'같은 "임베딩"이라는 말이 가리키는 세 가지 목적.',
 left:{t:'단어 임베딩', items:['토큰 하나 = 벡터 하나','문맥 무관, 정적','word2vec, GloVe']},
 right:{t:'문장 · 위치 임베딩', items:['문장 임베딩: 의미 검색·클러스터링용','위치 임베딩: 순서 정보 주입용','둘 다 문맥/역할에 따라 달라짐'], acc:true}
},

confuse:[
 {a:'임베딩(embedding)', b:'인코딩(encoding)', d:'임베딩은 데이터로부터 학습되어 의미적 거리를 갖도록 최적화된 벡터이고, 인코딩(원-핫 인코딩, 위치의 sin/cos 인코딩)은 규칙에 따라 결정론적으로 정해지는 변환이다. 학습된 위치 임베딩처럼 둘의 경계가 흐려지는 경우도 있지만, "학습되는가 vs 고정 규칙인가"가 핵심 구분이다.'},
 {a:'단어 임베딩', b:'문장 임베딩', d:'단어 임베딩은 토큰 단위의 정적 벡터라 같은 단어면 문맥이 달라도 벡터가 같고, 문장 임베딩은 문장 전체를 하나로 압축해 같은 단어라도 문맥에 따라 문장 벡터에 미치는 영향이 달라진다. 문장 임베딩은 대개 문맥화된(contextual) 토큰 표현을 풀링해서 만든다.'},
 {a:'임베딩 차원 수', b:'어휘 크기', d:'어휘 크기는 모델이 구분할 수 있는 서로 다른 토큰의 개수(수만~수십만)이고, 임베딩 차원은 그 각 토큰을 표현하는 벡터의 길이(수백~수천)다. 임베딩 테이블 전체 파라미터 수는 이 둘의 곱이다.'}
],

pitfalls:[
 '"임베딩 공간에서 가까우면 무조건 같은 의미"라는 것은 과장이다 — 학습 목적함수가 잡아낸 통계적 공기 관계(co-occurrence)를 반영할 뿐이라, 반의어가 비슷한 문맥에서 자주 쓰인다는 이유로 서로 가깝게 놓이는 경우도 흔하다(예: "덥다"와 "춥다").',
 '사전학습된 단어 임베딩(word2vec, GloVe)을 그대로 문장 유사도에 평균 내어 쓰면, [Sentence-BERT](#/p/sentence-bert) 같은 목적에 맞게 학습된 문장 임베딩보다 품질이 크게 떨어진다 — 학습 목표가 다르기 때문이다.',
 '임베딩 벡터의 차원 수가 크다고 항상 더 좋은 것은 아니다. 차원이 커지면 표현력은 늘지만 저장·연산 비용이 커지고, 데이터가 충분하지 않으면 과적합으로 오히려 품질이 떨어질 수 있다.'
],

papers:['word2vec','glove','sentence-bert'],
terms:['self-attention','positional-encoding','contrastive']
});
