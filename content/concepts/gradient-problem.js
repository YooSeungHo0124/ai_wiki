WIKI.concept({
slug:'gradient-problem',

tldr:'역전파에서 층마다 국소 미분이 곱해지는데, 이 값들이 1보다 계속 작으면 경사가 지수적으로 0에 가까워지고(소실) 1보다 계속 크면 지수적으로 발산한다(폭주).',

why:'깊은 네트워크가 왜 "그냥 층을 더 쌓으면" 안 좋아지는지, LSTM 의 게이트·ResNet 의 잔차·BatchNorm·경사 클리핑이 각각 정확히 무엇을 겨냥한 처방인지 구분하지 못하면 학습이 안 될 때(loss 가 NaN, 혹은 초기부터 전혀 안 줄어듦) 원인을 못 찾는다.',

sections:[
 {h:'왜 곱셈이 문제인가', d:'[역전파](#/c/backprop-c)는 층 $l$ 의 경사를 구하려고 그 뒤 모든 층의 국소 미분을 연쇄적으로 곱한다. 층이 $L$ 개면 이 곱은 $L$ 개의 항으로 이뤄진 곱셈이 된다. 각 항이 평균적으로 1보다 조금이라도 작거나 크면, $L$ 이 커질수록 전체 값은 $r^L$ 형태로 **지수적으로** 0 이나 무한대에 가까워진다 — 산술이 아니라 기하급수의 문제다.'},
 {h:'경사 소실의 흔한 원인', d:'sigmoid 의 미분은 최댓값이 0.25, tanh 는 1이지만 대부분의 구간에서 1보다 훨씬 작다. 깊은 네트워크에서 이런 활성 함수를 쓰면 각 층을 지날 때마다 경사가 크게 줄어, 입력에 가까운 층은 사실상 경사를 못 받아 학습이 멈춘 것처럼 보인다. RNN 은 같은 가중치 행렬을 시간축으로 반복 곱하므로 시퀀스가 길수록 이 문제가 특히 심하다.'},
 {h:'경사 폭주의 흔한 원인', d:'가중치 초기값이 크거나, 활성 함수 미분이 1보다 크게 나오는 구간이 반복되면 경사가 지수적으로 커진다. 손실이 갑자기 NaN 이 되거나 급등하는 학습 곡선은 대체로 이 신호다. RNN·매우 깊은 CNN 에서 특히 잘 관찰된다.'},
 {h:'처방들 — 각각 무엇을 푸는가', d:'서로 다른 원인을 겨냥한 서로 다른 처방이라 섞어 써도 되고, 하나로 모든 걸 해결하지도 않는다. [LSTM](#/p/lstm)의 게이트는 셀 상태를 곱셈이 아니라 **덧셈**으로 갱신해(잊음 게이트가 1에 가까우면 경사가 거의 그대로 흐름) 긴 시퀀스에서도 신호가 유지되게 한다. [ResNet](#/p/resnet)의 잔차 연결은 $y=x+f(x)$ 형태로 만들어, 경사가 $f$ 의 미분을 거치지 않고 **항등 경로로 곧장** 앞 층까지 흐를 수 있는 지름길을 만든다. [BatchNorm](#/p/batchnorm)은 각 층의 입력 분포를 정규화해 활성값이 포화 구간(미분이 0에 가까운 구간)에 덜 빠지게 한다. 경사 클리핑은 경사의 **크기**를 사후에 강제로 제한해 폭주만 막는 응급처치다. 좋은 초기화(Xavier/He)는 애초에 층을 지날 때 분산이 보존되도록 가중치 스케일을 맞춰 문제가 덜 생기게 한다.'}
],

math:[
 {tex:'\\frac{\\partial \\mathcal{L}}{\\partial \\theta^{(1)}}=\\frac{\\partial \\mathcal{L}}{\\partial a^{(L)}}\\prod_{l=2}^{L}\\frac{\\partial a^{(l)}}{\\partial a^{(l-1)}}\\cdot\\frac{\\partial a^{(1)}}{\\partial \\theta^{(1)}}',
  expr:'gradient as a product', d:'첫 층의 경사는 뒤따르는 $L-1$ 개 층의 국소 미분을 모두 곱한 것이다. 이 곱의 항 수가 층 수 $L$ 이라는 점이 핵심이다.'},
 {tex:'r<1 \\Rightarrow r^L \\to 0\\ (\\text{소실}),\\qquad r>1 \\Rightarrow r^L \\to \\infty\\ (\\text{폭주})',
  expr:'exponential decay/growth', d:'각 층의 평균 미분 크기를 $r$ 이라 하면, $L$ 층 후 누적 효과는 $r^L$. $r=0.9$, $L=50$ 이면 $0.9^{50}\\approx 0.005$ — 사실상 0이다. $r=1.1$, $L=50$ 이면 $1.1^{50}\\approx 117$ — 쉽게 발산한다.'},
 {tex:'y=x+f(x)\\ \\Rightarrow\\ \\frac{\\partial y}{\\partial x}=1+\\frac{\\partial f}{\\partial x}',
  expr:'residual gradient', d:'잔차 연결의 미분에는 상수항 1이 항상 포함된다. $\\partial f/\\partial x$ 가 아무리 작아져도(심지어 0이어도) 경사는 최소 1은 그대로 전달된다 — 이것이 잔차 연결이 소실을 막는 정확한 메커니즘이다.'}
],

diagram:{type:'matrix', cap:'깊이(층 수)와 층당 평균 미분(r) 이 누적 경사 크기를 어떻게 좌우하는가 — r^L 값(0~1로 정규화, 클수록 밝음).',
 cols:['L=10','L=50','L=100'],
 rows:[
  {t:'r=0.9 (소실)', v:[0.35,0.01,0]},
  {t:'r=1.0 (안정)', v:[0.6,0.6,0.6]},
  {t:'r=1.1 (폭주)', v:[0.9,1,1]}
 ]},

confuse:[
 {a:'경사 소실', b:'경사 폭주', d:'둘 다 "층이 깊어지며 경사가 곱해져 생기는" 같은 근본 원인(연쇄 법칙의 지수적 누적)에서 나오지만, 국소 미분의 평균 크기가 1보다 작은지(소실) 큰지(폭주)로 갈린다. 소실은 학습이 멈춘 것처럼 조용히 실패하고, 폭주는 loss 가 NaN 으로 튀며 시끄럽게 실패한다.'},
 {a:'경사 소실', b:'[죽은 ReLU](#/c/activation)', d:'경사 소실은 층이 깊어서 생기는 구조적 문제고, 죽은 ReLU 는 개별 뉴런의 입력이 음수 영역에 고정돼 그 뉴런 하나의 미분이 0이 되는 문제다. 둘 다 "경사가 0에 가깝다"는 증상은 같지만 원인과 처방(구조 변경 vs 활성 함수 교체)이 다르다.'},
 {a:'경사 클리핑', b:'[가중치 정칙화](#/c/regularization)(weight decay)', d:'경사 클리핑은 역전파로 구한 경사 벡터의 크기를 스텝마다 제한하는 학습 안정화 기법이고, weight decay 는 손실에 가중치 크기에 대한 벌점을 더해 과적합을 억제하는 기법이다. 둘 다 "크기를 억제한다"는 인상 때문에 헷갈리지만 대상(경사 vs 가중치)과 목적(안정화 vs 일반화)이 다르다.'}
],

pitfalls:[
 '"경사가 작다 = 수렴했다"가 아니다. 학습 초반에 경사가 이미 매우 작다면 수렴이 아니라 소실을 의심해야 한다 — 층별 경사 크기를 로그로 찍어 비교하는 것이 가장 빠른 진단이다.',
 'RNN 계열은 CNN·Transformer 보다 경사 소실에 훨씬 취약하다 — 시간축으로 같은 가중치가 반복 곱해지기 때문이다. 시퀀스가 길면 [LSTM](#/p/lstm)/GRU 나 어텐션 기반 구조를 우선 고려하는 이유다.',
 '경사 클리핑은 폭주의 증상을 억제할 뿐 원인(나쁜 초기화, 지나치게 큰 학습률)을 고치지 않는다 — 클리핑 없이는 아예 못 돌아가는 상황이라면 학습률과 초기화부터 점검해야 한다.'
],

papers:['lstm','resnet','batchnorm'],
terms:['backprop-c','activation','normalization']
});
