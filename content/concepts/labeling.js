WIKI.concept({
slug:'labeling',

tldr:'원본 데이터에 정답(레이블)을 사람 또는 자동화된 도구가 붙이는 과정으로, 모델이 실제로 도달할 수 있는 성능의 상한선을 사실상 결정하는 단계다.',

why:'"모델이 이상하게 특정 케이스에서 계속 틀린다"는 문제의 원인이 모델이 아니라 라벨 자체에 있는 경우가 실무에서 매우 흔하다. 라벨링 가이드라인이 모호하거나 라벨러 간 합의가 낮으면, 모델은 애초에 일관되지 않은 정답을 맞히려 애쓰는 셈이라 아무리 구조를 바꿔도 성능이 오르지 않는다. 라벨 품질을 측정하고 관리하는 것은 모델 튜닝만큼(때로는 그 이상) 중요한 실무 스킬이고, 정작 실무에서는 가장 소홀히 다뤄지는 단계이기도 하다.',

sections:[
 {h:'라벨링의 형태', d:'과제에 따라 라벨의 형태가 완전히 다르다 — 분류는 클래스 하나(또는 여러 개), 객체 검출은 [바운딩 박스](#/c/bbox-nms), 분할은 픽셀 단위 마스크, [SAM](#/p/sam)류는 클릭 몇 번으로 마스크를 뽑아내는 상호작용형 라벨링을 쓴다. 형태가 복잡해질수록 라벨 하나를 만드는 비용과 라벨러 간 편차가 함께 커지므로, 과제를 설계할 때 필요한 라벨 형태를 최소한으로 단순화할 수 있는지부터 검토하는 것이 예산을 아끼는 첫걸음이다.'},
 {h:'라벨러 간 합의도', d:'같은 샘플을 여러 라벨러에게 독립적으로 맡겨 결과를 비교하는 합의도 측정이 라벨 품질을 정량화하는 표준 방법이다. 범주형 라벨에는 Cohen kappa·Fleiss kappa를, 연속값에는 상관계수를 흔히 쓴다. 합의도가 낮다면 모델 탓을 하기 전에 가이드라인을 더 구체화하거나 애매한 경계 케이스에 대한 규칙을 추가해야 한다 — 이 상한이 곧 모델이 도달할 수 있는 이론적 상한과 맞닿아 있다.'},
 {h:'모델 보조 라벨링', d:'모델이 먼저 예측을 만들고 사람은 그것을 교정만 하는 human-in-the-loop 방식은 처음부터 사람이 전부 그리는 것보다 훨씬 빠르다 — [SAM](#/p/sam)의 데이터 엔진이 대표적으로, 모델이 점점 좋아질수록 사람의 개입이 클릭 하나로 줄어드는 식으로 라벨링 자체를 가속했다. 다만 모델이 이미 편향된 예측을 하면 사람이 그 편향을 그대로 승인하는(rubber-stamping) 위험이 있어, 무작위 표본은 처음부터 다시 라벨링해 검수하는 절차가 필요하다.'},
 {h:'품질 관리 체계', d:'실무에서는 라벨러 교육 세트와 정답이 이미 확실한 골드 세트(gold set)를 라벨링 큐 사이사이에 몰래 섞어 넣어, 라벨러별 정확도를 상시 모니터링한다. 여러 라벨러가 같은 샘플을 겹쳐서 라벨링하게 하고 다수결·평균으로 최종 라벨을 정하는 것도 개별 라벨러의 실수를 걸러내는 표준적인 방법이다. 이런 장치 없이 한 명이 한 번 매긴 라벨을 그대로 정답으로 쓰면, 그 한 사람의 실수·편향이 그대로 학습 데이터에 새겨진다.'},
 {h:'실무에서', d:'라벨링 가이드라인 문서는 코드 리뷰처럼 버전 관리하고, 새 경계 케이스가 발견될 때마다 가이드라인을 갱신하고 과거 라벨도 그 기준으로 재검토가 필요한지 판단한다. 라벨링 예산이 제한적일 때는 모든 데이터를 균등하게 라벨링하기보다, 모델이 가장 헷갈려하는 샘플을 우선 골라 라벨링하는 능동 학습(active learning) 전략으로 예산 대비 성능 개선을 극대화한다.'},
 {h:'외주·크라우드소싱', d:'라벨링을 외부 업체나 크라우드소싱 플랫폼에 맡기면 사내 전문가가 직접 라벨링하는 것보다 속도·비용 면에서 유리한 경우가 많지만, 도메인 지식이 필요한 과제(의료 영상 판독 등)에서는 합의도가 크게 떨어질 수 있다. 이런 경우 첫 단계는 비전문가도 판단할 수 있는 쉬운 질문으로 나누고, 전문 판단이 필요한 부분만 전문가에게 맡기는 식으로 작업을 단계별로 분리하는 것이 비용 대비 품질을 지키는 흔한 전략이다.'}
],

math:[
 {tex:'\\kappa=\\dfrac{p_o-p_e}{1-p_e}', expr:"Cohen's kappa", d:'$p_o$ 는 라벨러들이 실제로 일치한 비율, $p_e$ 는 두 라벨러가 우연히 일치할 확률(각 라벨러의 클래스 분포로 계산). 단순 일치율과 달리 "우연히 맞았을 확률"을 빼서 보정하므로, 클래스가 불균형할 때도 신뢰할 수 있는 합의도 지표다.'}
],

code:{lang:'python', d:'골드 세트를 활용해 라벨러별 정확도를 계산하는 최소 예 — 라벨링 큐에 골드 세트를 섞어 넣고 그 결과만 따로 집계한다.', src:'from sklearn.metrics import cohen_kappa_score\n\n# annotator_a, annotator_b: 같은 골드 세트에 매긴 라벨\nkappa = cohen_kappa_score(annotator_a, annotator_b)\nprint(f"agreement kappa = {kappa:.3f}")\n\n# 라벨러별 골드 세트 정확도로 품질 모니터링\nfor annotator_id, labels in per_annotator_gold_labels.items():\n    acc = (labels == gold_truth).mean()\n    if acc < 0.85:\n        flag_for_retraining(annotator_id)'},

diagram:{type:'flow', cap:'가이드라인과 합의도 측정이 맞물려 도는 품질 루프.',
 nodes:[
  {t:'가이드라인', s:'정의·경계 케이스'},
  {t:'라벨링', s:'사람 또는 모델보조'},
  {t:'합의도 측정', s:'kappa 등'},
  {t:'가이드라인 보완', s:'저합의 구간 개선'}
 ]},

confuse:[
 {a:'라벨링(annotation)', b:'[데이터 품질](#/c/data-quality)', d:'라벨링은 정답을 붙이는 과정 자체를 가리키고, 데이터 품질은 원본 데이터·라벨 모두를 포함해 중복·노이즈·오염까지 아우르는 더 넓은 개념이다. 라벨이 아무리 정확해도 원본 데이터가 중복·저품질이면 데이터 품질 문제는 그대로 남는다.'},
 {a:'라벨러 간 합의도', b:'모델 정확도', d:'합의도는 사람들끼리 서로 얼마나 같은 답을 내는가를 재는 것이고, 정확도는 모델 예측이 (합의된) 정답과 얼마나 일치하는가를 재는 것이다. 합의도가 낮은 과제에서는 정확도 수치 자체의 상한이 애초에 100%가 될 수 없다.'},
 {a:'능동 학습(active learning)', b:'무작위 샘플링 라벨링', d:'무작위 샘플링은 데이터 분포를 대표하는 라벨셋을 만들지만 예산 대비 정보량이 낮을 수 있고, 능동 학습은 모델이 불확실해하는 샘플을 우선 골라 같은 예산으로 더 큰 성능 개선을 노린다 — 다만 편향된 부분집합만 라벨링하게 될 위험은 따로 관리해야 한다.'},
 {a:'라벨링(annotation)', b:'[특징 공학](#/c/feature-engineering)', d:'라벨링은 예측해야 할 정답(y)을 만드는 과정이고, 특징 공학은 그 예측에 쓸 입력(x)을 설계하는 과정이다. 둘 다 사람의 도메인 지식이 크게 관여한다는 공통점이 있지만, 지도학습 파이프라인에서 서로 다른 변에 놓여 있다.'}
],

pitfalls:[
 '라벨러 간 합의도를 한 번도 측정하지 않은 채 낮은 모델 성능을 전부 모델 구조 문제로 돌리는 경우가 흔하다 — 라벨 자체의 일관성부터 먼저 확인해야 한다.',
 '모델 보조 라벨링에서 사람이 모델 예측을 그대로 승인만 하면, 모델의 기존 편향이 새 라벨에 그대로 굳어져 다음 학습에서 편향이 오히려 증폭될 수 있다.',
 '가이드라인이 문서로만 존재하고 실제 라벨에 일관되게 적용됐는지 검수하지 않으면, 시간이 지나며 라벨러마다 서로 다른 암묵적 기준으로 흘러가기 쉽다(라벨 드리프트).',
 '초기 소수 데이터로 만든 가이드라인을 전체 데이터에 그대로 적용하면, 초기 표본에 없던 새로운 경계 케이스가 나타났을 때 라벨러마다 임의로 다르게 처리해 라벨 일관성이 조용히 무너질 수 있다.'
],

papers:['imagenet','sam'],
terms:['data-quality','class-imbalance','bbox-nms','feature-engineering','synthetic-data']
})
