WIKI.paper({
slug:'mae',
venue:'CVPR 2022',
authors:'He et al. (Facebook AI Research)',
arxiv:'2111.06377',

tldr:'이미지 패치의 **75%를 가리고 픽셀을 복원**하도록 학습시키는 자기지도 사전학습. 인코더가 남은 25%만 보게 하는 **비대칭 encoder–decoder** 덕분에 사전학습이 3배 이상 빨라지고, 레이블 없이 학습한 ViT-Huge가 ImageNet-1k에서 87.8%에 도달한다.',

context:'[BERT](#/p/bert)는 NLP에서 마스킹 후 복원만으로 대규모 사전학습이 된다는 걸 보였고, 그 덕에 언어 모델은 레이블 없는 텍스트로 무한히 커질 수 있었다. 비전에서 같은 시도는 계속 실패했다. [ViT](#/p/vit) 원 논문도 마스킹 복원을 실험했지만 지도학습보다 한참 나빴다. 당시 비전 자기지도의 주류는 [SimCLR](#/p/simclr)·[MoCo](#/p/moco)·[BYOL](#/p/byol) 같은 **대조학습**이었는데, 이쪽은 강한 데이터 증강 설계와 큰 배치·모멘텀 인코더 같은 장치에 성능이 크게 의존했다. MAE의 질문은 정직하다 — **BERT식 마스킹이 비전에서 안 되는 이유가 무엇인가?** 논문은 세 가지를 짚는다. (1) 그때까지 비전 백본이 CNN이라 마스크 토큰이라는 개념을 넣기 어려웠고, (2) 언어는 정보 밀도가 높지만 이미지는 **공간적으로 매우 중복적**이며, (3) 복원 대상의 성격이 다르다 — 단어는 의미 단위지만 픽셀은 저수준 신호다.',

ideas:[
 {h:'75% 마스킹 — 중복성을 이기려면 이 정도는 가려야 한다',
  lead:'이미지는 공간적으로 중복돼 있어, 문장의 15%가 아니라 패치의 75%를 가려야 과제가 어려워진다.',
  d:'문장에서 단어 하나를 가리면 문법과 의미를 동원해야 복원할 수 있어서, [BERT](#/p/bert)는 15%면 충분했다. 이미지에서 패치 하나를 가리면 **바로 옆 패치를 보간하면 거의 맞아** 공간적 중복성 때문에 과제가 시시해지므로, MAE는 마스킹 비율을 극단으로 올린다. 75%를 무작위로 가리면 이웃 보간이 불가능해지고, 모델은 "이 위치에 무엇이 있어야 하는가"를 **장면 전체의 이해로부터** 추론해야 한다. ablation에서 최적 비율은 파인튜닝·선형 프로빙 모두 75% 부근이며, 이는 NLP의 15%와 확연히 다른 값이다.'},
 {h:'비대칭 설계: 인코더는 보이는 패치만 본다',
  lead:'인코더에는 마스크 토큰을 아예 넣지 않고 남은 25%만 통과시켜 사전학습을 3배 이상 가속한다.',
  d:'MAE의 인코더는 마스크 토큰을 **아예 입력받지 않는다**. 가려지지 않은 25%의 패치만 시퀀스로 만들어 통과시킨다. 이 한 가지 선택으로 인코더의 시퀀스 길이가 1/4이 되고, attention은 그 제곱이므로 사전학습이 **3배 이상** 빨라지며 메모리도 크게 준다. 마스크 토큰은 그 뒤 디코더 입력 단계에서야 삽입된다. "75%를 가린다"가 비용이 아니라 오히려 **속도 이득**이 되는 지점이 이 논문 설계의 핵심이다.'},
 {h:'디코더는 가볍고, 학습이 끝나면 버린다',
  lead:'픽셀 복원이라는 저수준 작업은 가벼운 디코더에 맡겨 인코더 표현을 추상적으로 유지한다.',
  d:'디코더는 인코더 출력 + 마스크 토큰 전체(100% 길이)를 받아 픽셀을 복원한다. 하지만 이건 사전학습에만 쓰이는 부속이므로 작게 만든다 — 기본 8블록·512차원으로, 토큰당 연산이 인코더의 10% 미만이다. 흥미로운 건 디코더 깊이가 **선형 프로빙 성능에는 크게 영향을 주지만 파인튜닝에는 거의 영향이 없다**는 점이다. 깊은 디코더가 픽셀 복원이라는 저수준 작업을 떠맡아줘야 인코더 마지막 층이 더 추상적인 표현을 유지한다는 뜻이다.'},
 {h:'복원 대상은 그냥 픽셀 — 단 패치별로 정규화한 픽셀',
  lead:'토크나이저 없이 패치별로 정규화한 원본 픽셀의 MSE만으로 가려진 부분을 복원한다.',
  d:'토크나이저(BEiT의 dVAE 코드북 등) 없이 **원본 픽셀의 MSE**를 목표로 하고, 손실은 가려진 패치에만 건다. 여기에 각 패치의 픽셀을 그 패치 내부의 평균·표준편차로 정규화하는 트릭을 더하면 표현 품질이 눈에 띄게 오른다. 대비가 강한 패치와 밋밋한 패치의 손실 스케일을 맞춰줘서, 모델이 밝기·대비 같은 저수준 통계를 맞추는 데 낭비하지 않게 하는 효과다.'},
 {h:'대조학습과 달리 증강에 의존하지 않는다',
  lead:'무작위 마스킹 자체가 매번 다른 과제를 만들어, 강한 데이터 증강 설계가 필요 없어진다.',
  d:'[SimCLR](#/p/simclr) 계열은 crop·color jitter 같은 증강 설계가 곧 과제 설계였고, 증강을 빼면 붕괴했다. MAE는 랜덤 크롭만으로 충분하고 color jitter는 오히려 해롭다. 무작위 마스킹이 매 스텝 다른 과제를 만들어내므로 **데이터 증강의 역할을 마스킹이 대신하기 때문**이다. 큰 배치도, 모멘텀 인코더도, 음성 샘플 관리도 필요 없다.'}
],

diagram:{type:'flow', cap:'MAE 사전학습 흐름. 굵은 상자가 비용의 대부분을 차지하는 인코더인데, 전체 패치의 25%만 통과시킨다는 것이 설계의 전부다.',
 nodes:[
  {t:'패치 분할', s:'196개 (16×16)'},
  {t:'75% 무작위 제거', s:'남은 49개만'},
  {t:'ViT 인코더', s:'보이는 패치만 · 길이 1/4', acc:true},
  {t:'마스크 토큰 삽입 + 위치', s:'다시 196개'},
  {t:'경량 디코더', s:'8블록 · 512d'},
  {t:'픽셀 MSE', s:'가려진 패치에만'}
 ]},

math:[
 {expr:'L = (1/|M|) · Σ_{i ∈ M} || x̂_i - norm(x_i) ||²',
  tex:'L=\\frac{1}{|M|}\\sum_{i\\in M}\\left\\|\\hat{x}_i-\\text{norm}(x_i)\\right\\|^2',
  d:'가려진 패치 집합 $M$ 에 대해서만 평균제곱오차를 계산한다. 보이는 패치에도 손실을 걸면 성능이 떨어진다 — 복사하면 되는 쉬운 항이 학습 신호를 희석하기 때문이다. $\\text{norm}$ 은 패치 내부 평균·분산 정규화.'}
],

numbers:[
 {k:'마스킹 비율', v:'75%', d:'[BERT](#/p/bert)의 15%와 대비. 이미지의 공간적 중복성 때문'},
 {k:'ImageNet-1k top-1 · ViT-H', v:'87.8%', d:'ImageNet-1k만 사용한 방법 중 최고 (448 해상도 파인튜닝)'},
 {k:'ImageNet-1k top-1 · ViT-L', v:'85.9%', d:'같은 ViT-L을 처음부터 지도학습하면 82.5%'},
 {k:'사전학습 가속', v:'3배 이상', d:'인코더가 25%만 처리하기 때문. 메모리도 함께 감소'},
 {k:'디코더', v:'8블록 · 512d', d:'토큰당 연산이 인코더의 10% 미만. 학습 후 폐기'},
 {k:'사전학습 길이', v:'1600 에폭', d:'그때까지도 성능이 포화하지 않음'}
],

figures:[
 {f:'fig1-architecture.png',
  cap:'입력 이미지에서 회색으로 가려진 패치(75%)는 인코더에 아예 들어가지 않는다 — 인코더(회색 사다리꼴)는 남은 파란 토큰 몇 개만 처리한다. 그 출력 뒤에야 마스크 토큰(회색 막대)이 원래 위치에 다시 채워져 디코더(작은 사다리꼴)로 들어가고, 디코더가 픽셀을 복원해 target을 만든다. 인코더와 디코더의 크기·입력 길이가 비대칭인 것이 이 그림에서 봐야 할 점.',
  src:'원문 Figure 1, p.1'},
 {f:'fig2-masking-reconstruction.png',
  cap:'각 세 장 묶음이 [가려진 입력 → MAE 복원 → 원본]이다. 80% 마스킹(196개 중 39개만 보임)인데도 복원 결과가 원본의 형태·질감을 상당히 되살린다. 자세히 보면 복원 이미지가 원본과 픽셀 단위로 똑같지는 않다 — 모델이 정답을 베낀 게 아니라 장면을 그럴듯하게 채워 넣었다는 뜻이다.',
  src:'원문 Figure 2, p.2'}
],

quotes:[
 {t:'Our MAE approach is simple: we mask random patches of the input image and reconstruct the missing pixels.',
  src:'Abstract, p.1'},
 {t:'This strategy largely reduces redundancy and creates a challenging self-supervisory task that requires holistic understanding beyond low-level image statistics.',
  src:'Section 1, p.2'}
],

impact:'비전에서 **레이블 없는 사전학습이 지도학습보다 낫다**는 것을 대형 모델 규모에서 확정한 논문이다. [ViT](#/p/vit)가 열어둔 "데이터가 있어야 한다"는 조건은 [DeiT](#/p/deit)가 증류로 한 번 완화했고, MAE는 그 조건에서 **레이블 자체를 제거**해 남은 절반을 처리했다. 실용적 효과도 크다 — 모델이 클수록 이득이 커지는(ViT-H에서 가장 큼) 성질 덕에 비전 백본의 스케일업 경로가 열렸고, 검출·분할로의 전이에서도 지도학습 사전학습을 앞섰다. 방법론적으로는 대조학습이 요구하던 증강 튜닝·큰 배치·모멘텀 인코더 같은 장치를 전부 걷어내고 **BERT식 단순한 복원 목적함수**로 되돌렸다는 점에서, 비전 자기지도의 무게중심을 옮겼다.',

legacy:[
 '**마스크 이미지 모델링(MIM)의 표준** — BEiT·SimMIM 등과 함께 MIM 계열이 대조학습과 나란히, 혹은 그보다 우세한 사전학습 축으로 자리잡음',
 '**다른 모달리티로 이식** — 비디오(VideoMAE), 오디오(AudioMAE), 포인트 클라우드 등 "중복성이 큰 신호는 높은 비율로 가려라"는 레시피가 그대로 옮겨감',
 '**[DINOv2](#/p/dinov2)로 수렴** — 이후 대형 비전 특징 학습은 MAE식 복원과 [DINO](#/p/dino)식 자기증류를 결합하는 방향으로 정리됨',
 '**멀티모달 인코더의 초기화** — 레이블 없이 학습된 ViT가 [CLIP](#/p/clip) 계열과 함께 비전-언어 모델의 시각 인코더 후보로 쓰임'
],

pitfalls:[
 '**선형 프로빙 성능은 대조학습 계열보다 약하다.** MAE의 강점은 **파인튜닝**에서 나온다. 인코더를 얼리고 선형 분류기만 붙이는 용도라면 [DINO](#/p/dino)·[MoCo](#/p/moco) 쪽이 유리할 수 있으며, 논문 자체가 이 격차를 인정한다.',
 '**75%가 마법의 숫자는 아니다.** 이미지의 중복성과 패치 크기에 딸린 값이다. 패치가 크거나 해상도가 낮거나 텍스처가 희소한 도메인(의료 영상 등)에서는 최적 비율이 달라지므로, 옮겨 쓸 때는 다시 탐색해야 한다.',
 '**복원 이미지가 흐릿한 것은 결함이 아니다.** 손실이 MSE라 여러 그럴듯한 복원의 평균이 나오는 것이 정상이다. MAE의 목표는 생성 품질이 아니라 인코더의 표현이므로, 복원 샘플의 선명도로 모델을 평가하면 안 된다.'
],

links:[
 {t:'arXiv 2111.06377 — Masked Autoencoders Are Scalable Vision Learners', u:'https://arxiv.org/abs/2111.06377'},
 {t:'facebookresearch/mae (공식 코드)', u:'https://github.com/facebookresearch/mae'}
]
});
