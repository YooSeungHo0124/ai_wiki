WIKI.paper({
slug:'dpr',
venue:'EMNLP 2020',
authors:'Karpukhin, Oğuz, Min, Lewis et al. (Facebook AI · U. Washington · Princeton)',
arxiv:'2004.04906',

tldr:'질문과 문단을 각각 [BERT](#/p/bert)로 인코딩해 **내적 하나로** 검색하는 dual-encoder를, 겨우 수천 개의 질문–문단 쌍만으로 학습시켜 BM25를 크게 앞선 논문. 희소 검색에서 밀집 검색으로 넘어가는 전환점이 됐다.',

context:'2020년까지 오픈도메인 QA의 검색 단계는 사실상 TF-IDF/BM25 고정이었다. 단어가 겹치는 문서를 찾는 방식이라 빠르고 튼튼하지만, **어휘 불일치**에 무력하다 — "Who is the bad guy in Lord of the Rings?"는 "Sala Baker played the villain Sauron"이라는 문단과 단어가 거의 겹치지 않는다. 밀집 벡터 검색 시도가 없었던 것은 아니지만, 당시 통념은 "**아주 많은 데이터로 사전학습해야만** 밀집 검색이 BM25를 이긴다"였고, 실제로 직전 연구인 ORQA는 Inverse Cloze Task라는 별도의 비지도 사전학습을 거쳐야 했다. 이 논문은 그 통념을 정면으로 부정한다 — 추가 사전학습 없이, **1000개 정도의 예시**만으로도 이미 BM25를 이긴다.',

ideas:[
 {h:'Dual-encoder: 문단은 미리, 질문은 나중에',
  lead:'질문과 문단을 따로 인코딩해 문단 벡터를 미리 계산해 둔다.',
  d:'질문 인코더 $E_Q$ 와 문단 인코더 $E_P$ 를 따로 둔다. 두 인코더는 각각 [BERT](#/p/bert)-base이고, `[CLS]` 토큰의 768차원 벡터의 **단순 내적**을 유사도로 쓴다. 문단 벡터 2100만 개는 오프라인에서 미리 계산해 FAISS 인덱스로 굽고, 질의 시점에는 질문 한 번만 인코딩하면 된다. cross-encoder(질문과 문단을 붙여 넣는 방식)가 정확도는 높지만 2100만 번의 forward가 필요해 검색에는 쓸 수 없다 — **정확도를 조금 포기하고 사전 계산 가능성을 사는 것**이 이 구조의 본질이다.'},
 {h:'In-batch negatives: 배치 안의 남의 정답이 내 오답이다',
  lead:'배치 안 다른 질문의 정답 문단을 공짜 음성 샘플로 재사용한다.',
  d:'대조학습에서 가장 비싼 부분은 음성 샘플 확보다. DPR은 배치 안의 $B$ 개 질문–문단 쌍을 모아, 질문 $i$ 에 대해 **문단 $j \\ne i$ 를 전부 음성으로** 쓴다. 추가 인코딩 비용 없이 배치 크기 128이면 질문당 127개의 음성이 생기고, 계산도 $Q P^\\top$ 행렬곱 한 번으로 끝난다. 논문의 ablation은 배치를 8→128로 키우는 것만으로 정확도가 계속 오른다는 것을 보인다.'},
 {h:'BM25 하드 네거티브 한 개',
  lead:'어휘는 겹치지만 정답이 없는 BM25 상위 문단을 오답으로 추가한다.',
  d:'무작위 음성은 대부분 너무 쉬워서 학습 후반에 신호를 주지 못한다. DPR은 각 질문마다 **BM25가 상위로 올렸지만 정답을 담지 않은 문단** 하나를 추가한다. 어휘는 겹치는데 답은 없는, 정확히 밀집 검색이 이겨야 할 종류의 오답이다. 배치 안에서 이 하드 네거티브도 공유되므로 실제로는 질문당 (127 + 배치의 하드 네거티브들)이 음성이 된다.'},
 {h:'문단 단위 100단어 고정 분할',
  lead:'위키피디아를 100단어 블록으로 쪼개고 제목을 붙여 검색 단위로 삼는다.',
  d:'위키피디아 문서를 겹치지 않는 100단어 블록으로 쪼개 2,101만 개의 검색 단위를 만들고, 각 블록 앞에 문서 제목을 붙인다. 이 "100단어 청크 + 제목" 규격은 이후 [REALM](#/p/realm)·[RAG](#/p/rag)를 포함한 오픈도메인 QA 연구의 사실상 표준 데이터가 됐다.'},
 {h:'검색이 좋아지면 리더는 단순해도 된다',
  lead:'검색만 밀집으로 바꿔도 평범한 리더로 정확도가 크게 오른다.',
  d:'검색기 위에 얹은 리더는 BERT 기반의 평범한 추출형 span 예측기다. 별도 재순위(re-ranking) 없이 검색만 밀집으로 바꿨는데 end-to-end EM이 NQ에서 32.6 → 41.5로 올랐다. **파이프라인의 병목이 리더가 아니라 검색이었다**는 진단이 이 논문의 실질적 메시지다.'}
],

diagram:{type:'compare', cap:'희소 검색과 밀집 검색. 왼쪽은 단어가 겹쳐야 찾고, 오른쪽은 의미가 가까우면 찾는다.',
 left:{t:'BM25 (희소)', items:['어휘 겹침 기반 역색인','학습 불필요 · 도메인 이식 자유','어휘 불일치에 취약','NQ top-20 정확도 59.1%','희귀 엔티티·정확 문자열에는 여전히 강함']},
 right:{t:'DPR (밀집)', items:['질문·문단을 각각 768d 벡터로','내적 하나로 순위 결정','FAISS로 근사 최근접 탐색','NQ top-20 정확도 78.4%','학습 데이터 도메인 밖에서는 약해짐']}},

math:[
 {expr:'sim(q, p) = E_Q(q)ᵀ E_P(p)',
  tex:'\\text{sim}(q,p)=E_Q(q)^{\\top}E_P(p)',
  d:'유사도가 내적 하나뿐이라는 점이 전부다. 이 단순함 덕분에 문단 벡터를 전부 미리 계산해 두고 MIPS(최대 내적 탐색)로 sub-linear 시간에 검색할 수 있다.'},
 {expr:'L = − log [ exp(sim(q, p⁺)) / ( exp(sim(q, p⁺)) + Σ_j exp(sim(q, p⁻_j)) ) ]',
  tex:'L=-\\log\\dfrac{\\exp(\\text{sim}(q,p^{+}))}{\\exp(\\text{sim}(q,p^{+}))+\\sum_j \\exp(\\text{sim}(q,p^{-}_j))}',
  d:'정답 문단 하나에 대한 softmax 교차엔트로피(InfoNCE 형태). 음성 $p^-_j$ 는 같은 배치의 다른 질문들의 정답 문단과 BM25 하드 네거티브로 채운다.'}
],

numbers:[
 {k:'NQ top-20 검색 정확도', v:'78.4%', d:'BM25 59.1% 대비 **+19.3%p**. 논문은 5개 데이터셋에서 9~19%p 절대 향상을 보고'},
 {k:'NQ top-100', v:'85.4%', d:'BM25 73.7%'},
 {k:'end-to-end EM (NQ)', v:'41.5', d:'BM25 리트리버 32.6. [REALM](#/p/realm) 40.4보다도 높다'},
 {k:'인덱스 크기', v:'21,015,324 문단', d:'2018년 12월 위키피디아를 겹치지 않는 100단어로 분할, 제목 부착'},
 {k:'배치 / 음성 구성', v:'128 · 127 in-batch + BM25 1', d:'배치를 키울수록 성능이 오름 — 음성 개수가 곧 신호'},
 {k:'SQuAD EM', v:'29.8 (BM25 38.1)', d:'**밀집이 지는 유일한 케이스**. 질문 작성자가 문단을 보고 썼기에 어휘가 과하게 겹친다'}
],

impact:'"검색은 BM25로 하고 그 위에 신경망을 얹는다"는 15년짜리 기본값이 이 논문 이후 뒤집혔다. 벡터 데이터베이스라는 제품 범주, 임베딩 모델을 따로 파는 시장, 그리고 오늘날 모든 RAG 파이프라인의 1단계가 사실상 여기서 시작한다. 기술적으로도 in-batch negatives는 이후 텍스트 임베딩 학습의 기본 레시피가 되어 [CLIP](#/p/clip)류의 멀티모달 대조학습과 같은 형식을 공유하게 됐다.',

legacy:[
 '**생성과의 결합** — DPR 리트리버를 그대로 가져다 seq2seq 생성기에 붙인 것이 [RAG](#/p/rag)이며, 오늘날 실무 RAG의 골격이 그 구성',
 '**검색기 사전학습** — 검색기를 미세조정이 아니라 사전학습 단계부터 함께 학습하는 [REALM](#/p/realm)과 같은 시기에 나와 서로 다른 답을 제시',
 '**하이브리드 검색의 정착** — 논문 자체가 BM25+DPR 조합이 일부 데이터셋에서 더 낫다고 보고했고, 실무에서는 지금도 희소+밀집 혼합(RRF 등)이 기본값',
 '**임베딩 모델 산업화** — dual-encoder + in-batch negatives 레시피가 범용 텍스트 임베딩 모델 학습의 표준 형태로 굳음'
],

pitfalls:[
 '**밀집 검색이 항상 이기지는 않는다.** SQuAD에서는 BM25(38.1)가 DPR(29.8)을 크게 이긴다. 희귀 고유명사, 제품 코드, 정확한 문자열 일치가 중요한 질의는 지금도 희소 검색이 유리하며, 그래서 실무 기본값은 순수 밀집이 아니라 **하이브리드**다.',
 '**학습 도메인 밖으로 잘 나가지 못한다.** DPR은 QA 데이터셋에서 지도학습된 검색기다. 학습 분포와 다른 도메인(사내 문서, 법률, 코드)에 그대로 얹으면 BM25보다 못한 경우가 흔하다. 도메인 데이터로의 재학습 또는 하이브리드가 필요하다.',
 '**인덱스는 정적이다.** 문단 인코더가 바뀌면 2100만 개 벡터를 전부 다시 계산해야 한다. 그래서 DPR은 문단 인코더를 학습 후 고정하고, 이 "인덱스 갱신 비용" 문제를 정면으로 다룬 것이 [REALM](#/p/realm)의 비동기 리프레시다.'
],

figures:[
 {f:'fig1-topk-accuracy.png',
  cap:'x축은 검색해 오는 문단 개수 k, y축은 그중에 정답이 실제로 들어있는 비율(top-k accuracy). 맨 아래 파란 BM25 선 위로 DPR 선들이 전부 올라가 있는데, 주황(학습 예시 단 1000개로 학습한 DPR)조차 k가 커지면 BM25를 앞지른다 — 밀집 검색기가 희소 검색기(BM25)를 이기는 데 방대한 학습 데이터가 필요하지 않다는 뜻이다. 위로 갈수록(초록→빨강→보라→갈색) 학습 데이터가 10k→전체 59k로 늘어난 결과다.',
  src:'원문 Figure 1, p.5'}
],

quotes:[
 {t:'While a simple dual-encoder approach can be made to work surprisingly well, we showed that there are some critical ingredients to training a dense retriever successfully.',
  src:'Section 6 (Conclusion), p.9'}
],

links:[
 {t:'arXiv 2004.04906 — Dense Passage Retrieval for Open-Domain Question Answering', u:'https://arxiv.org/abs/2004.04906'},
 {t:'공식 구현 (facebookresearch/DPR)', u:'https://github.com/facebookresearch/DPR'},
 {t:'FAISS — 대규모 유사도 검색 라이브러리', u:'https://github.com/facebookresearch/faiss'}
]
});
