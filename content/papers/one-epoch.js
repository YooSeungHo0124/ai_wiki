WIKI.paper({
slug:'one-epoch',
venue:'arXiv-only (2019)',
authors:'Aran Komatsuzaki (Georgia Tech)',
arxiv:'1906.06669',

tldr:'같은 데이터를 여러 epoch 재사용하는 대신, **데이터를 더 모아서 딱 한 번만 훑도록** 학습을 재구성하면 같은 연산 예산으로 더 좋은 언어모델을 얻는다는 것을 보인 논문. 표본을 반복하지 않으면 과적합이 사라지고 정규화(dropout)는 오히려 학습을 늦출 뿐이라는 실험적 관찰이 핵심이다.',

context:'2019년 당시 [GPT](#/p/gpt1)는 100epoch, [BERT](#/p/bert)는 40epoch, Sparse Transformer는 70~120epoch을 도는 것이 관행이었다. 문제는 이 관행이 지도학습 시절의 습관을 비지도 언어모델 학습에 그대로 옮겨온 것이라는 점이다. 지도학습은 라벨링 비용 때문에 데이터가 항상 부족하지만, 웹텍스트 기반 비지도 학습은 사실 **데이터를 모으는 비용이 학습 비용보다 훨씬 싸다** — GPT-2의 40GB WebText를 몇 배로 불리는 것은 인터넷 규모를 생각하면 어렵지 않은 반면, 그 데이터로 학습하는 것 자체는 이미 수만 달러가 든다. 그렇다면 같은 연산 예산에서 "적은 데이터를 여러 번 보는 것"과 "많은 데이터를 한 번만 보는 것" 중 무엇이 나은가라는 질문을 이 논문이 실험으로 처음 답한다.',

ideas:[
 {h:'one epoch 변환: 데이터를 늘리고 반복을 없앤다',
  lead:'같은 반복 횟수를 유지한 채 데이터셋만 (반복 횟수)배로 키워 표본 재사용을 없앤다.',
  d:'기존 multi-epoch 설정에서 $E$번 반복하던 것을, 데이터셋을 $E$배로 늘려 **정확히 1 epoch**만 도는 것으로 바꾼다. 반복 횟수(iteration 수)는 그대로이므로 연산량은 동일하다. $E$ epoch 학습은 원본 데이터셋의 $E$개 사본을 섞어 1 epoch 도는 것과 사실상 같으므로, 원본 대비 표본 다양성이 $E$배 낮았던 셈이다.'},
 {h:'정규화를 걷어낸다',
  lead:'표본을 두 번 보지 않으면 과적합이 원천적으로 없으므로 dropout 등 정규화가 필요 없다.',
  d:'과적합은 학습 분포에서 같은 표본을 반복 추출하는 데서 온다. 데이터 매니폴드에서 매번 새 표본을 뽑는 one epoch 설정에서는 이 반복이 없으므로 과적합이 관측되지 않았고, dropout을 켜면 오히려 학습 속도만 늦춘다(같은 반복 수에서 손실이 더 높다). 검증 세트로 조기 종료를 판단할 필요도 줄어든다 — 최근 $n$개 미니배치의 train loss 평균이 곧 test loss에 가깝기 때문이다.'},
 {h:'모델 크기·반복 수를 T/P ≈ 5로 재배분',
  lead:'토큰 수 T와 파라미터 수 P의 비율을 5 근처로 맞추도록 폭(width)과 반복 수를 재조정한다.',
  d:'연산 예산 $P \\times I$(파라미터 수 × 반복 수)를 고정한 채, LM1B 부분집합 실험에서 폭을 바꿔가며 최적 반복 구간을 찾으면 $T/P$(토큰/파라미터)의 최적 구간 교집합이 $[1.8, 11.5]$로 나온다. 이 구간의 기하평균이 약 5이므로, "파라미터당 토큰 5개"를 목표로 폭을 재조정하는 것이 두 번째 최적화 지렛대다. 이 결과는 3년 뒤 [Chinchilla](#/p/chinchilla)가 훨씬 큰 스케일에서 도출한 최적 토큰/파라미터 비율 논의의 초기 버전에 해당한다.'},
 {h:'속도 향상은 원래 epoch 수가 클수록 커진다',
  lead:'원래 10epoch 돌리던 설정을 1epoch로 바꾸면 1.9~3.3배, 크기 조정까지 더하면 3.3~5.1배 빨라진다.',
  d:'과파라미터화된 모델(d=1024, Bottom)과 적정 크기 모델(d=512, Left)에서는 3.3배, 저파라미터화된 모델(d=256, Right)에서는 1.9배의 속도 향상이 나왔다. 여기에 폭 재조정(size/iteration adjustment)을 더하면 Right 케이스는 2.7배가 추가로 곱해져 총 5.1배까지 향상된다. 원래 epoch 수가 클수록(BERT 40, Sparse Transformer 70~120) 이 효과가 더 크게 나타날 것이라고 저자는 추정한다.'}
],

diagram:{type:'compare', cap:'같은 연산 예산 안에서 데이터를 반복해서 보는 기존 방식과, 데이터를 늘려 한 번만 보는 방식의 차이.',
 left:{t:'기존: 소량 데이터 반복', items:['같은 45M 토큰을 10epoch','정규화로 과적합 억제','손실 안정화 후 정체']},
 right:{t:'이 논문: 1 epoch', items:['450M 토큰을 1epoch만','정규화 불필요·오히려 방해','같은 연산으로 손실 더 낮음']}
},

math:[
 {expr:'ℓ(x) = a·x^(-k)  (test loss vs iterations, log-log 선형 구간)',
  tex:'\\ell(x)=a\\,x^{-k}',
  d:'one epoch 학습에서는 test loss가 반복 수 $x$ 에 대해 멱함수 법칙을 따른다. 초반의 초다항적(super-polynomial) 감소 구간과 후반의 포화 구간 사이에, 로그-로그 그래프에서 직선으로 보이는 멱법칙 구간이 존재한다.'},
 {expr:'argmin |log(5) − log(T/P)|  subject to P·T = P0·T0',
  tex:'\\operatorname*{arg\\,min}_{P,T}\\;\\bigl|\\log 5-\\log(T/P)\\bigr|\\quad\\text{s.t. } PT=P_0T_0',
  d:'연산 예산 $P_0 T_0$ 를 고정한 채, 토큰/파라미터 비율이 5에 가장 가까워지도록 파라미터 수 $P$ 와 처리 토큰 수 $T$ 를 재배분하는 휴리스틱.'}
],

numbers:[
 {k:'one epoch 전환만의 속도 향상', v:'1.9~3.3배', d:'10epoch → 1epoch(데이터 10배 확장), 모델 폭 256/512/1024에서 각각 1.9/3.3/3.3배 (Table 2)'},
 {k:'크기·반복 재조정까지 결합', v:'3.3~5.1배', d:'one epoch 전환에 폭 재조정(T/P≈5)까지 더한 총 속도 향상 (Table 4)'},
 {k:'최적 토큰/파라미터 비율', v:'T/P ≈ 5', d:'LM1B 실험에서 얻은 최적 구간 [1.8, 11.5]의 기하평균'},
 {k:'실험 데이터셋', v:'LM1B', d:'1 Billion Word Language Model Benchmark, base Transformer decoder 사용'},
 {k:'권장 dropout', v:'p=0.1 초과 시 성능 저하', d:'one epoch 설정에서 정규화가 강할수록 학습만 느려짐'}
],

impact:'"데이터가 충분하면 반복하지 말고 한 번만 보라"는 이 결과는, 당시 지배적이던 다중 epoch 관행에 대한 실험적 반증이었다. 검증 손실 대신 train loss만으로 학습 상태를 판단할 수 있다는 관찰은 이후 대규모 사전학습에서 별도 held-out 검증을 최소화하는 관행과도 맞닿아 있다. 무엇보다 "토큰/파라미터 비율을 어느 값으로 맞출 것인가"라는 질문 자체가, 훨씬 큰 스케일에서 이를 정밀하게 답한 [Chinchilla](#/p/chinchilla) 스케일링 법칙보다 먼저 제기되었다는 점에서 계보상 의미가 있다.',

legacy:[
 '**토큰/파라미터 비율 논쟁의 초기 관측** — $T/P\\approx 5$라는 소규모 추정치가 이후 [Chinchilla](#/p/chinchilla)의 대규모 compute-optimal 비율 계산으로 정교화됨',
 '**"epoch 반복이 항상 나쁘다"는 아니라는 반례가 후속 연구에서 등장** — 데이터가 진짜로 제한된 경우 반복 epoch가 언제까지 이득인지는 [data-constrained 스케일링](#/p/data-constrained) 이 별도로 정밀하게 측정',
 '**정규화-데이터량 트레이드오프 관찰** — dropout이 데이터가 풍부할 때는 손해라는 이 논문의 관찰은 이후 대규모 사전학습에서 정규화를 거의 쓰지 않는 관행([GPT2](#/p/gpt2) 이후 계열)과 일치',
 '**검증셋 없는 학습 모니터링** — train loss 이동평균으로 test loss를 대신할 수 있다는 관찰은 이후 대규모 사전학습 로그에서 흔히 쓰이는 방식'
],

pitfalls:[
 '**슬러그 이름과 달리 이 논문은 "데이터가 부족할 때 몇 epoch까지 반복해도 되는가"를 다루지 않는다.** 그 질문(반복 epoch의 수확 체감을 정밀 측정)은 훨씬 뒤에 나온 별개의 논문 [Scaling Data-Constrained Language Models](#/p/data-constrained)가 다룬다. 이 논문(2019)의 주제는 정반대에 가깝다 — 데이터가 **풍부할 때** 굳이 반복하지 말고 한 번만 보라는 것이다.',
 '**실험 규모가 매우 작다.** 45M~128M 파라미터, 65,000 반복 수준의 LM1B 실험이며, GPT-2·BERT 같은 실제 SOTA 모델에 같은 배수의 속도 향상이 그대로 재현된다는 보장은 논문 스스로도 "추정"이라고 명시한다.',
 '**dropout 무용론은 "데이터가 충분한 unsupervised 사전학습"이라는 전제 아래에서만 성립한다.** 데이터가 실제로 부족한 지도학습이나 파인튜닝 상황에는 이 결론을 그대로 적용할 수 없다고 저자도 명시적으로 단서를 단다.'
],

figures:[
 {f:'fig1-onevmulti.png',
  cap:'파란 S(one epoch)가 빨강 M(10epoch 반복)·초록 MD(반복+dropout)보다 같은 반복 수에서 test loss가 꾸준히 낮다. 반복이 늘어날수록 M·MD는 성능 향상이 둔화되는 반면 S는 계속 떨어진다 — 과적합 없이 더 오래 개선되는 구간이 이만큼 넓다는 뜻.',
  src:'원문 Figure 1(좌상단), p.5'}
],

quotes:[
 {t:'Under one epoch training, no overfitting occurs, and regularization method does nothing but slows down the training.',
  src:'Abstract, p.1'}
],

links:[
 {t:'arXiv 1906.06669 — One Epoch Is All You Need', u:'https://arxiv.org/abs/1906.06669'}
]
});
