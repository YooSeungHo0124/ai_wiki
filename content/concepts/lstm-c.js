WIKI.concept({
slug:'lstm-c',

tldr:'셀 상태라는 별도의 기억 경로와 망각·입력·출력 세 게이트로 정보를 선택적으로 남기거나 지워, 순수 RNN이 못 푸는 [경사 소실](#/c/gradient-problem) 문제를 완화한 순환 구조.',

why:'LSTM은 2017년 이전 시퀀스 모델링(번역·음성인식·시계열)의 사실상 표준이었고, 지금도 소형 모델·스트리밍·시계열 예측에서 Transformer 대신 선택되는 경우가 있다. 게이트가 각각 무엇을 하는지 모르면 왜 LSTM이 긴 의존성을 다룰 수 있는지, 왜 그럼에도 대규모 언어모델에서 밀려났는지를 설명할 수 없다.',

sections:[
 {h:'셀 상태: 별도의 기억 컨베이어벨트', d:'LSTM은 은닉 상태 $h_t$ 외에 셀 상태 $C_t$를 따로 둔다. $C_t$는 게이트를 통한 원소별 곱셈·덧셈만으로 갱신되고 비선형 함수(tanh)를 반복해서 통과하지 않으므로, 그래디언트가 여러 스텝을 거쳐도 잘 죽지 않는 "지름길"이 된다.'},
 {h:'세 게이트가 하는 일', d:'**망각 게이트(forget gate)** $f_t$는 이전 셀 상태 중 무엇을 버릴지 결정한다. **입력 게이트(input gate)** $i_t$는 새 정보 중 무엇을 셀 상태에 더할지 결정한다. **출력 게이트(output gate)** $o_t$는 갱신된 셀 상태 중 무엇을 이번 스텝의 은닉 상태(외부로 보이는 출력)로 내보낼지 결정한다. 세 게이트 모두 시그모이드로 0~1 사이 값을 내 "얼마나 통과시킬지"를 원소별로 조절한다.'},
 {h:'GRU와의 차이', d:'[GRU](#/p/gru)는 셀 상태와 은닉 상태를 하나로 합치고, 게이트도 리셋·업데이트 둘로 줄인 경량화 버전이다. 파라미터가 적어 학습이 빠르고 작은 데이터셋에서 LSTM과 비슷하거나 더 나은 성능을 내는 경우가 많지만, 표현력 자체는 LSTM이 이론상 더 유연하다.'},
 {h:'왜 대부분 Transformer로 대체됐는가', d:'LSTM은 타임스텝을 순차적으로 처리해야 해서 GPU 병렬화가 근본적으로 제한되고, 게이트로 완화했다고 해도 아주 긴 시퀀스에서는 여전히 정보가 흐려진다. [셀프 어텐션](#/c/self-attention)은 모든 위치를 한 번에 직접 참조해 병렬 학습이 가능하고 임의의 두 토큰 사이 거리가 항상 1이라, 대규모 데이터·긴 문맥에서 LSTM을 압도했다. 그래도 파라미터가 적어 지연시간이 중요한 소형 모델, 데이터가 끊임없이 들어오는 스트리밍 처리, 관측치가 짧고 순서가 절대적인 시계열 예측에서는 계산 비용과 단순함 때문에 LSTM/GRU가 여전히 실무에서 쓰인다.'}
],

math:[
 {tex:'f_t=\\sigma(W_f[h_{t-1},x_t]+b_f),\\; i_t=\\sigma(W_i[h_{t-1},x_t]+b_i),\\; o_t=\\sigma(W_o[h_{t-1},x_t]+b_o)',
  expr:'세 게이트', d:'모두 이전 은닉 상태와 현재 입력을 이어 붙인 벡터에 각자의 가중치를 곱하고 시그모이드를 씌운 것. 값이 0에 가까우면 "닫힘", 1에 가까우면 "열림"이다.'},
 {tex:'C_t = f_t \\odot C_{t-1} + i_t \\odot \\tanh(W_C[h_{t-1},x_t]+b_C)',
  expr:'셀 상태 갱신', d:'망각 게이트가 이전 셀 상태를 얼마나 남길지, 입력 게이트가 새로 계산한 후보값을 얼마나 더할지를 각각 결정한다. $\\odot$는 원소별 곱이라, 이 갱신에는 그래디언트를 지수적으로 줄이는 반복된 행렬곱이 없다.'},
 {tex:'h_t = o_t \\odot \\tanh(C_t)',
  expr:'은닉 상태(출력)', d:'셀 상태를 tanh로 다시 압축한 뒤 출력 게이트로 걸러 이번 스텝의 외부 출력을 만든다. $C_t$ 자체는 다음 스텝으로 그대로 전달되는 내부 기억이다.'}
],

diagram:{type:'flow', cap:'셀 상태가 게이트들의 곱셈·덧셈만 거쳐 다음 스텝으로 넘어간다.',
 nodes:[
  {t:'C_{t-1}', s:'이전 셀 상태', a:'forget으로 스케일'},
  {t:'⊙ f_t + ⊙ i_t', s:'버릴 것·더할 것 결정'},
  {t:'C_t', s:'갱신된 셀 상태', a:'tanh 후 output으로 게이팅'},
  {t:'h_t', s:'이번 스텝 출력'}
 ]},

confuse:[
 {a:'LSTM', b:'GRU', d:'LSTM은 셀 상태·은닉 상태를 분리하고 게이트가 3개(망각·입력·출력)이며 표현력이 더 크다. GRU는 상태를 하나로 합치고 게이트가 2개(리셋·업데이트)로 파라미터가 적어 더 빠르고 작은 데이터에서 과적합이 덜하다. 어느 쪽이 항상 우월한지는 과제에 따라 다르다.'},
 {a:'셀 상태', b:'은닉 상태', d:'셀 상태 $C_t$는 게이트로 보호되는 장기 기억 경로로 외부로 직접 노출되지 않고, 은닉 상태 $h_t$는 그 셀 상태를 걸러서 만든 이번 스텝의 실제 출력값이자 다음 스텝의 입력 일부다.'},
 {a:'LSTM', b:'Transformer', d:'LSTM은 순차 처리로 정보를 암묵적인 상태 벡터에 압축해 나르고, Transformer는 [셀프 어텐션](#/c/self-attention)으로 모든 토큰 쌍을 직접 비교한다. LSTM은 시퀀스 길이에 비례한 순차 스텝이 필요해 느리지만 메모리가 일정하고, Transformer는 병렬이지만 길이의 제곱에 비례하는 연산·메모리가 든다.'}
],

pitfalls:[
 '"LSTM은 경사 소실을 완전히 없앤다"는 과장이다. 셀 상태의 지름길 덕분에 순수 RNN보다 훨씬 완화될 뿐, 아주 긴 시퀀스(수천 스텝)에서는 여전히 정보가 흐려질 수 있다.',
 '게이트가 3개라고 해서 항상 GRU보다 낫다고 단정할 수 없다 — 데이터가 적거나 시퀀스가 짧으면 파라미터가 적은 GRU가 과적합도 덜하고 더 빨리 수렴하는 경우가 흔하다.',
 '"지금은 아무도 LSTM을 안 쓴다"는 것도 사실이 아니다. 온디바이스 음성인식, 저지연 시계열, 아주 작은 모델이 필요한 임베디드 환경에서는 여전히 실용적인 선택으로 쓰인다.'
],

papers:['lstm','gru'],
terms:['rnn','gradient-problem','self-attention']
});
