WIKI.concept({
slug:'auroc',

tldr:'ROC 곡선은 임계값을 바꿔가며 재현율과 오탐률의 궤적을 그리고, AUROC 는 그 곡선 아래 면적으로 임계값에 무관한 분리력을 잰다.',

why:'단일 임계값에서의 [정밀도·재현율](#/c/precision-recall)은 그 임계값을 어떻게 고르느냐에 따라 흔들린다. AUROC 는 임계값을 아예 없애고 모델이 양성과 음성을 얼마나 잘 갈라놓는지를 하나의 숫자로 요약하지만, 불균형 데이터에서는 이 숫자가 실제보다 낙관적으로 보일 수 있다는 것을 알아야 제대로 쓸 수 있다.',

sections:[
 {h:'ROC 곡선이 그리는 것', d:'분류 점수(확률)에 대해 임계값을 1부터 0까지 낮춰가며 매 임계값에서 참양성비율(TPR, 재현율과 동일)과 거짓양성비율(FPR)을 찍는다. 임계값이 낮아질수록 더 많은 것을 양성이라 부르니 TPR 도 FPR 도 함께 오른다 — 이 궤적이 (0,0) 에서 (1,1) 로 가는 ROC 곡선이다. 완벽한 분류기는 왼쪽 위 모서리(FPR=0, TPR=1)를 스치고, 무작위 추측은 대각선을 그린다.'},
 {h:'AUROC 의 확률적 해석', d:'AUROC 는 곡선 아래 면적이면서 동시에 "무작위로 뽑은 양성 샘플의 점수가 무작위로 뽑은 음성 샘플의 점수보다 높을 확률"과 정확히 같다. 0.5 는 무작위 추측과 다를 바 없다는 뜻이고, 1.0 은 모든 양성이 모든 음성보다 항상 높은 점수를 받는다는 뜻이다. 이 해석 덕분에 AUROC 는 임계값을 정하기 전 단계에서 모델의 순위 매기기 능력만 따로 평가할 수 있다.'},
 {h:'불균형할 때는 AUPR 을 봐야 한다', d:'FPR 의 분모는 음성 전체($TN+FP$)라서, 음성이 압도적으로 많은 데이터에서는 FP 가 꽤 늘어도 FPR 은 거의 안 움직인다. 그 결과 AUROC 는 실제 체감 성능보다 낙관적으로 나온다. 반면 정밀도-재현율 곡선 아래 면적(AUPR)은 분모에 양성 예측 수($TP+FP$)가 들어가 소수 양성 클래스에서의 실패를 훨씬 민감하게 반영한다. [MSP 기준선](#/p/msp-baseline)이나 [ODIN](#/p/odin) 같은 OOD 탐지 연구가 AUROC 와 함께 AUPR·FPR@95%TPR 을 나란히 보고하는 이유가 여기 있다.'},
 {h:'실무에서', d:'클래스가 균형 잡혀 있고 임계값을 아직 못 정한 초기 모델 비교 단계에서는 AUROC 가 편리하다. 하지만 이상 탐지·OOD 탐지·의료 스크리닝처럼 양성이 희귀한 문제에서는 AUROC 하나만 보고 "잘 된다"고 결론 내리면 안 되고, 반드시 AUPR 이나 특정 재현율에서의 정밀도(예: recall@95%)를 함께 확인해야 한다.'}
],

math:[
 {tex:'TPR=\\dfrac{TP}{TP+FN},\\quad FPR=\\dfrac{FP}{FP+TN}', expr:'ROC 좌표', d:'TPR 은 재현율과 동일(진짜 양성 중 찾아낸 비율). FPR 은 진짜 음성 중 잘못 양성이라 판단한 비율 — 분모가 음성 전체라는 점이 AUROC 가 불균형에 둔감해지는 원인이다.'},
 {tex:'\\text{AUROC}=P(s^+ > s^-)', expr:'AUROC 의 확률 해석', d:'$s^+$ 는 무작위로 뽑은 양성 샘플의 점수, $s^-$ 는 무작위로 뽑은 음성 샘플의 점수. 이 확률(=Mann-Whitney U 통계량과 동치)이 곧 ROC 곡선 아래 면적이다.'}
],

confuse:[
 {a:'AUROC', b:'AUPR', d:'AUROC 는 FPR(분모=음성 전체)을 쓰고 AUPR 은 정밀도(분모=양성 예측 수)를 쓴다. 불균형이 심할수록 두 값의 격차가 벌어지며, 그럴 때는 AUPR 이 실제 어려움을 더 정직하게 드러낸다.'},
 {a:'AUROC', b:'정확도', d:'AUROC 는 모든 임계값에 걸친 분리력을 재는 순위 지표이고, 정확도는 하나의 임계값(보통 0.5)에서의 스냅샷이다. AUROC 가 높아도 실제 배포 임계값에서의 정확도·정밀도는 따로 확인해야 한다.'},
 {a:'ROC 곡선', b:'혼동 행렬', d:'[혼동 행렬](#/c/confusion-matrix)은 한 임계값의 결과이고, ROC 곡선은 그 임계값을 전부 훑은 궤적이다.'}
],

pitfalls:[
 '클래스 불균형이 심한 문제에서 AUROC 0.95 를 보고 "잘 작동한다"고 판단하면 안 된다 — 같은 상황에서 AUPR 은 훨씬 낮게 나오는 경우가 흔하다.',
 'AUROC 는 모델 간 순위 비교에는 유용하지만, 실제 배포 시 쓸 단일 임계값의 정밀도·재현율을 알려주지 않는다.',
 '데이터셋마다 양성 비율(prevalence)이 다르면 AUROC 를 그대로 비교해도 되지만 AUPR 은 비교가 불공평해진다 — 양성 비율 자체가 AUPR 의 기준선을 바꾸기 때문이다.'
],

papers:['msp-baseline','odin'],
terms:['precision-recall','confusion-matrix','calibration']
});
