WIKI.concept({
slug:'knn',

tldr:'새 데이터의 클래스나 값을, 학습 데이터 중 가장 가까운 k개 이웃의 다수결(분류) 또는 평균(회귀)으로 정하는, 명시적 학습 과정이 없는 사례 기반 모델이다.',

why:'"거리가 가까우면 비슷하다"는 가장 직관적인 가정만으로 동작해 베이스라인으로 빠르게 시도하기 좋고, [임베딩](#/c/embedding) 공간에서의 유사도 검색(추천, 이상 탐지, [FAISS](#/p/faiss) 기반 벡터 검색)이라는 형태로 딥러닝 시스템 안에도 살아남아 있다. "모델을 학습한다"는 말이 안 통하는 몇 안 되는 알고리즘이라 그 자체로 대조군 역할을 한다. RAG 시스템의 [시맨틱 검색](#/c/semantic-search) 도 본질적으로 임베딩 공간에서의 k-NN 검색이라, 이 개념을 모르면 검색 증강 파이프라인의 핵심 연산이 뭔지 설명할 수 없다.',

sections:[
 {h:'게으른 학습', d:'k-NN 은 학습 단계에서 사실상 아무것도 하지 않는다 — 그냥 학습 데이터를 저장해 둘 뿐이다. 예측할 때마다 새 데이터와 저장된 모든 데이터 사이의 거리를 계산해 가장 가까운 $k$ 개를 찾는다. 그래서 "학습이 빠르고 예측이 느린" 정반대 성격을 가진다 — 대부분의 모델과 반대다. 이런 방식을 사례 기반 학습(instance-based learning) 이라고도 부르는데, 파라미터를 요약해 저장하는 대신 데이터 자체를 모델로 쓰는 것이다.'},
 {h:'k 선택의 트레이드오프', d:'$k=1$ 이면 가장 가까운 점 하나만 보므로 학습 데이터의 노이즈에 매우 민감하다(분산이 큼, 결정 경계가 들쭉날쭉). $k$ 를 키울수록 여러 이웃의 평균을 내므로 노이즈에 강해지지만(분산 감소), 너무 크면 다른 클래스 영역까지 섞여 들어와 경계가 뭉개진다(편향 증가). $k$ 는 보통 홀수로 두어(이진 분류에서) 동점을 피하고, 교차검증으로 여러 $k$ 값을 실제로 비교해 정하는 것이 안전하다.'},

 {h:'거리 함수와 차원의 저주', d:'유클리드 거리가 기본이지만 데이터 성격에 따라 맨해튼 거리, 코사인 유사도 등을 쓴다. 차원이 높아질수록 모든 점 사이의 거리가 비슷해져 "가장 가까운 이웃"이라는 개념 자체가 흐려지는 [차원의 저주](#/c/curse-dimensionality) 문제를 정면으로 맞는다 — 고차원 원본 특징에 k-NN 을 그대로 쓰면 성능이 잘 안 나오는 이유다. 텍스트 임베딩처럼 방향이 중요하고 크기는 덜 중요한 벡터에는 유클리드 거리보다 코사인 유사도가 더 잘 맞는다는 것도 실무에서 자주 확인되는 경험칙이다.'},
 {h:'k-NN의 후예들', d:'원본 특징 공간에서의 순수 k-NN 분류는 데이터가 많아지면 예측마다 전체 데이터와 거리를 재야 해서 느려 잘 안 쓴다. 대신 학습된 [임베딩](#/c/embedding) 공간에서 근사 최근접 이웃 검색(ANN, [FAISS](#/p/faiss) 등)을 쓰는 형태로 추천 시스템·시맨틱 검색·중복 탐지에 광범위하게 쓰인다. [소수샷 학습](#/p/prototypical)의 여러 방법도 결국 "임베딩 공간에서 가장 가까운 클래스 대표를 찾는다"는 k-NN 의 아이디어를 재사용한다.'},
 {h:'k-NN 회귀', d:'분류와 마찬가지로 회귀에도 그대로 쓸 수 있다 — 다수결 대신 $k$ 개 이웃의 타깃값 평균(또는 거리에 반비례한 가중 평균)을 예측으로 쓴다. 부동산 가격처럼 "비슷한 조건의 최근 거래가들의 평균"이라는 직관이 자연스러운 문제에서 여전히 직관적인 베이스라인으로 쓰인다 — 다만 [선형회귀](#/c/linear-regression)만큼 외삽에 강하지 않다는 점은 유의해야 한다.'}
],

math:[
 {tex:'d(x,x_i)=\\sqrt{\\textstyle\\sum_j (x_j-x_{i,j})^2}', expr:'유클리드 거리', d:'새 점 $x$ 와 학습 데이터 점 $x_i$ 사이의 거리. 모든 학습 데이터에 대해 이 값을 계산해 가장 작은 $k$ 개를 고른다. 특징이 $d$ 개, 학습 데이터가 $n$ 개면 예측 한 번에 $O(nd)$ 의 계산이 필요하다는 점이 대규모 서빙에서 병목이 되는 이유다.'},
 {tex:'\\hat y=\\text{mode}\\{y_i : x_i \\in N_k(x)\\}', expr:'k-NN 분류', d:'$N_k(x)$ 는 $x$ 의 $k$ 개 최근접 이웃 집합. 그 이웃들의 라벨 $y_i$ 중 최빈값(다수결)을 예측으로 쓴다. 회귀는 최빈값 대신 평균을 쓴다. 거리에 반비례한 가중치를 곱해 더 가까운 이웃에 더 큰 투표권을 주는 가중 k-NN 변형도 흔히 쓰인다.'}
],

diagram:{type:'flow', cap:'거리를 재고 가까운 k개의 다수결로 정한다.',
 steps:['새 점 x 입력','전체 학습 데이터와 거리 계산','거리순 정렬','가장 가까운 k개 선택','다수결(분류)/평균(회귀)']},

confuse:[
 {a:'k-NN', b:'[k-평균 군집](#/c/kmeans-c)', d:'이름의 k 가 뜻하는 게 완전히 다르다 — k-NN 의 k 는 "몇 개의 이웃을 볼지"이고 k-평균의 k 는 "몇 개의 군집으로 나눌지"다. k-NN 은 라벨이 있는 지도학습(분류·회귀)이고, k-평균은 라벨이 없는 비지도학습(군집화)이다. 이름의 유사성 때문에 두 알고리즘을 같은 계열로 착각하는 경우가 실무에서도 흔하다.'},
 {a:'정확한 k-NN', b:'근사 최근접 이웃(ANN) 검색', d:'k-NN 은 정확한 최근접 이웃을 전수 비교로 찾는 알고리즘 원리를 말하고, ANN 은 대규모 데이터에서 정확도를 약간 희생해 훨씬 빠르게 근사 이웃을 찾는 실전 구현 기법([FAISS](#/p/faiss) 등)이다. 실무 시스템에서 "k-NN 검색을 쓴다"고 하면 대개 내부적으로는 HNSW·IVF 같은 인덱스 구조를 쓰는 ANN 을 쓴다.'},
 {a:'k가 작다', b:'모델이 단순하다', d:'직관과 반대로 $k$ 가 작을수록(특히 $k=1$) 모델은 더 복잡하고 유연한(결정 경계가 들쭉날쭉한) 쪽에 가깝고, $k$ 가 클수록 더 단순하고 매끈한 경계를 만든다 — 다른 모델의 "복잡도"와 방향이 반대로 느껴지기 쉬워 헷갈린다. $k$ 를 늘리는 것을 [정규화](#/c/regularization)를 강하게 주는 것에 대응시켜 생각하면 이 방향을 기억하기 쉽다.'}
],

pitfalls:[
 '특징 스케일을 맞추지 않으면 값의 범위가 큰 특징이 거리 계산을 지배해버린다 — [특징 스케일링](#/c/scaling-features)은 k-NN 에서 선택이 아니라 필수다.',
 '고차원 원본 특징에 유클리드 거리 기반 k-NN 을 그대로 쓰면 차원의 저주로 성능이 잘 안 나오는데, 이를 모델 자체의 결함으로 오해하는 경우가 있다 — 차원 축소나 임베딩 학습이 먼저다.',
 '데이터가 크면 예측 시점의 거리 계산 비용이 커서 실시간 서빙에 그대로 쓰기 어렵다 — 대규모 서비스에서는 근사 최근접 이웃 인덱스가 필요하다.',
 'k-NN 은 범주형 특징이 섞여 있으면 유클리드 거리 정의 자체가 애매해진다 — 원-핫 인코딩 후 거리 왜곡을 감수하거나 혼합형 거리 함수(Gower distance 등)를 별도로 설계해야 한다.'
],

code:{lang:'python', d:'k-NN 분류와 근사 최근접 이웃 검색(FAISS)의 규모 차이',
 src:'from sklearn.neighbors import KNeighborsClassifier\n\n# 소규모: 정확한 k-NN\nknn = KNeighborsClassifier(n_neighbors=5)\nknn.fit(X_train, y_train)   # 스케일링을 먼저 했다고 가정\n\n# 대규모: 임베딩 공간에서 근사 검색 (개념적 예시)\n# index = faiss.IndexFlatL2(dim)\n# index.add(embeddings)          # 벡터 저장\n# D, I = index.search(query, k)  # 근사 k개 이웃 검색'},

papers:['faiss','prototypical'],
terms:['kmeans-c','curse-dimensionality','embedding','scaling-features','semantic-search']
});
