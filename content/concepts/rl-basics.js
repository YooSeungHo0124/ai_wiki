WIKI.concept({
slug:'rl-basics',

tldr:'에이전트가 환경과 상호작용하며 얻는 보상의 누적합을 최대화하도록 행동 방식(정책)을 학습하는 프레임워크로, 정답 라벨이 아니라 시행착오에서 나오는 스칼라 신호로 학습한다는 점이 지도학습과 다르다.',

why:'[RLHF](#/c/rlhf)·게임 에이전트·로봇 제어를 이해하려면 상태·행동·보상·정책·가치라는 최소 어휘가 먼저 있어야 한다. 온폴리시/오프폴리시 구분을 안 하면 "왜 이 알고리즘은 리플레이 버퍼를 쓰고 저건 못 쓰는지" 같은 실무적으로 중요한 제약을 이해할 수 없다.',

sections:[
 {h:'기본 요소', d:'**상태(state)** $s$ 는 환경의 현재 상황, **행동(action)** $a$ 는 에이전트가 그 상태에서 취하는 선택, **보상(reward)** $r$ 은 그 행동 직후 환경이 주는 스칼라 피드백이다. **정책(policy)** $\\pi(a|s)$ 은 상태를 행동으로 매핑하는 규칙(또는 확률분포)이고, 학습의 목표는 이 정책을 개선하는 것이다. **할인율(discount factor)** $\\gamma\\in[0,1]$ 은 먼 미래의 보상을 현재 가치로 환산할 때 얼마나 깎을지를 정한다 — 1에 가까울수록 장기적 이득을 중시한다.'},
 {h:'가치 함수와 벨만 방정식', d:'가치 함수 $V(s)$ 는 상태 $s$ 에서 정책을 따랐을 때 기대할 수 있는 할인된 누적 보상이다. 이 값은 "지금 받는 보상 + 다음 상태의 가치를 할인한 것"으로 재귀적으로 정의되는데, 이것이 벨만 방정식이다. 이 재귀 구조 덕분에 전체 미래를 다 펼쳐보지 않고도 한 스텝씩 값을 갱신하며 학습할 수 있다 — Q-learning·[DQN](#/p/dqn) 같은 방법들의 이론적 기반이다.'},
 {h:'가치기반 vs 정책기반', d:'가치기반(value-based) 방법은 각 상태(-행동)의 가치를 먼저 추정하고, 그 가치가 가장 높은 행동을 고르는 식으로 정책을 간접적으로 얻는다 — [DQN](#/p/dqn)이 대표적이며 이산 행동 공간에 강하다. 정책기반(policy-based) 방법은 정책 자체를 파라미터화해서 기대 보상을 높이는 방향으로 정책 파라미터를 직접 경사 상승시킨다 — 연속 행동 공간이나 확률적 정책이 필요할 때 유리하다. [PPO](#/p/ppo)·[TRPO](#/p/trpo)는 정책기반(더 정확히는 가치 추정도 함께 쓰는 액터-크리틱) 계열이고, 큰 정책 업데이트가 성능을 무너뜨리지 않도록 업데이트 폭을 제한하는 것이 핵심 아이디어다.'},
 {h:'온폴리시 vs 오프폴리시', d:'온폴리시(on-policy)는 지금 개선하려는 바로 그 정책이 수집한 경험만으로 학습한다 — 데이터가 최신 정책의 행동 분포를 정확히 반영하지만, 매 업데이트마다 새로 데이터를 모아야 해서 샘플 효율이 낮다. [PPO](#/p/ppo)가 온폴리시다. 오프폴리시(off-policy)는 과거의(다른) 정책이 남긴 경험(리플레이 버퍼)을 재사용해 지금 정책을 학습할 수 있다 — 샘플 효율은 높지만 "누가 모은 데이터인가"와 "누구를 개선하는가"가 달라 학습이 불안정해질 위험을 보정하는 장치가 필요하다. [DQN](#/p/dqn)이 오프폴리시의 대표 예다.'}
],

math:[
 {tex:'V^\\pi(s)=\\mathbb{E}_\\pi\\big[r_t+\\gamma V^\\pi(s_{t+1})\\mid s_t=s\\big]', expr:'Bellman equation', d:'상태 $s$ 의 가치는 그 다음 한 스텝의 보상 $r_t$ 와, 그 다음 상태 $s_{t+1}$ 의 가치를 할인율 $\\gamma$ 로 깎은 값의 합에 대한 기댓값이다. 이 재귀 관계가 있어서 전체 에피소드를 끝까지 기다리지 않고도 가치를 한 스텝씩 갱신(부트스트래핑)할 수 있다.'}
],

diagram:{type:'loop', cap:'에이전트-환경 상호작용의 기본 순환.',
 nodes:[
  {t:'상태 관측', s:'$s_t$'},
  {t:'정책이 행동 선택', s:'$a_t\\sim\\pi(a|s_t)$'},
  {t:'환경→보상·다음상태', s:'$r_t, s_{t+1}$'},
  {t:'정책/가치 갱신', s:'누적 보상 최대화 방향'}
 ]},

confuse:[
 {a:'강화학습', b:'지도학습', d:'지도학습은 각 입력에 대한 "정답"이 즉시 주어지고 그 오차로 직접 경사를 계산한다. 강화학습은 행동에 대한 즉각적인 정답이 없고, 나중에야(때로는 여러 스텝 뒤에) 스칼라 보상만 주어져 "그 행동이 좋았는지"를 간접적으로, 그리고 지연된 신호로 추론해야 한다.'},
 {a:'가치기반', b:'정책기반', d:'가치기반은 "각 행동이 얼마나 좋은가"를 먼저 계산한 뒤 최댓값을 고르는 간접적 방식이라 이산 행동에 자연스럽다. 정책기반은 정책을 직접 파라미터화해 기대 보상의 그래디언트를 따라가므로 연속 행동이나 확률적 탐색에 유리하다. 액터-크리틱(PPO 등)은 이 둘을 합쳐 가치 추정(크리틱)으로 정책 그래디언트(액터)의 분산을 줄인다.'},
 {a:'온폴리시', b:'오프폴리시', d:'온폴리시는 지금 정책이 만든 데이터만 쓰므로 안정적이지만 매번 새로 수집해야 해 비싸다. 오프폴리시는 과거 데이터를 재사용해 싸지만, 데이터를 만든 정책과 학습 대상 정책의 괴리를 보정하지 않으면 편향된 학습이 된다.'}
],

pitfalls:[
 '보상 함수를 잘못 설계하면 에이전트가 의도와 다른, 그러나 보상 수치상으로는 "최적"인 행동을 찾아낸다 — [RLHF](#/c/rlhf)에서 말하는 보상 해킹과 본질적으로 같은 문제다.',
 '할인율 $\\gamma$ 를 1에 너무 가깝게 두면 먼 미래까지 다 고려하려다 학습이 불안정해지고, 너무 작게 두면 근시안적인 정책이 된다.',
 '온폴리시 알고리즘에 오프폴리시 데이터(리플레이 버퍼)를 그대로 섞어 쓰면 이론적 보장이 깨진다 — 알고리즘이 어느 쪽을 전제하는지 확인하지 않고 데이터를 재사용하는 것은 흔한 구현 실수다.'
],

papers:['dqn','ppo','trpo'],
terms:['rlhf','few-shot']
});
