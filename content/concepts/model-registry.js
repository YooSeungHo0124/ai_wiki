WIKI.concept({
slug:'model-registry',

tldr:'학습이 끝난 모델 체크포인트를 이름과 버전을 붙여 관리하는 중앙 저장소로, "지금 프로덕션에 떠 있는 모델이 정확히 어느 버전이고 어떻게 만들어졌는지"를 언제든 추적할 수 있게 한다.',

why:'모델 파일 하나는 그냥 바이너리 덩어리다 — 어떤 데이터로, 어떤 코드로, 언제 학습됐는지가 파일명("model_final_v2_real_final.pt")에만 의존하면 곧 관리가 무너진다. 장애가 나서 롤백해야 할 때, 또는 "지난달 모델과 이번 모델이 정확히 뭐가 다른가"를 답해야 할 때 레지스트리 없이는 답할 수 없다.',

sections:[
 {h:'왜 필요한가', d:'모델은 코드나 데이터와 달리 git 으로 버전 관리하기 부적합하다 — 수백MB~수십GB 크기의 바이너리라 diff 도, 브랜치 병합도 의미가 없다. 그래서 모델은 별도의 저장소(레지스트리)에 두고, 각 버전에 메타데이터(학습에 쓴 코드 커밋, 데이터 버전, 평가 지표, 학습 날짜)를 함께 붙인다. 이 메타데이터가 있어야 "이 모델이 왜 이렇게 동작하는가"를 나중에 추적할 수 있다.'},
 {h:'버전 관리와 스테이지', d:'레지스트리는 보통 모델을 스테이지(개발 중 → 스테이징 → 프로덕션 → 폐기)로 관리한다. 같은 이름의 모델이라도 버전 번호가 올라가며 여러 개 공존하고, 어느 버전이 "지금 실제로 서빙 중인지"를 명시적으로 표시한다. 이 표시가 없으면 배포 스크립트가 어느 파일을 가져와야 할지 사람이 매번 확인해야 하고, 그 확인 과정에서 실수가 난다.'},
 {h:'롤백이 실무의 핵심 이유', d:'새 모델을 배포했는데 예상 못 한 문제(정확도는 올랐지만 특정 사용자군에서 오작동, 지연시간 급증 등)가 발견되면 즉시 직전 버전으로 되돌려야 한다. 레지스트리가 없으면 "직전 버전"이 무엇인지부터 찾아야 하고, 그 사이 파일이 덮어써졌을 수도 있다. 레지스트리는 이전 버전을 항상 보존해 롤백을 "배포 파이프라인의 몇 초짜리 명령"으로 만든다 — 이것이 모델 레지스트리를 두는 가장 직접적인 실무 이유다.'},
 {h:'실험 추적과의 관계', d:'[실험 추적](#/c/experiment-tracking)이 "무엇을 시도했는가"를 전부 기록한다면, 레지스트리는 그중 실제로 쓸 만하다고 판단된 결과만 선별해 배포 자산으로 승격한다. 실무 파이프라인은 보통 실험 추적 시스템에서 최선의 실행을 고른 뒤, 그 체크포인트를 레지스트리에 등록하고, 등록된 버전만 [서빙 아키텍처](#/c/serving-arch)가 로드하도록 접근을 제한한다 — 검증 안 된 체크포인트가 실수로 프로덕션에 올라가는 것을 막는 게이트 역할을 한다.'}
],

diagram:{type:'stack', cap:'레지스트리에 오른 모델이 거치는 스테이지.',
 layers:['개발 중 (실험 추적에만 존재)','등록 (레지스트리에 버전 부여)','스테이징 (검증 환경 배포)','프로덕션 (실 트래픽 서빙)','폐기 (보관만, 서빙 중단)']},

confuse:[
 {a:'모델 레지스트리', b:'모델 허브(예: [Hugging Face](#/p/huggingface))', d:'모델 허브는 누구나 공개 모델을 올리고 내려받는 공유 플랫폼에 가깝고, 레지스트리는 한 조직 내부에서 "우리가 만든 모델의 배포 이력"을 관리하는 내부 시스템이다. 목적(모델을 찾고 버전 관리한다)은 비슷하지만 신뢰 경계와 접근 통제 수준이 다르다.'},
 {a:'버전(version)', b:'스테이지(stage)', d:'버전은 "몇 번째로 학습된 모델인가"를 나타내는 불변의 식별자이고, 스테이지는 같은 버전이 지금 파이프라인의 어느 단계(개발/스테이징/프로덕션)에 있는지를 나타내는 가변 상태다. 버전 3이 오늘은 스테이징에, 내일은 프로덕션에 있을 수 있다.'},
 {a:'체크포인트(checkpoint)', b:'등록된 모델(registered model)', d:'체크포인트는 학습 도중 임의 시점에 저장한 가중치 스냅샷으로 수십~수백 개가 생길 수 있고, 등록된 모델은 그중 사람이 명시적으로 "이건 쓸 만하다"고 판단해 레지스트리에 올린 소수의 후보다.'}
],

pitfalls:[
 '모델 파일만 저장하고 그 모델이 어떤 데이터·코드·평가 결과로 나왔는지 메타데이터를 안 남기면, 나중에 문제가 생겨도 원인 추적이 불가능하다.',
 '"프로덕션" 스테이지 표시를 수동으로 관리하면 배포 스크립트와 레지스트리의 표시가 어긋나는(실제로는 다른 버전이 떠 있는) 상황이 생긴다 — 배포 자동화가 레지스트리 상태를 갱신하는 것까지 포함해야 한다.',
 '오래된 버전을 무기한 다 보관하면 저장 비용이 계속 늘어난다 — 롤백에 필요한 최근 몇 개만 남기고 나머지는 아카이브하는 보존 정책이 필요하다.'
],

papers:['huggingface'],
terms:['experiment-tracking','serving-arch','reproducibility']
});
