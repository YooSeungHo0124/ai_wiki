WIKI.concept({
slug:'distributed-training',

tldr:'여러 GPU에 학습을 나누는 세 축으로, 데이터 병렬은 배치를 쪼개고 텐서 병렬은 한 층의 행렬을 쪼개며 파이프라인 병렬은 층 자체를 쪼갠다.',

why:'"GPU를 늘리면 무조건 빨라진다"는 말은 틀렸다 — 무엇을 쪼개느냐에 따라 필요한 통신량과 풀리는 문제가 완전히 다르다. 모델이 GPU 한 장에 안 들어가는 문제와 배치를 키워 학습을 빨리 끝내고 싶은 문제는 서로 다른 병렬화로 풀어야 하는데, 이 구분 없이 "분산 학습"을 하나로 뭉뚱그리면 잘못된 설정으로 GPU만 태우고 속도는 안 느는 상황에 빠지기 쉽다. 수십~수백 대 GPU 클러스터를 실제로 쓰는 팀이라면 이 세 축을 구분하지 못하는 것 자체가 곧 예산 낭비로 직결된다.',

sections:[
 {h:'데이터 병렬', d:'모델 전체를 GPU마다 똑같이 복제해 두고, 배치를 GPU 수만큼 나눠 각자 다른 부분 배치로 순전파·역전파를 수행한다. 각 GPU가 계산한 그래디언트를 all-reduce로 평균 낸 뒤 모든 복제본을 동일하게 갱신한다. 가장 구현이 단순하고 통신량도 상대적으로 적지만, 모델 전체가 각 GPU 메모리에 통째로 들어가야 한다는 전제가 있다 — 모델이 이미 GPU 하나에 안 들어가면 이 방식만으로는 시작도 못 한다.'},
 {h:'텐서 병렬: 행렬을 쪼갠다', d:'하나의 큰 가중치 행렬(예: FFN의 선형층 하나)을 여러 GPU에 조각내 나눠 갖고, 한 번의 순전파·역전파 안에서 그 조각들이 서로 통신하며 협력해 계산을 완성한다. [Megatron](#/p/megatron)이 이 방식을 크게 대중화했다. 모델 하나가 GPU 한 장의 메모리에 안 들어갈 때, 또는 배치를 더는 못 키우는 상황에서 층 자체의 연산을 쪼개 각 GPU의 부담을 줄이는 데 쓴다. 다만 한 층을 계산하는 도중에도 GPU끼리 계속 통신해야 해서 통신 대역폭이 빠른 같은 서버 안(NVLink 등)에서 쓰는 것이 일반적이다.'},
 {h:'파이프라인 병렬', d:'모델을 층 단위로 여러 단계(stage)로 나눠 GPU마다 서로 다른 층 뭉치를 맡긴다. 데이터는 stage 1 GPU → stage 2 GPU → … 순서로 흘러간다. 문제는 한 GPU가 계산하는 동안 다른 GPU는 놀 수 있다는 것(파이프라인 버블)인데, [GPipe](#/p/gpipe)는 배치를 작은 micro-batch 여러 개로 쪼개 여러 stage가 동시에 서로 다른 micro-batch를 처리하게 해 이 버블을 줄인다. 텐서 병렬처럼 매 연산마다 통신하지 않고 stage 경계에서만 통신하므로, 서버를 넘나드는(느린 네트워크) 연결에도 상대적으로 잘 맞는다.'},
 {h:'ZeRO·FSDP는 다르다', d:'[ZeRO](#/p/zero)와 [FSDP](#/p/fsdp)는 새로운 4번째 병렬화 축이 아니라, **데이터 병렬 그룹 안에서** 각 GPU가 모델 파라미터·그래디언트·옵티마이저 상태 전체를 복제해 들고 있던 것을 GPU 수만큼 나눠 샤딩하는 메모리 최적화다. 계산이 필요한 순간에만 해당 조각을 모아(all-gather) 잠깐 완전한 형태로 재구성했다가 다시 흩뿌린다. 즉 "무엇을 쪼개는가" 축으로 보면 데이터 병렬의 변형이며, 텐서·파이프라인 병렬과 결합해 같이 쓰는 경우가 많다(3D parallelism).'},
 {h:'언제 무엇을 쓰는가', d:'모델이 GPU 한 장에 아예 안 들어가면 텐서 병렬이나 파이프라인 병렬(또는 둘 다)이 필수고, 모델은 들어가지만 메모리 여유가 부족해 배치를 못 키우면 ZeRO/FSDP로 메모리를 절약하는 것이 우선이다. 반대로 모델이 GPU 하나에 넉넉히 들어가고 그냥 학습을 더 빨리 끝내고 싶을 뿐이라면 순수 데이터 병렬이 통신 비용이 가장 적어 유리하다. 실무의 대규모 학습은 이 세 축과 ZeRO/FSDP를 함께 조합한 3D(또는 4D) parallelism으로 굴러가며, 여기에 [체크포인팅](#/c/checkpointing)(활성값을 저장하지 않고 필요할 때 다시 계산)까지 더해 메모리 여유를 추가로 확보하는 것이 일반적이다.'}
],

math:[
 {tex:'\\text{bubble\\ ratio} \\approx \\dfrac{p-1}{m}',
  expr:'파이프라인 버블 비율', d:'$p$ 는 파이프라인 stage 수, $m$ 은 배치를 나눈 micro-batch 개수다. stage가 많을수록(=GPU를 많이 쓸수록) 첫 GPU가 마지막 GPU의 결과를 기다리며 노는 구간이 늘어나 버블이 커지지만, micro-batch 수 $m$ 을 늘려 여러 조각을 동시에 파이프라인에 흘리면 그만큼 버블 비율이 줄어든다. $m$ 이 $p$ 보다 훨씬 크면 버블은 무시할 만큼 작아진다.'}
],

diagram:{type:'split', cap:'같은 "GPU 여러 장에 나눈다"는 목표를 세 가지 다른 축에서 자른다.',
 from:{t:'단일 GPU 학습', s:'모델+배치가 한 장에'},
 branches:[
  {t:'데이터 병렬', s:'배치를 쪼갬'},
  {t:'텐서 병렬', s:'층 내부 행렬을 쪼갬'},
  {t:'파이프라인 병렬', s:'층 뭉치를 쪼갬'}
 ],
 join:'실무에서는 세 축 + ZeRO/FSDP 샤딩을 함께 조합해 쓴다(3D parallelism)'},

confuse:[
 {a:'데이터 병렬', b:'텐서 병렬', d:'데이터 병렬은 모델을 통째로 복제하고 배치(입력 샘플들)를 쪼갠다 — GPU마다 다른 데이터를 본다. 텐서 병렬은 반대로 모든 GPU가 같은 배치를 보되, 하나의 행렬 연산 자체를 GPU끼리 나눠 계산한다.'},
 {a:'텐서 병렬', b:'파이프라인 병렬', d:'텐서 병렬은 같은 층 안에서 한 번의 연산을 여러 GPU가 동시에 나눠 계산해 매 연산마다 통신이 필요하다. 파이프라인 병렬은 서로 다른 GPU가 서로 다른 층을 맡아 데이터가 순서대로 흘러가며, 통신은 stage 경계에서만 일어난다.'},
 {a:'[ZeRO](#/p/zero)/[FSDP](#/p/fsdp) 샤딩', b:'텐서 병렬', d:'둘 다 "모델 상태를 여러 GPU에 나눈다"는 점은 같아 보이지만, ZeRO/FSDP는 연산 자체는 각 GPU가 온전한 파라미터를 모아서 기존과 똑같이 수행하고 저장 방식만 나눈 것이고, 텐서 병렬은 연산 자체를 쪼개 GPU들이 협력해서 계산한다 — 후자가 통신 빈도가 더 높다.'}
],

pitfalls:[
 '"GPU를 더 사면 그만큼 빨라진다"는 기대는 통신 병목을 무시한 것이다. 텐서 병렬을 네트워크가 느린 서버 간에 걸치면 통신 대기 시간이 계산 이득을 다 잡아먹을 수 있다.',
 'ZeRO/FSDP로 메모리를 아꼈다고 해서 학습 속도까지 항상 빨라지는 것은 아니다 — 파라미터를 다시 모으는 all-gather 통신이 추가로 들어가므로, 메모리와 통신 사이의 트레이드오프로 봐야 한다.',
 '파이프라인 병렬의 버블은 micro-batch 수를 늘릴수록 줄어들지만, micro-batch가 너무 작으면 GPU 활용률(각 연산의 효율)이 떨어져 다른 손해가 생긴다 — 무조건 잘게 쪼갠다고 좋은 것은 아니다.',
 '"병렬화 축을 하나만 골라야 한다"고 생각하는 것도 오해다. 실무의 대형 모델 학습은 서버 내부는 텐서 병렬, 서버 사이는 파이프라인 병렬, 그 위에 데이터 병렬과 ZeRO/FSDP를 겹쳐 쓰는 식으로 여러 축을 동시에 조합하는 것이 표준이다.'
],

code:{lang:'text', d:'세 축이 각각 무엇을 GPU 사이에 나누는지 한눈에.',
 src:'데이터 병렬:     [GPU0: 전체모델, 배치 1/4] [GPU1: 전체모델, 배치 1/4] ...\n텐서 병렬:      [GPU0: 층L 행렬의 절반] [GPU1: 층L 행렬의 나머지 절반]\n파이프라인 병렬:  [GPU0: 층 1~8] -> [GPU1: 층 9~16] -> [GPU2: 층 17~24]\nZeRO/FSDP:      [GPU0: 파라미터 1/N 조각] + 필요시 all-gather로 재조립'},

papers:['zero','megatron','gpipe','fsdp'],
terms:['mixed-precision-c','param-count','batch-epoch','checkpointing']
});
