WIKI.concept({
slug:'actor-critic',

tldr:'정책(액터)이 행동을 고르고 가치 함수(크리틱)가 그 행동을 평가해 정책 그래디언트의 분산을 줄여주는 구조로, PPO를 포함한 현대 정책기반 알고리즘 대부분이 이 형태를 따른다.',

why:'RLHF 파이프라인에서 "정책 모델"과 "가치 모델(크리틱)"을 따로 학습시키는 이유, 그리고 그 둘의 학습이 안정적으로 맞물려야 하는 이유가 여기서 나온다. 크리틱 없이 순수 정책 그래디언트만 쓰면 왜 학습이 그렇게 느리고 불안정한지, 크리틱을 더하면 무엇을 얻는지를 설명하는 것이 이 개념의 핵심이다.',

sections:[
 {h:'순수 정책 그래디언트의 문제', d:'REINFORCE 같은 순수 정책 그래디언트 방법은 에피소드 전체의 리턴 $G_t$ 를 그대로 그래디언트 가중치로 쓴다. 이 리턴은 [몬테카를로](#/c/mc-vs-td) 추정치라 비편향이지만 분산이 매우 크다 — 우연히 운이 좋았던 에피소드와 정책이 실제로 잘한 에피소드를 구분하지 못한다. 분산이 크면 그래디언트 추정이 노이즈에 묻혀 학습이 느려지고 불안정해진다.'},
 {h:'크리틱이 하는 일', d:'크리틱은 가치 함수 $V(s)$(또는 $Q(s,a)$)를 별도로 학습해 "기준선(baseline)"으로 쓴다. 리턴 $G_t$ 그대로 쓰는 대신 어드밴티지 $A(s,a)=G_t-V(s)$ 를 쓰면, "이 상태에서 평균적으로 기대되는 보상"을 빼고 남은 "이 행동이 평균보다 얼마나 나았는가"만 그래디언트에 반영된다. 기댓값은 그대로 유지하면서(기준선을 빼도 그래디언트의 기댓값은 안 바뀐다는 것이 이론적으로 증명된다) 분산만 줄이는 것이 크리틱의 역할이다.'},
 {h:'액터와 크리틱의 역할 분담', d:'액터(정책 $\\pi_\\theta$)는 "무엇을 할지"를 결정하고 어드밴티지가 양수인 행동의 확률을 높이는 방향으로 갱신된다. 크리틱($V_\\phi$)은 "그 행동이 실제로 좋았는지"를 판단하기 위해 자신도 [TD 학습](#/c/mc-vs-td)이나 [벨만 방정식](#/c/bellman)으로 계속 갱신된다. 둘은 서로 다른 파라미터(때로는 신경망 자체도 분리)를 갖지만 매 스텝 함께 학습된다 — 크리틱이 부정확하면 액터가 잘못된 방향으로 갱신되므로 크리틱의 학습 속도와 안정성이 전체 성능을 좌우한다.'},
 {h:'RLHF에서의 액터-크리틱', d:'[PPO](#/p/ppo) 기반 RLHF 는 언어모델 자체가 액터(정책)이고, 별도의 가치 헤드(또는 별도 모델)가 크리틱 역할을 한다 — 크리틱은 지금까지 생성된 토큰 시퀀스가 최종적으로 받을 보상 모델 점수를 예측하도록 학습된다. [GAE](#/p/gae)가 이 크리틱의 예측을 이용해 각 토큰 위치의 어드밴티지를 계산하는 방식이 바로 이 구조의 실제 구현이다.'}
],

math:[
 {tex:'\\nabla_\\theta J(\\theta)=\\mathbb{E}\\big[\\nabla_\\theta \\log\\pi_\\theta(a|s)\\,A(s,a)\\big]', expr:'actor-critic policy gradient', d:'정책 파라미터 $\\theta$ 를 개선하는 방향은 로그 확률의 그래디언트에 어드밴티지 $A(s,a)$ 를 곱한 값의 기댓값이다. $A(s,a)>0$ 인(평균보다 좋았던) 행동은 그 로그 확률을 높이는 방향으로, $A(s,a)<0$ 인 행동은 낮추는 방향으로 정책이 갱신된다.'}
],

diagram:{type:'loop', cap:'액터와 크리틱이 상호작용하며 갱신되는 순환.',
 nodes:[
  {t:'액터가 행동 선택', s:'$a\\sim\\pi_\\theta(a|s)$'},
  {t:'환경이 보상 반환', s:"$r,s'$"},
  {t:'크리틱이 어드밴티지 계산', s:"$A=r+γV(s')-V(s)$"},
  {t:'액터·크리틱 동시 갱신', s:'$\\theta,\\phi$ 업데이트'}
 ]},

confuse:[
 {a:'액터-크리틱', b:'순수 정책 그래디언트(REINFORCE)', d:'REINFORCE 는 실제 리턴 $G_t$ 를 그대로 쓰고([몬테카를로](#/c/mc-vs-td) 방식), 액터-크리틱은 학습된 가치 함수로 어드밴티지를 대체해 분산을 줄인다. 액터-크리틱이 REINFORCE 의 개선판이라고 보면 된다.'},
 {a:'크리틱', b:'보상 모델(reward model)', d:'RLHF 맥락에서 특히 혼동되는 짝이다. 보상 모델은 사람 선호 데이터로 미리 학습되어 고정된 채(또는 별도로) 각 응답에 점수를 매기는 모델이고, 크리틱은 PPO 학습 도중 그 보상을 예측하며 함께 갱신되는 가치 함수다. 보상 모델의 출력이 크리틱이 맞추려는 "정답"에 가깝다.'},
 {a:'크리틱', b:'가치기반(value-based) 방법의 $Q$-네트워크', d:'[DQN](#/p/dqn)의 $Q$-네트워크는 그 자체로 정책을 결정한다(argmax). 액터-크리틱의 크리틱은 정책을 직접 결정하지 않고, 액터가 이미 고른 행동을 "평가"해 그래디언트 신호만 제공한다 — 크리틱만으로는 행동을 고를 수 없다.'}
],

pitfalls:[
 '액터와 크리틱을 같은 학습률로 갱신하면 한쪽이 다른 쪽을 따라잡지 못해 불안정해질 수 있다 — 크리틱을 더 빠르게(또는 여러 스텝 먼저) 수렴시키는 것이 흔한 관행이다.',
 '크리틱이 부정확한 학습 초반에는 어드밴티지 추정 자체가 노이즈가 커서, 액터가 잘못된 방향으로 급격히 갱신될 위험이 있다 — [PPO](#/p/ppo)의 클리핑은 이런 급격한 갱신을 막는 안전장치다.',
 '"크리틱이 있으면 편향이 전혀 없다"고 오해하기 쉬운데, TD 기반 어드밴티지는 [몬테카를로 vs TD](#/c/mc-vs-td)에서 다뤘듯 크리틱 자체의 부정확성에서 오는 편향을 안고 간다 — 분산을 줄이는 대가다.'
],

papers:['a3c','ppo','sac'],
terms:['policy-value','mc-vs-td','onpolicy-offpolicy','bellman']
});
