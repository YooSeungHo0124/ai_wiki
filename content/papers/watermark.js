WIKI.paper({
slug:'watermark',
venue:'ICML 2023',
authors:'Kirchenbauer, Geiping, Wen, Katz, Miers, Goldstein (University of Maryland)',
arxiv:'2301.10226',

tldr:'LLM이 다음 토큰을 뽑을 때마다 이전 토큰으로 시드를 잡아 어휘를 무작위로 절반씩 "green/red list"로 나누고 green 쪽을 살짝 더 선호하게 만들어, 생성된 텍스트에 사람 눈에는 안 보이지만 통계 검정(z-검정)으로는 탐지되는 표식을 심는 방법. 모델 파라미터나 API 접근 없이도 짧은 텍스트 조각만으로 탐지가 가능하다.',

context:'ChatGPT급 모델이 등장하면서 가짜 뉴스·과제 대필·SNS 봇처럼 "이 텍스트가 사람이 썼는지 기계가 썼는지" 구분해야 하는 상황이 갑자기 실용적 문제가 됐다. 기존 접근은 사후에 텍스트의 통계적 특징(퍼플렉서티, 스타일)으로 분류기를 학습시키는 방식이었는데, 분류기는 재학습된 모델이나 우회 문구 앞에서 쉽게 무너진다. 이 논문은 아예 접근을 바꿔 **생성 시점에 신호를 심어 두는** 쪽을 택한다 — [GPT-3](#/p/gpt3) 이후의 "생성 API를 운영하는 주체가 직접 서비스에 통계적 표식을 새긴다"는 발상이다. 탐지는 모델 가중치 없이 공개 알고리즘만으로 가능하게 설계했다는 점이 핵심 제약이다.',

ideas:[
 {h:'이전 토큰으로 시드를 잡아 어휘를 green/red로 분할',
  lead:'각 스텝에서 직전 토큰의 해시로 PRNG를 시드해, 어휘 전체를 무작위 green list와 red list로 나눈다.',
  d:'green list 크기 비율은 $\\gamma$(보통 0.5)로 정한다. 이 분할은 이전 토큰만 알면 재현 가능하므로, 탐지자는 전체 생성 이력이나 모델 자체에 접근하지 않고도 **같은 해시 함수와 시드 규칙**만으로 각 위치의 green list를 다시 계산해 낼 수 있다. 이것이 "생성은 모델이 필요하지만 탐지는 필요 없다"는 비대칭을 만든다.'},
 {h:'hard red list: 아예 금지 vs soft red list: 로짓에 δ만 더하기',
  lead:'초기 버전은 red list 토큰을 완전히 금지하지만, 실용판은 green list 로짓에 상수 δ만 더해 부드럽게 유도한다.',
  d:'hard 방식은 낮은 엔트로피 텍스트(예: "Barack" 다음의 "Obama"처럼 사실상 결정적인 다음 단어)를 강제로 못 쓰게 막아 품질을 심하게 해친다. soft 방식은 softmax 이전 green list 로짓에 δ를 더해 확률을 밀어 올릴 뿐이라, 이미 확률이 1에 가까운 결정적 토큰은 red list라도 여전히 선택되고(품질 유지), 후보가 여럿인 고엔트로피 지점에서만 watermark가 강하게 작동한다.'},
 {h:'검출은 이항비율 z-검정 하나로',
  lead:'귀무가설 "이 텍스트는 red list 규칙을 모르고 쓰였다"를 세우고 green list 토큰 비율로 z-점수를 계산한다.',
  d:'귀무가설 아래 green list 토큰 수는 평균 $T/2$, 분산 $T/4$를 갖는 이항분포를 따른다고 근사할 수 있어 $z=2(|s|_G-T/2)/\\sqrt{T}$ 로 정의된다. $z=4$를 기준으로 하면 위양성률이 $3\\times10^{-5}$까지 낮아지면서도, 논문 실험에서는 단 16토큰짜리 짧은 문장도 탐지할 수 있다.'},
 {h:'낮은 엔트로피 텍스트라는 근본적 한계',
  lead:'코드나 정형화된 문장처럼 다음 토큰이 거의 정해져 있으면 green/red 선택의 여지가 없어 워터마크가 심어지지 않는다.',
  d:'"The quick brown fox jumps over the lazy dog" 같은 관용구나 반복적인 코드 패턴은 애초에 어떤 화자든 같은 토큰을 고를 수밖에 없어, red list를 피했는지 원래 그 단어를 쓸 수밖에 없었는지 구별이 안 된다. 논문은 spike entropy라는 척도로 이 정도를 정량화하고, 저엔트로피 구간이 감지 실패(type-II error)의 주 원인임을 실증적으로 보인다.'},
 {h:'beam search로 워터마크를 "눌러 담기"',
  lead:'빔서치로 여러 후보 시퀀스를 탐색하면 green list 토큰 밀도가 높은 후보를 자연히 더 많이 찾아 품질 손실을 줄인다.',
  d:'단순 샘플링은 워터마크 강도와 품질(퍼플렉서티)이 트레이드오프 관계지만, 8-way 빔서치를 쓰면 같은 δ에서도 훨씬 적은 퍼플렉서티 손실로 높은 z-점수를 얻는다. 즉 탐색 예산을 늘리는 것이 이 트레이드오프를 통째로 유리하게 밀어 준다.'}
],

diagram:{type:'flow', cap:'매 생성 스텝마다 직전 토큰이 다음 어휘 분할을 결정하고, green list 쪽으로 살짝 편향된 확률로 샘플링한다.',
 nodes:[
  {t:'직전 토큰', s:'s(t-1)'},
  {t:'해시 → 시드', s:'PRNG seed'},
  {t:'어휘 이분할', s:'green γ|V| · red', acc:true},
  {t:'green 로짓 +δ', s:'softmax 전'},
  {t:'다음 토큰 샘플링', s:'s(t)'}
 ]},

math:[
 {expr:'z = 2(|s|_G − T/2) / √T',
  tex:'z \\;=\\; \\frac{2\\left(|s|_G - T/2\\right)}{\\sqrt{T}}',
  d:'귀무가설(워터마크 규칙을 모르고 쓴 텍스트) 아래 green list 토큰 수 $|s|_G$는 평균 $T/2$, 분산 $T/4$이므로 정규화하면 표준정규분포를 따른다. $z>4$면 위양성률 $3\\times10^{-5}$로 워터마크 존재를 판정한다.'},
 {expr:'p̂_k = exp(l_k + δ) / Σ_{i∈R} exp(l_i) + Σ_{i∈G} exp(l_i + δ)   (k ∈ G)',
  tex:'\\hat p_k^{(t)} = \\frac{\\exp(l_k^{(t)}+\\delta)}{\\sum_{i\\in R}\\exp(l_i^{(t)}) + \\sum_{i\\in G}\\exp(l_i^{(t)}+\\delta)}, \\quad k\\in G',
  d:'soft red list 규칙. green list 토큰의 로짓에만 $\\delta$를 더한 뒤 softmax를 적용한다. 이미 확률이 거의 1인 토큰은 $\\delta$를 더해도 여전히 최댓값이라 선택되므로, 저엔트로피 텍스트의 품질은 거의 보존된다.'}
],

numbers:[
 {k:'탐지 임계값', v:'z > 4', d:'위양성률(type-I error) $3\\times10^{-5}$에 대응, 최소 16토큰이면 탐지 가능'},
 {k:'실험 모델', v:'OPT-1.3B / OPT-6.7B', d:'C4 RealNewsLike 프롬프트로 생성한 텍스트에 워터마크 적용'},
 {k:'경험적 탐지율', v:'98.4% (멀티노미얼 샘플링)', d:'z=4, 200토큰 기준. 4-way 빔서치 사용 시 99.6%로 상승'},
 {k:'워터마크 제거 난이도', v:'토큰의 약 1/4 이상 수정 필요', d:'1000토큰 시퀀스에서 200토큰을 적대적으로 바꿔도 z≈6.3으로 여전히 탐지됨'},
 {k:'평균 spike entropy', v:'S = 0.807', d:'약 500개 생성 샘플 평균 — 이 값에서 이론적 하한 sensitivity 98.6%'}
],

impact:'"생성 시점에 통계적 표식을 심고 사후에 가설검정으로 탐지한다"는 틀을 LLM 텍스트에 처음 체계적으로 적용해, 이후 상용 LLM 제공사들의 워터마킹·콘텐츠 출처 표시(C2PA 등) 논의의 기술적 기준점이 되었다. 동시에 low-entropy 텍스트에서의 근본적 한계, 재작성(paraphrase) 공격에 대한 취약성 논쟁을 촉발해, "워터마킹이 만능 해법은 아니다"라는 인식을 업계에 심었다. 코드 생성처럼 정형화된 출력에는 이 방식이 거의 무력하다는 점도 후속 연구에서 반복 확인된다.',

legacy:[
 '**유사 워터마킹 표준 논의** — SynthID(Google DeepMind) 등 상용 LLM 워터마크가 이 green/red list + z-검정 틀을 계승하거나 변형해 채택',
 '**재작성 공격 연구 계열 촉발** — 이후 연구들이 패러프레이징 모델로 워터마크를 씻어내는 공격과, 이에 대응하는 semantic watermark(의미 기반 해시) 연구로 이어짐',
 '**[학습 데이터 추출](#/p/extracting-training-data)·[레드팀](#/p/red-teaming)과 함께 LLM 안전 도구상자의 한 축**으로 자리잡음 — 유해 콘텐츠 생성 자체를 막는 게 아니라 사후 추적 가능성을 확보하는 상보적 접근',
 '**정책 논쟁의 기술적 근거** — AI 생성 콘텐츠 표시 의무화 관련 규제 논의에서 "기술적으로 가능한가"를 뒷받침하는 대표 인용 논문이 됨'
],

pitfalls:[
 '**코드나 표·날짜처럼 정형화된 저엔트로피 출력에는 거의 적용되지 않는다.** 논문 스스로 실패 사례의 상당수가 낮은 엔트로피 시퀀스나 데이터 암기(모델이 원문을 거의 그대로 재현하는 경우)에서 나온다고 밝힌다.',
 '**패러프레이징에 취약하다.** 이 논문의 위협 모델은 토큰을 부분적으로 바꾸는 공격을 다루지만, 문장 전체를 다른 모델로 재작성하면 green/red 패턴 자체가 무너져 탐지력이 크게 떨어질 수 있다 — 이는 후속 연구들이 지적한 지점이다.',
 '**"워터마크가 없다"가 "사람이 썼다"를 의미하지 않는다.** 검정은 비대칭적이다 — 워터마크가 있으면 기계 생성일 확률이 매우 높다고 말할 수 있지만, 워터마크가 없는 텍스트는 워터마크 없는 모델로 생성됐을 수도, 사람이 썼을 수도 있어 아무것도 결론지을 수 없다.'
],

figures:[
 {f:'fig1-example.png',
  cap:'같은 프롬프트에 대해 워터마크 없는 출력(위)과 있는 출력(아래)을 나란히 놓은 예시. 초록 배경이 green list 토큰, 빨강이 red list 토큰이다. 워터마크 없는 텍스트는 사람이 썼어도 나올 법한 green 비율(56개 중 소수)인 반면, 워터마크 텍스트는 36개 토큰 중 28개가 green이라 z=7.4, p=6e-14로 기계 생성이 명백해진다.',
  src:'원문 Figure 1, p.1'},
 {f:'fig2-tradeoff.png',
  cap:'가로축이 텍스트 품질(Oracle 모델 기준 퍼플렉서티, 왼쪽일수록 좋음), 세로축이 워터마크 강도(z-score, 위일수록 강함). 왼쪽(멀티노미얼 샘플링)은 강한 워터마크(δ 큼)일수록 품질이 나빠지는 뚜렷한 트레이드오프를 보이지만, 오른쪽(빔서치, 색이 빔 개수)은 8-way 빔서치를 쓰면 같은 z-score를 훨씬 적은 품질 손실로 달성한다.',
  src:'원문 Figure 2, p.8'}
],

quotes:[
 {t:'The watermark can be embedded with negligible impact on text quality, and can be detected using an efficient open-source algorithm without access to the language model API or parameters.',
  src:'Abstract, p.1'}
],

links:[
 {t:'arXiv 2301.10226 — A Watermark for Large Language Models', u:'https://arxiv.org/abs/2301.10226'},
 {t:'공식 구현 (GitHub: jwkirchenbauer/lm-watermarking)', u:'https://github.com/jwkirchenbauer/lm-watermarking'}
]
});
