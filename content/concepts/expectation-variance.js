WIKI.concept({
slug:'expectation-variance',

tldr:'기대값은 확률 분포의 무게중심(평균적으로 기대되는 값), 분산은 그 값이 기대값 주위로 얼마나 퍼져 있는지를 나타내는 통계량이다.',

why:'손실 함수는 사실상 "기대값을 최소화하라"는 것이고([expectation-variance] 없이는 [MLE](#/c/mle)나 정책경사(policy gradient)의 수식을 못 읽는다), [BatchNorm](#/p/batchnorm)은 미니배치의 평균·분산을 실시간으로 계산해 정규화하며, [GAE](#/p/gae) 같은 강화학습 기법은 "분산을 줄이면서 편향은 최소로" 라는 트레이드오프를 정확히 이 둘의 정의로 다룬다.',

sections:[
 {h:'기대값: 확률 가중 평균', d:'확률 변수 $X$ 의 기대값 $\\mathbb{E}[X]$ 는 각 값에 그 값이 나올 확률을 곱해 더한 것 — "무한히 많이 반복했을 때의 평균"이다. 손실 함수 $\\mathbb{E}_{(x,y)\\sim D}[\\ell(f(x),y)]$ 는 "데이터 분포 $D$ 에서 뽑은 샘플에 대한 손실의 평균을 최소화하라"는 뜻이고, 실무에서는 전체 분포를 모르니 미니배치 평균으로 이 기대값을 근사한다.'},
 {h:'분산: 퍼짐의 정도', d:'분산 $\\text{Var}(X)=\\mathbb{E}[(X-\\mathbb{E}[X])^2]$ 은 값이 평균에서 얼마나 멀리 흩어져 있는지를 제곱해서 잰다(제곱근을 취하면 표준편차). 분산이 크면 예측·추정이 불안정하다는 뜻이다 — SGD 의 gradient 추정이 미니배치마다 크게 흔들리는 것도 분산이 크기 때문이고, [momentum](#/c/momentum)은 여러 스텝의 gradient 를 평균 내 이 분산을 줄이는 기법이다.'},
 {h:'BatchNorm 의 이유', d:'[BatchNorm](#/p/batchnorm)은 각 층의 입력을 미니배치 단위로 평균 0, 분산 1이 되도록 정규화한다: $\\hat{x}=(x-\\mu_B)/\\sqrt{\\sigma_B^2+\\epsilon}$. 층을 거칠 때마다 활성값의 분포(평균·분산)가 계속 바뀌는 현상(내부 공변량 변화로 설명되곤 했으나 이후 연구에서는 손실 곡면을 매끄럽게 하는 효과가 더 핵심이라는 분석도 있다)을 억제해 학습을 안정시킨다.'},
 {h:'편향-분산의 원재료', d:'추정량(estimator)의 오차는 편향(bias, 평균적으로 정답에서 얼마나 벗어나는가)과 분산(추정이 샘플마다 얼마나 흔들리는가)으로 나뉜다 — [bias-variance](#/c/bias-variance) 문서에서 다루는 이 트레이드오프의 "분산"이 바로 여기서 정의한 분산이다. [GAE](#/p/gae)는 강화학습의 어드밴티지 추정에서 "몇 스텝을 내다볼지"를 조절해 이 편향-분산 트레이드오프를 직접 튜닝하는 기법이다.'}
],

math:[
 {tex:'\\mathbb{E}[X]=\\sum_x x\\,p(x) \\ \\ (\\text{이산}),\\qquad \\mathbb{E}[X]=\\int x\\,f(x)\\,dx \\ \\ (\\text{연속})',
  expr:'기대값', d:'각 값 $x$ 에 그 확률(PMF $p(x)$ 또는 PDF $f(x)$)을 가중치로 곱해 합/적분한 것. [probability-dist](#/c/probability-dist)에서 정의한 PMF·PDF 위에서 계산된다.'},
 {tex:'\\text{Var}(X)=\\mathbb{E}[(X-\\mathbb{E}[X])^2]=\\mathbb{E}[X^2]-(\\mathbb{E}[X])^2',
  expr:'분산의 두 표현', d:'왼쪽은 정의(편차 제곱의 기대값), 오른쪽은 계산에 자주 쓰는 등가 형태다. 표준편차 $\\sigma=\\sqrt{\\text{Var}(X)}$ 는 원래 값과 같은 단위를 가져 해석이 더 직관적이다.'}
],

diagram:{type:'compare', cap:'기대값은 중심, 분산은 퍼짐을 잰다.',
 left:{t:'기대값 E[X]', items:['확률 가중 평균','분포의 무게중심','손실 함수의 목표량']},
 right:{t:'분산 Var(X)', items:['평균 주위 퍼짐','SGD 추정의 흔들림','편향-분산 트레이드오프']}},

confuse:[
 {a:'모집단 분산', b:'표본 분산', d:'전체 분포를 알 때의 분산(모분산)과, 유한한 샘플로 추정한 분산(표본분산)은 다르다. 표본분산은 흔히 $n-1$ 로 나눠 편향을 보정한다(Bessel 보정) — 미니배치 통계로 분산을 추정하는 BatchNorm 도 이 편향을 안고 근사한다.'},
 {a:'분산(variance)', b:'[정칙화(regularization)](#/c/norm)', d:'둘 다 "모델이 흔들린다"는 문제와 연관되지만 분산은 통계량이고 정칙화는 그 흔들림(과적합)을 줄이는 기법이다 — 정칙화가 줄이려는 대상 중 하나가 모델 예측의 분산이라고 이해하면 연결된다.'}
],

pitfalls:[
 '미니배치 크기가 작으면 배치 평균·분산의 추정 분산 자체가 커져서 BatchNorm 이 오히려 불안정해질 수 있다 — 배치가 아주 작을 때 [LayerNorm](#/c/normalization) 류를 쓰는 이유 중 하나다.',
 '"기대값"을 "가장 흔한 값(최빈값)"이나 "중앙값"과 혼동하는 경우가 있다 — 분포가 한쪽으로 치우쳐 있으면(skewed) 셋이 서로 다른 값이 된다.',
 '표본 크기가 작을 때 표본평균만 보고 "기대값을 안다"고 단정하기 쉬운데, 표본평균 자체도 확률 변수라서 표본마다 값이 흔들린다 — 이 흔들림의 크기가 바로 표본평균의 분산이고, 표본이 클수록 작아진다.'
],

code:{lang:'python', d:'미니배치 평균·분산 추정이 배치 크기에 따라 얼마나 흔들리는지 확인.',
 src:'import numpy as np\npop = np.random.randn(100000)          # 모집단\nfor n in [4, 32, 256]:\n    means = [pop[np.random.choice(len(pop), n)].mean() for _ in range(1000)]\n    print(n, np.std(means))  # 배치가 작을수록 평균 추정의 분산이 커진다'},

papers:['batchnorm','gae'],
terms:['probability-dist','bias-variance','momentum']
});
