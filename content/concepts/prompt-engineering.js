WIKI.concept({
slug:'prompt-engineering',

tldr:'모델 가중치를 바꾸지 않고 입력 텍스트(지시·예시·형식)를 설계해 원하는 출력을 끌어내는 작업으로, 효과가 확인된 기법과 근거가 약한 민간요법이 뒤섞여 있는 실무 영역이다.',

why:'파인튜닝 없이 즉시 품질을 바꿀 수 있는 가장 값싼 손잡이지만, 인터넷에 떠도는 "요령"의 상당수는 특정 모델·특정 태스크에서만 우연히 통했을 뿐이다. 어떤 기법이 실제로 검증됐는지 구분하지 못하면 프롬프트를 계속 미신적으로 고치게 되고, 모델이 바뀔 때마다 같은 시행착오를 반복하게 된다.',

sections:[
 {h:'효과가 확인된 것', d:'역할 부여("당신은 ~전문가입니다")는 출력의 어휘·형식을 그 역할에 맞는 분포로 끌어당기는 효과가 문헌([프롬프팅 서베이](#/p/prompt-survey))에서 반복 관찰된다. 예시 제공(few-shot)은 모델이 태스크의 입출력 형식을 문맥만으로 추론하게 해 zero-shot 대비 안정적으로 정확도를 올린다 — 다만 예시의 질과 다양성이 나쁘면 오히려 편향을 주입한다([좋은 문맥 예시](#/p/good-in-context)). 단계 분해([CoT](#/p/cot) 계열, "단계적으로 생각하라")는 추론이 필요한 태스크에서 중간 계산을 명시적으로 드러내게 해 오류를 줄인다. 출력 형식 명시(JSON 스키마·글머리 기호·글자 수 제한)는 후처리 파싱 안정성을 직접적으로 높이며, 이는 모델 능력과 무관하게 항상 이득이다.'},
 {h:'근거가 약한 것', d:'"제발 정확하게 답해줘" 류의 감정적 호소나 "팁을 주겠다"는 인센티브 문구가 품질을 올린다는 주장은 일부 모델·일부 실험에서 우연히 관찰된 것이지 일관되게 재현되는 효과가 아니다. 프롬프트에 마침표를 더 찍는다거나, 특정 이모지를 넣는다거나 하는 식의 미세 조정도 근거 없는 민간요법에 가깝다. 이런 것들은 모델 버전이 바뀌면 효과가 사라지거나 뒤집히기 쉽다 — 재현 실험 없이 "이렇게 하면 좋다더라"만 믿고 프롬프트에 쌓아 올리면 유지보수만 어려워진다. 판별 기준은 단순하다 — 서베이 논문이나 재현 가능한 벤치마크로 뒷받침되면 기법, 개인 경험담이나 캡처 이미지 한 장으로만 퍼졌으면 민간요법으로 분류하고 별도로 검증하기 전엔 신뢰하지 않는다.'},
 {h:'왜 통하거나 안 통하는가', d:'LLM 은 프롬프트를 조건으로 다음 토큰 분포를 좁히는 조건부 생성기다. 역할·예시·형식처럼 학습 코퍼스에 실제로 대량 존재했던 패턴(전문가 글, Q&A 포맷, 코드 블록)은 모델이 그 패턴에 맞춰 이어 쓰는 경향을 강하게 학습했기 때문에 안정적으로 작동한다. 반대로 학습 코퍼스에 체계적으로 존재하지 않았을 문구(인위적인 감정 호소, 특수 기호 규칙)는 모델마다, 버전마다 반응이 들쭉날쭉하다.'},
 {h:'실무에서', d:'새 기법을 도입할 때는 "이게 논문/서베이에서 확인된 것인가, 아니면 커뮤니티 경험담인가"를 구분해서 적용한다. 프롬프트 변경 전후로 실제 태스크 셋에 대한 정량 평가(정확도·형식 준수율)를 반드시 비교한다 — 감으로 "더 좋아 보인다"는 판단은 모델·프롬프트가 바뀔 때마다 재현되지 않는다.'},
 {h:'문맥 예시 고르는 법', d:'few-shot 예시는 아무거나 넣는다고 다 도움되는 것이 아니다. [좋은 문맥 예시](#/p/good-in-context) 연구가 보여주듯, 실제 태스크 입력과 의미적으로 가까운 예시를 고를수록, 그리고 예시들의 레이블 분포가 한쪽으로 치우치지 않을수록 few-shot 이 안정적으로 작동한다. 예시 순서도 영향을 준다고 보고되는데, 이는 모델·태스크마다 편차가 커서 보편 규칙이라기보다 검증이 필요한 관행에 가깝다 — 순서를 바꿔가며 실제 평가셋으로 확인하지 않고 "이 순서가 정석"이라고 단정하면 안 된다.'},
 {h:'프롬프트만의 한계', d:'프롬프트 엔지니어링은 모델이 이미 가진 능력을 최대한 끌어내는 것이지, 없는 능력을 만들어내지는 못한다. 모델이 애초에 학습 데이터에서 본 적 없는 형식의 추론(예: 특정 도메인의 전문 계산)은 프롬프트를 아무리 정교하게 짜도 안정적으로 되지 않는다 — 이 경계를 넘어서려면 파인튜닝이나 도구 연결([RAG](#/c/rag-c), 함수 호출)이 필요하다. 프롬프트 개선으로 계속 성능이 안 오른다면, 문제가 프롬프트가 아니라 모델 능력 자체의 한계일 수 있다는 것을 의심해야 한다.'}
],

code:{lang:'text', d:'효과가 확인된 네 요소를 한 프롬프트에 조합한 예 — 역할, 예시, 단계 분해 지시, 출력 형식을 모두 명시한다.', src:
'역할: 당신은 계약서 조항을 요약하는 법무 보조원입니다.\n\n'+
'예시:\n'+
'입력: "임차인은 매월 5일까지 임차료를 지급한다."\n'+
'출력: {"의무자": "임차인", "행위": "임차료 지급", "기한": "매월 5일"}\n\n'+
'지시: 아래 조항을 단계적으로 분석한 뒤(누가/무엇을/언제) 최종 결과만 JSON으로 출력하세요.\n\n'+
'조항: "..."'},

diagram:{type:'compare', cap:'효과가 확인된 기법과 근거가 약한 민간요법의 차이.',
 left:{t:'근거 있는 기법', items:['역할 부여 — 서베이에서 반복 확인','예시 제공 — few-shot 정확도 개선 확인','단계 분해(CoT) — 추론 태스크에서 확인','출력 형식 명시 — 파싱 안정성 직접 개선']},
 right:{t:'근거 약한 민간요법', items:['감정적 호소 문구','인센티브 문구("팁을 주겠다")','특정 기호·이모지 삽입','모델 버전마다 뒤집히는 효과']}},

confuse:[
 {a:'few-shot 프롬프팅', b:'파인튜닝', d:'few-shot 은 가중치를 바꾸지 않고 문맥에 예시를 넣어 추론 시점에만 행동을 유도하는 것이고, 파인튜닝은 실제로 가중치를 갱신해 행동을 영구히 바꾸는 것이다. few-shot 효과는 대화가 끝나면 사라지지만 파인튜닝은 남는다.'},
 {a:'프롬프트 엔지니어링', b:'[Chain-of-Thought](#/c/cot-c)', d:'CoT 는 프롬프트 엔지니어링의 한 기법(단계 분해)이다. 프롬프트 엔지니어링은 역할·예시·형식 지정 등을 포괄하는 더 넓은 범주이고, CoT 는 그중 추론 과정을 명시적으로 끌어내는 특정 패턴이다.'},
 {a:'system 프롬프트', b:'user 프롬프트', d:'system 프롬프트는 대화 전체에 걸쳐 유지되는 역할·규칙을 정의하는 층이고, user 프롬프트는 매 턴 바뀌는 구체적 요청이다. 역할 부여는 보통 system 프롬프트에 두는 것이 관행이지만, 모델에 따라 user 메시지에 넣어도 비슷하게 작동하는 경우가 있다 — 이는 구현체마다 다르다.'}
],

pitfalls:[
 '커뮤니티에서 떠도는 "이 문구를 넣으면 성능이 20% 오른다" 류의 주장은 대부분 특정 모델 한 버전에서의 일화적 관찰이지 재현된 벤치마크 결과가 아니다.',
 '프롬프트를 계속 길게 만들면서 효과 없는 문구를 쌓아 올리면, 실제로 도움 되는 지시가 긴 문맥 속에 묻혀 오히려 정확도가 떨어질 수 있다.',
 '한 모델에서 통했던 프롬프트 요령이 다른 모델·다음 버전에서도 똑같이 통한다고 가정하면 안 된다 — 학습 데이터와 정렬 방식이 다르면 반응이 다르다.',
 '프롬프트 하나만 몇 번 테스트해보고 "효과 있다/없다"를 판단하는 것은 샘플링 변동을 실제 효과로 착각하는 것이다 — 같은 프롬프트라도 온도가 0보다 크면 결과가 매번 달라질 수 있다.',
 '출력 형식을 "JSON으로 답하라"고 지시만 하고 실제 파싱 검증을 안 하면, 형식이 깨진 응답이 조용히 다음 단계로 흘러들어가 오류가 늦게 발견된다.'
],

papers:['gpt3','prompt-survey','cot','good-in-context'],
terms:['cot-c','hallucination','rag-c']
});
