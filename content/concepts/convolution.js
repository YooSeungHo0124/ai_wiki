WIKI.concept({
slug:'convolution',

tldr:'작은 커널을 입력 위에서 슬라이딩시키며 가중합을 계산하는 연산으로, 커널 크기·스트라이드·패딩·팽창이 출력 크기와 수용 영역을 결정한다.',

why:'출력 크기 공식을 못 외우면 아키텍처를 설계할 때마다 shape 에러를 손으로 디버깅하게 되고, 1×1 합성곱이나 depthwise separable 합성곱이 왜 존재하는지 모르면 MobileNet류 경량 모델의 설계 의도를 읽을 수 없다.',

sections:[
 {h:'연산 정의', d:'커널(필터)을 입력 위에서 한 칸씩(또는 스트라이드만큼) 옮기며, 겹친 영역의 원소별 곱을 모두 더한 값을 출력 한 칸으로 삼는다. 커널 하나가 입력 채널 전체 깊이만큼 확장되어 있고, 출력 채널 수만큼 서로 다른 커널을 쓴다.'},
 {h:'출력 크기 공식', d:'입력 한 변의 길이 W, 커널 K, 스트라이드 S, 패딩 P, 팽창 D가 주어지면 출력 크기는 아래 수식으로 정확히 계산된다. 패딩이 0이면 커널이 딱 들어맞는 위치까지만 슬라이딩하므로(`valid`) 출력이 줄고, 입력과 출력 크기를 같게 만들고 싶으면(`same`) 필요한 만큼 0을 채운다.'},
 {h:'1×1 합성곱', d:'공간 방향으로는 아무것도 안 보고 채널 방향으로만 가중합을 하는 특수 케이스다. 공간 해상도를 그대로 둔 채 채널 수를 줄이거나 늘려 연산량을 조절하는 "병목(bottleneck)" 용도로 쓰인다 — ResNet의 bottleneck 블록, Inception의 채널 축소가 대표적이다.'},
 {h:'depthwise separable 합성곱이 싼 이유', d:'표준 합성곱을 (a) 채널마다 따로 도는 depthwise 합성곱과 (b) 채널을 섞는 1×1 pointwise 합성곱 둘로 쪼갠 것. [MobileNet](#/p/mobilenet)이 이 구조로 연산량을 8~9배 줄였다. 파라미터 수를 직접 비교하면 왜 싼지 바로 보인다: 표준 합성곱은 $K^2 C_{in}C_{out}$, separable은 $K^2 C_{in} + C_{in}C_{out}$이다. $K=3$, $C_{in}=C_{out}=256$이면 표준은 약 59만, separable은 약 6.8만으로 약 8.7배 차이가 난다.'}
],

math:[
 {tex:'W_{out} = \\left\\lfloor \\dfrac{W_{in} + 2P - D\\,(K-1) - 1}{S} \\right\\rfloor + 1',
  expr:'출력 크기 공식', d:'$W_{in}$ 입력 크기, $P$ 패딩, $D$ 팽창(dilation, 기본 1), $K$ 커널 크기, $S$ 스트라이드. 예를 들어 $W_{in}=32,K=3,S=1,P=1,D=1$이면 $(32+2-2-1)/1+1=32$로 크기가 그대로 보존되는 `same` 패딩이 된다.'},
 {tex:'\\#\\text{params}_{std}=K^2 C_{in}C_{out}\\quad \\text{vs}\\quad \\#\\text{params}_{sep}=K^2C_{in}+C_{in}C_{out}',
  expr:'표준 vs depthwise separable 파라미터 수', d:'separable은 depthwise 단계($K^2 C_{in}$)와 pointwise 단계($C_{in}C_{out}$)의 합이라, 표준 합성곱의 곱셈 구조보다 훨씬 적다. 비율은 대략 $1/C_{out} + 1/K^2$로 수렴한다.'}
],

diagram:{type:'compare', cap:'표준 합성곱과 depthwise separable 합성곱의 구조 차이.',
 left:{t:'표준 합성곱', items:['커널 하나가 공간+채널을 동시에 처리','파라미터 K²·Cin·Cout','표현력은 높지만 연산량도 큼']},
 right:{t:'depthwise separable', items:['depthwise: 채널마다 독립적인 K×K 필터','pointwise(1×1): 채널을 섞어 조합','파라미터·연산량 5~9배 절감'], acc:true}
},

confuse:[
 {a:'합성곱(convolution)', b:'상관(cross-correlation)', d:'수학적 정의의 합성곱은 커널을 180도 뒤집고서 슬라이딩하지만, 딥러닝 프레임워크가 "convolution"이라 부르는 연산은 사실 커널을 뒤집지 않는 상관이다. 커널이 학습되는 파라미터이므로 뒤집힌 채로 학습되든 안 되든 결과에 차이가 없어 이 혼용이 실무에 문제가 되지 않는다.'},
 {a:'stride', b:'padding', d:'stride는 커널이 몇 칸씩 건너뛰며 슬라이딩할지를 정해 출력 크기를 줄이는 데 쓰이고, padding은 입력 가장자리에 값을 채워 넣어 출력 크기를 늘리거나 보존하는 데 쓰인다. 둘 다 출력 크기 공식의 항이지만 반대 방향으로 작동한다.'},
 {a:"'same' 패딩", b:"'valid' 패딩", d:"'same'은 스트라이드 1일 때 출력 크기를 입력과 같게 맞추도록 패딩을 자동 계산하고, 'valid'는 패딩을 아예 쓰지 않아 커널이 딱 들어맞는 만큼만 출력한다. same이 항상 정수 패딩으로 딱 떨어지는 것은 아니라 프레임워크마다 좌우 비대칭 패딩 처리 방식이 조금씩 다르다."}
],

pitfalls:[
 '"same 패딩 = 출력이 입력과 완전히 동일한 값 분포"가 아니다. 크기만 같을 뿐 가장자리 픽셀은 패딩된 0과 섞여 계산되므로 경계 효과가 남는다.',
 '1×1 합성곱을 "아무것도 안 하는 연산"으로 오해하기 쉽지만, 채널 간 선형 결합 + 비선형 활성함수가 붙으면 사실상 위치별로 공유된 작은 MLP다.',
 'dilation(팽창)을 키우면 파라미터 수 변화 없이 [수용 영역](#/c/receptive-field)만 커진다는 점 때문에 연산량도 그대로라고 착각하기 쉬운데, 실제 곱셈 횟수는 커널 크기($K^2$)에만 비례하므로 변하지 않는 것이 맞다 — 다만 해상도를 유지한 채 문맥을 넓히고 싶을 때(세그멘테이션) 유용하다.'
],

papers:['lenet','vgg','deeplab'],
terms:['cnn','pooling','receptive-field']
});
