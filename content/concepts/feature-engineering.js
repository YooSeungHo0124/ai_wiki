WIKI.concept({
slug:'feature-engineering',

tldr:'원본 데이터를 모델이 더 쉽게 패턴을 찾을 수 있는 형태의 입력 변수(특징)로 가공·조합·변환해 학습에 넣는 작업.',

why:'표 데이터(정형 데이터) 문제에서는 지금도 [XGBoost](#/p/xgboost) 같은 트리 모델이 딥러닝을 이기는 경우가 많은데, 그 성능 차이의 상당 부분은 모델 자체가 아니라 특징을 얼마나 잘 설계했는가에서 온다. "모델을 바꿔도 성능이 안 오른다"는 정체 상황에서 다음 수를 특징 공학에서 찾아야 할 때가 많다는 것을 아는 것이 실무 감각이며, 캐글 등 실전 대회 상위권의 비결도 대부분 여기서 갈린다.',

sections:[
 {h:'무엇을 하는가', d:'날짜에서 요일·월·공휴일 여부를 뽑아내고, 위도·경도에서 두 지점 간 거리를 계산하고, 여러 컬럼을 비율·차이로 조합하는 식으로 원본 컬럼에는 없던 정보를 명시적인 숫자로 만들어 준다. 모델이 원본 데이터에서 스스로 이런 관계를 찾아낼 수도 있지만, 데이터가 적거나 관계가 비선형적이면 사람이 먼저 힌트를 줘야 모델이 훨씬 쉽고 빠르게 그 관계를 배운다.'},
{h:'트리 모델과의 관계', d:'[결정 트리](#/c/decision-tree)·부스팅 계열은 한 번에 변수 하나씩만 기준으로 분기하기 때문에, "A와 B의 비율"처럼 두 변수의 관계 자체가 중요한 패턴은 트리가 여러 번 반복 분기해야 근사적으로만 흉내 낼 수 있다. 이 비율을 사람이 미리 계산해 새 컬럼으로 주면 트리가 한 번의 분기로 바로 그 패턴을 잡아낼 수 있다 — [트리 모델이 표 데이터에서 딥러닝을 이기는 이유](#/p/trees-still-win)를 다루는 연구들도 특징의 질이 결과를 크게 좌우한다는 점을 지적한다.'},
 {h:'범주형·결측치 처리', d:'범주형 변수는 [원-핫 인코딩](#/c/one-hot), 목표 인코딩(target encoding, 각 범주를 그 범주의 평균 타깃값으로 치환) 등으로 숫자로 바꾼다. 결측치는 단순 평균·중앙값 대치 외에, "이 값이 결측이었는가" 자체를 새로운 이진 특징으로 추가하면 결측 여부 자체가 신호인 경우(예: 특정 센서가 고장 나서 값이 없는 경우)를 모델이 활용할 수 있다. 목표 인코딩은 특히 데이터 누수에 취약해, fold를 나눠 각 fold의 값은 다른 fold의 통계로만 계산하는 등 세심한 처리가 필요하다.'},
 {h:'데이터 누수 위험', d:'특징을 만들 때 미래 정보나 예측 대상과 직접 연관된 정보가 실수로 섞여 들어가면([데이터 누수](#/c/data-leakage)), 검증 성능은 비정상적으로 높게 나오지만 실제 운영에서는 그 정보가 없어 성능이 무너진다. 시계열 데이터에서 미래 시점의 집계값(전체 평균 등)을 과거 시점 특징에 그대로 쓰는 실수가 대표적이며, 검증 성능이 이상하리만치 좋을 때 가장 먼저 의심해야 할 원인이기도 하다.'},
 {h:'실무에서', d:'딥러닝이 특징을 자동으로 학습한다고 해서 특징 공학이 필요 없어지는 것은 이미지·텍스트 같은 비정형 데이터에 국한된 이야기에 가깝다. 정형 데이터·추천·이상 탐지 실무에서는 도메인 지식을 반영한 특징 몇 개가 모델 구조를 바꾸는 것보다 훨씬 큰 성능 개선을 주는 경우가 흔하고, 이런 특징을 여러 모델·여러 팀이 재사용하도록 관리하는 것이 [피처 스토어](#/c/feature-store)의 역할이다.'},
{h:'집계·시간 특징', d:'추천·이상 탐지·시계열 예측에서는 특히 "최근 7일간 평균", "지난 세션 이후 경과 시간"처럼 여러 행에 걸친 집계·시간 특징이 특히 중요하다. 이런 특징은 계산 시점에 미래 정보가 섞이지 않도록 항상 "예측하려는 시점 이전"의 데이터만으로 집계해야 하며, 배치로 만들 때와 실시간 서빙 때 같은 로직으로 계산되는지 확인하지 않으면 학습·서빙 특징이 어긋나는(training-serving skew) 문제가 생긴다.'}
],

code:{lang:'python', d:'날짜·비율 특징을 만들고, 결측 여부 자체를 새 특징으로 추가하는 흔한 패턴. 통계(중앙값 등)는 학습셋에서만 계산해 검증·시험셋에 그대로 적용해야 누수를 막을 수 있다.', src:'import pandas as pd\n\ndf["dow"] = df["date"].dt.dayofweek\ndf["is_weekend"] = df["dow"].isin([5, 6]).astype(int)\ndf["ratio_ab"] = df["a"] / (df["b"] + 1e-6)  # 0 나눗셈 방지\n\n# 결측 여부 자체를 신호로 사용\ndf["c_was_missing"] = df["c"].isna().astype(int)\nmedian_c = df_train["c"].median()  # 반드시 학습셋 기준\ndf["c"] = df["c"].fillna(median_c)'},

diagram:{type:'flow', cap:'원본 컬럼에서 모델이 바로 쓸 특징 벡터가 나오기까지.',
 nodes:[
  {t:'원본 컬럼', s:'날짜·위경도 등'},
  {t:'조합·변환', s:'비율·거리·집계'},
  {t:'인코딩', s:'범주·결측 처리'},
  {t:'특징 벡터', s:'모델 입력'}
 ]},

confuse:[
 {a:'특징 공학(Feature Engineering)', b:'[특징 스케일링](#/c/scaling-features)', d:'특징 공학은 어떤 정보를 특징으로 **만들지** 설계하는 것이고, 스케일링은 이미 만들어진 특징의 값 **범위**를 표준화하는 후처리다. 둘 다 전처리 단계에 속하지만 목적이 다르다 — 특징 공학은 정보를 추가하고, 스케일링은 정보량은 그대로 둔 채 값의 스케일만 바꾼다.'},
 {a:'특징 공학', b:'[표현 학습(representation learning)](#/c/embedding)', d:'특징 공학은 사람이 도메인 지식으로 특징을 직접 설계하는 것이고, 표현 학습은 신경망이 원본 데이터(픽셀·토큰)에서 유용한 특징을 스스로 학습하는 것이다. 이미지·텍스트는 표현 학습이 사람의 특징 공학을 상당 부분 대체한 대표적 영역이지만, 정형 데이터에서는 아직 사람의 특징 공학이 우위인 경우가 많다.'},
 {a:'데이터 누수', b:'과적합', d:'데이터 누수는 예측 시점에는 존재하지 않을 정보가 특징에 섞여 들어간 설계상의 오류이고, 과적합은 정보 자체는 정당하지만 모델이 학습 데이터의 노이즈까지 외운 것이다. 누수는 검증 성능조차 신뢰할 수 없게 만든다는 점에서 더 근본적인 문제다.'},
 {a:'특징 공학', b:'[차원 축소(PCA 등)](#/c/pca-c)', d:'특징 공학은 보통 도메인 지식을 반영해 특징을 늘리거나 새로 조합하는 쪽에 가깝고, 차원 축소는 이미 있는 특징들을 더 적은 수의 새로운 축으로 압축하는 쪽이다. 실무에서는 특징을 충분히 만든 뒤 필요하면 차원 축소로 정리하는 순서를 많이 따른다.'}
],

pitfalls:[
 '타깃(정답)값을 이용해 특징을 만들면서(목표 인코딩 등) 학습셋 전체로 계산한 값을 그대로 쓰면 그 샘플 자신의 정답 정보가 특징에 새어 들어가는 데이터 누수가 된다 — 교차검증 fold 밖에서 계산해야 한다.',
 '시계열에서 미래 시점까지 포함한 집계 통계(전체 평균 등)를 특징으로 쓰면 검증 성능은 좋아 보이지만 실제 운영에서는 재현되지 않는다.',
 '특징을 계속 추가하기만 하고 불필요한 특징을 정리하지 않으면 차원이 늘어나 [차원의 저주](#/c/curse-dimensionality)와 과적합 위험이 커진다.',
 '배치 파이프라인에서 만든 집계 특징의 계산 로직과 실시간 서빙에서 쓰는 로직이 조금이라도 다르면, 학습 때 본 특징 분포와 서빙 때 들어오는 특징 분포가 어긋나 배포 후 성능이 예상 없이 떨어진다.',
 '범주형 변수의 카디널리티(고유값 개수)가 매우 높을 때 원-핫 인코딩을 그대로 쓰면 차원이 폭발한다 — 목표 인코딩이나 임베딩처럼 카디널리티에 덜 민감한 방식으로 바꿔야 한다.'
],

papers:['xgboost','trees-still-win'],
terms:['scaling-features','one-hot','data-leakage','decision-tree','feature-store','boosting']
})
