WIKI.concept({
slug:'rlhf',

tldr:'사람이 매긴 응답 선호도로 보상 모델을 학습시킨 뒤, 그 보상을 최대화하도록 언어모델을 강화학습으로 미세조정해 사람이 원하는 방향으로 행동을 정렬하는 3단계 절차다.',

why:'ChatGPT류 모델이 "그럴듯한 다음 토큰"을 넘어 사람이 실제로 선호하는 응답을 내놓는 이유가 여기 있다. 보상 해킹과 KL 벌점을 모르면 RLHF 로 미세조정한 모델이 왜 가끔 장황해지거나 이상하게 아첨하는지(sycophancy) 설명할 수 없고, DPO 가 뭘 없앴는지도 이해가 안 된다.',

sections:[
 {h:'1단계: 지도 미세조정(SFT)', d:'[사전학습](#/c/pretrain-finetune)된 base model을 사람이 작성한 (지시, 모범 응답) 쌍으로 지도학습한다. 이 단계가 끝난 모델이 이후 단계들의 출발점(정책의 초기값)이 된다.'},
 {h:'2단계: 보상 모델(RM) 학습', d:'같은 프롬프트에 대해 SFT 모델이 생성한 여러 응답을 사람이 순위 매기거나 둘 중 하나를 고르게 한다([InstructGPT](#/p/instructgpt), [요약 RLHF](#/p/summarize-hf) 방식). 이 선호 비교 데이터로 별도의 신경망(보상 모델)을 학습시켜, 응답 하나를 입력받아 "사람이 이걸 얼마나 좋아할지"를 스칼라 점수로 예측하게 만든다. 정답이 절대적 점수가 아니라 상대적 순위라는 점이 핵심이다 — Bradley-Terry 모델처럼 "A가 B보다 선호될 확률"을 점수 차이로 설명하는 방식으로 학습한다.'},
 {h:'3단계: 강화학습', d:'SFT 모델을 정책으로 놓고, 프롬프트를 넣어 응답을 생성한 뒤 보상 모델이 매긴 점수를 보상으로 삼아 [PPO](#/p/ppo) 같은 정책기반 [강화학습](#/c/rl-basics)으로 정책을 갱신한다. 즉 "사람이 준 라벨"이 아니라 "보상 모델이 예측한 점수"가 학습 신호가 되며, 이 단계에서만 실제로 정책 파라미터가 선호 방향으로 움직인다.'},
 {h:'보상 해킹과 KL 벌점', d:'보상 모델은 사람의 선호를 근사한 대리 지표일 뿐이라, 정책이 실제 유용성과 무관하게 보상 모델의 허점을 파고들어 점수만 높이는 응답(과도하게 길게 쓰기, 특정 문구 남발, 확신 없는 걸 확신하는 척하기)을 학습할 수 있다 — 이것이 보상 해킹(reward hacking)이다. 이를 막기 위해 목적함수에 정책과 원래 SFT 모델 사이의 KL divergence 를 벌점으로 넣는다. 이 항은 정책이 보상을 좇다가 SFT 모델의 언어 분포에서 너무 멀리 벗어나지 못하게 붙잡아두는 닻 역할을 한다 — 벌점 계수가 너무 작으면 보상 해킹이, 너무 크면 정렬 효과 자체가 사라진다.'},
 {h:'DPO: 보상 모델을 없앤 흐름', d:'[DPO](#/p/dpo)는 "KL 벌점이 있는 RLHF의 최적해가 보상 모델 없이 선호 데이터만으로 직접 표현될 수 있다"는 것을 수학적으로 보여, 별도의 보상 모델 학습과 PPO 강화학습 루프 전체를 (선호, 비선호) 응답 쌍에 대한 단일 분류 손실 하나로 대체했다. 결과적으로 3단계 파이프라인이 SFT + 선호 데이터 직접 최적화의 2단계로 단순화되고, 강화학습 특유의 불안정성(보상 모델 과적합, 하이퍼파라미터 민감성)도 함께 사라진다는 것이 DPO의 실무적 장점이다.'}
],

diagram:{type:'flow', cap:'RLHF 3단계와 각 단계의 데이터·산출물.',
 nodes:[
  {t:'base model', s:'사전학습 완료'},
  {t:'SFT', s:'(지시,응답) 쌍 → 정책 초기값'},
  {t:'보상 모델 학습', s:'선호 비교 데이터 → RM'},
  {t:'PPO 강화학습', s:'RM 점수 - KL 벌점 → 정책 갱신'}
 ]},

confuse:[
 {a:'RLHF', b:'[SFT](#/c/pretrain-finetune)', d:'SFT는 정답이 하나로 고정된 (입력,정답) 쌍에 대한 평범한 지도학습이다. RLHF는 여러 후보 응답의 상대적 선호를 보상 모델로 근사한 뒤 그 보상을 강화학습으로 최대화한다 — 정답이 절대적이지 않고 비교로만 주어진다는 점, 그리고 정책이 스스로 생성한 응답으로 학습한다는 점(온폴리시)이 SFT와 다르다.'},
 {a:'보상 모델', b:'가치 함수(value function)', d:'보상 모델은 응답 전체에 대해 사람의 선호를 예측하는, RLHF 파이프라인에서만 쓰는 별도의 신경망이다. 가치 함수는 [강화학습 일반](#/c/rl-basics)에서 상태의 기대 누적 보상을 추정하는 개념으로, PPO는 보상 모델의 점수를 보상 삼아 내부적으로 별도의 가치 함수(크리틱)를 함께 학습해 분산을 줄인다. 즉 RLHF 안에는 보상 모델과 가치 함수가 둘 다 존재하며 역할이 다르다.'},
 {a:'RLHF', b:'[DPO](#/p/dpo)', d:'RLHF는 보상 모델 학습 + 강화학습이라는 두 개의 별도 최적화 단계를 거친다. DPO는 이 둘을 선호 쌍에 대한 단일 지도학습 손실로 접어, 강화학습 루프 자체가 없어진다 — 목표(KL 제약 하 선호 최적화)는 이론적으로 동일하다고 주장하지만 구현·안정성 면에서 크게 다르다.'}
],

pitfalls:[
 '보상 모델 점수가 오른다고 실제 사람 만족도가 같이 오른다고 가정하면 안 된다 — 보상 해킹이 시작되면 둘이 갈라진다.',
 'KL 벌점 계수를 고정값으로 한 번 정하면 학습 도중 정책이 변하면서 최적 계수도 바뀔 수 있다 — 적응적으로 조절하는 구현이 많은 이유다.',
 '선호 데이터를 매긴 사람(또는 그룹)의 편향이 그대로 보상 모델에, 그리고 최종 정책에 새겨진다 — "정렬됐다"는 게 누구의 선호에 정렬됐는지를 항상 물어야 한다.'
],

papers:['instructgpt','dpo','summarize-hf'],
terms:['rl-basics','pretrain-finetune','few-shot']
});
