WIKI.concept({
slug:'sgd',

tldr:'미니배치로 경사를 추정해 파라미터를 갱신하는 방식으로, 추정 잡음이 있지만 스텝이 싸고 그 잡음이 일반화에 도움된다는 관점도 있다.',

why:'딥러닝에서 "SGD 를 쓴다"고 하면 거의 항상 미니배치 SGD 를 뜻한다. Adam 을 포함한 모든 옵티마이저가 결국 이 갱신식 위에 모멘텀·적응적 학습률을 얹은 것이므로, 갱신식과 잡음의 정체를 정확히 알아야 그 위에 쌓인 것들이 무엇을 "고치려는" 시도인지 읽힌다.',

sections:[
 {h:'갱신식', d:'매 스텝마다 미니배치 $B$ 안 샘플들의 손실 평균에 대한 경사를 구해, 그 반대 방향으로 학습률 $\\eta$ 만큼 파라미터를 옮긴다. 전체 데이터를 쓰는 배치 경사 하강과 형태는 같지만 경사를 추정하는 데 쓰는 샘플 수가 다르다.'},
 {h:'왜 미니배치인가', d:'샘플 전부로 경사를 구하면 정확하지만 스텝 하나의 비용이 데이터 크기에 비례해 커지고, 샘플 하나로 구하면 싸지만 추정 분산이 너무 크다. 미니배치(보통 32~4096개)는 이 둘의 절충이면서, GPU 의 행렬 연산으로 배치 내 샘플들을 병렬 처리할 수 있어 배치를 어느 정도 키워도 벽시계 시간이 거의 늘지 않는다는 실용적 이유가 크다.'},
 {h:'노이즈와 일반화', d:'미니배치 경사는 참 경사 $\\nabla_\\theta\\mathcal{L}$ 의 불편 추정량이지만 분산이 있다. 이 분산은 손실 곡면을 매끈하게 밟는 대신 조금씩 흔들며 지나가게 만드는데, 이 흔들림이 손실 표면의 좁고 가파른 극솟값(sharp minima)을 지나쳐 더 평평한 영역(flat minima)에 정착하도록 돕는다는 관찰이 있다 — 평평한 극솟값이 처음 보는 데이터에도 더 잘 버틴다는 것이 일반화가 좋아지는 이유로 꼽힌다.'},
 {h:'실무에서', d:'맨 SGD 는 잘 안 쓰고 거의 항상 [모멘텀](#/c/momentum)을 얹어 쓴다. 배치 크기를 바꾸면 경사 추정의 분산도 바뀌므로 학습률을 함께 조정하는 것이 관행이며([대배치 SGD](#/p/lr-scaling)), 비전 CNN 학습에서는 잘 튜닝된 SGD+모멘텀이 지금도 [Adam](#/p/adam) 계열과 경쟁한다([옵티마이저 고르기](#/c/optimizer-choice) 참고).'}
],

math:[
 {tex:'\\theta_{t+1}=\\theta_t-\\eta\\,\\dfrac1{|B|}\\sum_{i\\in B}\\nabla_\\theta \\ell_i(\\theta_t)',
  expr:'mini-batch SGD update', d:'$\\theta_t$ 는 스텝 $t$ 의 파라미터, $\\eta$ 는 학습률, $B$ 는 미니배치, $\\ell_i$ 는 샘플 $i$ 의 손실. 배치 안 샘플들의 경사를 평균해 한 번 갱신한다.'},
 {tex:'\\mathbb{E}_{B}\\big[\\nabla_\\theta \\hat{\\mathcal{L}}_B(\\theta)\\big]=\\nabla_\\theta \\mathcal{L}(\\theta),\\qquad \\mathrm{Var}\\big[\\nabla_\\theta \\hat{\\mathcal{L}}_B(\\theta)\\big]\\propto \\dfrac{\\sigma^2}{|B|}',
  expr:'gradient estimator variance', d:'미니배치 경사의 기댓값은 참 경사와 같지만(불편 추정), 분산은 배치 크기 $|B|$ 에 반비례해 줄어든다. 배치를 4배 키우면 잡음의 표준편차는 절반이 된다는 뜻.'}
],

diagram:{type:'compare', cap:'경사 추정에 쓰는 샘플 수가 정확도·비용·잡음을 함께 바꾼다.',
 left:{t:'작은 배치', items:['스텝당 계산 저렴','경사 추정 분산 큼','sharp minima 회피에 유리']},
 right:{t:'큰 배치', items:['경사 추정 정확','GPU 병렬성 활용 극대화','학습률도 비례해 키워야 함']}},

confuse:[
 {a:'SGD (원조 알고리즘)', b:'SGD (라이브러리 클래스)', d:'"확률적(stochastic)"이라는 이름은 샘플 하나로 경사를 추정하던 원조 알고리즘에서 왔지만, `torch.optim.SGD` 같은 실제 구현은 미니배치를 쓰고 모멘텀 옵션까지 내장한다. 실무에서 "SGD"는 거의 항상 이 미니배치+모멘텀 버전을 가리킨다.'},
 {a:'배치 크기', b:'학습률', d:'배치 크기는 경사를 얼마나 정확하게 추정하는지를, 학습률은 그 추정된 방향으로 얼마나 이동하는지를 정한다. 배치를 키워 잡음이 줄면 한 스텝을 더 크게 내디뎌도 안전해지므로, 관행적으로 배치 크기에 비례해 학습률도 키운다([학습률 스케줄](#/c/lr-schedule)의 선형 스케일링 규칙).'},
 {a:'경사 잡음', b:'발산', d:'미니배치 SGD 의 손실 곡선이 들쑥날쑥한 것은 정상적인 추정 분산 때문이지 학습 실패가 아니다. 반면 손실이 추세적으로 계속 커지면 그건 학습률이 너무 크거나 발산하는 것 — 둘을 구분해야 한다.'}
],

pitfalls:[
 '"배치를 키우면 항상 더 빠르고 좋다"는 틀렸다 — 총 갱신 횟수가 줄고 잡음도 줄어 오히려 일반화 성능이 떨어질 수 있다는 보고가 있다.',
 '"노이즈가 일반화에 도움이 되니 배치는 작을수록 좋다"도 과도한 단순화다 — 너무 작으면 학습이 불안정해지고 GPU 활용률도 떨어진다.',
 '배치 크기만 바꾸고 학습률은 그대로 두면 잡음 수준이 달라져 이전과 다른 지점에 수렴하거나 발산할 수 있다 — 배치를 바꿨다면 학습률도 같이 검토해야 한다.'
],

papers:['adam','lr-scaling'],
terms:['momentum','adaptive-optimizer','learning-rate','gradient-descent','batch-epoch']
});
