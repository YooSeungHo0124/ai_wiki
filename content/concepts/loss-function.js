WIKI.concept({
slug:'loss-function',

tldr:'모델 출력과 정답의 차이를 하나의 스칼라로 요약해 경사 하강이 최소화할 목표를 정의하는 함수로, 문제 유형(회귀/분류)에 맞는 손실을 골라야 학습이 제대로 된다.',

why:'손실 함수를 잘못 고르면 지표는 그럴듯해 보여도 경사가 죽어 학습이 멈추거나, 이상치에 모델이 과민해지거나, 평가에서 보고 싶은 것과 다른 것을 최적화하게 된다. "왜 분류에 MSE 를 안 쓰는가" 하나만 제대로 알아도 실수의 절반은 막는다.',

sections:[
 {h:'회귀 손실 — MSE·MAE·Huber', d:'MSE(평균제곱오차)는 오차를 제곱하므로 큰 오차에 강하게 반응해 이상치에 민감하다. MAE(평균절대오차)는 오차에 선형으로 반응해 이상치에 강건하지만 0 근처에서 미분이 불연속이라 수렴이 흔들릴 수 있다. Huber 손실은 오차가 작으면 MSE 처럼, 크면 MAE 처럼 행동하도록 임계값 $\\delta$ 로 둘을 이어 붙인 절충안이다.'},
 {h:'분류 손실 — 교차 엔트로피', d:'분류는 정답 클래스에 모델이 부여한 **확률**을 손실로 쓴다. 이진 분류는 binary cross-entropy, 다중 분류는 categorical cross-entropy(softmax 출력과 짝을 이룸)를 쓴다. 확률이 0에 가까워질수록 $-\\log p$ 가 무한히 커지므로, 자신 있게 틀린 예측에 큰 벌점을 준다.'},
 {h:'왜 분류에 MSE 를 쓰면 안 되는가', d:'출력을 sigmoid 로 확률화한 뒤 MSE 를 쓰면, 손실의 경사에 $\\sigma\\prime(z)=\\sigma(z)(1-\\sigma(z))$ 항이 곱해져 들어간다. 모델이 확실하게 틀렸을 때(예측이 0에 가깝고 정답이 1일 때) 이 값이 0에 가까워지므로 **가장 고쳐야 할 순간에 경사가 가장 작아진다**. 반면 교차 엔트로피는 그 항이 소거돼 오차 크기에 비례하는 경사가 그대로 나온다 — 아래 수식 참고.'},
 {h:'다른 문제, 다른 손실', d:'표준 회귀·분류 밖에서는 문제 구조에 맞춘 손실이 따로 있다. 클래스 불균형이 심한 검출에는 쉬운 샘플의 기여를 깎는 [Focal Loss](#/p/focal-loss), 바운딩 박스 회귀에는 겹치지 않는 박스에도 경사를 주는 [GIoU](#/p/giou) 처럼, 손실 함수는 "평가하고 싶은 것을 실제로 최적화 가능한 형태로 바꾸는 도구"다.'}
],

math:[
 {tex:'\\mathcal{L}_{MSE}=\\dfrac1n\\sum_i (y_i-\\hat y_i)^2', expr:'MSE', d:'제곱이라 오차가 2배면 손실은 4배 — 큰 오차에 과민하게 반응한다.'},
 {tex:'\\mathcal{L}_{Huber}=\\begin{cases}\\tfrac12(y-\\hat y)^2 & |y-\\hat y|\\le\\delta\\\\ \\delta(|y-\\hat y|-\\tfrac12\\delta) & \\text{otherwise}\\end{cases}', expr:'Huber loss', d:'$\\delta$ 안쪽은 매끄러운 이차, 바깥쪽은 완만한 선형 — 이상치가 손실을 지배하는 것을 막으면서 0 근처 미분 불연속 문제도 피한다.'},
 {tex:'\\mathcal{L}_{CE}=-\\sum_i y_i\\log \\hat y_i', expr:'cross-entropy', d:'$y_i$ 는 원-핫 정답, $\\hat y_i$ 는 softmax 확률. softmax+교차엔트로피를 함께 미분하면 경사가 정확히 $\\hat y-y$ 로 단순해진다 — 예측이 정답에서 먼 만큼 경사가 크다.'},
 {tex:'\\frac{\\partial \\mathcal{L}_{MSE}}{\\partial z}=2(\\sigma(z)-y)\\,\\sigma(z)(1-\\sigma(z))\\quad\\text{vs}\\quad\\frac{\\partial \\mathcal{L}_{CE}}{\\partial z}=\\sigma(z)-y', expr:'gradient comparison', d:'왼쪽(MSE+sigmoid)은 $\\sigma\\prime$ 항 때문에 $z$ 가 크게 틀린 값(포화 구간)일 때 경사가 0에 가까워 학습이 멈춘다. 오른쪽(교차엔트로피)은 이 항이 상쇄돼 오차 크기 그대로가 경사가 된다.'}
],

diagram:{type:'split', cap:'같은 sigmoid 출력, 어떤 손실을 얹느냐에 따라 경사가 달라진다.',
 from:{t:'sigmoid σ(z)'},
 branches:[
  {t:'MSE 로 학습', s:'포화 구간에서 경사 소실'},
  {t:'교차 엔트로피로 학습', s:'오차 크기에 비례한 경사'}
 ]},

confuse:[
 {a:'손실 함수', b:'평가 지표', d:'손실은 경사 하강이 최소화하도록 **미분 가능**해야 하는 학습용 함수다. 평가 지표(정확도·[F1](#/c/precision-recall)·[BLEU](#/c/bleu-rouge))는 사람이 성능을 읽기 위한 것으로 미분 불가능해도 된다. 교차 엔트로피를 최소화해도 정확도가 지표로는 정체될 수 있다 — 둘의 목적이 다르기 때문이다.'},
 {a:'MAE', b:'MSE', d:'MAE 는 오차에 선형 반응(이상치에 강건), MSE 는 제곱 반응(이상치에 민감). 데이터에 노이즈가 많다면 MAE·Huber 를, 큰 오차를 특히 못 참는 문제라면 MSE 를 우선 고려한다.'},
 {a:'교차 엔트로피', b:'KL divergence', d:'교차 엔트로피 $H(y,\\hat y)$는 $H(y)+D_{KL}(y\\|\\hat y)$ 로 분해된다. 정답 분포 $y$ 가 원-핫이면 $H(y)=0$ 이라 둘이 사실상 같아지지만, 정답이 소프트 라벨(증류 등)이면 둘의 차이가 실제로 드러난다.'}
],

pitfalls:[
 '분류 문제에 회귀용 MSE 를 습관적으로 쓰면 학습이 "잘 되는 것처럼 보이다가" 특정 지점 이후 경사가 죽어 정체된다 — 손실 곡선이 평평해졌다고 수렴이라 단정하지 말 것.',
 '클래스 불균형이 심한데 표준 교차 엔트로피를 그대로 쓰면 다수 클래스만 맞혀도 손실이 낮게 나와 소수 클래스 학습이 묻힌다 — 클래스 가중치나 [Focal Loss](#/p/focal-loss)를 고려해야 한다.',
 '손실 값 자체의 절대 크기는 배치 크기·정규화 방식에 따라 달라지므로, 서로 다른 설정의 손실 곡선을 숫자로 직접 비교하면 안 된다.'
],

papers:['focal-loss','giou'],
terms:['gradient-descent','activation','precision-recall']
});
