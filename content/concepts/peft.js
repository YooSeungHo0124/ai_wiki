WIKI.concept({
slug:'peft',

tldr:'모델 전체 가중치를 갱신하는 대신 새로 추가하거나 골라낸 소수의 파라미터만 학습해, 적은 메모리·시간으로 큰 모델을 특정 과제에 맞추는 미세조정 방법군이다.',

why:'수십억 파라미터 모델을 과제마다 전체 미세조정하면 GPU 메모리도, 저장 비용도(모델 전체를 과제 수만큼 복제) 감당이 안 된다. LoRA 하나가 지금 오픈소스 LLM 미세조정 생태계의 사실상 표준이 된 이유와, QLoRA·어댑터·프리픽스 튜닝이 서로 뭐가 다른지 구분하지 못하면 도구 선택을 감으로 하게 된다.',

sections:[
 {h:'전체 미세조정이 왜 비싼가', d:'학습해야 할 것은 가중치만이 아니다. Adam 같은 옵티마이저는 파라미터마다 1차·2차 모멘트를 따로 저장하므로, 가중치 자체(예: FP16 2바이트)에 더해 옵티마이저 상태만 가중치의 몇 배(모멘트 2개 + 마스터 가중치 FP32 사본 등, 합쳐서 파라미터당 12바이트 안팎)가 추가로 필요하다. 여기에 활성화 저장까지 더해지면 7B 모델 전체 미세조정에도 수십 GB의 GPU 메모리가 필요해진다 — 가중치 수십억 개를 "다 학습 대상으로 두는 것" 자체가 비용의 원천이다.'},
 {h:'LoRA 의 저랭크 분해', d:'[LoRA](#/p/lora)는 원래 가중치 $W_0$ 를 그대로 얼려 두고, 그 옆에 저랭크 행렬 두 개 $A,B$ 의 곱만큼을 더해 미세조정 효과를 낸다. $A\\in\\mathbb{R}^{d\\times r}$, $B\\in\\mathbb{R}^{r\\times k}$ 이고 $r$(랭크)은 원래 차원보다 훨씬 작게(예: 4~64) 둔다. 학습되는 파라미터는 $A,B$ 뿐이라 전체 가중치 대비 훨씬 적고, 옵티마이저 상태도 그만큼만 필요하다. 추론 시에는 $W_0+BA$ 를 미리 합쳐두면 추가 지연시간도 없다는 것이 실무적 장점이다.'},
 {h:'왜 저랭크로 충분한가', d:'미세조정이 과제에 맞춰 가중치를 바꾸는 폭 자체가 사전학습된 가중치 공간 전체에 비하면 낮은 "내재적 차원(intrinsic dimension)"을 갖는다는 경험적 관찰이 LoRA 의 전제다. 즉 큰 모델을 완전히 다른 모델로 바꾸는 게 아니라 이미 있는 표현을 살짝 비트는 정도의 변화면 충분하다는 것이다.'},
 {h:'QLoRA · 어댑터 · 프리픽스 튜닝 비교', d:'[QLoRA](#/p/qlora)는 LoRA에 [양자화](#/c/quantization)를 더한 것이다 — base model 가중치 $W_0$ 를 4비트로 양자화해 얼리고, 그 위에 LoRA의 저랭크 행렬만 (보통 BF16으로) 학습해 메모리를 한층 더 줄인다. [어댑터](#/p/adapter)는 저랭크 행렬을 기존 가중치에 더하는 대신, 트랜스포머 블록 사이에 작은 병목(bottleneck) 레이어를 아예 새로 끼워 넣고 그것만 학습한다 — 구조가 하나 늘어나므로 LoRA와 달리 추론 시 합쳐서 없앨 수 없어 약간의 지연시간이 추가된다. [프리픽스 튜닝](#/p/prefix-tuning)은 가중치는 전혀 건드리지 않고, 각 레이어의 어텐션 앞에 학습 가능한 "가상 토큰" 벡터(prefix)를 덧붙여 그 벡터들만 학습한다 — 모델 파라미터 수는 그대로고 시퀀스 길이가 prefix 만큼 늘어나는 형태다.'}
],

math:[
 {tex:'W=W_0+\\Delta W=W_0+BA,\\quad A\\in\\mathbb{R}^{r\\times k},\\;B\\in\\mathbb{R}^{d\\times r},\\;r\\ll \\min(d,k)', expr:'LoRA low-rank update', d:'$W_0$ 는 사전학습된 원래 가중치(고정), $BA$ 는 그 위에 더해지는 저랭크 보정. $r$ 이 작을수록 학습 파라미터 $r(d+k)$ 가 원래 $dk$ 보다 훨씬 적어진다 — 예를 들어 $d=k=4096$, $r=8$ 이면 학습 파라미터는 원래의 약 0.4%.'}
],

diagram:{type:'compare', cap:'가중치를 새로 더하느냐(LoRA), 구조를 끼우느냐(어댑터), 입력을 늘리느냐(프리픽스 튜닝).',
 left:{t:'LoRA / QLoRA', items:['기존 가중치는 고정','저랭크 행렬 BA만 학습','추론 시 W0+BA로 합쳐 지연시간 없음']},
 right:{t:'어댑터 · 프리픽스 튜닝', items:['어댑터: 블록 사이 새 레이어 삽입','프리픽스: 가상 토큰 벡터만 학습','둘 다 구조/입력이 늘어 약간의 오버헤드']}},

confuse:[
 {a:'PEFT', b:'[전이학습](#/c/transfer-learning) 전체 미세조정', d:'전체 미세조정도 전이학습의 한 형태지만 모델 전체를 갱신한다. PEFT는 "미세조정을 어떻게 값싸게 할 것인가"에 대한 답으로, 학습 파라미터 수를 원래의 1% 미만으로도 줄이면서 성능은 전체 미세조정에 근접하게 유지하는 것을 목표로 한다.'},
 {a:'LoRA', b:'[양자화](#/c/quantization)', d:'LoRA는 학습 파라미터 수를 줄이는 것이고 양자화는 가중치의 정밀도(비트)를 줄이는 것이라 서로 다른 축이다. QLoRA는 이 둘을 결합해 "양자화된 고정 가중치 위에 LoRA만 학습"하는 방식으로 메모리 절감을 이중으로 얻는다.'},
 {a:'프리픽스 튜닝', b:'프롬프트 엔지니어링/[문맥 내 학습](#/c/few-shot)', d:'둘 다 입력 쪽에 무언가를 붙인다는 점은 비슷해 보이지만, 프리픽스 튜닝의 벡터는 사람이 읽을 수 있는 텍스트가 아니라 경사 하강으로 학습되는 연속 벡터이고 모델 가중치는 고정한 채 그 벡터만 최적화 대상이 된다. 프롬프트 엔지니어링/문맥 내 학습은 아무 파라미터도 학습하지 않고 텍스트 프롬프트만 사람이 설계한다.'}
],

pitfalls:[
 '랭크 $r$ 을 무작정 키운다고 성능이 계속 좋아지지 않는다 — 특정 지점을 넘으면 이득이 줄고 메모리만 늘어, 과제별로 작은 범위(4~64)에서 탐색하는 것이 일반적이다.',
 'LoRA 를 어느 가중치 행렬(쿼리·값 투영만 vs 모든 선형 층)에 적용하느냐에 따라 성능 차이가 크다 — 일부만 적용하면 파라미터는 더 줄지만 표현력이 부족해질 수 있다.',
 'QLoRA 로 학습한 뒤 4비트 base 를 다시 FP16으로 병합하지 않고 그대로 서빙하면, 양자화 오차가 추론 때마다 반복 적용돼 미묘한 성능 저하가 남을 수 있다.'
],

papers:['lora','adapter','prefix-tuning'],
terms:['quantization','transfer-learning','few-shot']
});
