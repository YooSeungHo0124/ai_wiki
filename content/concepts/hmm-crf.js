WIKI.concept({
slug:'hmm-crf',

tldr:'은닉 마르코프 모형(HMM)은 관측을 만들어내는 숨은 상태의 확률적 생성 과정을 모델링하고, 조건부 무작위장(CRF)은 관측이 주어졌을 때 라벨 시퀀스 전체의 조건부 확률을 직접 모델링하는 시퀀스 라벨링 모델이다.',

why:'Transformer 이전 십수 년간 품사 태깅·개체명 인식·음성 인식의 표준 도구였고, 지금도 그 구조(상태 전이, 시퀀스 전체의 정합성)에 대한 감각은 [CTC](#/p/ctc) 손실이나 구조화 예측 문제를 이해하는 데 필요하다. "토큰을 하나씩 독립적으로 분류하지 않고 시퀀스 전체의 일관성을 어떻게 강제하는가"라는 질문에 대한 원조 답이 여기 있다. BIO 태깅처럼 라벨 시퀀스에 문법적 제약(개체명 중간에서 갑자기 새 개체명이 시작될 수 없다 등)이 있는 문제는 지금도 이 구조화 예측 관점 없이는 제대로 다루기 어렵다.',

sections:[
 {h:'HMM: 생성 모델', d:'HMM 은 눈에 보이지 않는 상태(품사, 음소 등)가 마르코프 연쇄로 전이하면서 매 상태마다 관측(단어, 음향 신호)을 확률적으로 내뱉는다고 가정한다. 학습은 상태 전이 확률과 상태별 관측 확률을 데이터로부터 추정하는 것이고, 추론(가장 그럴듯한 상태 시퀀스 찾기)은 비터비(Viterbi) 알고리즘으로 동적 계획법을 써서 푼다.'},
 {h:'CRF: 판별 모델', d:'CRF 는 관측 시퀀스 전체를 보고 라벨 시퀀스의 조건부 확률 $P(y|x)$ 을 직접 모델링한다 — HMM 처럼 관측이 어떻게 "생성"되는지는 신경 쓰지 않는다. 이 덕분에 서로 겹치거나 상관된 특징(주변 단어, 대문자 여부, 품사 등)을 마음껏 조합해 입력으로 쓸 수 있다 — HMM 이 독립 가정 때문에 못하는 일이다. 선형체인 CRF(linear-chain CRF)는 이웃한 라벨 사이의 전이만 고려해 HMM 과 비슷한 계산 복잡도를 유지한다.'},
 {h:'CTC와 CRF의 관계', d:'[CTC](#/p/ctc) 는 입력과 출력의 길이가 다른(음성 프레임 수 ≠ 글자 수) 시퀀스 라벨링을 다루기 위해 빈 토큰(blank)과 정렬을 모르는 상태에서의 합산 규칙을 도입한 것으로, 본질적으로 CRF 와 마찬가지로 시퀀스 전체에 대한 조건부 확률을 동적 계획법으로 계산한다. 다만 CTC 는 라벨 간 전이에 특별한 구조(같은 라벨 반복·빈 토큰 규칙)를 가정해 정렬을 명시하지 않고도 학습할 수 있게 만든 특수화된 형태에 가깝다.'},
 {h:'왜 대체됐는가', d:'HMM·CRF 는 특징을 사람이 설계해야 하고(단어의 접미사, 앞뒤 품사 등), 먼 거리의 문맥을 다루려면 상태 공간이나 특징 조합이 기하급수적으로 커진다. [Transformer](#/c/transformer-block) 의 [셀프 어텐션](#/c/self-attention) 은 임의 거리의 토큰 간 관계를 특징 설계 없이 데이터로부터 직접 학습하고, [사전학습](#/c/pretrain-finetune)된 대규모 문맥 표현 위에서 시퀀스 라벨링을 붙이면 HMM·CRF 없이도 훨씬 높은 정확도가 나온다. 다만 지금도 신경망 출력 위에 CRF 층을 얹어(BiLSTM-CRF, Transformer-CRF) 라벨 간 정합성을 명시적으로 강제하는 하이브리드 구조는 개체명 인식 등에서 여전히 쓰인다.'}
],

math:[
 {tex:'P(x,y)=\\prod_{t} P(y_t|y_{t-1})\\,P(x_t|y_t)', expr:'HMM 의 결합 확률', d:'$y_t$ 는 $t$ 시점의 은닉 상태, $x_t$ 는 그 시점의 관측. 전이 확률 $P(y_t|y_{t-1})$ 과 방출(관측) 확률 $P(x_t|y_t)$ 의 곱으로 전체 시퀀스의 결합 확률을 분해한다 — 각 관측은 오직 같은 시점의 상태에만 의존한다는 가정이 들어 있다.'},
 {tex:'P(y|x)=\\dfrac{1}{Z(x)}\\exp\\Big(\\sum_t \\sum_k \\theta_k f_k(y_{t-1},y_t,x,t)\\Big)', expr:'선형체인 CRF', d:'$f_k$ 는 임의로 설계한 특징 함수(예: "이 단어가 대문자로 시작하고 라벨이 인명이면 1"), $\\theta_k$ 는 그 특징의 가중치, $Z(x)$ 는 모든 가능한 라벨 시퀀스에 대한 합으로 확률을 정규화하는 상수. HMM 과 달리 특징 함수에 어떤 정보든 자유롭게 넣을 수 있다.'}
],

diagram:{type:'compare', cap:'무엇을 확률로 모델링하는지가 근본적으로 다르다.',
 left:{t:'HMM (생성 모델)', items:['P(x,y) 결합확률 모델링','상태→관측 인과 가정','특징 조합 자유도 낮음']},
 right:{t:'CRF (판별 모델)', items:['P(y|x) 조건부 직접 모델링','임의 특징 자유롭게 결합','생성은 못함']}},

confuse:[
 {a:'HMM', b:'CRF', d:'HMM 은 상태가 관측을 "생성"한다고 보는 생성 모델이라 $P(x,y)$ 를 모델링하고, CRF 는 관측이 주어졌을 때 라벨을 직접 예측하는 판별 모델이라 $P(y|x)$ 를 모델링한다. CRF 는 HMM 의 독립 가정 없이 임의의 겹치는 특징을 쓸 수 있어 같은 데이터에서 대개 더 정확하다.'},
 {a:'HMM', b:'[MDP](#/c/mdp)', d:'둘 다 "은닉 상태가 마르코프 성질을 따라 전이한다"는 구조를 공유하지만, HMM 은 상태를 관측으로부터 추론하는 것이 목적(지도 없는 상태 추정)이고 MDP 는 상태에서 행동을 선택해 보상을 최대화하는 것이 목적(강화학습)이다. 행동(action)과 보상(reward)이 없다는 것이 HMM 과 MDP 의 결정적 차이다.'},
 {a:'CRF', b:'[CTC](#/p/ctc)', d:'둘 다 시퀀스 전체에 대한 조건부 확률을 동적 계획법으로 계산하는 구조화 예측 기법이라는 점에서 뿌리가 비슷하다. CRF 는 입출력 길이가 같은 라벨링(품사 태깅)에 주로 쓰이고, CTC 는 입출력 길이가 다르고 정렬을 모르는 음성·필기 인식에 특화된 변형이다.'}
],

pitfalls:[
 'HMM 의 "관측은 오직 현재 상태에만 의존한다"는 독립 가정이 실제 언어·음성 데이터에서는 자주 깨지는데, 이를 무시하고 결과를 과신하는 경우가 있다 — 실제로는 이전 몇 단어까지 보고 다음 단어가 정해지는 경우가 흔하다.',
 'CRF 를 학습할 때 정규화 상수 $Z(x)$ 계산이 라벨 집합 크기에 비례해 비싸진다는 것을 간과하고 라벨 종류를 과도하게 늘리면 학습이 느려진다.',
 '"요즘은 Transformer 가 다 대체했으니 HMM·CRF 는 몰라도 된다"고 넘기기 쉽지만, 신경망 위에 CRF 층을 얹어 라벨 시퀀스의 정합성을 강제하는 하이브리드 구조는 지금도 실무에 쓰인다 — CRF 를 모르면 그 층이 왜 있는지 이해할 수 없다.'
],

code:{lang:'python', d:'비터비 알고리즘의 골격(동적 계획법으로 최적 상태열 찾기)',
 src:'def viterbi(obs, states, start_p, trans_p, emit_p):\n    V = [{s: start_p[s]*emit_p[s][obs[0]] for s in states}]\n    path = {s: [s] for s in states}\n    for t in range(1, len(obs)):\n        V.append({})\n        newpath = {}\n        for s in states:\n            best = max(states, key=lambda p: V[t-1][p]*trans_p[p][s])\n            V[t][s] = V[t-1][best]*trans_p[best][s]*emit_p[s][obs[t]]\n            newpath[s] = path[best] + [s]\n        path = newpath\n    end = max(states, key=lambda s: V[-1][s])\n    return path[end]   # 가장 그럴듯한 상태(라벨) 시퀀스'},

papers:['ctc','deeplab'],
terms:['transformer-block','self-attention','pretrain-finetune']
});
